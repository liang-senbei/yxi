# GOALS · 怎么把 Yxi 做完

> 配套 [PLAN.md](./PLAN.md)。这里是**给 `/goal` 用的、可直接粘贴的目标语句**。
> 一次只设一个，做完了你验一遍，再设下一个。

---

## 三条设定原则（先读这个，比语句本身重要）

### ① 一个 goal = 一件**你能亲自验证**的事，不是「阶段完成」

`/goal` 挂的是 Stop hook：**条件不满足就不让会话停**。所以：

| ❌ 别这么写 | ✅ 这么写 |
|---|---|
| 「完成 Phase 1」 | 「**我能在手机上连上 station 并开出终端**」 |
| 「做完 Yxi」 | 「**我能在锁屏通知上批准一个权限请求，Claude 继续跑**」 |

前者我说完成就完成了，你没法验；后者你一试就知真假。
**这也天然防镀金**——功能清单已有 19 条决策、11 个附录，不设边界很容易顺手多做。

### ② 一个 goal 的工作量 ≤ 一天

超过一天就拆。理由：中间没有你能插手纠偏的点，等发现方向错了已经错了一大截。
下面 12 条就是按这个粒度拆的（PLAN 里的 Phase 1/3/4 都拆开了）。

### ③ **必须带逃生出口**

有些验收**我做不了**——装进你的手机、看锁屏通知、真机上试中文输入、去某个后台点开关。
不写清楚，我就会卡在那儿干耗（条件永远不满足，又不让停）。

所以每条都以这句结尾（下面已经写进去了）：

> **卡在需要你操作的事情上，说清楚卡在哪就停，别绕路自己硬凑。**

---

## 每条 goal 都默认包含的纪律

不用每次重复写，我已经记在 `handover.md` 和 `CLAUDE.md` 里：

- 只做本条 goal 的验收项，**别顺手加功能**（想加就说出来，别直接做）
- 自检要**真跑通**，不是写完了就算
- 做完更新 `handover.md`（进度 + 决策记录）和 `TROUBLESHOOTING.md`（踩的坑）
- commit + push

---

## 12 条目标语句（按顺序，可直接粘）

### G1 · 空壳 APK 能装上（半天）
```
/goal 建好 Yxi 的 Gradle 工程（minSdk 26 / compileSdk 36），编出 debug APK，adb 装进模拟器并确认能启动、显示界面。做完把 APK 路径给我，真机我自己装。卡在需要我操作的事情上，说清楚卡在哪就停，别绕路自己硬凑。
```

### G2 · 能连上一台机、开出终端（1 天）
```
/goal 让 Yxi 能连上本机并 attach 进 cc-root，终端里跑 ls 有彩色输出、能打字。SSH 用 mwiede/jsch，终端控件先试 termux/terminal-view。动手前先读 ConnectBot 的 transport/ 分层（PRD 附录 B.2）。卡在需要我操作的事情上，说清楚卡在哪就停，别绕路自己硬凑。
```

### G3 · 能连任意新服务器（1 天）
```
/goal 让我能在 App 里现加任意新服务器：任意 IP、任意端口、用户名、密码或密钥，并能一键装公钥（密码登一次后免密）。验收：连上 station 和 inst2 当普通 SSH 终端用；故意改一台的 host key 后 App 拒绝连接而不是静默接受。卡在需要我操作的事情上，说清楚卡在哪就停，别绕路自己硬凑。
```

### G4 · 会话看板 + 给任意会话发指令（1 天）
```
/goal 让我在手机上看到本机全部 cc-* 会话和各自状态（等你/干活中/已完成），并能不进终端就给任意会话发一句话。状态读 ~/.cloud-status/*.json（cc-state 已在写）。验收：给 cc-Yxi 发一句话，服务器上能看到它收到了。卡在需要我操作的事情上，说清楚卡在哪就停，别绕路自己硬凑。
```

### G5 · 能像读 Claude App 一样读对话（1 天）
```
/goal 让我在手机上读 cc-Yxi 的对话：消息气泡、思考默认折叠、markdown 渲染带代码高亮。数据源是 tail -f 转录 jsonl，不刮屏。渲染库 Markwon。验收：打开一个真实会话能顺畅读完一整段。卡在需要我操作的事情上，说清楚卡在哪就停，别绕路自己硬凑。
```

### G6 · 工具卡片和交互卡片（1 天）✅ 已完成
```
/goal 在对话界面里把 tool_use 渲染成工具卡片：Bash（命令+输出+退出码，占 3177 次先做它）、Edit/Write 渲染成 diff、Read、Agent。AskUserQuestion 渲染成可点选项、ExitPlanMode 渲染成计划卡。⚠️ 先实测「点选项要往 TUI 送什么按键」，通不了就做成只读并明确告诉我。卡在需要我操作的事情上，说清楚卡在哪就停，别绕路自己硬凑。
```
**结果**：按键这关**通了** —— 单选送数字直接确认，多选数字勾选 + `Right`+`1` 提交（TROUBLESHOOTING #29）。
所以选项是**可点的**，不用退回只读，D-Pad 也不再是选项交互的必需品（它在 G8 里还有通用导航的用处）。
但撞上一件计划里没预料的事：**待答的 `tool_use` 不落转录**，「此刻在等你」只能抓屏幕（#28）。
真实调用次数也纠正了一下（扫本机 145 个转录）：Bash 5536 · Edit 935 · WebFetch 890 · Read 679 ——
不是原来写的 3177，但排序结论没变，Bash 仍然第一。

### G7 · 文件模式（1 天）✅ 已完成
```
/goal 让我能在任意 SSH 主机上翻文件：走 SFTP 不依赖 yxi-agent，起点是会话 cwd，点面包屑能跳转（会话所在/最近/收藏/直接输入路径）。md 支持「阅读⇄源码」切换，图片能显示，json 折叠树，代码高亮。验收：在没装任何东西的 station 上打开一个带图的 README.md，图片能显示出来（相对路径解析对了）。只读，不做写删。卡在需要我操作的事情上，说清楚卡在哪就停，别绕路自己硬凑。
```

### G8 · 三模式切换 + D-Pad（1 天）✅ 已完成
```
/goal 顶部做成 [终端│对话│文件] 三模式切换：切换不断连、光标和滚动位置不丢、记住每会话偏好、没 yxi-agent 时对话模式置灰并说明原因。再做 D-Pad：圆形四向+中央 Enter，两个上角可配置，长按连发，压住往一边推持续走。验收：用 D-Pad 在 Claude Code 的权限提示里上下选 + Enter 确认。卡在需要我操作的事情上，说清楚卡在哪就停，别绕路自己硬凑。
```

### G9 · 终端真正好用（1 天）🔄 大部分完成（中文输入待真机验）
```
/goal 把终端打磨到能干活：键盘工具条（esc/tab/ctrl/方向键/^C/tmux 前缀）、CJK 输入、断线重连（切网后两秒内恢复且光标位置不丢）、横竖屏尺寸同步。验收：打中文进终端、切网恢复、工具条按出 ^C 能中断命令——这三条我在真机上试，你先在模拟器过一遍再告诉我。卡在需要我操作的事情上，说清楚卡在哪就停，别绕路自己硬凑。
```

### G10 · 手机会主动响（1 天）✅ 已完成
```
/goal 让 Claude 干完活或需要我时手机主动响：yxi-hook 追加写 ~/.yxi/events.jsonl，App 前台服务常驻一条 exec channel tail -f 它，收到就发本地通知。不用 FCM。手机没连着时产生的事件，连上后要能补收到。验收：锁屏收到「cc-mail 干完了」并点开直达——这条我在荣耀 Magic7 上试（MagicOS 杀后台狠，是最严场景）。卡在需要我操作的事情上，说清楚卡在哪就停，别绕路自己硬凑。
```

### G11 · 锁屏上批权限（1 天）⭐ 全项目最关键 ✅ 已完成（架构有意改动，见 handover）
```
/goal 让我能在锁屏通知上直接批准一个权限请求，Claude 继续跑。两条验收都要过：① 真批一次，Claude 继续 ② 把 ~/.yxi/ 改成不可写后再触发，Claude 退回终端问我而不是自动放行。test_yxi.py::test_approval_fail_closed 必须真跑通——这条测试比其它所有加起来都重要，它守的是「手机连不上时会不会自动放行」。卡在需要我操作的事情上，说清楚卡在哪就停，别绕路自己硬凑。
```

### G12 · 附件 + 语音 + 用量（1 天）
```
/goal 补三样：① 附件上传到 /root/src/tmp/<项目>/，编号成「图片1/附件1」，发送时头部带路径映射，3 天自动清理（清理脚本路径写死、-xdev、不跟符号链接、删前记日志）② 语音输入用系统 SpeechRecognizer，⚠️ 终端模式下必须先显示识别结果、确认才发送 ③ 用量用 ccusage 读本机 ~/.claude，主机列表紧凑条 + 会话看板 5小时/本周详情卡，ccusage 探测不到就隐藏不显示假数据。卡在需要我操作的事情上，说清楚卡在哪就停，别绕路自己硬凑。
```

---

## 之后

G1–G12 走完就是 **P0 完成**。再往后是 [PLAN.md](./PLAN.md) 的 Phase 8（P1：多语言、剪贴板、最近目录、跳转、服务器端 whisper）和 Phase 9（发布准备：keystore、GitHub Release、拆公开/私有仓）。

**视差过渡和悬浮排列**（D17/D18）不单列 goal——它们是 G4（看板）和 G8（切换）的一部分，
在那两条里作为体验要求实现。视觉稿上已经能滑，手感对不对你先在批注页上定。
