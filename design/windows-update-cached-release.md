# 已缓存旧包时继续检查新版

`06f37f7`：自动检查允许 Ready 状态重新读取 feed，仅选择高于已安装版本和已缓存版本的 Yxi Full 包；手动检查也先读取 feed，不再直接返回旧包已就绪。下载新版时，之前打开的安装确认不能触发旧包安装。查询失败前不改变原 Ready 状态。

隔离构建 `run.iLc4m2` 成功；`run.5EzPBs` 的 UpdaterTest 通过，包含已缓存旧包仍选择新版、相同/更高缓存不重复下载，以及排除其他 PackageId。此证据覆盖选择逻辑，不覆盖完整安装/下载中断 UI 链路。

开发线修复，尚未发布；线上仍是已验证的 1.4.12。旧客户端已经缓存旧版时，可使用已发布的直接 Setup 链接升级。

本次构建前仅清理已确认重复的暂存发布文件。清单位于 hk13 `windows-release/verified-duplicate-cleanup-1790055088307861996.json`。保留线上 1.4.12、1.4.9 回滚包、元数据及 1.4.12 唯一 jar，不影响现有下载源。
