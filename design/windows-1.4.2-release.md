# Windows 1.4.2 候选范围

状态：开发检查中，尚未启动正式构建，尚未发布。线上版本为 1.4.1。

本批交付同一 Codex 对话应用当前服务器线路：保留任务编号、历史、草稿、附件与队列；空闲时重新连接并从项目目录读取有效配置。自动发送暂停，由用户确认后恢复。

界面区分本次连接配置与历史线程记录，避免显示旧提供方。线路页显示原对话入口。配置读取失败保留旧连接，恢复失败给出重新连接入口；无历史、活动轮次、待审批和不明投递状态需要先处理。

协议依据：hk13 使用 Codex CLI 0.153.4 和本地模拟 Responses 服务，确认有历史同编号恢复后提供方覆盖影响后续请求，旧历史保持。零轮次未持久化历史是另一个场景，不据此否定有历史恢复能力。

发布前收取 Agent 产品接线定向结果，并通过既有 Windows 构建与安装启动流程。当前配置分层测试已合入；切线状态处理测试仍在收尾。无需重复运行旧全量回归。

真实 Windows 交互、第三方服务可用性由用户按 [操作说明](windows-route-resume.md) 验收；不进行生产模型调用。尚未确认的三个月权益规则仍不包含在本批内。

正式构建通过后记录冻结提交、构建编号、产物校验值和回退备份，再发布更新清单。

并行构建准备：产品源码冻结3badfe6，已委派cc-yxi推送codex/windows-release-1.4.2并启动既有CI；目前未取得run ID。pilot测试修正继续独立收尾，发布须收齐结果。


定向检查已收齐：Workspace 12/12、Controller修正测试后20/20。原失败为新增测试未更新队列revision，产品校验正确，未修改产品以绕过检查。覆盖同ID及配置参数传递、配置读取失败保留原连接、恢复失败后重新打开、空历史/未知投递/审批拦截。Windows UI仍待用户验收。

