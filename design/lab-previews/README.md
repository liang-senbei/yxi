# 实验室里推过的网页预览（源文件留档）

这些是用 `yxi-lab add` 推到手机实验室给用户审的单文件 HTML（logo 用 data URI 内联，遵守 `yxi-lab spec`）。
留在仓库里是为了以后能重推 / 改，**App 不引用它们**（D25 / D27）。

- `brush / ink / drop / particles`：开屏方案第一轮（笔触书写 / 墨迹落定 / Y 落下 / 粒子聚合）
- `particles-stardust / -wave / -vortex / -sparkle`：学 Gemini 的四版粒子（星尘 / 点阵波场 / 旋涡 / 星点）
- `at-deepspace / at-ring / at-neon`：学 Active Theory 官网的三版（深空墨光 / 虹环 / 霓虹笔画），深底

用户已定：冷启动四款轮播 = particles + stardust + vortex + sparkle（原生版在 `android/.../ui/splash/`）。
