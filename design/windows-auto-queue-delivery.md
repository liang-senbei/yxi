# 自动排队接续交付范围

状态：开发检查中，尚未发布；官网当前1.4.7。

本批直接落实用户反馈：工作中发送的消息默认进入队列，本轮结束且无需审批时自动发送下一条；无需再点击发送。排队行提供调整方向、编辑、删除。

终端队列运行在应用层，离开聊天页仍工作。手动发送、自动发送与状态检查共享互斥锁，已发送任务等待忙转空闲或服务器完成事件。未知投递保持阻塞，不靠重复发送判断结果。Codex默认自动逐轮发送，方向调整走原生活动轮次接口；Claude请求结束当前轮次后优先使用所选指令。

检查责任：hk13构建Agent负责集中编译，pilot负责队列/轮次边界定向检查，不向生产Agent发送测试消息。用户验收：连续排队三条、切换任务后自动继续、当前轮次中调整方向、审批暂停、断线恢复与取消队列项。

同批配置预览遮罩已收齐5项通过结果，dotenv文件变体隐藏修复已合入；该项不应拖住自动队列交付。实际发布需先解决队列检查发现的具体缺陷。
