# Windows 1.4.9 候选

状态：已发布官网。公开更新清单返回1.4.9，安装包链接HTTP200；未替用户安装本机版本。

发布产物来自已冻结CI35612716002，传输903902378字节完成，更新包大小与清单SHA256核对通过。更新包SHA256：93ADFE994F3129E28216CB109D51AC5E64D125D7AC3B0F50F8AF6553EEA92D9A；Setup SHA256：37225d04e7b18703342e24c2f2b7534e3a769068bfc64c9809039d4d90083291。回滚备份：`/var/www/yxi/desktop-rollback-1.4.8-before-1.4.9`。更新清单最后替换，旧版本包保留。

构建结果：CI35612716002成功，Windows产物10644449863（903902378字节）。已开始传输至hk13目录`/root/src/workspace/yunxi/windows-release/1.4.9-run35612716002`，下载进程PID1427394；完成与哈希尚待确认。模型controller在整合主线0e9df85上13个定向用例通过。

冻结源码：7e9bb18dc29a39301cd77c4ca7b02354d4e685b9；分支codex/windows-release-1.4.9；Windows CI：https://github.com/liang-senbei/yxi/actions/runs/35612716002 。后续开发提交不自动包含在此候选中。

## 本批变化

- 对话增量显示、字节续读、纯转录流、消息去重和长回复跟随底部。
- Codex 风格的服务器入口、置顶/收藏筛选，Codex 会话也可收藏和重新打开。
- 账户头像与设置分类布局，版本号、手动检查更新和官网下载入口。
- 第三方供应商编辑页、模板、模型映射、1M 后缀和高级 JSON；代理请求改写等能力仍未实现。
- 模型与思考强度菜单，Claude 切换状态持久记录，Codex 按服务器支持的强度选择。
- 网页链接单击右侧预览、Ctrl 外部浏览器；打开失败保留地址并结束加载状态。
- 任务级自动排队开关，编辑待发消息；关闭不取消当前轮次。

## 验证与边界

0e9df85 的主代码和测试代码编译通过。队列与转录修复在146ce9c完成26个定向用例；纯转录流壳层5项检查通过。模型controller独立分支13项通过，整合后的单类结果待回收。Windows安装包启动检查由既有CI执行，真实UI体验按人工清单记录，不扩大生产验证。

仍需关注：Claude切换可能要求原生确认；命令已发送但没有新转录回执时保持待确认，不声称成功。一次同时选择模型与强度时按阶段处理，可能要等模型新回执后再应用强度。第三方模型别名与实际报告名不一致时可能不能自动确认。原生转录尚未写入的内容无法提前显示，首次读取仍为最近400行。

中文尾标点裸链接修复尚待合入。排队侧边聊天、精确历史消息回退、一键模拟器完整流程、代理层和自动Agent协作仍是未完成项。Android环境检测属于内部基础模块，不宣称已经交付模拟器。

## 人工体验清单

1. 长回复持续更新，尾段可见；上翻不会被强制拉回，返回最新可恢复跟随。
2. 工作中连续发送两条消息，完成后自动接续；关闭自动排队保留待发内容，重开后继续。
3. 当前模型和待切换选择区分展示；结果不明时查看终端或选择沿用当前配置，无自动重复发送。
4. 置顶/收藏切换、服务器过滤、Codex原会话重开、设置头像布局。
5. 普通网页链接在右侧打开，Ctrl点击在外部浏览器打开；失败地址可重试。
6. 设置内版本显示与官网下载；实际自动更新需官网发布后再检查。
