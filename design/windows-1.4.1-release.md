# Windows 1.4.1 发布候选

状态：准备构建，尚未上线。线上版本仍为 1.4.0。

本次更新集中改善项目侧栏：项目标题提供新建任务和更多菜单，展示完整路径、服务器和任务数量；编辑项目显示名称后保存在本机，搜索支持新名称，同名项目显示路径区分。协作组与组规入口打开所属服务器的组管理，需选择实际协作组。

Codex 恢复时找不到历史文件，会说明可能原因并保留任务编号。未自动创建替代任务或重发指令。

本次不宣称已支持同线程切换第三方线路；恢复覆盖参数仅为开发基础。项目分类仍来自已有会话工作目录，空项目长期保留及独立项目注册未包含在这批实现中。

发布检查交给 hk13 Agent：对最终候选集中编译并使用既有 Windows CI 构建与安装启动检查，收取项目菜单定向检查报告。真实 UI 操作参见 windows-manual-verification.md。禁止用生产会话或真实模型跑测试。

发布时记录冻结提交、CI 编号、安装包和更新包校验值；备份 1.4.0 后替换产物，最后更新清单。实际发布完成前保持本文件状态为候选。

构建已启动：GitHub Actions 35448869719，冻结提交 676f3f766bc2347e408ed84a080f9e119b914162，分支 codex/windows-release-1.4.1。当前运行中，尚无通过或发布结论。


定向检查：cc-yxi_entertainment 对 cb9ec4f 编译成功，并以真实 WorkspaceNavigation 类检查名称/折叠持久化、主机隔离、空名恢复及长度限制，通过。该证据覆盖导航数据层；不代表后续新增入口或 Windows GUI 已验。最终冻结提交由正在运行的 Windows CI 编译。

