# 对话历史回退验收记录

## 2026-09-22 开发线，未发布

用户要求：在聊天界面选择任意历史用户消息，回到该轮上下文，编辑并继续；不要求用户到终端手动选轮次，不把载入草稿当作回退。

- `5938444` 导入隔离适配器，补上实际执行所需的 `-p`、提示参数结束符、literal tmux 投递、投递失败不报成功、缺少实例身份时拒绝重启，以及 shell 轮询字符串转义。暂未连接界面。
- `cefe1ea` 将分支日志接入 `Transcript.Incremental`。新的用户轮次指向已知较早祖先时，重建可见消息、工具结果和上下文；服务器 JSONL 不修改。普通追加仍增量解析，旧 UUID 重放不切换叶子，progress 仅保留父链元数据，不保留其大负载。
- 新增四项分支用例：跨批多轮回退、同 UUID 更新与 assistant 兄弟节点、progress 父链、尾部窗口外祖先。测试已交给现有隔离代理，尚无本轮通过结果。

## 不能据此声称完成

1. 界面尚未提供一键回退并继续，当前发布版仍是草稿和原生选择器入口。
2. 回退和重启两阶段之间还需要持久的发送队列保护及失败恢复，单次操作共用锁不能替代完整状态机。
3. 原进程的线路、启动参数、权限和注册信息必须保留；未能保留时不能悄悄换配置。
4. 当前分支识别需要已载入祖先。祖先在尾部窗口外时需按 UUID 补取历史；首轮回退和 Codex 运行器尚待接入。
5. 分支重建需要保留当前窗口的原始可渲染记录，会增加客户端内存；需测长历史并结合历史分页解决，不能称已优化所有内存问题。
6. 必须通过隔离真实 shell 参数投递测试，再完成界面操作、分支渲染、队列暂停恢复和继续发送的端到端验收。既有 29 项适配器单测不证明这些流程。

测试限制：不向生产会话试投递，不使用付费模型请求，不改变运行器权限。hk13 上 Gradle 串行、2 workers、1536 MiB，不停止其他代理的 daemon。

## 本轮已核对的测试结果

隔离 worktree `windows-test/cc-yxi_pilot/wt-tbranch`，代码基线 `cae69cf`，补测提交 `b832239`（合入为 `e185b8e`）。root 直接读取 2026-09-21 19:32:45–46 UTC 的 XML：

- TranscriptBranchTest：5 项，0 失败、0 跳过。
- TranscriptBranchEdgeTest：3 项，0 失败、0 跳过；覆盖工具结果、元消息和分支上下文重建。
- RewindDeliveryGateTest：3 项，0 失败、0 跳过；覆盖中断重启、错误 ticket、损坏主记录与旧空备份。

补测初版两处预期错误已经纠正：从 u1 分支不能保留旧子节点 a1；上下文量取最后一次 assistant usage，不能跨轮相加。未因此修改产品以迁就错误预期。

这些结果只证明解析和本地发送闸的相应行为，不证明 CLI 回退、重启后运行器上下文或完整界面流程。`47bb9ec` 的远端用户轮次统计修复不在上述测试基线内，仍需针对性验证。

## 历史快照续接验证

`624fed9` 基线与补测 `4aead4d`：root 核对 `TranscriptBranchSnapshotInspectionTest` XML（2026-09-21 20:07:53 UTC），7 项、0 失败、0 跳过。通过真实 Python 进程执行生产 snapshot 脚本，再将结果喂给产品 Transcript/Entry，覆盖：450 条弃支不进入返回记录、残行续接字节位置、队列重复内容与出队、最新 UUID 内容、祖先重放顺序、旧格式回退标识。

快照路径与早期连续字节区间方案不同：已有证据证明返回记录不包含被弃支。服务器仍扫描原文件元数据，完整性能与历史分页尚未验收；不能继续沿用“snapshot 仍传全部弃支”的旧描述。无链旧格式仍使用 tail 回退，实际 SSH 网络中断不在这 7 项的覆盖范围内。
