# 对话历史回退验收记录

## 首轮控制器编排待验证

`7e64e43` 增加 NativeFirstTurnController，串联身份与菜单核对、确认前持久 ticket、多行粘贴和恢复后只读核验；尚未连接应用入口。核验等待期间同时检查新的根消息链，避免登记仍显示上一轮 idle 时立刻检查旧链并失败。

2026-09-22 隔离构建在启动前被 4 GiB 可用磁盘门槛拒绝（hk13 约剩 3.8 GiB）。因此此提交尚未编译或完成控制器集成测试，不作为首轮功能完成证据。业务会话恢复仍等待用户选择范围。

同期 `4e7af8e` 修复重建同名 tmux 会话沿用旧状态：`run.J07cWN` 的 SessionStatusIncarnationTest 为 3 tests、0 failures、0 errors、0 skipped，构建证据 `run.PWk0KI`。该修复尚未发布，不表示当前安装版本状态已改变。

## 首轮多行文字提交

`fbb675e` 增加经 stdin 传输的首轮粘贴步骤：核对待恢复文字摘要，拒绝终端控制字符，用 operationId 命名的专属 tmux 缓冲区进行 bracketed paste，随后清理缓冲区；回车仍走独立屏幕/进程/历史校验。操作命令增加超时上限，正文不进入 shell 参数。

`run.qZFgIk` 真实 CLI 用例通过包含换行、中文和 emoji 的首轮编辑：最终当前链只有这一条完整用户消息，摘要和同进程核验通过，原消息与工具结果未进入新请求。此步骤尚未由应用首轮控制器编排；历史菜单滚动/长文本、首轮图片等仍未完成。

## 首轮菜单步骤身份与内容校验

`792b57f` / `02598cc` 增加 NativeFirstTurnKeys：每一步核对 tmux runtime/pane、实际 Claude PID/exe/进程树、交互登记的会话 ID、历史内容摘要和刚读取的屏幕摘要；只接受固定菜单动作。`run.e8iRjS` 真实 CLI 用例通过错误 PID、旧屏幕和错误历史大小拒绝，随后使用该模块完成打开菜单、定位、确认与清空原输入，并通过首轮恢复核验。

原先 size/mtime 守卫在 `run.KUf95i` / `run.wf6NDG` 打开菜单后拒绝；诊断无新增记录。改为先在 inspected size/mtime 下取得稳定 SHA-256，后续逐步核对相同长度和完整内容摘要，并检查每次读取期间文件身份稳定；正常菜单流程通过，内容变化仍拒绝。编辑文字提交仍由测试驱动完成，尚未组成应用首轮控制器；滚动/长文本仍待处理。

## 首轮持久恢复记录

`2f2e376` 为发送保护增加独立 nativeRoot 核验记录，写入前验证运行身份、原首条 UUID 和编辑文字摘要，不存编辑正文；禁止与普通 anchor/restart 记录混用。重新检查入口识别该记录，调用同进程根分支核验后才清除保护。

`run.Epu1Hq` 首轮记录重载/损坏拒绝测试通过；`run.9Bijuy` 4 项普通发送保护测试通过；`run.a9Pes5` 在真实原生首轮探针前登记记录、从磁盘重新加载，完成原生恢复后经实际 recheckRewindRecovery 清除，端点请求计数不增加，磁盘记录最终为空。首轮操作控制器与应用入口仍未接入，滚动和长文本也仍待完成。

## 首轮同进程恢复核验

`84cc7b1` 增加独立 NativeRootVerification：复用运行实例登记检查但要求同一 PID，再检查稳定转录的当前链已排除原首条、仅有一个根用户消息且 SHA-256 与编辑文字一致，已有助手回复；最后再次核对运行实例。普通 print 回退仍要求新 PID，不混用两种语义。补正登记进程本身就是 pane PID 时的合法进程树判断。

`run.AtV6aA` 首轮链校验测试通过；`run.C64K33` 原 RewindLiveVerificationTest 通过；`run.KaPsqz` 真实 Claude 首轮能力探针执行新核验命令，输出 `__YXI_REWIND_ROOT__:ok`，既有整链也通过。隔离 runner 已能按明确包名运行 core 测试。首轮持久 ticket、驱动控制器、滚动/长文本和应用入口仍未完成。

## 首轮原生菜单只读校验

`5992637` 新增 FirstTurnRewindMenu，按已验证 2.1.278 布局核对完整可见菜单行、当前项、选中位置和逐行文本；确认页必须匹配目标文字并明确 code unchanged、选中 Restore conversation。未知版本、滚动、未知副行、重复选中标记和不完整菜单均拒绝。重复文本仍按位置保留，必须同时匹配全行列表。

`run.xed2HI` 3 项解析测试通过；`run.qITb77` 把首轮能力探针改用该实际解析器后，真实 CLI 全链继续通过。当前仅支持可完整显示的已知布局，不支持截断长文本/滚动菜单，尚未接入应用首轮操作或持久恢复状态；此边界不能作为最终缩减需求。

## 首轮原生恢复能力验证

`6051870`，`run.GnGqZ7`：在独立容器中的真实 Claude 2.1.278 会话，测试驱动 `/rewind`，核对无滚动菜单包含当前三轮、选中首条，再核对确认页高亮为 Restore conversation 且 code unchanged；确认后清空恢复到输入框的原文字，发送 ROOT 标记。端点请求排除了原三轮及其 Read 结果；实际增量缓存仅剩新首轮；原生登记仍为同一个 sessionId。全部用例通过。

这是首轮能力探针，驱动只针对该短菜单测试数据，不是可直接上线的通用菜单定位器。应用仍未接入首轮；须完成严格定位、重复文本/滚动边界、持久发送保护和同进程恢复核验。此前未合入 NativeRewind WIP 仍不能直接启用，其首轮拒绝及不完整守卫尚在。

## 过期图片选择在请求前拒绝

`f01e7e3`，`run.NS27g5`：实际 ConversationRewind.restore 对过期 size 快照和越界图片序号分别拒绝；核对本地模型端点请求计数未增加，gate 未被无效操作锁住。随后同一会话正常保留/移除图片回退继续通过。该验证没有模拟界面失败状态重开，后者的图片选择保留目前仍为代码接线证据。

## 图片选择接入实际回退执行链

`76e7d04` 把编辑弹窗的图片选择绑定到读取时的 RewindTarget，提交前重新 inspect 并要求快照一致；原消息由服务器重读后构建结构化输入。SSH 通道并行写入输入和读取输出，完成写入后只关闭 stdin。图片模式使用 CLI stream-json，读取器排空非结果事件并仅保留最终 result/退出码，避免把大图片回显当作最终回复。失败状态保留图片选择；重开编辑时快照不匹配不会重置成另一组图片。

`run.IsF6b2` 2 项结构化输出测试通过；`run.YR8ebj` 真实 SSH 大输入/EOF/SHA-256 一致性通过；`run.aToCfB` 真实 Claude 控制器整链通过选取第二张图片（解码像素核对），再次回退移除全部图片后 API 请求无 image 块，gate 最终为空。原有文字、工具结果、连续回退、断线恢复用例一并通过。

图片回退入口已接通，不再保持“传输未接入”的禁用文案。仍未完成 Windows 整个界面到运行器的单一联动验收、首轮、非图片附件、Codex，以及所有中断阶段自动恢复；不能据此宣称整个需求完成或已发布。

## 历史图片编辑界面

`7d7ea31` 接入编辑弹窗的原消息读取和 RewindImageDraft 缩略图，复用附件托盘提供真实图片预览与 X 移除；历史图片使用原始序号作为标签，隐藏无意义的上传状态。`98e2c43` 修正 Material 最小触控尺寸把 X 撑大的显示问题。`run.qhxVFD` 实际 Compose 测试通过预览、Esc 只关闭预览、移除第一张后保留 index=1；人工检查截图确认绿色第二张仍提示“图片2”，X 为紧凑圆按钮。`run.fJd9Iq` 的普通编辑弹窗回归通过。

当前图片选择仍未接到实际回退传输，含原图片的自动回退按钮继续禁用并说明状态；“仅载入文字”明确不会携带图片。预览界面已经接线，不代表整个图片回退功能完成。截图位于本地 verification-artifacts/rewind-images/。

## 绑定快照的原消息读取

`ff0bbd5` 增加 RewindTargets.loadMessage：仅在检查过的 size/mtime 仍匹配时返回指定用户 UUID 的原 message（包括图片），读取结束核对 fd 与路径的设备/inode/size/mtime，拒绝元消息/旁支标记、缺失目标及过大记录。`run.nvqA24` 实际 Python 文件读取测试通过，覆盖图片正文原样读取、错误 UUID、时间变化、追加内容和元消息拒绝。此模块尚未接入编辑弹窗；它依赖先前 inspect 的当前分支核验，不单独替代该核验。

## 编辑图片的明确保留与移除

`92faaa5` 为输入构建模块增加 keepImageIndices：null 保留全部，空集合删除全部，其余值严格对应原始图片序号；负数或越界选择拒绝。`run.7pUtod` 5 项测试通过，包含保留第二张、全部删除和无效选择。`run.zCtvdA` 的真实 CLI 测试输入两张不同颜色图片，只保留第二张，并解码端点收到的 PNG 像素确认没有发错；已有回退整链同时通过。该模块仍待接入编辑弹窗和回退传输，不能宣称图片删除按钮已经可用。

## 保留图片的编辑输入构建模块

`a976011` 增加核心 RewindMessageInput：替换文字并保留嵌入图片的字节、媒体类型和排列位置，不修改原消息；限制最多 10 张、解码后合计 20 MiB，拒绝未知块、远程图片来源、无效 base64 和多个文字块。`run.yZXFMC` 4 项测试通过，覆盖保留顺序、不修改输入、仅图片消息补文字及拒绝情形；`run.x5eYgu` 改用该模块生成图片输入，真实 CLI 与原有回退整链通过。

模块尚未接入应用历史附件读取、预览/删除和回退传输，原附件拒绝仍保留。媒体类型/base64 校验不等于完整图片解码检查，实际格式仍由 CLI 验证；完整附件编辑验收未完成。

## 原生图片结构化输入协议

`6e9003d`，`run.OawsWq`：通过 stdin stream-json 向真实 Claude 2.1.278 发送含 PNG base64 image 块和文字的用户消息，检查假端点收到可解码的 image/png 图片块。回退丢弃该轮后，后续请求不含旧 image 块；既有实际控制器、Read 工具、连续回退、缓存及断线用例同时通过。输出必须使用 stream-json 和 verbose，最终从 result 事件取结果；最初 `run.ZRG0QZ` 证明 input stream-json + output json 会被 CLI 拒绝。

这确认了后续附件恢复实现所需协议，不等于应用已经支持编辑带附件的历史消息；ConversationRewind 的附件拒绝仍在，结构化图片保留/删除、预览与大小限制仍待接入。

## 编辑弹窗实际输入与按钮

`05309e5`，`run.uMtERa`：真实 Compose 窗口中用 Robot 点击输入框、Ctrl+A 替换全文为 `a`，点击主按钮后回调收到精确的新文字并关闭弹窗；禁用态点击不提交；Esc 取消不触发载入草稿或原生选择器。820/460 两种宽度通过，XML 1 项、0 失败/错误/跳过。测试使用组件实际布局边界定位，按键留出焦点/选择事件处理时间。此前独立系统窗口假设和无间隔按键的失败记录保留为 `run.6Tpyqq`、`run.Z2U3Ti`。

这是 Linux 虚拟桌面中的实际组件交互，回调为测试接收器；真实 SSH/CLI 控制器另有整链证据，但尚未把 Windows 完整聊天界面点击与运行器连成一个验收用例。

## 实际 Compose 编辑弹窗

`96ea149` 把聊天中的编辑弹窗提取为同一个 RewindMessageDialog 组件，并增加明确取消按钮；`48dbd34` 修正窄窗口下操作按钮换行顺序。隔离 Xvfb 桌面实际打开 820/460 宽窗口，截图人工检查中文、文字换行、按钮和禁用态无裁切；Esc 取消不触发发送/载入草稿/原生选择器。`run.GL0wWV` XML 1 项、0 失败/错误/跳过。截图保存在本地 `verification-artifacts/rewind-dialog/*-final.png`。

最初 `run.Unow4Y` 因镜像只有 headless Java 失败；`1f4ace4` 为测试镜像补全桌面 JRE 和中文字体，测试运行仍无外部网络。此处是 Linux 上的真实 Compose 组件验证，不是 Windows 整个聊天界面的操作验收，也未点击主回退按钮连接真实控制器。

## 完成反馈与重复入口

`bdcc126` 补上此前没有显示的成功反馈：5 秒后自动消失、可手动关闭；成功后恢复末尾跟随并聚焦输入框。原生选择器入口在自动回退中、发送保护未解除或断线时禁用。隔离镜像 `run.tNt9g9` 编译通过；此项尚未进行 Windows 界面截图或点击验收，不能把编译当成视觉验证。

## 实际 Read 工具结果随分支恢复

`4528695` 的 stub 向真实 CLI 返回 Read 工具调用，读取测试项目中的两份不同文件；没有权限绕过参数。连续回退后增量缓存和 SSH 冷加载均仅保留祖先的 Read 结果，且结果非错误、正文匹配保留文件。`run.hhDhZa` 完整用例通过；直接检查请求记录确认被弃用轮次曾实际返回另一份文件内容，而恢复后的请求保留祖先读取内容并排除该旧结果。既有回复正文、连续回退与断线核验同时覆盖。

这验证 Read 结果的分支归属，不等于 Edit/Bash 等工具的副作用回滚；当前功能回退对话历史，不承诺撤销文件或服务器修改。附件与首轮回退、Windows UI 仍待完成。

## 不同回复正文的对应关系

`cfd5c22` 将协议 stub 改为按各轮唯一标记生成不同回复。实际 CLI 执行、应用连续回退、断线恢复、增量缓存和 SSH 冷加载路径均保留；逐项检查留下的 AssistantText 正文属于对应祖先与最新编辑轮，而非仅检查两条相同 `ok` 的数量。`run.W0ioM6` XML：1 项、0 失败/错误/跳过。由此补上上一批“相同回复无法区分正文对应”的证据缺口，仍不涵盖工具调用/附件或 Windows UI 操作。

## 实际转录的增量缓存与重新打开

`0860105` / `a6c887f` 将真实 CLI 转录按完整行字节增量交给实际 DesktopTranscriptMemory.Entry；两次回退及断线恢复后，逐次断言 UserText 只剩祖先和最新编辑消息。再经真实 SSH 调用 TranscriptBranchStart.load 重建冷缓存，与增量结果一致。补充检查助手回复仅有两条 stub 回复，不残留额外回复。`run.US27Di` XML：2026-09-22，1 项、0 失败/错误/跳过，先前控制器与断线用例同时通过。

此证据验证提供给聊天组件的数据内容和回复数量，不是 Windows 截图或界面点击验收；stub 回复都为 `ok`，不单独证明不同正文回复的身份映射、工具结果或附件渲染。

## 模型请求前持久化核验身份

`f43f57a`：带 inspect 结果的应用回退在模型请求前，将核验 Query 与发送保护 ticket 一次写入磁盘，避免先改历史再补核验身份的空档。Query 在写入前必须与目标会话、运行实例及消息 UUID 匹配；不匹配时不创建记录。`run.OM8L3d` 持久化测试通过；`run.vu48gc` 真实控制器连续回退和断线恢复回归通过，检查模型结果返回后、重启前从磁盘已可读到 Query。

这补齐核验身份的保存时序；不代表模型执行中断线后已能自动判定生成结果、自动重启或安全撤销旧保护。旧进程未退出、目标仍在链上时，核验仍会保持阻塞。

## 重启投递后、最终核验前断线恢复

`58b8206`，证据 `run.gRHKHS`：在第三次实际应用控制器回退的「正在确认运行器与历史上下文」阶段主动断开真实 SSH 并中断操作，重新构造 RewindDeliveryGate 从磁盘读取，确认仍阻塞且保存核验身份；重新连接后调用实际 recheckRewindRecovery 完成只读核验并解除保护。断线前后请求计数不变，编辑消息只请求一次。此前连续两次回退断言在同一测试中一并通过。

这覆盖指定阶段的断线、磁盘恢复记录重载和不重复发送，不代表进程崩溃、模型请求进行中断线、核验记录写入前中断或完整 Windows 应用重启都已验证。更早中断阶段的恢复仍需补齐。

## 连续两次应用回退验证

`9397e8c`，`run.SGfsPg`：同一真实 Claude 会话连续调用两次产品 `ConversationRewind.restore`，第一次回到较早用户消息，第二次编辑刚生成的末轮；两次均完成原生进程恢复及发送保护核验。测试检查第二次重启中 gate 仍阻塞，成功后内存及磁盘重读 gate 均解除，并检查请求保留祖先、不含两次被替换的旧内容。隔离真实 SSH/tmux/native CLI，模型为本地 stub，未验证 Windows 界面点击。

此前 `run.0jtUdl` / `run.sxGDsJ` 证明第二次被 CLI 的可选 `--resume-drops-turn` 拒绝：其范围检查从已弃用的兄弟分支消息开始，与当前分支目标不一致。应用现统一采用已用于多轮的分支回退参数，保留 inspect 当前分支、UUID 及 size/mtime 预检；不在失败后改参数重发，既有失败票保持阻塞。低层 CLI 参数测试仍保留该附加选项的独立覆盖。尚未完成断线恢复、首轮/附件/Codex 和完整 UI 验收。

## 实际应用控制器整链：普通文字多轮回退通过

`283c8db`，证据 `run.uXkgxd`（2026-09-22 00:40:26 UTC）：真实 Claude 2.1.278、真实 OpenSSH/JSch、真实独立 tmux，直接调用产品 `ConversationRewind.restore`。预先注入已加载转录缓存，选择当前链上的历史用户 UUID，完成 inspect、print 回退、持久发送保护、退出旧进程、恢复原会话、新进程与分支核验、清除发送保护。XML 1 项、0 失败/错误/跳过；保存的 gate 为 `[]`；假端点请求保留第一轮并排除被编辑轮及后续旧上下文。

该整链曾在 `run.2PDfaT` / `run.Hl1eQj` 失败：tmux 将仍有 Claude 子进程的窗格报告为 bash，旧控制器误把恢复 shell 命令输入 Claude。修复改为重新核对实际 Claude 子进程身份，并等待旧 PID 退出后才投递恢复命令。原失败的 gate 保持阻塞，没有伪报成功。`run.NICEBV` 的 9 项 shell 回归及 `run.KxsgXp` 的 SSH 目录恢复回归复测通过。

边界：容器无外部网络，模型为回环协议 stub；未点击 Windows Compose 界面；未验证两次连续应用回退、故障断线恢复、首轮、附件、Codex 或所有 CLI 版本。此证据不能扩大成完整功能验收或已发布。

## 真实交互式 CLI 续聊验证与下一阻塞项

`3b1e38f` 在隔离容器内以真实 Claude 2.1.278 恢复已回退的会话，通过实际 tmux 终端输入跟进消息，检查本地假端点收到的请求仍保留祖先及编辑后的消息、不含被撤回的旧分支。`run.A1BWvN` XML：2026-09-22 00:23:14 UTC，1 项执行、0 失败/错误/跳过。终端就绪使用产品 Model.borrowable 判据；测试只信任自身临时项目和假密钥，不设置权限绕过。测试未调用整个 ConversationRewind.restore，不能算应用端到端验收。

代码核对发现：captureCommand 将 `--resume` 归入未保留参数，inPlaceAllowed 因此拒绝已恢复会话；这也会阻止用户第二次回退。后续需识别并严格核对原启动会话 UUID，不能笼统放行全部 resume 参数或忽略它。真实 SSH、原生运行器与整个控制器仍需合并验证。

`7765e05` 已修正上述明确 UUID 的情形：仅捕获唯一的 `--resume <UUID>`，原会话 ID 必须与回退计划一致，命令生成和重新启动也复核这一约束。名称选择器、缺失值、重复参数、混合等号形式仍拒绝。`run.lBucld` RewindShellTest 9 项通过（含六种真实 argv 捕获变体）；`run.lyVswc` 复测既有 RewindTest。此证据是隔离真实 shell/进程与命令解析，不代表连续两次应用界面回退已通过。

## 控制器原项目目录恢复

`7ce7fe3` 修正 relaunch 使用后来窗格目录而非回退报告目录的问题，避免恢复时载入其他项目配置。`5449861` 的隔离 SSH/tmux 测试调用实际 `RewindController.relaunch`：窗格位于另一个目录，检查被启动的测试替身实际工作目录仍是原项目（含空格路径）。`run.lBi3Mg` 测试成功；它覆盖真实 SSH、会话探测、控制器与 tmux 按键投递，替身不是 Claude，不能视作完整真实 CLI/UI 回退验收。初次 `run.gddB5m` 因测试替身名称不符合既有 Claude 执行路径校验失败，只修正了替身名称，没有放宽产品校验。

## 当前状态：2026-09-22，开发线未发布

界面现已接入「回到这里并继续」及持久发送保护；下文早期记录中的“未连接界面”仅描述当时状态。完整应用端到端验收仍未通过，首轮、附件和 Codex 回退仍待完成。

新增真实 Claude 2.1.278 CLI 回退验证：镜像源码 `67945e53810651816752b04d34bf074bd97c8ab4`，隔离运行证据 `/root/.cache/yxi-isolated-tests/run.30gqde`。直接核对 XML：2026-09-22 00:05:03 UTC，1 项执行、0 失败、0 错误、0 跳过。

测试创建三轮真实 CLI 会话，从第二轮持久化记录取得实际父 UUID，以 `--resume-session-at` 编辑第二轮，再启动独立 CLI 进程恢复相同会话。检查两次本地假端点请求均包含第一轮和编辑后的第二轮、不包含旧第二轮及第三轮。使用假密钥、仅回环网络、独立 HOME，不调用生产模型。

此结果证明该版本 CLI 的多轮回退和重启后的请求上下文，不证明产品 SSH 控制器、交互窗格重启、界面分支显示、发送队列及故障恢复已完成联调。测试直接调用 CLI 参数，并未调用整个 ConversationRewind 控制器。

后续 `b9b7f52` 将回退执行替换为产品 `Rewind.command` 和 `Rewind.parse`，增加末轮编辑的 `--resume-drops-turn` 实测，以及含单引号、命令替换文字、换行和中文的输入。`run.e32OBy` 的 XML（00:08:25 UTC）1 项执行、0 失败/错误/跳过；7 次请求均使用假凭据，单轮及多轮回退后的上下文检查通过，提示中的 `touch` 未执行。独立检查确认容器已经删除。此轮覆盖产品命令生成与返回解析，仍不覆盖整个 SSH/UI 控制器。

`0873f75` 修复恢复记录缺少目标会话 ID 交叉检查的问题：创建核验记录和磁盘恢复时均须与原 ticket 的 sessionId 一致。`run.dYNYtV` 的 XML（00:10:54 UTC）RewindDeliveryGateTest 4 项、0 失败/错误/跳过，覆盖不匹配的会话 ID（包括同步修改转录路径）、重启读入被篡改的记录、旧 ticket、损坏主文件和旧空备份。编译通过；应用端到端验收仍待完成。

## 2026-09-22 开发线，未发布

用户要求：在聊天界面选择任意历史用户消息，回到该轮上下文，编辑并继续；不要求用户到终端手动选轮次，不把载入草稿当作回退。

- `5938444` 导入隔离适配器，补上实际执行所需的 `-p`、提示参数结束符、literal tmux 投递、投递失败不报成功、缺少实例身份时拒绝重启，以及 shell 轮询字符串转义。暂未连接界面。
- `cefe1ea` 将分支日志接入 `Transcript.Incremental`。新的用户轮次指向已知较早祖先时，重建可见消息、工具结果和上下文；服务器 JSONL 不修改。普通追加仍增量解析，旧 UUID 重放不切换叶子，progress 仅保留父链元数据，不保留其大负载。
- 新增四项分支用例：跨批多轮回退、同 UUID 更新与 assistant 兄弟节点、progress 父链、尾部窗口外祖先。测试已交给现有隔离代理，尚无本轮通过结果。

## 不能据此声称完成

1. 界面尚未提供一键回退并继续，当前发布版仍是草稿和原生选择器入口。
2. 回退和重启两阶段之间还需要持久的发送队列保护及失败恢复，单次操作共用锁不能替代完整状态机。
3. 原进程的线路、启动参数、权限和注册信息必须保留；未能保留时不能悄悄换配置。
4. 当前分支识别需要已载入祖先。祖先在尾部窗口外时需按 UUID 补取历史；首轮回退和 Codex 运行器尚待接入。
5. 分支重建需要保留当前窗口的原始可渲染记录，会增加客户端内存；需测长历史并结合历史分页解决，不能称已优化所有内存问题。
6. 必须通过隔离真实 shell 参数投递测试，再完成界面操作、分支渲染、队列暂停恢复和继续发送的端到端验收。既有 29 项适配器单测不证明这些流程。

测试限制：不向生产会话试投递，不使用付费模型请求，不改变运行器权限。hk13 上 Gradle 串行、2 workers、1536 MiB，不停止其他代理的 daemon。

## 本轮已核对的测试结果

隔离 worktree `windows-test/cc-yxi_pilot/wt-tbranch`，代码基线 `cae69cf`，补测提交 `b832239`（合入为 `e185b8e`）。root 直接读取 2026-09-21 19:32:45–46 UTC 的 XML：

- TranscriptBranchTest：5 项，0 失败、0 跳过。
- TranscriptBranchEdgeTest：3 项，0 失败、0 跳过；覆盖工具结果、元消息和分支上下文重建。
- RewindDeliveryGateTest：3 项，0 失败、0 跳过；覆盖中断重启、错误 ticket、损坏主记录与旧空备份。

补测初版两处预期错误已经纠正：从 u1 分支不能保留旧子节点 a1；上下文量取最后一次 assistant usage，不能跨轮相加。未因此修改产品以迁就错误预期。

这些结果只证明解析和本地发送闸的相应行为，不证明 CLI 回退、重启后运行器上下文或完整界面流程。后续远端预检的验证结果见下节。

## 历史快照续接验证

`624fed9` 基线与补测 `4aead4d`：root 核对 `TranscriptBranchSnapshotInspectionTest` XML（2026-09-21 20:07:53 UTC），7 项、0 失败、0 跳过。通过真实 Python 进程执行生产 snapshot 脚本，再将结果喂给产品 Transcript/Entry，覆盖：450 条弃支不进入返回记录、残行续接字节位置、队列重复内容与出队、最新 UUID 内容、祖先重放顺序、旧格式回退标识。

快照路径与早期连续字节区间方案不同：已有证据证明返回记录不包含被弃支。服务器仍扫描原文件元数据，完整性能与历史分页尚未验收；不能继续沿用“snapshot 仍传全部弃支”的旧描述。无链旧格式仍使用 tail 回退，实际 SSH 网络中断不在这 7 项的覆盖范围内。

## 控制器发送闸接线复核

`38b06dd`（基于 `5b2f5e3`，控制器改动合入为 `1104454`）隔离工作树，root 核对 2026-09-21 20:12:32–35 UTC XML：RewindControllerGateTest 2、RewindDeliveryGateTest 3、RewindShellTest 8、RewindTest 34，均 0 失败、0 跳过。

控制器在首次可能修改转录的 exec 前持发送锁创建 ticket；尚未确认加载完成时不清除。控制器新增两例只证明探不到会话不建票和 Target 字段映射，不能代替成功回退、取消和恢复的完整控制器测试。`42d5040` 的当前进程捕获修复晚于该测试基线，另待复测。运行器加载确认和 ChatPane 一键执行仍未完成。

## 进程定位与结构化预检补测

root 核对 `42d5040` 基线 20:15:30–32 UTC XML：RewindCaptureLivePaneTest 4、RewindShellTest 8、RewindTest 34 均通过。新增 fixture 验证旧登记指向另一个仍存活的 Claude 不会被误选、双候选拒绝、窗格进程本身为 Claude、没有 Claude 不回退旧登记。

root 核对 20:25:30 UTC RewindPreflightRealExecutionTest XML：8 项通过（补测合入 `1cc4a0e`）。真实隔离转录验证不带 isMeta 的命令回执不计人类轮次、不能选作目标，但快照仍保留模型与强度回执；真 bash/Python 执行验证空格 JSON、正文伪 UUID、错误 parent、size/mtime 变化及显式文件路径。均不是生产会话试跑或完整回退验收。

`ae63dfd` 与补测 `f9aa5fe` 工作树，root 核对 20:27:42–43 UTC XML：RewindCommandStreamTest 3 项和 RewindControllerGateTest 2 项通过。证明 UTF-8 输出、字节上限、超限后仍排空输出等单元行为；不等于真实 SSH 断线/取消及多会话并发已经端到端验证。实现已将模型执行移出主机级发送锁，单独 exec 流仍需联调。
