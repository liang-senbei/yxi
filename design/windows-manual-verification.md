# Yxi Windows 手动验收清单

本清单供你在功能交付后自行验证，不需要现在执行。按你的要求，后续开发以功能实现为主，不再主动运行测试、交叉验证或专项检查。历史验证结果保留在 `windows-workbench-progress.md`，不反复重跑。

请使用最终交付的 Windows 包；之前的测试包可能不包含最新改动。问题反馈时尽量附上构建标识，而不只写界面版本号。

## 建议先验这几项

| 项目 | 怎么操作 | 预期结果 |
| --- | --- | --- |
| 安装与启动 | 安装/打开新包，再正常退出、重新打开 | 能进入工作台，窗口布局正常，旧配置仍在 |
| 主机保存 | 添加两台测试服务器，修改名称和端口，退出后重开 | 两台记录和修改仍在，不需要重新绑定 |
| 主机切换 | 两台服务器使用同名项目或会话，来回切换 | 消息、文件、终端、预览和插件始终属于选中的服务器 |
| 新对话 | 选择服务器、目录和运行器，启动新任务 | 在所选目录创建任务，运行器正确，不重复创建 |
| 基本对话 | 发消息、查看回复、切换任务，再返回 | 对话、任务状态和输入草稿保持正确 |
| 文件和网页 | 从任务打开 Markdown、源码及网页预览 | 能在右栏查看，编辑或刷新后显示对应内容 |

## 主机、密码与恢复

- 使用测试服务器分别尝试密码和私钥文件登录；密码错误、私钥不存在时，应清楚提示，不能假装已连接。
- 已保存的服务器在重启、断网、重新登录 Yxi 账号后仍应保留。是否自动重连应遵循设置，不应自动新建 Agent。
- 给服务器设置地区标签，核对侧栏、选择器和搜索结果；仅修改地区不应断开现有连接，导出再导入应保留标签。
- 升级旧版时，检查旧漫游目录中的服务器是否迁移成功；名称、ID、地址、端口和用户名应保持一致。
- Windows 密码应由当前 Windows 用户的系统保护保存。迁移失败或新旧记录冲突时，应保留原数据并提示处理。
- **仅在测试配置副本上**模拟主配置丢失，再从恢复入口选择副本：未选中或取消时不恢复；无法验证的副本不可选；恢复后核对服务器列表。
- 私钥文件属于外部文件管理范围。移动、删除私钥后，检查提示是否明确。

仍需重点关注：活动密文和备份都存在但无法读取、不同 Windows 用户、旧版本降级、长历史副本列表。

## 服务器导入 / 导出

- 从主机菜单导出 JSON：应包含服务器信息，不包含密码、私钥内容或私钥路径。
- 导入刚导出的文件：先出现预览，再确认保存；取消不改变列表。
- 同一地址、端口和用户名的重复项应跳过，原有认证信息不被覆盖。
- 标识冲突但地址不同的记录应作为新服务器处理，不替换原服务器。
- 新导入的服务器需要自行填写认证信息，不能套用另一台服务器的密码。
- 导入期间若原列表发生变化，应提示重新预览。无效文件不应导致原配置丢失。

## 对话、运行器、模型与排队指令

- 点击麦克风后确认识别服务器，开始录音、停止识别、编辑文字并加入草稿；不应自动发给Agent。当前需服务器安装 `yxi-asr`，不是桌面本机离线识别。
- 若服务器没有识别服务，可从语音窗口打开安装步骤并导出安装包，上传到Linux服务器后执行包内脚本。安装会下载较大的模型，不修改Agent钩子；系统依赖需先准备。
- 可选择麦克风并刷新设备列表；重开后应保留选择，拔掉所选设备时应提示重新选择，不能悄悄换用另一台设备。
- 连续录制多段，识别文字应追加而不是覆盖已编辑内容；失败后可以重试原录音或丢弃，关闭窗口后不再保留该录音。
- 识别文字可复制、清空及撤销清空；清空后编辑框仍应保留，便于继续输入。
- 检查麦克风权限关闭、录音太短、取消、120秒自动结束和连接中断时的提示；退出前应停止麦克风。服务器临时录音清理在断网情况下也需关注。

- 分别选择已支持的运行器，在包含空格/中文的项目路径中启动任务。
- 切换模型或第三方线路，核对生效范围、提示及实际使用结果；不要只依据下拉框文字判断。
- A 服务器的线路、密钥不能自动进入 B 服务器。
- 编辑、重排、取消排队提示词，检查主输入框草稿是否保留。
- 点击输入框旁的历史图标，搜索本任务之前保存的提示词；追加后原草稿应保留，不自动发送，历史附件需重新添加。
- 输入历史支持展开全文和替换草稿；已有文字时替换应先确认，取消应保留原草稿。
- 点击对话搜索图标，在当前已加载的消息、回复和工具记录中搜索并定位；分组工具记录应展开所在组。
- 缩窄对话栏时，模型和上下文信息应换行，发送按钮及输入工具仍应可用。
- 发送后断网或重启：结果不明确的指令应显示待确认，不能自动重复发送。
- 自动下一轮投递、运行器确认回执、协作模式等，以最终交付的功能说明为准；当前不能把所有排队条目都当作已有自动执行能力。

## 右侧文件与 Markdown

- 打开不同服务器上的同名文件，切任务后核对路径和内容。
- 检查 Markdown 渲染、源码视图、分栏、图片、长文档和中文。
- 点击“查找”或在文档内按 Ctrl+F，输入文字后使用上一个/下一个；应切到源码并选中对应文字，Enter 查找下一个、Shift+Enter 查找上一个，不能修改文件内容。
- 修改后切换任务再回来，未保存内容应保留。
- 在外部或 Agent 中同时改同一文件：应出现冲突处理，不能静默覆盖。
- 保存失败时编辑内容应保留；退出时应提示未保存内容。

## 网页实时预览

- 配置项目地址和开发服务，启动、查看日志、停止；重新打开后检查地址恢复。
- 修改网页源码，观察热更新。没有热更新能力的项目，检查手动刷新及最终交付的自动刷新行为。
- 切换自适应、桌面、平板和手机视口，检查响应式布局和缩放显示；这只切换页面尺寸，不等同完整手机浏览器、触摸或 User-Agent 模拟。
- 切换网页主题的系统/亮色/暗色，观察支持系统明暗偏好的页面；未实现主题样式的网站不一定发生变化。此开关不改Yxi自身主题或网页源码。
- 尺寸与主题位于“预览设置”中；收起设置后页面应获得更多空间，设置仍保留，“恢复默认”应回到自适应尺寸和系统主题。
- 点击页面查找，输入关键词，使用前后匹配及大小写开关；Enter/Shift+Enter应定位，关闭应清理查找高亮。
- 页面未就绪、端口被其他程序占用、服务退出时，状态不能误报成功。
- 选择元素，尝试字号、字体、颜色、间距、圆角和纯文字试调；检查撤销、重置。
- 临时试调应明确区别于源文件已保存。加入对话后，检查地址、元素和修改要求是否对应当前任务。
- 切服务器后检查预览不串台；复制截图时确认画面属于当前页面。

## 插件

- 在 A 主机安装测试插件，在 B 主机确认没有被误报为已安装。
- 检查目录搜索、安装范围和项目路径确认，取消操作不应安装。
- 依次尝试启用、停用、更新、卸载；核对实际结果与界面提示，其他插件配置应保留。
- 更新后核对版本，不能仅凭“命令成功”判断功能正常；在新会话中实际调用插件。
- 从操作历史查询旧操作，或选择恢复配置/插件包。后续配置已经变化时，不应直接覆盖。
- 卸载应保留持久数据，不自动清理未指定依赖。
- 断线、超时或重启后，待确认操作不能自动重复执行。

仍需重点关注：真实远程市场、不同作用范围、权限和依赖声明、精确升级目标版本、依赖原安装路径的插件、跨文件恢复中断。安装记录、运行器识别和插件功能可用是不同状态。

## 账号及其他能力

- 登录、取消登录、重新登录、退出账号，核对资料和权益展示。
- 验证信箱的阅读、领取、删除及工单的新建、回复、重开查看。需要真实提交的步骤，可先用测试账号。
- 对照安卓版逐项检查语音、发送历史、置顶、消息提醒等生产力功能。
- “三个月权益”“Laize 的具体运行器定义”“官方协作模式”的验收，需依据最终确认的产品规则，不能凭现有占位或名称判断完成。

## UI 与 Windows 体验

- 你最关心的美观性：整体留白、层级、字体、图标、配色、卡片和按钮是否统一；避免堆砌技术说明。
- 尝试窗口缩窄、最大化，以及 100% / 125% / 150% 缩放。检查裁切、遮挡、弹窗大小和滚动。
- 使用中文输入法，检查 Enter、Shift+Enter、复制粘贴、快捷键、拖拽附件。
- 检查亮色/暗色、右栏开合、预览和终端上方菜单是否被遮挡。
- 实际日常使用后，再决定是否需要性能或长时间运行专项，无需一开始穷举所有边界。

## 反馈问题时复制这段即可

```text
构建标识 / 版本：
Windows 版本和显示缩放：
所在页面 / 服务器别名：
操作步骤：
预期结果：
实际结果：
是否能重复出现：
截图或错误提示：
```

截图和日志请遮住密码、令牌、私钥等秘密。关机、主动关闭测试程序、网络中断等情况请注明，便于区分环境中断与功能问题。

## 任务快速切换（第六十九轮）

以下步骤留给你后续验收，本轮没有执行验证。

1. 连接有任务的服务器，点击侧栏「切换任务」或按 Ctrl+K。输入任务名、路径、服务器名或地区，查看搜索结果。
2. 点击结果或按 Enter 打开第一项，确认进入对应服务器的任务；侧栏收起时也可使用快捷键。
3. 有置顶和归档任务时，查看置顶排序及「包含归档」开关；无结果时应显示空状态。

范围：搜索当前已加载的会话，不会主动连接未连接的服务器。

## 排队指令快捷操作（第七十轮）

1. 在任务运行时加入多条提示词，使用每行更多菜单上移、下移或移到待发送首位，确认正文和附件跟随原条目。
2. 点击垃圾桶撤回一条，再点「撤销」，确认原指令回到队列且主输入框草稿保留；撤销本身不会发送。
3. 切换任务或重启后查看保存的队列顺序；对状态待确认的指令只进行核对，本地重排不能绕过它发送后续指令。
4. 查看窄窗口下行内操作、完整内容弹窗和更多菜单是否清晰可用。

本轮没有执行上述检查。自动按轮投递和运行中引导仍依赖运行器接收协议，尚未完成。

## 整页设计反馈（第七十一轮）

1. 打开网页，选择手机视口或暗色主题，点击「整页反馈」，输入整体布局意见并加入草稿；查看地址、服务器、项目和显示设置是否对应。
2. 草稿原有文字应保留，反馈不自动发送；切换任务后反馈应进入各自任务。
3. 输入后选择「稍后继续」，重新打开应保留文字；关闭预览时可收起保留或丢弃。尚未加入的反馈仅在本次应用运行内保留。
4. 选择元素后加入反馈，查看项目与视口设置一并附带。这里记录的是设置值，不代表网页已成功采用这些设置。

以上由用户后续操作，本轮未执行验证。

## 文档查找替换（第七十二轮）

1. 在右栏打开文本或 Markdown，按 Ctrl+H，输入查找词和替换词；先查找选中，再替换选中项。
2. 使用全部替换，查看匹配数、分栏渲染和未保存状态；留空替换词应删除匹配文字，匹配不区分大小写且不解释正则表达式。
3. 点击撤销替换恢复原编辑内容；替换后继续编辑时撤销按钮应禁用。点击保存才写回远端，冲突仍走既有处理。
4. 在查找框、替换框和正文里分别按 Ctrl+A，确认只全选当前输入区域。

本轮没有运行编译或上述操作。

## 预览空间调整（第七十三轮）

1. 在宽窗口打开网页或文件右栏，拖动分隔条调整宽度，关闭应用后重新打开预览，查看宽度是否恢复。
2. 点击「展开」让预览占据主工作区，再点「恢复分栏」回到对话旁；查看未发送草稿和文档编辑是否保留。
3. 缩窄窗口时继续使用单面板；恢复分栏需要窗口有足够空间。宽度保存失败时查看工作区提示。

上述操作留待用户验证，本轮未执行。

## 终端投递记录（第七十四轮）

1. 用测试任务发送提示词，在状态待确认的核对弹窗点击「查询投递记录」，查看是否显示终端写入完成；再通过真实对话人工确认运行器是否收到。
2. 投递中断后查询，应显示登记未完成或无记录；查询本身不会重发或解除阻塞。旧版指令无记录也不能视为未发送。
3. 若后续安排重复 ID 场景，检查同一任务实例与 ID 不会二次写入终端。记录位于服务器用户目录 ~/.yxi/instruction-deliveries，仅含摘要目录和时间。

上述由用户后续验证，本轮没有执行。

## 投递核对信息保留（第七十五轮）

1. 查询待确认指令后关闭弹窗、切换任务或重启应用，再打开核对弹窗，查看最近查询的时间与结果是否保留。
2. 查询失败时旧记录应继续保留；查询不会把待确认改成已接收。
3. 查询期间人工解除阻塞，晚到结果不能覆盖新的处理状态。旧版指令记录没有查询字段时仍可读取。

本轮未执行编译或操作验证。

## 线路应用目标（第七十六轮）

1. 在测试主机准备 Claude 与 Codex 任务，打开应用线路确认框，查看运行器、模型、项目范围和任务清单。
2. 同运行器同范围有任务运行时应拒绝应用，另一种运行器的运行任务不应单独阻塞；用户级范围仍可能影响同用户的其他任务。
3. 应用期间不能切换运行器选项；结果依然提示配置写入与重开需求，不能当成真实模型请求已验证。

本轮未执行编译、测试或真实线路切换。

## 多模型线路管理（第七十七轮）

1. 点击已有线路的「复制线路」，修改名称、模型和备注后保存，查看新增独立记录且原线路保留。复制会带入当前服务器已有认证，不通过剪贴板导出。
2. 取消复制弹窗时不新增记录；保存副本不应自动应用到运行器。
3. 用名称、地址、模型或备注搜索，切换 Claude/Codex 分类；窄窗口下横向滚动操作按钮。

以上留待用户执行，本轮未编译或测试。

## 推理强度（第七十八轮）

- Claude 线路填写运行器支持的 effortLevel，保存并应用后查看配置与重开提示；留空后检查该线路强度设置移除。实际模型是否支持由你后续试用。
- Codex 模型写入尚未实现，当前只保存模型记录，不应以线路匹配标签认定模型已生效。
- 本轮未执行编译或测试。

## Codex 模型写入（第七十九轮，替代上一轮限制）

1. 在测试配置已有顶层模型/提供方/推理强度时应用另一条 Codex 线路，查看管理块写入新值，旧行以注释保留；重开任务后自行确认实际模型。
2. 同端点同密钥、不同模型的线路应按模型区分；清空线路模型/强度后保留用户原值，恢复默认线路应还原旧顶层行。
3. 检查其他表（如 MCP）保留。顶层多行字符串暂不支持自动改写，应提示而非继续写入。
4. 此改动位于共享核心，安卓版线路切换也请后续试用。未执行任何上述验证。

## 插件说明预览（第八十轮）

- 在有插件的测试服务器点击「使用说明」，查看主机、安装范围、版本及 README 是否对应，Markdown 和复制文字是否可用。
- 切换服务器应读取另一台服务器的安装包；缺 README、断线或长说明时查看提示及重新读取。此入口不执行插件安装或运行命令。
- 本轮未执行上述操作、编译或测试。

## 插件说明加入任务（第八十一轮）

- 选中同服务器任务，打开插件说明，填写要求后加入草稿，查看服务器、目录和原草稿是否保留；不应自动发送。
- 未选任务或任务已改变时不能添加；长 README 会截取并标注。加入后再次点击应禁用，修改要求后可再次添加。
- 本轮未执行上述验证。

## 桌面协作组（第八十二轮）

- 在配置过 groups.json 的测试服务器打开「协作组」，查看组名、组规、成员、路径和打开成员任务是否对应。
- 修改服务器分组后点击刷新；不存在的成员显示未找到，无文件时显示暂无分组，读取失败显示错误。
- 此入口仅读取，不发送消息、不修改组规、不启动自动对话。未执行编译或操作验证。

## 协作组编辑（第八十三轮）

- 新建组，勾选成员并填写组规，保存后刷新查看；编辑已有组保留未移除的离线成员及其他分组。
- 同名新建或编辑期间被其他客户端修改，应提示重新读取；保存失败时保留编辑内容。
- 保存不广播消息；组规由现有服务器协作机制读取。极短同时写入窗口尚非事务保护。未运行编译或测试。

## 移除协作组（第八十四轮）

- 在测试分组点击移除，确认主机、成员和组规；取消应无变化，确认后该组消失且成员任务、其他组和文件保留。
- 组内容被其他客户端改动时应要求刷新。运行中 Agent 可能保留旧上下文，移除组不等于停止协作进程。
- 未运行编译或测试。

## 协作记录（第八十五轮）

- 打开协作记录，查看服务器现有 hub.log，搜索成员/组名/正文并查看上下文，清空搜索恢复原始内容。
- 超过 256 KiB 显示截取提示，无文件显示暂无记录；切换服务器后来源应对应。这里只读，不发送消息；日志不能证明 Agent 已处理。
- 本轮未执行任何操作验证或编译。

## 成员指派（第八十六轮）

- 在协作成员卡片点击指派，填写分工后加入待发送，查看进入正确任务、队列包含目标项目和指派 ID、原草稿保留。
- 指令应先留在本地，由你在目标任务点击发送；已有待确认条目仍阻塞后续投递。取消不生成指令，目标实例已变化时不能继续添加。
- 此功能不是 Agent 自动互聊，本轮未执行操作验证。

## 组内指派追踪（第八十七轮）

- 新建成员指派后回到协作组，查看要求、指派 ID、队列状态与目标跳转；撤回、投递待确认及人工核对后状态应相应变化。
- 重启后关联保留，两台服务器同名组不应混用；目标任务已结束仍保留记录。旧版无关联字段的指派不自动归组。
- 本机记录不代表服务器全部协作记录，已人工核对也不代表执行完成。未运行验证。

## 协作投递事件（第八十八轮）

- 后续自行在测试环境安装新版 yxi-hub 并发送消息，查看对方输入附消息 ID，hub-events.jsonl 中同 ID 有 attempting 和 terminal-written 事件；不应将其理解为处理回执。
- 桌面切换两种日志来源，搜索 ID、发送者或接收者；旧服务器没有事件文件时显示暂无记录。
- 中断可能只留下尝试或 unknown；不要自动重发。每次命令生成新 ID，跨调用去重尚未实现。
- 本轮没有部署、发消息、编译或测试。

## 协作回复关联（第八十九轮）

- 后续在测试环境安装脚本，A 发消息给 B，B 使用 yxi-hub reply <消息ID> 回复，查看事件的 replyTo 及 A 收到的回复标记。
- 不属于当前会话的消息 ID、无效 ID、原发送者已离组时应拒绝回复；不会用名称片段另找接收人。
- 目前仍按 tmux 会话名识别成员，同名重建实例需谨慎；重复调用会产生新消息。未部署或执行验证。

## 协作消息卡片（第九十轮）

- 在带 ID 事件页查看同 ID 的开始/完成事件合成一张卡片；点击关联或原消息查看当前读取范围内的回复。
- 截断导致缺正文时应提示，未知或不完整事件不能冒充完成；清空搜索恢复消息列表。未运行验证。

## 固定消息 ID 去重（第九十一轮）

- 后续在测试环境用相同目标、正文、固定 UUID 两次执行 say，第二次应报告已有终端完成记录且不重复输入；reply 的第四参数是本次回复 UUID，第二参数是原消息 ID。
- 同 ID 改正文应拒绝；只存在开始/unknown 记录时不自动重发。不要删除事件日志后假定历史去重仍有效。
- all 群发尚无调用级去重。不传 ID 是新消息。未部署、未发消息、未运行测试。

## 群发批次（第九十二轮）

- 测试环境运行 all <组名> <正文> <固定UUID> 后重复同命令，已写入的成员不应重复收到；每位成员消息 ID 不同但重试稳定。
- 同批次改正文或成员应拒绝，未知投递不能自动重发；没有显式 UUID 时记下输出批次号用于重试。保留 hub-batches 和事件日志。
- 本轮未部署、未发消息或运行验证。

## 协作任务实例关联（第九十三轮）

- 测试环境发送消息后重建同名目标或来源任务，固定 ID 重试和回复原 ID 应拒绝新实例。旧消息缺少实例字段时 reply 应提示人工确认。
- 查看事件里的 senderInstance/recipientInstance；发送期间目标消失应留待确认状态。仍存在检查与终端写入间的短暂竞争窗口。
- 未部署、未发消息或运行验证。

## 协作消息暂停（第九十四轮）

- 测试环境 pause <组名> 后 say/reply/all 的新消息应被阻止，status 显示 paused；resume 后可继续新投递。现有会话与记录保留。
- 双方共享多个组时，任一共同组暂停都会阻止消息；已开始投递或运行中的任务不由此中断。状态文件损坏时应拒绝投递而不是忽略。
- 未部署、未执行操作验证。

## 桌面协作投递控制（第九十五轮）

- 测试服务器安装新版 hub 后，在组视图暂停/恢复并刷新，查看真实状态及更新时间。另一客户端修改后刷新应同步。
- 旧版、不存在、断线或回复错误时显示未确认与说明，不能伪装暂停成功。操作不应自动发送待处理指令或关闭成员会话。
- 未部署或执行验证。

## 协作预算（第九十六轮）

- 测试环境设小额度 limit <组名> <消息数> <分钟>，查看新消息达到条数或截止时间后被拒绝；暂停/恢复不重置预算。再次 limit 才开始新预算。
- 群发每位接收者计一条；未知投递占用一次，已有完成记录重试不重复计数。保留事件日志以维持计数。
- 不代表模型 token/费用额度，本轮未部署或验证。

## 桌面协作预算（第九十七轮）

- 新版测试服务器组视图设置小额度，查看服务器计数与截止时间；到条数或时间后刷新应显示到限。
- 保存新预算重置计数，暂停不自动恢复；输入范围外不能保存，旧版本不显示设置入口。读数是刷新时快照。未部署或验证。

## 回复循环限制（第九十八轮）

- 在测试链中重复相同正文，第三次应拒绝；累计达到 16 条的回复链不能继续追加。缺失原链记录时提示核对。
- 不同正文不应按完全相同处理；同 ID 已完成重试仍走去重。此限制不覆盖换措辞或新建 say 链，需同时设置消息预算。未部署或验证。

## 协作脚本导出（第九十九轮）

- 在协作组导出 yxi-hub，上传测试服务器后按说明安装；保留旧脚本备份，确认 SSH 与 Agent 的 PATH 都选择新版脚本。
- 原组配置、日志与预算继续使用；新安装不会自动注册 SessionStart 钩子或发消息。旧钩子需指向新版路径。
- 本轮未打包、部署或执行验证。

## 协作上下文钩子（第一百轮）

- 测试环境先安装新版 hub，导出并运行 configure-hub.py install，查看备份及其他钩子保留；重复安装不应新增重复命令。
- 新开或恢复已分组 Claude 会话后自行查看组规注入。remove 移除本地新版命令，不改其他钩子；如需还原旧路径，从备份合并恢复。
- Codex 不使用这项 Claude 钩子。未打包、部署或验证。

## 启动提示词（第一百零一轮）

- 测试服务器分别新建 Claude/Codex，填写多行提示词，查看是否交给正确运行器；留空保持空会话。
- 内容包含引号或以连字符开头时应按文字处理；最多 16000 字符。相同请求失败后重试复用任务，改动提示词后为新请求。
- CLI 版本需支持启动位置参数及 -- 结束选项。尚未自动为新任务加入协作组。未创建会话、编译或测试。

## 组内新建成员（第一百零二轮）

- 测试组内新建 Claude/Codex，编辑预填上下文后创建，查看分组包含新任务且启动提示词收到组规与队友；不覆盖已有成员。
- 分组被移除时不能启动，启动失败留下的离线成员可手动移除。相同请求重试不重复创建任务。
- Codex 只在启动时获得快照，后续通过 hub 查询当前状态。未创建真实任务或运行验证。

## 协作组界面分区（第一百零三轮）

- 在窄窗口切换组和成员/指派/组规/投递控制标签，查看主要操作和空状态；多组时横向滚动选择。
- 进入投递控制后读取服务器状态，切换标签不应创建任务或修改组配置。未运行验证。

## 协作负责人（第一百零四轮）

- 编辑组时指定/取消负责人，保存后查看组标题及成员标识；移除负责人成员应取消负责关系。
- 新建成员上下文和新版 Claude hub context 包含负责人，不代表额外权限。共享核心变更后也请试用安卓版分组编辑。旧版客户端可能不保留 owners。
- 未部署、编译或测试。

## 负责人汇总（第一百零五轮）

- 设置在线负责人后，从指派页生成汇总要求，查看成员、最近指派摘要和 ID；编辑后进入负责人的本地待发送队列。
- 不应自动发送、丢失负责人草稿或合并文件；未指定或离线负责人时禁用。最多引用 20 条指派，仍需负责人看实际成果。未运行验证。

## 独立 Git worktree（第一百零六轮）

- 测试仓库新建任务勾选独立 worktree，确认 Agent 位于源仓库旁的独立目录，初始内容来自 HEAD，不包含源目录未提交改动。
- 同一请求重试复用目录；无提交仓库、无 Git/Python 或目录冲突时提示错误，不删除已有文件。
- detached HEAD 中的提交需自行保留/合并；关闭 Agent 不删除工作树。本轮未创建工作树、编译或测试。

## Git 改动预览（第一百零七轮）

- 在测试仓库准备已修改、已暂存、未跟踪文件，打开改动标签，选择文件查看对应范围差异并打开文件右栏。
- 重命名/删除文件应列出，删除文件打开可能提示不存在；大 diff 显示截取。此页不暂存、提交或撤销。
- 本轮没有运行 Git 检查、编译或测试；以上操作交给用户验收。

## 差异阅读（第一百零八轮）

- 查看新增/删除行颜色、差异块和文件头层级，选择多行复制；截取时增删统计应注明仅当前部分。未运行界面或编译验证。

## 差异反馈（第一百零九轮）

- 选择文件差异，填写修改意见后加入对话，查看服务器、路径和暂存范围正确，原草稿保留且不自动发送；长差异注明截取。
- 取消弹窗不发送内容；返回对话后的实际修改由 Agent 处理。本轮未运行验证。

## 网页自动刷新（第一百一十轮）

- 无 HMR 测试页面开启自动刷新，修改服务端输出后等待重载；选择元素或写反馈时查看暂缓提示，清除选择/处理反馈后继续。
- 截图、重连和弹窗期间不应触发定时重载；关闭预览或恢复默认停止。页面表单状态可能随重载丢失，开发页面按需启用。
- 未运行上述验证。
