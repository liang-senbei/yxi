# Windows 1.4.0 待交付范围

状态：已冻结版本并启动正式Windows构建，尚未发布。1.3.0发布记录保持独立，本页不代表官网已升级。

冻结源码：`d8b343ac64c9bf0387378031ac6818ab6579b563`。
流水线：<https://github.com/liang-senbei/yxi/actions/runs/35446691527>。

## 本批已实现

- Codex对话滚动修复、阅读位置记忆、普通文件附件、输入历史终态/失败原因、待答复草稿保护。
- 真实目标读取和状态展示、模型请求与运行器报告区分、本轮额外权限请求交互。
- 后台任务通知、置顶过滤、单任务静音、按稳定实例点击定位。
- 会员/余额券兑换、钱包订单、自动续费设置、商城确认与同请求编号重试。
- 信箱筛选与临期提示、可选输入历史保留期限、保留去重身份的正文清理。
- 终端转录增量缓存、收藏启动入口、Windows系统听写入口、可配置本机whisper.cpp离线识别。

## 发布前收尾

1. 收取离线ASR现有3例假CLI检查结果，合入必要测试修正，不追加矩阵。
2. 固定版本与源码，集中构建Windows安装包，沿用现有发布流水线。
3. 更新使用说明：离线ASR须自备程序/模型，系统语音需要联网；收藏新建不会自动恢复旧对话/协作组。
4. 用户实机验收项继续在windows-manual-verification.md维护，不将模拟测试表述为真实交易/麦克风/模型验收。

## 仍不能据此宣称完成的部分

- 三个月权益和Laize名称/命令仍待用户明确，未自动赠送或发明启动命令。
- 目标set/clear的持续执行语义尚未确认，当前不提供假暂停/继续/启动操作。
- 已有Codex线程的提供方热切换未实现；采用等待空闲、保存配置和明确新建任务入口。
- 商城现有后端不提供原子报价锁，客户端复核目录不能冒充锁价；真实交易未由Agent执行。
- 本批代码已实现不等于所有PRD验收场景已完成，具体证据以windows-validation-summary.md为准。
